# Ledger — 2026-07-23-blog-tts

- Phase 1: research complete (current-tts, blog-architecture, tts-landscape, origin-and-dedup, design-analysis).
- Founder rulings captured in plan.md (no consent gate, no cancel, 4 workers as-is, no E2E, short disclosure copy).
- plan.md written; awaiting Phase 2 approval.
- Copy amended (G6: "Local text to speech. First listen downloads...").
- T4 UI rev 2: permanent sticky reader bar, no popups/floating chrome; mock rewritten.
- T4 UI rev 3: highlight toggle = icon button + shared Tooltip (chat's ToggleButtonWithTooltip pattern).
- T4 UI rev 4 (07-24): card right of author in header band (avatar-top → tags-bottom), ~15rem, borderless (G9), centered buttons, 2-line disclosure, shared download bar above controls; new T5 extracts the bar from accessibility audio.tsx; sticky superseded. Mock rewritten.
- T4 UI rev 5 (07-24): container background removed too — bare elements on the page (G9 = no border, no background). Still awaiting Phase 2 approval.
- 2026-07-24: Phase 2 APPROVED (founder "execute"). T1 amended: ground host list in legacy CSP + build a real anti-regression test (founder-directed).
- Dispatch: research(legacy-csp+testability) + T2 implementer, parallel. packages/ui single-writer = T2 only right now (T5 held to avoid package.json race).
- T1 finalized (HF hosts via shared constant + self-host onnxruntime WASM same-origin, no jsdelivr; unit derivation test + one E2E block in marketing-roadmap.spec.ts; 2 auditors, security-lens). Founder approved all 3 decisions + the scoped single-test CSP check.
- Interfaces amended: voice type = TtsVoice (not TtsVoiceId); offsets index normalizeForSpeech(blockEl.textContent), T3 remaps to raw nodes.
- T2 impl DONE (100% cov, self-gates green). DISPATCHED T2 audit + T1 implementer in parallel (file-disjoint).
- T2 AUDIT: PASS (clean, no findings, 100% cov; offset contract independently verified; report typo 26→24 tests, no code impact). T2 CLEAN.
- DISPATCH T3 (highlighter) — parallel with in-flight T1 (disjoint files). T5 held (shares ui barrel with T3).
- T3 impl DONE (100% cov, clean gates; matcher rests on normalization-never-adds-significant-chars invariant; URL→link degrades to block). Touched only its files; tts.worker diffs are T1s.
- DISPATCH T3 audit (1 auditor, not sensitive). T5 still held until T3 CLEAN (shared ui barrel). T1 still in flight.
- NOTE: repo mid-rebase per founder git state; agents run no git writes.
- T1 impl DONE_WITH_CONCERNS: own deliverables green (HF hosts in connect-src, jsdelivr=0, both dist have self-hosted WASM, 100% cov on new files). E2E authored, run deferred to close.
- Concerns triaged: (a) T1 saw test:ui RED on T3 chunk-highlighter = concurrent-edit snapshot, T3 audit is authority; (b) scripts seed.ts/seed-run failure = ambient (seed.ts not a T1 file), attribute in close; (c) ort-assets-plugin impl-before-test, backfilled 100% — security auditor to weigh.
- DISPATCH T1 x2 auditors (correctness + security lens), scoped off T3 files + ambient. T3 audit still in flight.
- T3 AUDIT: PASS (clean, no findings, 100% cov; matcher exactness independently proven; report over-counted v8-ignore 2→1, no code impact). T3 CLEAN.
- T1 T3-breakage flag RESOLVED: was a concurrent-edit snapshot; T3 final state green.
- RELEASE T5 (shared download bar) — ui barrel now free (T2/T3 done). Parallel with in-flight T1 audits (disjoint files). T4 still waits on T5.
- T1 SECURITY audit: PASS (clean; only connect-src changed = +huggingface.co +*.hf.co, jsdelivr=0, no other directive touched; disallowed-host test fails closed; WASM root-relative /ort/ same-origin; shared tts-hosts.ts feeds CSP+worker+ORT path). Awaiting T1 correctness audit (2-auditor task).
- NOTE for close: widget copy says "88 MB", blog G6 says "about 90 MB" — minor copy reconciliation, surface as doc proposal, do not change mid-run.
- In flight: T1 correctness audit + T5 impl. T4 blocked on T5.
- T1 AUDITS SPLIT: security PASS, correctness FAIL [Critical]. Orchestrator VALIDATED directly: kokoro-js@1.2.1 exported env = {wasmPaths} only; env.backends.onnx.wasm.wasmPaths THROWS at import -> breaks TTS worker (incl existing chat TTS). Worker test false-green (fabricated env mock).
- Criterion-3 AMENDED: cannot pin remoteHost via kokoro-js wrapper; transformers default = huggingface.co = constant host; rely on default + CSP derivation test; no new transformers dep. WASM fix = env.wasmPaths = TTS_ORT_WASM_PATH (verified works).
- DISPATCH T1 fixer (cycle 1), 2 validated findings. Process fix: test MUST ground in real kokoro-js env. CSP/tts-hosts/ort-plugin/e2e untouched (security-validated). Fixer scoped to tts.worker.ts+test; T5 concurrent on disjoint ui files.
- T5 impl DONE_WITH_CONCERNS: TtsDownloadBar extracted (borderless role=status, percent/label/showLabel; subpath @hushbox/ui/accessibility/tts-download-bar). 60 audio-section tests pass UNMODIFIED, new file 100% cov, gates green. Concerns: role progressbar->status (criteria-mandated), knip unused-until-T4 (close), package.json multi-writer (verify at audit).
- DISPATCH T5 audit (1 auditor). T1 fixer still in flight (disjoint files). T4 waits on T5 clean.
- T1 FIX (cycle 1) DONE: worker uses real env.wasmPaths API; test reworked to reproduce the throw (watched RED); dead remoteHost removed per amended crit-3; tts.worker.ts 100% cov; @hushbox/ui green. Bounds respected (worker+test only).
- DISPATCH T1 re-audit (correctness lens, must import real kokoro-js env). Security lens NOT re-run (fix confined to worker+test; CSP/tts-hosts/ort untouched).
- NOTE for T4: accessibility/store/schema.ts shows 0% (excluded/type-only, gate green) — T4 edits it, keep coverage green.
- T5 AUDIT: PASS (clean, no findings; 100% new-file cov, 60 audio tests unmodified; package.json valid w/ T2+T5 exports; role change criteria-mandated; schema.ts 0% = excluded/type-only, gate green). T5 CLEAN.
- Note: chunk-highlighter exposed via lib barrel, no dedicated subpath (T3 choice, audit-passed) — T4 may add its subpath if the lazy split needs it (T4 owns package.json export edits).
- DISPATCH T4 (final task; blog UI). Deps T2/T3/T5 all clean. Independent of T1 (re-audit in flight, read-only, no conflict). T4 is sole writer now.
- T1 RE-AUDIT: PASS (clean). Auditor imported real kokoro-js env, confirmed worker no longer throws + test guards regression. T1 CLEAN.
- STATUS: T1,T2,T3,T5 all CLEAN. T4 (blog UI) sole task in flight. Close phase pending T4 clean.
- T4 impl DONE_WITH_CONCERNS: BlogReadAloud built, 100% cov, G2 PROVEN from built bundle (no engine in initial marketing JS), gates green. Only @hushbox/blog-reader export added; relative dynamic imports (no chunk-highlighter subpath needed).
- Concerns: (1) pref lives in packages/shared/schemas/accessibility-preferences.ts (SSOT the plan pointed at; store/schema.ts was a re-export) — adds field to server-LWW-synced prefs, AUDIT the anon-visitor + server-validation path; (2) iOS unlockAudio after await dynamic-import may lose gesture context — REAL limitation, raise to founder; (3) shared/documents/bridge.ts lint fail = ANOTHER run untracked file, attribute out, do NOT touch; (4) band right edge = max-w-5xl > article col on lg, design-review at close.
- DISPATCH T4 audit (1 thorough auditor).
- T4 AUDIT: PASS. All 10 criteria met; G2 proven from built bundle (zero engine bytes in initial marketing JS, engine in lazy chunks); server-synced pref safe (additive default / anon local persist / non-strict server validation); 100% cov. One finding = [Minor/design-escalation] iOS unlockAudio-after-await → founder decision. T4 CLEAN.
- ALL TASKS CLEAN (T1 T2 T3 T4 T5). Entering CLOSE PHASE.
- Dispatch: (a) full unscoped gate-runner + attempt CSP E2E; (b) completeness critic. Escalations pending for founder: iOS limitation, 88-vs-90MB copy, layout lg-alignment visual, E2E-run confirmation.
- COMPLETENESS CRITIC: all integration seams verified consistent (container selector, T2<->T3 offsets, T5<->T4 props, voice source, /chat dedup end-to-end incl same-origin + /ort self-host in both dists). Gaps: P1 nothing run end-to-end (E2E+design-review still open); P2 copy inconsistent 3 ways (88/90/stale-80 comment @ tts-download-progress.ts:7); P3 TECH-STACK.md omits kokoro-js+onnxruntime-web; v1-accept edges (images/code/long/2nd-post/concurrency).
- Holding for gate-runner (full gates + E2E attempt) before consolidated founder close report.
- GATE-RUNNER: typecheck PASS, lint PASS, test:ui PASS, test:shared PASS, jscpd PASS. E2E CSP test PASSED in real browser (blocked.invalid -> violation; huggingface.co -> no violation; 44 passed). This-run actionable failures: NONE. Ambient reds (scripts seed/refresh/generate-env VAPID, knip sandbox/webpush) attributed out, untouched.
- CLOSE VERIFICATION COMPLETE. Pending founder decisions only: iOS unlock, MB-copy number, TECH-STACK doc, design-review-on-rendered-post.
- Founder decisions: iOS=FIX, size=verify-actual, design-review=founder-manual, docs=APPLY.
- Applied TECH-STACK rows. Dispatch: analyst(iOS feasibility) + T7(copy). T6(iOS) gated on analyst + T7.
- IOS ANALYST: decisive. ONLY working G2-safe fix = Option B (create AudioContext in-gesture in island, inject into engine) which REQUIRES an additive engine-API change (unlockAudio(existing?: AudioContext)) = REVERSAL of G1/G8. Option A (standalone helper, critic idea) DOES NOT WORK on iOS (per-context unlock). C breaks G2. D sacrifices accessibility. Analysis persisted to research/ios-audio-unlock.md.
- ESCALATE to founder: "fix it" now known to require G1/G8 engine-API reversal (backward-compatible, chat unaffected). Holding T6 for founder go on the engine change. T7 (copy) still in flight.
- FOUNDER APPROVED the G1/G8 engine-API reversal for iOS (additive unlockAudio injection). T6 written into plan.
- T7 DONE: verified size 92,887,010 B (q8 onnx 92,361,116 + config/tokenizer 3,654 + voice 522,240) = 88.6 MiB / 92.9 MB -> "about 90 MB"; shared const TTS_MODEL_DOWNLOAD_MB in packages/shared/tts-model-download.ts; widget 88->90; stale 80MB comment fixed; drift guarded by tests both surfaces. Gates green.
- T7 raised: widget copy reads "90 MB, one-time download." (no "about") — founder nit, flag not fix.
- DISPATCH T7 audit ALONE. T6 HELD until T7 clean (shared blog-read-aloud.tsx; avoiding the concurrent-write/read false-finding seen with T1/T3).
- T7 AUDIT: PASS (clean). Auditor independently re-fetched HF model tree: 92,361,116 + 3,654 + 522,240 = 92,887,010 B exactly as reported; q8 -> model_quantized.onnx confirmed; one voice .bin per first listen confirmed at kokoro-js source. Anti-drift tests real (both surfaces derive from constant). T7 CLEAN.
- T7 audit Minor (valid, out of T7 bounds): tts.worker.ts:38 comment still says "~80 MB" -> FOLDED into T6 as a comment-only one-liner (~90 MB; keep the ~330 MB fp32 contrast).
- test:shared env.config 4 failures = AMBIENT (other workstreams added VAPID_*/SANDBOX_ORIGIN_URL to backendEnvSchema). Not ours.
- DISPATCH T6 (iOS unlock, founder-approved G1/G8 additive reversal) + folded comment fix. Last task. 2 auditors after: correctness + chat-regression.
- T6 impl DONE: engine API now unlockAudio(existing?: AudioContext) (additive); document-reader.start(audioCtx?) threads it; island creates context ONCE + re-primes per listen (justified: engine keeps first adopted ctx). Comment fix ~80->~90MB done. G2 re-verified from fresh marketing build; G1 model config byte-identical. test:ui 1851 pass, 4/4 typecheck+lint, 41 apps/web chat-TTS tests pass unmodified.
- DISCOVERY (flag to founder, do NOT fix here): chat has NO unlockAudio call site in production code — only document-reader called it; chat uses load/stop and relies on lazy this.audioCtx ??=. Suggests a possible PRE-EXISTING iOS unlock weakness in chat read-aloud, out of scope for this run.
- DISPATCH T6 x2 auditors: correctness lens + chat-regression lens.
- T6 CHAT-REGRESSION audit: PASS (clean, no findings). Independently confirmed: chat has NO unlockAudio call site (sole non-test caller = document-reader.ts:233); no-arg unlockAudio byte-identical behavior (existing ?? new AudioContext()); scheduleAudio/stop/teardown untouched; G1 constants (MODEL_ID/DTYPE/wasm/pool/voices/protocol) intact.
- CROSS-SURFACE SINGLETON VERDICT: NO RISK. marketing(Astro) -> /chat(SPA) is a document load, singleton does not survive; no Astro view transitions; audioCtx never .close()d; at most one context per document.
- Ambient noted: motion-provider.tsx coverage-attribution flake in one turbo test:ui run (100% in isolation, 2 clean re-runs, file untouched since 05-29). Not ours; watch in CI.
- Awaiting T6 correctness-lens audit (last gate of the run).
- T6 CORRECTNESS audit: PASS (clean, no findings). (a) sync-before-await ordering VERIFIED (nothing but sync ops precede primeAudioContext; first await is the dynamic import); (b) adopted ctx provably the one that plays (full chain traced; the only ignore-path unreachable on a marketing page); (c) re-prime deviation JUDGED CORRECT and load-bearing (engine keeps first ctx forever; fresh-per-click would play through an un-re-primed instance). Ordering test is revert-red, not a tautology.
- G2 nuance (honest, attributed out as PRE-EXISTING): blog island chunk = ZERO engine markers, BUT the page also loads the a11y widget chunk which statically imports tts-engine.js (sections/audio.tsx:12, present at HEAD). The heavy kokoro/onnxruntime weight lives ONLY in the lazy tts.worker chunk. G2 spirit holds; the letter was already imperfect before this run.
- ALL TASKS CLEAN: T1 T2 T3 T4 T5 T6 T7. RUN COMPLETE. Uncommitted. Open founder items: manual iOS/visual pass, widget "about" copy nit, pre-existing chat-iOS-unlock observation, motion-provider coverage flake watch.
- PROD VERIFICATION (real prod-target build): TTS WILL WORK IN PROD. /ort/ has both ORT files byte-identical to installed transformers@3.8.1; _headers connect-src has huggingface.co + *.hf.co, no jsdelivr; ALL 31 CSP blocks incl. every /blog/<slug> (bare+trailing-slash) carry IDENTICAL connect-src (generator splices only per-page script hashes, so a narrower connect-src is structurally impossible); merge safe (identical bytes, _headers generated AFTER merge); blog HTML has the island; both worker chunks contain wasmPaths=`/ort/` matching on-disk; deploy = ci.yml wrangler pages deploy apps/web/dist.
- TWO CRITICAL DEPLOY BLOCKERS, NOT FROM THIS RUN (sandbox/documents workstream, uncommitted): (1) generate-headers.ts now hard-requires SANDBOX_ORIGIN_URL but CI/release pass only VITE_API_URL -> headers step FAILS; generate-env.ts:517 hardcodes headers-env as [VITE_API_URL]. (2) pnpm build fails at untracked apps/sandbox because turbo strips ESM_CDN_URL (not in build.passThroughEnv/globalEnv). Flagged to founder, NOT fixed (other workstream).
- Optional cleanup flagged: Rollup also emits 2 extra copies of the 21.6MB ORT wasm (ORT internal new URL fallback, inert since wasmPaths is set) -> ~65MB of 162MB dist. Works (under Pages per-file limit) but wasteful.
- Residual: no live model download against the built artifact; E2E proves CSP allowance with a stubbed fetch.

## Post-ship founder report 2026-07-24 (8 issues + debloat)
- 1 TTS slow, "like a single worker" (old impl had many) — suspect blog path serializes speak() so pool of 4 is idle; chat uses stream-feeder. INVESTIGATING.
- 2 disclosure renders 3 lines, want 2 (keep 3 on mobile).
- 3 component must be right-justified over the ARTICLE column, not over "On this page" (confirms T4-flagged lg alignment issue).
- 4 move download bar BETWEEN byline/tags block and tts component; fills the gap up to a max; must NOT add height.
- 5 restrict tts component height to the byline+tags block height.
- 6 highlight toggled off->on does not reappear until the next chunk starts.
- 7 cached model: download bar flashes ~1s then disappears.
- 8 download starts displaying 100%, drops to 0%, then really downloads.
- + debloat: 3 copies of 21.6MB ORT wasm in dist (~65MB).
- Orchestrator dispatched 3 parallel analysts (perf/worker-pool; the 3 code bugs 6-8; debloat options). Layout 2-5 to be specced with an updated mock after investigation.
- Founder round-2 rulings recorded; Phase 2 plan (B1-B7) written. Ambiguity A1 (legacy concurrency = unbounded vs pool-bounded) NAMED in plan, resolved as pool-bounded, flagged for founder override.
- 2026-07-24: Phase 2 APPROVED (founder "execute"). DISPATCH B1 (staged load) + B2 (progress aggregation) + B3 (prefetch/concurrent synthesis) + B6 (debloat+verifier) in parallel, file-disjoint. B4 gated on B2; B5 on B4; B7 on B6.
- B1 impl DONE (staged load, RED->GREEN, 63/63 engine tests, 41/41 chat tests unmodified, cov 99.33%). Amended 5 tests (2 beyond brief, same approved class) -> auditors verifying each. DISPATCHED B1 x2 audits.
- Orchestrator verified independently: ::highlight(tts-reading) SURVIVES minification with :: intact in BOTH web and _astro/_slug_ CSS (lightningcss warning is cosmetic passthrough). zz-scratch.test.ts no longer on disk.
- B2 impl DONE (worker-side aggregation, RED reproduced exactly: first pct 100 -> now <5; tts.worker.ts 100% cov; widget pre-existing bug cured with no widget change).
- B2 CONTRACT for B4: loadProgress shape unchanged; during download loaded=Σloaded_i, total=max(Σtotal_i, TTS_MODEL_DOWNLOAD_BYTES); exactly ONE loaded===total message emitted immediately BEFORE loadDone (pre-warmup) => "pct<100" is a valid incompleteness gate; bar may unmount at 100 without waiting for speaking.
- INFRA: concurrent pnpm test:ui runs abort each other (shared packages/ui/coverage/.tmp). Workaround for all remaining briefs: pnpm exec vitest run --coverage --coverage.reportsDirectory=<scratchpad> from packages/ui.
- AMBIENT (not ours, do not fix): untracked packages/shared/src/notifications/index.test.ts has 3 eslint errors, currently failing @hushbox/shared lint for everyone.
- DISPATCH B2 x2 audits. B4 HELD until B2 CLEAN (its gate depends on B2 contract correctness).
- B3 impl DONE_WITH_CONCERNS: bounded window, doc-reader 100% cov, both hazards proven by MUTATION not assertion, window bound pinned across a 10-chunk read.
- ORCHESTRATOR RULING: orphan-audio-on-error is a real contract regression introduced by the window -> MUST fix (error => silence, as before). Plan amended. Dispatching B3 fix cycle 1 before audit.
- B3 amended a 5th test (stop() test pinned the bug itself via spoken.length<3) — same approved class, auditor to verify.
- B3 CROSS-TASK FINDING: vi.fn attaches its own settled-result handler, so ANY "no unhandled rejection" assertion on a vi.fn-returned promise is VACUOUS (probe: 3 unhandled via plain fn, 0 via vi.fn). Relevant to any future task asserting on unhandled rejections.
- B3 couples to B1: document-reader.ts now imports WORKER_POOL_SIZE from tts-engine.ts (read-only, avoids a magic number). Audit must confirm against B1 final state.
- B6 impl DONE_WITH_CONCERNS: HAZARD RESOLVED — Rolldown DOES honor resolve.conditions in the dedicated-worker sub-build (3 builds). dist 166,637,384 -> 123,362,053 B (-43,275,331, -25.97%); files 1013->1011; dist/ort sha256 UNCHANGED (wasm c46655e8, mjs 08fb86ec); both worker chunks still wasmPaths=/ort/. Assertions 2+3 proven RED pre-change; 1+4 green as forward guards.
- B6 deviations for auditor: distributionDir naming (unicorn rule), ORT_DIR exported so verifier does not re-derive, verifier injected as a `verify` DI dep matching existing merge/exec shape, assertion 3 scoped to .js with a test pinning that boundary (source maps legitimately name the asset).
- B6 flagged ONE transient non-reproducible build failure on first turbo --force after the config edit (serviceWorkerBuildPlugin closeBundle / .vite-temp frame); 3 later --force runs + 2 standalone vite builds exited 0. No evidence tied to resolve.conditions.
- AMBIENT (not ours): untracked apps/web/src/components/settings/notifications-card.test.tsx prettier failure breaks web#lint; scripts/generate-env VAPID drift; stale scripts/node_modules/.vite ssr-optimizer artifact for @hushbox/db.
- DISPATCH B6 audit. B7 HELD until B6 CLEAN.
- B1 CORRECTNESS audit: PASS clean. Walked EVERY load post-site: 4-way fan-out unreachable (concurrent load returns in-flight promise; retry only after slot-0 cache entry exists; timeout empties workers; preloadVoice posts warmup not load). All 5 amended tests sound; fail-fast split is a strict superset. zz-scratch confirmed gone repo-wide; ui lint fully clean.
- B2 WIDGET/CHAT audit: PASS clean. All 4 widget displays now correct (bar/bytes/rate/ETA); negative rate structurally impossible; chat unaffected (only widget + document-reader consume progress); G1 intact; protocol diff empty. B1xB2 COMPOSE: slots 1-3 emit cached 0->100 but engine slot-0 filter drops it before UI. Also checked reverse pathology (missing Content-Length) — still monotone.
- Noted (pre-existing, cosmetic): widget byte readout shows 89 MB (MiB-based formatter) vs disclosure 90 MB (MB). Documented in tts-download-progress.ts; not a defect from this run.
- Pending: B1 regression audit, B2 correctness audit, B3 fix cycle 1, B6 audit.
- B1 CHAT-REGRESSION audit: PASS clean. Warm-path serialization = no user-visible regression (prewarm fire-and-forget; speak-during-load already rejected+gated; preloadVoice correctly left unstaged). Load timeout now EASIER to meet (cold budget 370MB->92MB). B1 CLEAN (2/2).
- B2 CORRECTNESS audit: PASS clean. Monotonicity verified under adversarial interleavings; denominator provably CANNOT grow. Auditor corrected impl reasoning in our favor: voice .bin bypasses the progress callback (raw fetch -> kokoro-voices cache), so observable Sigma-total ~92,364,770 < floor 92,887,010 => denominator CONSTANT, monotonic BY CONSTRUCTION. Cosmetic: in-flight bar tops ~99.4% then snaps to 100. B2 CLEAN (2/2).
- B2 CONTRACT CONFIRMED for B4: 100% (loaded===total) emitted EXACTLY ONCE on every success path, always immediately BEFORE loadDone and before warmup, even when zero progress events fired. Error path emits neither, but status leaves loading, so the gate is not left hanging.
- DISPATCH B4 (blog bugs 6+7). B5 after B4; B7 after B6.
- B6 AUDIT: PASS clean. All numbers independently re-measured to the byte (123,362,053 B; 1,011 files; sha256 wasm c46655e8 / mjs 08fb86ec; zero ort-wasm outside dist/ort; wasmPaths=/ort/ in both worker chunks). Coverage 100% reproduced exactly. B6 CLEAN.
- Assertion-3 .js scoping judged NO HOLE: extension census shows only 1 .mjs in dist and it is our own emitted ORT file; assertion 2 independently catches the same regression; boundary pinned behaviorally by a .map fixture test.
- Transient build failure ASSESSED, not hand-waved: SW plugin uses a nested build({configFile:false}) that does NOT inherit resolve.conditions; auditor ran resolveConfig both ways and proved SSR conditions unchanged (rolldown-vite d.ts default comment is STALE). Signature is a .vite-temp config-loader unlink/import race under concurrent turbo. Watch item, not a finding.
- CORRECTION to carry forward: `pnpm preview` does NOT go through buildWebBundle (scripts/preview.ts:12 builds web directly, no merge) — so LOCAL PREVIEW IS NOT VERIFIER-GATED. Both deployable paths (build:web --target=prod, build:e2e) are. Criterion 5 still met.
- DISPATCH B7 (onnxruntime-web dep investigation) — B6 clean, verifier available as the proof harness.
- B3 FIX DONE: tts.stop() inside the ordered-loop catch, AFTER the pre-existing readState!==speaking guard (which already discriminates stop-induced rejections). Mutation-proven: hoisting above the guard => stopCalls===2. Used plain-fn harness (not vi.fn) so the unhandled-rejection assertion is real. 100% per-file cov, no new ignores. DISPATCHED B3 audit.
- B4 impl DONE_WITH_CONCERNS: both fixes test-first; blog-read-aloud.tsx 100% cov; error-path gate proven not to hang (first conjunct is status===loading; leaving loading falsifies it AND clears the pending timer).
- ORCHESTRATOR APPROVAL: B4 amended 2 tests beyond the named set ("keeps the download bar visible when the reader reports loading", "forwards download progress to the bar"). Both asserted bar presence under the OLD unconditional-loading rule = same founder-approved class (tests encoding buggy behavior). Assertions kept, timers advanced past the dwell. Auditor to verify nothing gutted. Recorded transparently for founder.
- ORCHESTRATOR BRIEF ERROR (2nd time, same class as B3 note): demanded RED-first on B4 repro test 2 ("does not repaint a finished read"), which cannot be RED — nothing repaints today; it is a regression guard. Mutation validation is correct. Not an implementer deviation.
- B4 note for B5: paintChunk param type narrowed from RunContext to a new HighlightHandles supertype; no call site changed; file left lint/typecheck clean.
- DISPATCH B4 audit. B5 after B4 clean.
- B3 AUDIT: PASS clean. Bound genuinely enforced (exact count min(POOL,remaining) pinned at all 10 steps); error-path stop fires EXACTLY once and only for genuine failures (verified both sides + traced through engine stop); unhandled-rejection assertions REAL (verified @vitest/spy source + runtime probe); 5th amended test tightened not loosened; 100% cov read from raw coverage-final.json. POLE GATE resolved by MEASUREMENT: 38ms vs 15,000ms floor. B3 CLEAN.
- B4 AUDIT: PASS with 1 Minor (valid). blog-read-aloud.test.tsx:328-339 "hides the download bar once speaking begins" now passes VACUOUSLY (real timers => dwell never elapses => bar never on screen => assertion pins nothing). Pre-B4 it pinned a real show->hide transition. Fix: fake timers, advance past dwell with pct:40, assert bar present, then onState(speaking), assert gone.
- B4 audit CONFIRMED: cached load shows NO bar (dwell + percent<100 conjunct, verified end-to-end against B2 contract incl. floored total making it non-zero on an empty map); error path CANNOT hang (first conjunct is status===loading; leaving loading falsifies gate AND cleanup clears the timer); all THREE amended tests kept their assertions (bar assertion re-homed into a new dedicated test, nothing lost).
- DISPATCH B4 fix cycle 1 (the vacuous 4th sibling). B5 after B4 clean.
- B7 DONE_WITH_CONCERNS — PREMISE DISPROVED: onnxruntime-web is NOT dead. transformers browser build externalizes bare onnxruntime-common and re-exports its Tensor; that pkg is absent from transformers peer dir so it resolves via pnpm hoist dir, and THIS direct dep decides the version. Removing it flips shipped Tensor 1.26.0 -> 1.21.0 (byte 2165 of worker chunk). Stop-and-report clause correctly triggered; kept + documented branch taken.
- Orchestrator VERIFIED B7 lockfile claims: git diff pnpm-lock.yaml touches onnxruntime ZERO times (ORT restored to HEAD); lock header/overrides byte-identical to HEAD; pnpm-workspace.yaml override `vite: npm:rolldown-vite@7.3.1` intact; apps/web still resolves rolldown-vite 7.3.1. The 592-line lock diff (vite@8.1.5 peers + apps/sandbox) belongs to the untracked sandbox workstream, NOT our bundler path.
- FOUNDER ITEMS from B7: (a) ^1.26.0 pin is ACCIDENTAL + UNSTABLE — floated to 1.27.0 on a fresh install during the task, which would silently bump the shipped Tensor; B7 restored lock ORT entries to HEAD 1.26.0. (b) Coherent fix = declare onnxruntime-common directly, exact-pinned to transformers 1.22.0-dev.20250409-89f8206ba4, drop onnxruntime-web — dependency change, NOT taken, needs founder approval.
- DISPATCH B7 audit.
- FOUNDER APPROVED the coherent ORT dep fix -> written as B8. Flagged explicitly: this CHANGES the shipped Tensor from an accidental 1.26.0 to transformers expected version (correctness improvement, but a real runtime change, not a no-op). B8 HELD until B7 audit clears (same files: package.json + knip.jsonc; avoiding the concurrent-write/read false-finding pattern).
- B7 AUDIT: PASS clean. Determination independently verified from installed pkgs + shipped bytes: .Tensor is the ONLY runtime member taken from onnxruntime-common (1 runtime match vs 14 JSDoc); byte-2165 corroborated (worker chunk has versions:{common:`1.26.0`}, literal starts 2162); knip comment real+actionable; dist/ort sha256 verified; zero onnxruntime lines in lock diff. B7 CLEAN.
- KEY CONTEXT FOR B8 (from B7 audit, pre-existing): the shipped worker chunk carries TWO onnxruntime-common copies — hoisted 1.26.0 AND a 1.22.0-dev inlined by ort.min.mjs. Two versions:{common:...} sites, two Tensor constructors. B8 pinning to transformers expected version should CONVERGE them, not just swap a number. Added as an explicit B8 criterion.
- DISPATCH B8 (founder-approved ORT dep fix). B5 still gated on B4 re-audit.
- B4 RE-AUDIT: PASS clean. Test now mutation-sensitive (present->absent transition proven); component verified byte-coherent with NO leftover mutation (read end-to-end against all 3 mutation sites); two-mechanism correction accurate (gate conjunct + unconditional setDwellElapsed(false) on status change — prior audit suggested the wrong probe, finding still real). Both "legitimate absence" tests spot-checked non-vacuous. B4 CLEAN.
- Auditor agreed the two-mechanism hide is NOT a no-backup-mechanisms violation (one gate + an ordinary useState reset in a status-keyed effect).
- DISPATCH B5 (layout, founder issues 2-5) — LAST task. B8 still implementing.
- B5 impl DONE_WITH_CONCERNS: rev-6 layout landed; blog-read-aloud.tsx 100% cov; 94 files/1874 tests; typecheck+lint 4/4; jscpd 0 clones.
- ORCHESTRATOR VERIFIED B5s load-bearing assumption in REAL BUILT HTML: apps/web/dist/blog/*/index.html emit `astro-island,astro-slot,astro-static-slot{display:contents}` inline. Two-root component structure is safe.
- B5 DEVIATIONS, ALL APPROVED by orchestrator: (1) error line moved into the gap slot too — leaving it in the reader stack would put it under issue-5 clipping (bug avoided); (2) reader 18rem vs mock 17rem + breakpoint sm->md + text-[0.7rem] — required for the non-wrapping two-line disclosure and a usable gap slot at 640px; mock was a design target, not spec; (3) disclosure split moved sentence-boundary -> balance point — that IS the issue-2 fix, combined text unchanged and still asserted verbatim; (4) h1 left full-width (mock draws it in the column) — surgical, but FLAG to founder as a visual judgment for their pass.
- B5 did NOT run a marketing build (B8 concurrently editing package.json/lock; failures would be unattributable). Auditor to do it once B8 settles.
- DISPATCH B5 audit. B8 still implementing.
- B8 DONE_WITH_CONCERNS: VERSION CONVERGED (worker chunk 2 distinct ORT versions -> 1; zero 1.26.0 bytes in dist). Both versions:{common:} sites + Tensor ctors remain (2->2) — convergence is on VERSION not module count; structural dedup impossible without patching ORT pre-bundled ort.min.mjs. dist/ort sha256 UNCHANGED. All TTS suites green. pnpm --frozen-lockfile: "Lockfile is up to date"; overrides + rolldown-vite override untouched.
- B8 HAZARD: aborted `pnpm install --force` (ENOTEMPTY, fuse-hidden rolldown binary held by a concurrent process) left hoist dir 34/1215 + astro patch UNAPPLIED, breaking the marketing build. Repaired + re-verified. CONCURRENT AGENTS IN THAT ~5min WINDOW MAY HAVE SEEN SPURIOUS FAILURES — B5 audit was running; attribute accordingly.
- B8 DEVIATION for auditors: lockfile edit was HAND-APPLIED, not written by pnpm install. Rationale: a real --lockfile-only resolve emitted 528 extra lines incl. removal of all 24 vite@8.1.5 refs owned by the untracked apps/sandbox workstream. Reverted that; hand edit validated by pnpm reporting lock up-to-date. NEEDS CAREFUL AUDIT.
- B8 RESIDUAL (not introduced by it, demonstrated real): hoist selection is deterministic on a CLEAN install (CI) but one incremental install recorded onnxruntime-common@1.21.0 as hoist target. The change improves odds (direct dep = depth 1, shallower than the 1.21.0 competitor) but NOTHING PINS IT. Fix = a versions:{common:} assertion in scripts/verify-web-bundle.ts (B6-owned, correctly not touched). QUEUE as B9 after B8 clears.
- DISPATCH B8 x2 audits (correctness + regression/lockfile).
- B8 CORRECTNESS audit: PASS clean. Version independently derived from TWO exact upstream pins (transformers -> onnxruntime-web 1.22.0-dev -> onnxruntime-common 1.22.0-dev); pin matches, no range. Census on a NEWER build than the report: zero 1.26.0 bytes in either dist, 1 distinct version across both worker chunks, 2 Tensor impls remain (version convergence, not module dedup — framing accurate). /ort sha256 match in BOTH web and marketing dists. All 3 STOP conditions respected, none triggered.
- C2 CLEARED: node_modules/.modules.yaml now records onnxruntime-common@1.22.0-dev as hoisted; the stale 1.21.0 record is gone. node_modules damage confirmed fully repaired (hoist dir 1215 entries, astro patch present, no residue).
- B9 now DOUBLY recommended (B8 implementer + B8 correctness auditor, independently): a versions:{common:} assertion in scripts/verify-web-bundle.ts would permanently pin the shipped ORT version AND would have auto-caught the original 1.26.0/1.22.0-dev split. Dispatch after the B8 lockfile audit clears.
- B5 AUDIT: PASS with 1 Minor + 1 design question. Verified: B4 behavior INTACT byte-level (dwell 900 + comment, 3-conjunct gate, status-keyed effect, lastChunkRef + symmetric toggle + nulling at all 4 sites, HighlightHandles, T6 audio-unlock ordering); marketing build run (26 blog pages); island emits two data-slot roots as direct flex children with display:contents inline; idle slot genuinely :empty; all layout utilities compiled. G2 SURVIVED the island restructure (blog-reader.js 8.6KB, no kokoro/onnxruntime).
- B5 Minor (VALID): blog-read-aloud.tsx:136-139 comment claims the status slot "contributes no height of its own" — but align-self:center only POSITIONS; cross size still participates. Height invariance is real but CONTINGENT (bar ~28-34px vs byline ~70px, ~32px tagless). A future 2nd line in that slot (widget already renders bytes/ETA) breaks issue-4 silently while the comment says it cannot.
- B5 design question -> ORCHESTRATOR RULING: FIX IT. [slug].astro:73 and :129 duplicate the literal `lg:grid lg:grid-cols-[1fr_220px] lg:gap-10` with a comment saying they must keep matching = the banned keep-in-sync contract (CODE-RULES One Implementation Shared). Drift silently breaks criterion 1 (right-edge alignment) = founder issue 3. Fix = one frontmatter const in the same file.
- Auditor could not verify "no B4 test edited" TEXTUALLY (blog-reader/ untracked, no git baseline) but confirmed behavioral equivalence: every B4 criterion still pinned by live meaningful tests. Accepted.
- DISPATCH B5 fix cycle 1 (comment + grid-const). B8 lockfile audit still running; B9 queued.
- B8 LOCKFILE audit: PASS clean. (a) Clean CI install CANNOT resolve differently — 3 legs: CI runs --frozen-lockfile on pnpm 10.26.0 (same version); isolated replica of all 17 manifests validates (negative control correctly fails with ERR_PNPM_OUTDATED_LOCKFILE); FORCED FULL RE-RESOLVE reproduced ORT entries IDENTICALLY = the hand-edit is what pnpm would write. (b) Hoist reasoning holds and is STRONGER than B8 knew — clean-room: WITH pin -> 1.22.0-dev, WITHOUT -> 1.21.0 (so deleting the dep outright, as originally proposed, would have shipped 1.21.0 Tensor vs a 1.22.0-dev runtime = worse than status quo). Hand-edit fabricates NO integrity hash (repoints at a digest already at HEAD). B8 CLEAN (2/2).
- CLOSE-OUT ITEM (NOT OURS, sandbox workstream, FOUNDER-FACING): the lock records 24 vite@8.1.5 refs (0 at HEAD) though pnpm-workspace.yaml overrides vite: npm:rolldown-vite@7.3.1, and a genuine resolve DELETES them. --frozen-lockfile installs them happily => CI would build some packages against real Vite 8 instead of rolldown-vite. Whoever commits this lock commits a state no pnpm install regenerates.
- CLOSE-OUT ITEM: a Tensor version change could break inference in a way NO test here catches (no test loads real onnxruntime or runs a model). Evidence is indirect but points the SAFE direction (shipped Tensor now matches the runtime transformers was built against; deployed code currently mismatches). Manual browser smoke of blog Listen + chat read-aloud is the only real proof.
- DISPATCH B9 (verifier version assertion) with the auditor caveat: expected version must be DERIVED from installed metadata (transformers -> onnxruntime-web -> onnxruntime-common), never hardcoded, or it becomes the banned mirrored-constant sync contract.
- B5 RE-AUDIT: PASS clean, no findings. Comment flex reasoning now technically correct (states contingent invariant + names the concrete hazard: a bytes/speed/ETA second line, which the widget audio section already renders). Const refactor proven output-identical FROM BUILT HTML (both rows emit char-identical class string; Tailwind still emits the arbitrary utility). mt-4 left duplicated — judged sound under the CODE-RULES drift test (independent spacing, not the alignment contract). B4/T6 logic verified intact. B5 CLEAN.
- CLOSE-PHASE ITEM (real, pre-existing from T4/round-1, NOT in the fix scope): blog-read-aloud.tsx comments at ~:22 and ~:254-255 cite run-plan constraint IDs (G6/G2/G5) that no reader outside this run can resolve = durable-naming violation (CODE-RULES bans plan-section refs in shipped comments; also matches the known "SDD plan IDs leak into comments" pattern). Sweep ALL files touched this run for G#/T#/B#/plan refs during close.
- STATUS: B1-B8 ALL CLEAN. Only B9 (verifier version assertion) in flight. Close phase next.
- B9 AUDIT: PASS clean. Derivation fallback is STRONGER than the upstream-chain approach: each worker chunk carries TWO independent version sites, and transformers VENDORS its expected version literal into its own dist (a value our pin does not control) — so requiring all sites to equal our pin catches a transformers bump even with our pin untouched. Our declaration is checked against an independent authority, not itself. No vacuous-pass path found in 3 attempts (zero-sites dist-wide = violation; unresolvable bind = violation, not skip). 11 pre-existing assertions survived — proven 3 ways incl. that the seeded fixture chunk is inert. Teeth proven by MUTATION on the real minified chunk. B9 CLEAN.
- ALL 9 TASKS CLEAN (B1-B9). ENTERING PHASE 2 CLOSE.
- Close dispatch: (a) full unscoped gates via the REAL pnpm test:* scripts (so coverage+pole gates run as CI runs them) + plan-ID sweep; (b) completeness critic over the integrated whole. NOTE: machine heavily loaded by another workstream (load 30+, concurrent large suites) — attribute timeouts, do not mistake load for failure.
- CLOSE GATES: zero this-run actionable failures. test:ui PASS, test:shared PASS (forced), duplication PASS 1.01%. scripts test / typecheck-lint / knip fail 100% AMBIENT (stale .vite, VAPID drift, document-sandbox prettier, config/sandbox knip). Pole detector RAN, nothing close (largest tts.worker.test.ts 2.0s / ~6% share). Coverage 100% on all run files except tts-engine.ts 99.26% (PRE-EXISTING uncovered catch at 405-406, clears the 95 gate).
- COMPLETENESS CRITIC: seams VERIFIED SAFE — B3xB1 cannot collide (playChunks runs only after load() resolves = all 4 slots loadDone+warmupDone); B2xB1xslot-0 filter fine; B3xB4 highlight fine (onChunk on ordered path); T6 iOS ordering survived B4+B5; G2 holds; /chat cache dedup intact. NO Phase-1 regressions found.
- CRITIC GAPS -> FOUNDER: (1) B1 created a silent stage-2 — bar unmounts at slot-0 100%, then NOTHING on screen while slots 1-3 load from cache + 4 warmups run; the a11y widget keeps its bar through that same window, so surfaces now diverge. One-conjunct fix. (2) issue 7 only heuristic and B1 may have WEAKENED it — percent hits 100 only when slot-0 from_pretrained returns (92MB cache read + ORT session); if >900ms dwell, the flash returns shifted later. (3) B1s mechanism NEVER EXECUTED — if the cache-population premise is wrong, first listen is 4x SERIAL = worse than the parallel 4x it replaced. (4) neither perf fix measured; fillWindow issues POOL speaks at t=0 so chunk 0 competes with 1-3 -> time-to-first-audio may REGRESS on low-core/mobile. (5) the B6/B9 verifier NEVER RUNS ON THE SHIPPING ARTIFACT — CI build job hand-rolls turbo+merge+headers and uploads that dist; Android bypasses too; only a dev-mode sibling (build:e2e) is verified. PRE-EXISTING divergence, defeats the 25MiB landmine guard for the deployed bytes. Needs CI change = founder approval. (7) layout arithmetic: reader content ~73px vs band ~70px = the disclosure last line MAY ALREADY CLIP; a tagless post (~32px band) would clip badly (latent, all 5 posts have tags). (8) no pause/resume — Stop is full teardown, next Listen restarts at sentence 1.
- DOCS: TECH-STACK onnxruntime-web row is NOT made wrong by B8 (shipped runtime is still onnxruntime-web via transformers; only the direct declaration moved to onnxruntime-common as a resolution pin). No other doc mentions TTS/pool/download/verifier.
- Close fixer extended via SendMessage with 2 leaks the gate sweep missed: blog-read-aloud.tsx:18 (cites a run-dir research doc) and chunk-highlighter.test.ts:259 (test name "auto-scroll (G3)").
- FOUNDER RULINGS: gap1 leave dead, gap5 leave for now, gap8 PAUSE/RESUME = build it. Dispatched analyst for design.
- FOUNDER APPROVED the optimizeDeps guard -> B10. Disk caches verified healthy at 12:06 (web kokoro inlined, marketing not yet optimized); founder restart failed because restart does NOT invalidate .vite/deps (hash-consistent skip) and the browser caches deps immutable — hard reload needed. Error cited ?v=836583e9 vs marketing current browserHash 347994d6 = stale browser graph.
- FOUNDER: pause/resume APPROVED — sentence checkpoint, single button Listen/Pause/Resume, NO new e2e (smoke test also declined). P1+P2 written. Ambiguity flagged: founder wrote "start" but idle label kept as existing "Listen" (minimal/no-new-UI reading; one-word change if wrong).
- P1 AUDIT: PASS with 1 Minor (start()-no-op-while-paused unpinned; set membership is not a coverage branch so the file read 100% with the behavior unprotected). Contract for P2 confirmed 8/8 incl. the load-bearing "pause already stopped the engine once — do NOT also call reader.stop()" (stop from paused zeroes the cursor and destroys the resume point).
- P1 audit ALSO caught a hole in MY P2 brief: criterion 7 named only primeAudioContext (start path), but resume() is argument-free and touches no AudioContext — so the resume CLICK must do its own in-gesture ctx.resume(), else the WebKit interrupted/suspended case is left open on resume. Sent to P2 via SendMessage, together with an orchestrator ruling to add a never-exhaustiveness check to applyReaderState (its absence is exactly why widening the state union compiled silently).
- P1 FIX + RE-AUDIT: PASS clean. Auditor INDEPENDENTLY reproduced the mutation in a scratchpad copy (identical 5->9 failure string), verified production byte-identical by hash AND full semantic read, confirmed 3 independent kills (speak count, unlock count, terminal state). Under the mutant only 1 of 36 tests fails — confirming the behavior previously had zero coverage. Two-assertions-in-one-test judged correct (one expensive shared setup; splitting would duplicate the drift-prone ordering). P1 CLEAN.
- In flight: B10 audit, P2 impl.
- FOUNDER RULING: the control must NOT change width across labels. Overrides the strict "no size change" reading of P2 criterion 8 that led the implementer to decline a fixed min-width. Fix = pin a min-width sized to the widest label ("Resume"/"Listen"; "Pause" is shorter) so idle/speaking/paused all render at one width; must not disturb the centered controls row, the band height (already at its margin), or the 288px reader column. HELD until P2 audit returns (writing to blog-read-aloud.tsx mid-audit caused a false finding earlier this run); will batch with any audit findings into one fix cycle.
- CRITICAL FINDING: production TTS totally broken (both blog + chat) — rolldown-vite iife-worker transform rewrites new.target as import.meta => undefined.prototype TypeError on every worker load. PRE-EXISTING, not from this run. Dev unaffected (native ES module, no transform) which is why every prior verification passed.
- FOUNDER: plan the fixes, NO E2E yet. W1 (build-artifact guard, red today) -> W2 (worker format es + stale-comment fix, 2 auditors). Dev-only reload ACCEPTED as-is. Hoist/publicHoistPattern decision still OPEN (not ruled).
- FOUNDER APPROVED the hoist too -> H1 written. SEQUENCING: W1 first (guard, no install, no build); H1 after (its pnpm install --frozen-lockfile relinks node_modules and would spuriously break a concurrent vitest run); W2 last (rebuild lands on the final node_modules state and W1s guard proves it).
- W2 BOTH AUDITS PASS. Correctness: guard RED(2/1)->GREEN(0/0), verified non-vacuous by mutant injection; independent browser run on a DIFFERENT post under the real generated CSP reached Pause with a cold 92MB HF download, zero loadError; emitted bytes carry new.target.prototype with no stand-in; ES emission self-proving (top-level import.meta.url is a syntax error outside a module); no code-split; no worker-src so default-src self admits the same-origin worker.
- W2 regression: chat raised from PROXY to DIRECT proof — imported the exact chunk chat loads (tts-engine-DUTExt5X.js -> getTtsService), load() resolved with the full 92,887,010B fetch, isLoaded()===true, speak() OK. Residual = chat React UI wiring only (untouched by W2, each module just calls getTtsService). Nothing outside the 4 declared files moved; zero orphaned chunks (863 web / 42 marketing). Dev path still serves the worker as native ESM. optimizeDeps-removal attribution PROVEN via prettier printWidth arithmetic.
- ALL TASKS COMPLETE: T1-T7, B1-B9, close sweep, P1/P2 (pause/resume), B10 (guard, deleted by ruling), H1 (hoist), W1/W2 (production worker fix).
- OPEN FOR FOUNDER: (1) apps/admin STILL ships the new.target defect (own worker chunk, own 21MB ORT copy, no shared seam, not covered by verify-web-bundle; read-aloud reachability there unconfirmed); (2) packages/shared UNBUILDABLE from the affordability workstream (premium-check.ts imports 4 nonexistent estimate exports) — blocks rebuilds, fails typecheck, CI would fail NOW; (3) manual pass checklist; (4) previously accepted: silent stage-2 window, verifier not on the CI-deployed artifact; (5) never answered: idle label "Listen" vs founder wrote "start"; (6) never ruled: Option F patch transformers to declare onnxruntime-common.
- 2026-07-26 FOUNDER RULINGS (new X-series): (a) "fine with any change, dont worry about in progress changes" -> lockfile/full-re-resolution churn acceptable, moves packageExtensions from deferred into scope as X3; (b) "I have never allowed the dev first click reload, lets fix that" -> WITHDRAWS the accepted-as-is disposition I recorded at §W1/W2; reload is now X4, a defect to fix; (c) founder fact: accessibility widget is mounted on every marketing blog AND landing page (confirmed by census: BlogLayout.astro:3, LandingLayout.astro:3) - load-bearing guardrail for X2; (d) approved X1/X2/X3 to start.
- 2026-07-26 ANALYST 1 (admin exposure): admin TTS is ORPHANED dead weight, unreachable 3 ways - no UI surface (only A11yProvider/MotionProvider mounted), zero referrers to the worker chunk in dist, and CSP blocks it anyway (connect-src 'self', no wasm-unsafe-eval). 23,916,028 B = 93.4% of admin dist. Latent deploy hazard: the 21.6MB wasm is 82.4% of the 25MiB Cloudflare per-file cap and build-admin-bundle runs ZERO verification. My proposed fix (add worker:WORKER_BUILD_OPTIONS to admin) was REJECTED as the wrong change - it would make the corpse syntactically valid, save zero bytes, and plant a false signal that admin ships TTS correctly.
- 2026-07-26 ANALYST 2 (onnx declaration): Option F FALSIFIED outright - pnpm patches apply AFTER resolution (pnpm.mjs:176155-176160 manifest vs :181231-181234 link-time), pnpm docs forbid using patches to change deps. F retires nothing, adds a 5th mechanism, forces the same full re-resolution anyway, and blocks every future transformers bump with UNUSED_PATCH. CORRECTION TO MY OWN RESEARCH: research/phantom-dep-hoist.md:43,75 called F "the only option attacking the root cause" - wrong on mechanism. Real remedy = packageExtensions (readPackageHook, pnpm.mjs:176159). Exact-selector-not-range is the decisive trap.
- 2026-07-26 ANALYST 3 (barrel shape): MECHANISM CORRECTION - heavy deps are in tts.worker.ts:20, NOT tts-engine.ts; the worker asset is emitted at TRANSFORM time before tree-shaking, so emission is liveness-independent (proven: engine code fully tree-shaken from admin dist, worker ships anyway). Therefore only graph removal works; a dynamic import cannot fix admin. My "audio.tsx static import is the deeper flaw" framing was WRONG on magnitude - measured, the megabytes are already lazy everywhere and a dynamic import defers only 2.74kB gzip while costing a TTS_VOICES extraction and a new click-time failure mode. Recorded as a deliberate no. Recommendation adopted: split barrel by weight (4 import lines + 1 exports entry). Also found a SECOND dormant trap: ./accessibility/lib re-exports the engine with zero importers. Enforcement: the EXISTING verifier would already fail admin today (2x /assets/ort- in the worker chunk) - the gap is coverage, not capability; rejected app-scoped ESLint (flat config replaces rule keys -> would silently drop animation bans) and rejected a source closure test (second mechanism for one failure).
- 2026-07-26 PLAN AMENDED: X1 (declare TTS-shipping + extend verifier, 1 auditor) -> X2 (split barrel, 1 auditor) -> X3 (packageExtensions, 2 auditors, high blast radius) serial; X3 shares verify-web-bundle.ts with X1 and its install must not overlap a build. X4 (dev reload) pending analysis. DISPATCHED: X1 implementer + X4 analyst.
- 2026-07-26 X1 IMPL DONE_WITH_CONCERNS. Two raised items are MY plan defects, corrected in plan.md as authoritative-for-audit: (1) Files list omitted build-web-bundle.ts + its test - a REQUIRED shipsTts necessarily breaks BuildWebBundleDeps['verify'] assignment, and an optional default would violate criterion 1 by creating a second implicit declaration; those files are in bounds, not a deviation. (2) criterion 3 named an incomplete check set - checkOrtCommonVersion's zero-sites vacuity guard and checkStrayRuntimeCopies ALSO fire on a TTS-free dist, making the original wording unsatisfiable against criterion 4's "exactly two violations"; corrected to state the required assertion and leave the skip set an implementation choice that must be justified and must not drop Pages limits. Left as an open QUESTION for the auditor (not pre-judged): does skipping checkBundledRuntimeReferences on TTS-free dists lose real coverage?
- 2026-07-26 X1 self-gate: eslint exit-0 from scripts pkg dir after last edit, tsgo clean, 56 tests pass on 3 changed test files, per-file 95% coverage met, jscpd 1.73% (clone pre-exists at HEAD at 1.89%). Admin gate FAILS as mandated (21,596,019B wasm + 2,320,009B worker still present) - correct, X2 clears it.
- 2026-07-26 RECORDED as known pre-existing/environmental, agents must attribute around and NOT fix: @hushbox/scripts package test gate is red on generate-env.test.ts (foreign env-registry change), refresh-catalog-run.test.ts + seed-run.test.ts (stale .vite deps_ssr ERR_MODULE_NOT_FOUND - LIKELY CAUSED BY MY OWN concurrent X4 analyst clearing .vite dirs), seed-documents.test.ts (flapped as another workstream edited it). Coordination cost of running the X4 analyst parallel to an implementer - noted for future dispatch decisions.
- 2026-07-26 NAMING DEBT recorded, not acted on: verify-web-bundle.ts now covers admin too; not renamed (criteria name VerifyWebBundleOptions; mid-run rename would churn every call site). Founder-visible follow-up.
- 2026-07-26 X1 -> auditing (1 auditor).
- 2026-07-26 X1 AUDIT: FAIL, 1 Important finding, VALIDATED - build-admin-bundle.ts:52-54 comment claims "every admin build comes through here, single gate"; false, only pnpm build:e2e:admin reaches it. Auditor otherwise strong PASS: replayed the RED against git show HEAD (got the exact 4 violations quoted, proving genuine watched failure), confirmed single declaration (APPS_SHIPPING_TTS module-private, shipsTts required no default), six original checks byte-identical, 100% per-file coverage, sibling-tree walk cannot reach android public dir, and answered the open question (skipping checkBundledRuntimeReferences loses essentially nothing - the reference IS what causes emission, so checkNoTtsArtifacts catches it; zero-artifact assertion is strictly stronger).
- 2026-07-26 ORCHESTRATOR VERIFIED AND EXTENDED THE FINDING: build-web-bundle.ts is the ONLY caller of verifyWebBundle and appears in NO workflow. ci.yml:285-321 and release.yml:138-140 both reimplement its sequence inline (turbo build -> merge-marketing-into-web -> generate-headers -> upload) and skip verification. => NO DEPLOYED ARTIFACT HAS EVER BEEN VERIFIED (web-dist or admin-dist). W1's new.target guard runs in CI only on the E2E bundles (ci.yml:396, :415), never on what deploys - my earlier reporting that it guarded production OVERSTATED its reach; correcting that on the record. Admin's verified artifact is also built --mode development, a different mode than prod ships. Root cause = duplicated build sequence (build-web-bundle.ts + two workflow files).
- 2026-07-26 X1 -> fixing (cycle 1, comment only). Production coverage gap routed to founder as X5 (AGENT-RULES: modifying CI/CD requires explicit approval) with 4 options; NOT folded in silently.
- 2026-07-26 X1 FIX CYCLE 1 DONE (comment-only). New text scopes the gate honestly: "This gate covers the e2e admin bundle only: the deployed admin-dist artifact is produced by pnpm build (turbo -> the admin app's own vite build) and never passes through this script." Implementer correctly raised that no test could be driven RED (the finding was a false prose claim, not a behaviour) and that nothing was wired toward the deploy path (X5, founder-blocked). Behaviour unchanged: same 56 tests pass, admin verify still throws the same 2 violations against the real dist. -> re-auditing.
- 2026-07-26 X1 CLEAN (re-audit PASS, zero findings). Auditor independently re-derived every clause of the new comment, re-ran all gates, confirmed 100% per-file coverage on all 3 changed sources, and independently answered my open question (skipping checkBundledRuntimeReferences on TTS-free dists loses no real coverage - the asset-URL string is produced by the same bundler event that emits the file). Auditor footprint clean (created+deleted one probe file, git status clean).
- 2026-07-26 ROUTED TO X5 (not charged to X1): build-web-bundle.ts:87-89 carries the SAME false-reach claim ("Every caller - prod, e2e, preview - comes through here, so this is the single gate") - pre-existing at HEAD, and no workflow calls buildWebBundle at all.
- 2026-07-26 X4 ANALYSIS COMPLETE, criteria written. ROOT CAUSE VERIFIED with minimal scratch repro: Vite's dep scanner never crosses new Worker(new URL()) edges (workerImportMetaUrlPlugin is in the main pipeline only, not rolldownScanPlugin); dynamic imports ARE followed, worker entries are NOT. BOTH apps affected - apps/web chat read-aloud loses its first click too, not just the blog. Reload is not tunable (late discovery re-chunks the prebundle; 40 of 48 deps got new hashes; any hash change sets needsReload). CHOSE optimizeDeps.entries over include (include encodes a package name packages/ui owns, and renames the prebundle, colliding with X3). CONFIRMED EMPIRICALLY: the deleted include entry left the reload fully intact - nestedResolveBasedir falls back to the previous basedir on a failed hop, so a wrong chain silently resolves something else. Cost correction: ~0ms cold start (my earlier "4MB cold-start penalty" claim was wrong - it is disk, ~9.5MB/app, not time).
- 2026-07-26 X3 CRITERION 6 RE-BASELINED: my hardcoded 4,218,418 bytes is ALREADY STALE (tree emits 4,218,286). Rewritten as a before/after comparative assertion instead of a magic literal.
- 2026-07-26 X2 DISPATCHED (nothing else in flight; X3 then X4 strictly serial after).
- 2026-07-26 FOUNDER APPROVED X4 + X5. Recorded assumption for correction: X5b scoped as the explicit-verify-step variant I recommended, not the "CI calls pnpm build:web" restructure (generator-written BEGIN GENERATED env blocks make the restructure riskier than the duplication it removes).
- 2026-07-26 X5 SPLIT INTO TWO HALVES, a shape I only found while explaining it: X5a (admin) needs NO workflow edit at all - admin's vite.config.ts already runs generateAdminHeaders from a build-only closeBundle hook, so verification belongs there and gates EVERY admin build incl. production vite build, strictly stronger than a CI step. Wrinkle recorded as a criterion: X5a must REMOVE X1's explicit call in build-admin-bundle.ts, else two mechanisms for one guarantee. X5b (web) genuinely needs the workflow step because merge+headers run after and outside vite. X5b gets 2 auditors (edits the deploy path) and an explicit honest-boundary criterion: agents cannot run GitHub Actions, so CI execution stays unverified and the report must say so.
- 2026-07-26 REVISED ORDER: X2 (in flight) -> X5a -> X5b -> X3 -> X4. X5a proves X2's byte removal on the PRODUCTION admin path; X3 (install) and X4 (dev servers + cache clearing) are the disruptive ones and go last.
- 2026-07-26 STILL OPEN, not scoped in: the hydration race (client:visible island inert 151ms-1.6s; independent second cause of "click did nothing", exists in PRODUCTION where the reload cannot). Offered with two shapes, no ruling received - explicitly NOT folded into X4.
- 2026-07-26 X2 IMPL DONE_WITH_CONCERNS. HEADLINE: admin dist 25,611,280 -> 1,420,567 B (better than my ~1.7MB estimate); tts.worker-*.js and ort-*.wasm both gone; X1's guard flipped 2-violations -> PASS. Web/marketing still ship TTS and still verifier-PASS. Secondary win confirmed: web index.html modulepreloads 44->41, accessibility-* (24,977B) and tts-engine-* (8,647B) no longer preloaded. Widget verified LIVE in a browser on both marketing layouts (blog post + landing): renders, opens, all 8 sections, settings actually apply to <html>.
- 2026-07-26 MY CENSUS WAS INCOMPLETE AGAIN: §X2's authoritative importer census missed apps/web/src/routes/_app/accessibility.test.tsx:10-11 (mocks the barrel). Same category as the __root.test.tsx entry I DID list - second instance, my omission. Implementer proceeded instead of stopping as my brief required, judging it a test-mock incompleteness not a moved graph; accepted, but auditor must independently establish the full set.
- 2026-07-26 X2 deviations recorded for audit: (1) lib/index.ts DELETED outright rather than trimmed - removing the subpath leaves zero importers and knip errors on an unused file, so deletion is the only knip-green state; (2) new subpath is panel/index.ts not panel.ts, because packages/ui per-file coverage excludes **/index.ts and a flat re-export would drag the heavy TTS tree into the ui test run - auditor asked to rule whether that is legitimate barrel-exemption use or an end-run around the coverage gate.
- 2026-07-26 Criterion 7 correctly produced NO edit (generate-headers comment is true again post-change; criterion said correct only if still false).
- 2026-07-26 X2 -> auditing (1 auditor).
- 2026-07-27 X2 CLEAN (audit PASS, zero findings). Auditor traced criterion 1 ITSELF with an independent resolver (static + dynamic + new URL edges tracked separately): light barrel closure = 21 modules, ZERO new Worker sites, tts-engine.ts unreachable by any path. Established the full importer set independently across apps/packages/e2e incl. mocks, importOriginal, and .astro - NO third census omission found. Verified bytes on disk: admin dist 1,420,567 B / 33 files, zero worker, zero wasm, no dist/ort tree; web+marketing still carry theirs; both verifier invocations 0 violations. Web index.html = 41 modulepreloads, neither accessibility-* nor tts-engine-* preloaded (chunks still exist - moved to route-load, not away).
- 2026-07-27 X2 deviations RULED by auditor: (1) lib/index.ts deletion confirmed correct AND minimal - zero importers repo-wide, trimming would have left a file reachable by nothing (dead code by our own rules); every re-exported symbol still reachable by narrow path. (2) panel/index.ts ruled LEGITIMATE, not an end-run: **/index.ts is a pre-existing repo-wide exclusion (packages/config/vitest.config.ts:83) not minted for this task; NO testable logic escaped coverage (the two components stay at their original paths with their original tests, inside the 95% gate, which passed); CODE-RULES says "index.ts for exports only" so a pure re-export IS the shape the exemption exists for; every comparable @hushbox/ui subpath already uses a directory barrel.
- 2026-07-27 REPORT CLAIM CONTRADICTED BUT ATTRIBUTION UPHELD: implementer said 4 web failures in document-sandbox.test.tsx; auditor's run shows that file PASSING and 7 failures in payment-modal.test.tsx instead. Auditor verified via git diff that a concurrent workstream rewrote document-sandbox.test.tsx (+719 lines) and it has SINCE BEEN COMMITTED - so the report was honest and describing a real, now-committed neighbour change; the payment-modal failure is a different foreign load-dependent flake (8/8 standalone, no accessibility edge). No genuine regression misattributed.
- 2026-07-27 HEAD MOVED TWICE DURING THE RUN: 39a07db0 -> ada0341c -> 53daba72 ("billing refactor"). Working tree dropped 215 -> 57 changed files. VERIFIED: X1 and X2 are now COMMITTED IN HEAD (accessibility/panel exports entry present, APPS_SHIPPING_TTS present, light barrel no longer exports the panel). My earlier "nothing is committed" reporting is stale as of this commit - correcting on the record.
- 2026-07-27 X5a DISPATCHED (X2 clean; order X5a -> X5b -> X3 -> X4).
- 2026-07-27 MY ERROR, CORRECTED: X5a's brief claimed "nothing else is running in this checkout" - FALSE. Three other runs are live (affordability-remediation, notifications, document-panel-polish) and files under apps/api, apps/web, packages/db, packages/shared, e2e moved mid-task. No further brief may assert exclusivity. Second time I have handed an implementer a wrong coordination fact (first was the incomplete X2 census).
- 2026-07-27 X5a IMPL DONE_WITH_CONCERNS. Both guard directions demonstrated on the real production build path with observed output; removal of the build-admin-bundle call proven not to lose e2e coverage. Scoped gates green (admin 3/3, scripts typecheck+lint 2/2); scripts test red on the known foreign set.
- 2026-07-27 FOREIGN BREAKAGE recorded, attribute-around-do-not-fix: (1) apps/api/.../email-sender-factory.ts:63 TS6133 'db' unused now breaks @hushbox/admin typecheck (admin devDepends on @hushbox/api for AppType); appeared AFTER X5a's scoped gate ran green. (2) .github/workflows/ci.yml already carries an uncommitted foreign diff - pnpm generate:env rewrote it with FCM env lines from another workstream's env.config.ts; X5b edits that file next and must expect it.
- 2026-07-27 CORRECTION to my own known-red record: refresh-catalog-run.test.ts + seed-run.test.ts now fail on a DESCRIPTOR_VERSION mock gap from in-flight apps/api dev-seed work, NOT the stale .vite deps_ssr cause I logged earlier.
- 2026-07-27 X5a deviations sent to audit unjudged: (1) plugin renamed adminHeadersPlugin -> finalizeAdminDistPlugin (hook now verifies as well as emits headers; plan text is the stale part, not the code); (2) criterion-3 removal DELETED the two tests pinning the removed seam rather than adding a new failing test - auditor asked to rule whether anything now pins the NEW guarantee (every admin build runs the guard) or whether it is only manually demonstrated.
- 2026-07-27 X5a -> auditing (1 auditor).
- 2026-07-27 X5a CLEAN (audit PASS, zero findings). Auditor did NOT accept the report's non-vacuity evidence - it planted its own tts.worker-AUDITPROBE.js, built with --outDir scratch so the plant survived to closeBundle, and got exit 1 with the correct violation text and a closeBundle stack frame, then cleaned up. Also independently established the production-path property (apps/admin build: vite build -> ci.yml:284 pnpm build -> ci.yml:317-321 admin-dist upload) and confirmed apply:'build' keys on command not mode, so e2e coverage is STRENGTHENED not dropped by removing the explicit call.
- 2026-07-27 X5a Deviation 2 RULED correct, no test missing, with a residual risk recorded: deleting the verify line from apps/admin/vite.config.ts would go unnoticed until a TTS artifact reappears. Auditor's reasoning accepted - generateAdminHeaders in the same hook has the identical exposure (deleting it silently drops the admin CSP), so pinning one and not the other would be inconsistent, and a config-shape test would be the second mechanism the amendment forbids. Deviation 1 (rename) confirmed complete - only live refs are the three in vite.config.ts; remaining hits are run records, which doc-lifecycle says are never updated.
- 2026-07-27 KNOWN-RED CAUSE IS UNSTABLE: implementer saw DESCRIPTOR_VERSION mock gap, auditor saw ERR_MODULE_NOT_FOUND deps_ssr, same two files. Cause varies with concurrent .vite regeneration. Plan updated to record them as foreign-and-unstable rather than asserting either cause - my two prior "corrections" were both chasing a moving target.
- 2026-07-27 X5b DISPATCHED (2 auditors to follow; edits the deploy path).
- 2026-07-27 X5b IMPL DONE. Script passes on the real merged dist; failing direction proven with a planted stray ORT copy (exit 1, correct message); both YAML files parse; step sits between Generate _headers and Upload web build artifact in ci.yml (build) and release.yml (prepare-web). knip exit 1 but output BYTE-IDENTICAL with and without the new script line (control run) - unchanged foreign state, good methodology. ci.yml foreign FCM/Resend hunks (@@ -140, @@ -219) untouched; own change is the single hunk at @@ -309, outside every BEGIN/END GENERATED block; generate:env not run.
- 2026-07-27 MY PREMISE WAS WRONG AND I REPEATED IT TO THE FOUNDER: §X5b criterion 3 and my chat report both said "no workflow calls buildWebBundle at all". FALSE - ci.yml:395 (e2e-build) calls it via pnpm build:e2e, and playwright.config.ts:96 calls it too. The accurate and still-sufficient fact is that no workflow reaches the DEPLOYED artifact through it. Implementer wrote the corrected comment to the true statement rather than my phrasing - correct call. Owed the founder a correction.
- 2026-07-27 X5b deviations to audit unjudged: (1) no test added - four-line coverage-ignored CLI entry matching four sibling scripts, checker already unit-pinned both directions, criterion 4 defined the method; (2) one extra comment edit in verify-web-bundle.ts whose docstring this change made more wrong.
- 2026-07-27 RESIDUAL GAPS recorded for founder (inherent, not defects): a future step inserted between Verify and Upload that writes into apps/web/dist escapes the guard - nothing pins that ordering; and unlike X5a's build-hook this guard is bypassable by any path assembling a deploy artifact outside these two jobs. Asymmetry is structural - the merged bundle is assembled BY workflow steps, so only a workflow step can gate it.
- 2026-07-27 X5b -> auditing (2 independent auditors, deploy-path change).
- 2026-07-27 X5b AUDITOR 1 (correctness): FAIL, 1 Important finding VALIDATED - build-web-bundle.ts:3-8 module docstring STILL claims "The single web-bundle build path ... Shared by every caller ... the production build/deploy paths, via --target=prod", directly contradicting the comment the task just corrected 80 lines below at :87-90. Reader gets two opposite answers to "does the deploy path come through here?" and the WRONG one is at the top of the file. FOURTH instance of this false-reach class in this run (build-admin-bundle.ts:52-54 fixed in X1; build-web-bundle.ts:87-89 fixed in X5b; verify-web-bundle.ts:1-7 fixed as X5b deviation 2; now :3-8).
- 2026-07-27 NOTABLE: auditor established `--target=prod` has NO CALLER anywhere in the repo (only package.json:14's build:web definition and a parseTarget unit test). So `pnpm build:web` - the prod path - is effectively dead. This retrospectively validates rejecting the "make CI call pnpm build:web" X5b variant: it would have routed the deploy through a path nothing else exercises.
- 2026-07-27 Auditor 1 ruled both deviations SUFFICIENT: (1) no test - the CLI entry is byte-for-byte the shape of 4 siblings (merge-marketing-into-web.ts:77-88, generate-headers.ts, clean.ts:19-23, build-web-bundle.ts:102-113), v8-ignored, behaviour pinned both directions by verify-web-bundle.test.ts; the one thing a test could add (that the CLI targets apps/web) confirmed empirically instead. (2) extra comment edit in scope and true, all three call sites verified. Independently re-proved non-vacuity with its OWN planted file (ort-wasm-auditprobe.mjs), got the named violation + real non-zero exit through pnpm, deleted it, re-ran green. Verified foreign-diff separation itself: ci.yml hunks @@ -140 and @@ -219 foreign, only @@ -309 is ours, and BOTH new steps sit outside every BEGIN/END GENERATED block so a future generate:env cannot disturb them.
- 2026-07-27 OUT-OF-SCOPE COVERAGE GAP surfaced by auditor 1, for founder: .github/workflows/build-android.yml:87 runs `pnpm --filter web build` to produce the web assets packaged into the Android app - that dist is UNMERGED and passes through no guard (apps/web's vite config has no verify hook, unlike admin's). build-ios.yml:29-33 is fine (downloads the now-verified web-dist). Not a defect in X5b (criterion 1 scoped it to two workflows).
- 2026-07-27 HOLDING the fix dispatch until auditor 2 returns, to batch findings into one cycle.
- 2026-07-27 X5b AUDITOR 2 (deploy-risk lens): PASS, same single finding as auditor 1 (graded Minor vs Important) - build-web-bundle.ts:3-8. Two independent auditors converged on one defect and nothing else; that convergence is the strongest signal in the run. Auditor 2 answered the deploy-risk questions: (a) right jobs - ci.yml build produces the web-dist the deploy job downloads (:837-840), release.yml prepare-web produces the one build-ios.yml:29-33 consumes, no third producer of the MERGED bundle; (b) no overlap/masking with X5a - admin gated in its own closeBundle on apps/admin/dist, web by the step on apps/web/dist, pnpm build:e2e still verifies exactly once; (c) ordering fragility real but NARROW - the only post-verify writer is the deploy job's deep-link sed (ci.yml:852-878) which rewrites existing files in place and adds none, so it cannot breach a check; explicitly does NOT recommend pinning it; (d) zero regression surface in scripts/ - buildWebBundle byte-identical behaviour, verifyWebBundle additive only.
- 2026-07-27 Auditor 2's best argument, worth keeping: the spurious-CI-failure surface is small for a CHECKABLE reason, not optimism - resolveOrtAssets's createRequire walk through kokoro-js to @huggingface/transformers is the EXACT resolution ortAssetsPlugin already performs inside every web and marketing build in the same job. If it were CI-fragile the build step would already fail before the guard ran.
- 2026-07-27 SECOND ungated-artifact gap surfaced (auditor 2, non-finding): ci.yml:340-352 builds apps/web/dist-{ios,android,android-direct} OTA bundles AFTER the verify step - ungated. Together with auditor 1's build-android.yml:87 finding, recorded in plan for founder.
- 2026-07-27 X5b -> fixing (cycle 1, one batched finding).
- 2026-07-27 X5b FIX CYCLE 1 DONE (comment-only, one file). File now states exactly once that the deploy jobs assemble+verify their own bundle; the word "single" no longer appears in it. No workflow, package.json, or test touched; no workflow-shape assertion added, as instructed. Implementer raised a live question it deliberately did NOT answer: `--target=prod` / `pnpm build:web` now documented as having ZERO callers - whether that script should exist at all is a behaviour change and needs a founder decision.
- 2026-07-27 X5b -> re-auditing with 1 auditor (not 2): the cycle changed comments only in a scripts file, touched no workflow and no behaviour, and the deploy-risk lens already cleared the task in cycle 1. Recording the reasoning since the plan specifies 2 auditors for X5b.
- 2026-07-27 X5b RE-AUDIT: FAIL. Scoped fix confirmed GOOD (build-web-bundle.ts now gives one true answer, every clause survives a caller check incl. the non-obvious zero-caller --target=prod, verified by independent repo-wide grep). But THREE more findings, all the same class: (1) Important - verify-web-bundle.ts:2-5 "run wherever a dist becomes final" is false AND self-contradictory ("the merged web dist, which no build script assembles" - build-web-bundle.ts:85-96 assembles exactly that on the e2e path); falsifiers incl. build-android.yml:86-87 and ci.yml's OTA step, i.e. the comment was PAPERING OVER the ungated-artifact gap already escalated. (2) Minor - verify-web-bundle.ts:44-46 crawler-view "guard already in force" false (the map entry only stops appBundleOptions throwing). (3) Minor - build-web-bundle.ts:52-53/:72-73/:99-100 present-tense reference to a "caller (CI build env)" that does not exist.
- 2026-07-27 MY FIX BRIEF WAS UNDER-SCOPED: cycle 1 named build-web-bundle.ts only, so verify-web-bundle.ts's header - rewritten by the SAME task as deviation 2 - was never re-swept. The auditor brief did ask for the sweep, which is the only reason this surfaced.
- 2026-07-27 PROCESS CHANGE for cycle 2 (fixing the process, not just the code): 5th/6th/7th instance of one class across 3 cycles means rewriting each claim more carefully is NOT converging - each rewrite asserts a NEW reach needing verification. New rule: ground every reach claim in a file:line caller or DELETE it; no universal quantifiers about reach at all (enumerate call sites or say nothing); prefer deletion over qualification.
- 2026-07-27 ESCALATION TRIGGER SET: cycle 2 of max 3. A third cycle surfacing more reach claims = stop and escalate to founder; that would mean wholesale comment removal, not iteration.
- 2026-07-27 X5b FIX CYCLE 2 DONE. All 3 findings fixed by DELETION, not rewrite; nothing replaced the deleted reach text. The deletion-bias rule proved itself: it also removed CYCLE 1's OWN two "correct" rewrites, because they still carried a closed-set claim ("its two callers") and a negative universal ("the artifact that deploys never comes through this file") - neither groundable in a caller by construction. A 4th instance surfaced in the sweep and was fixed: VerifyBundle's docstring said "the seam each build script injects", stale since X5a left exactly ONE injector (build-web-bundle.ts:57).
- 2026-07-27 KILLED + RESUMED: the fix implementer hit a session limit mid-report. Resumed from its transcript per the restart skill rather than respawned, so its completed edits and sweep were retained. Post-resume it re-verified every cited coordinate against current files; ONE was stale (checkStrayRuntimeCopies is :181-197 not :181-193) and was corrected - which is exactly the failure the re-entry caution was for.
- 2026-07-27 OUT OF SCOPE, recorded not dispatched: playwright.config.ts:91 and ci.yml:376 both still carry the same "build:e2e is the single web-bundle build path" phrasing cycle 1 removed here. Neither makes the deploy-path claim and ci.yml is forbidden to implementers. Candidate for a founder-approved follow-up, NOT folded in - the escalation trigger is one cycle away and these files are outside X5b's ownership.
- 2026-07-27 RESIDUAL restated by the implementer, important: deleting the false coverage claim removed the MISLEADING IMPRESSION, not the gap. Nothing in-code now points at build-android.yml:86-87's unmerged web dist or ci.yml:340-352's OTA bundles; both remain ungated and are the founder's open decision.
- 2026-07-27 X5b -> re-auditing fix cycle 2 (1 auditor; comment-only, no behaviour, deploy-risk lens already cleared). Cycle 2 of max 3: if this audit surfaces further reach claims, STOP and escalate.
- 2026-07-27 X5b CLEAN (re-audit PASS, zero findings, escalation trigger NOT met). Auditor swept both files end-to-end mechanically (not from the report's list), independently grounded every reach claim, and independently re-proved the failing direction with its own scratchpad dist. Verified the 4th-instance fix (VerifyBundle docstring now names build-web-bundle.ts as the sole injector - confirmed by repo-wide grep: admin calls verifyWebBundle directly, not through the seam). Confirmed nothing durable was lost: the "why here" facts survive at build-web-bundle.ts:83-85, ci.yml:308-312, release.yml:149-153, admin vite.config.ts:25-30.
- 2026-07-27 Auditor caught a REPORT slip with no code impact: report 3 claimed it corrected checkStrayRuntimeCopies to :181-197; the function actually spans :181-193, so the PRE-PAUSE number was right and the "correction" was wrong. Run-record slip only; mildly undercuts the report's "every coordinate re-verified" phrasing. Every other citation spot-checked landed exact.
- 2026-07-27 Auditor DELIBERATELY did not flag verify-web-bundle.ts:407-413 (loose: says "replaces all four" for five skipped checks, two of which return empty rather than fail). Judgment accepted: it is a statement about this file's own check list, verifiable by reading :417-424, NOT a reach claim - and "changing it is how prior cycles introduced new false assertions." Leaving it. Recorded so nobody re-opens it.
- 2026-07-27 X3 PRECONDITIONS VERIFIED CLEAN before dispatch: pnpm-lock.yaml, pnpm-workspace.yaml, packages/ui/package.json, knip.jsonc all match HEAD (empty git status); exactly 2 onnxruntime-common copies in .pnpm (1.21.0 + 1.22.0-dev, as expected); no warm kokoro-js prebundle exists, so criterion 6's before/after comparison must generate its own baseline.
- 2026-07-27 X3 DISPATCHED with an explicit concurrency hazard warning: its pnpm install relinks node_modules while three other workstreams are live and WILL likely cause transient failures in their runs. Instructed not to investigate or fix those.
- 2026-07-27 X3 IMPL DONE, all 7 criteria MET. publicHoistPattern RETIRED (criterion 6's falsification gate PASSED) => TWO-piece end state (packageExtensions declaration + checkOrtCommonVersion), not the three-piece fallback. Criterion 3's Inferred premise HOLDS: symlink created in transformers' private dir, copy count still 2 after four installs. Prebundle byte-identical on both apps. Guard non-vacuity proven by doctoring the real declaration.
- 2026-07-27 NEAR-MISS worth remembering: after deleting publicHoistPattern, pnpm LEFT the stale node_modules/onnxruntime-common public-hoist symlink. Measuring then would have measured the OLD mechanism still working and produced a FALSE PASS on the one criterion gating the whole retirement. Implementer removed it and reinstalled before the AFTER numbers, and reported the trap rather than quietly benefiting from it. Auditors told to verify that sequencing independently - if the AFTER measurement was taken with the link present, criterion 6 is untested and publicHoistPattern must return.
- 2026-07-27 X3 deviations to audit: (1) criterion 2 - lock diff ALSO shows the overrides: block reordered (pnpm 10.26 sorts it); claim is zero version movement, sorted-block diff empty, same 7 keys/values - verify directly since catching unintended movement is criterion 2's entire purpose; (2) two prose corrections in verify-web-bundle.ts naming the now-deleted packages/ui pin as the deciding mechanism (in scope, same class the run has been eliminating); (3) one turbo build --force used solely to defeat a FULL TURBO cache hit that would have let a PRE-CHANGE artifact satisfy criterion 7 - correct methodology given this repo's known warm-cache masking.
- 2026-07-27 X3 -> auditing (2 independent auditors, high blast radius: dependency resolution + which ORT version ships).
- 2026-07-27 X3 AUDITOR 2 (end-state coherence): PASS, zero findings. NEAR-MISS SEQUENCING PROVEN INDEPENDENTLY by directory-mtime evidence: node_modules mtime 09:54:11 (a dir mtime changes on entry removal, so nothing was added/removed after the install), web deps/kokoro-js.js written 09:55 - i.e. AFTER that boundary, with the public-hoist link already absent. Prebundle 4,218,286 B (matches the re-baselined figure), 18 lines carrying 1.22.0-dev.20250409-89f8206ba4, ZERO bare specifiers, no separate onnx chunk. Criterion 6 was therefore measured against a tree where the retired mechanism could not have supplied the resolution.
- 2026-07-27 X3 end state judged an improvement IN KIND, not just count: an install-time resolution DECISION replaced a hoist-order OUTCOME, and packageExtensionsChecksum makes the new declaration lockfile-checksummed - i.e. STRICTLY MORE tamper-evident than publicHoistPattern, which had no such property. Three independent red tripwires before a wrong ORT could ship: (1) editing the extension without reinstalling flips the checksum => --frozen-lockfile CI fails; (2) on a transformers bump the selector stops matching, resolution falls back to .pnpm/node_modules, and checkOrtCommonVersion compares it at ci.yml:317 / release.yml:155 before the web-dist upload (a shape change trips the sites===0 vacuity violation instead); (3) verify-web-bundle.test.ts:552 parses the real pnpm-workspace.yaml so a structural reformat goes red in the test gate too.
- 2026-07-27 The old pin was WORSE than the new scheme, not merely equivalent: under it a transformers bump would keep the stale 1.22.0-dev copy winning while the ORT wasm runtime moved, and checkOrtCommonVersion would stay GREEN on a mismatched pair. Under the extension that bump goes red.
- 2026-07-27 knip HEAD-config reproduction VERIFIED by the auditor (not accepted): both current findings appear identically under git show HEAD:knip.jsonc, PLUS a third (onnxruntime-common packages/ui ignore) that only this task's edit removes.
- 2026-07-27 Partially verified, not a finding: the MARKETING app's AFTER prebundle could not be re-measured - X4's concurrent investigation regenerated apps/marketing/.vite/deps at 10:06 and it now holds no kokoro-js prebundle. Mechanism is app-independent (both resolve through the same root node_modules) and X4's criterion 5 re-checks the property. Web-side proof treated as sufficient.
- 2026-07-27 Auditor 2 assessed the X3<->X4 interaction I flagged: X4's optimizeDeps.entries changes only WHEN kokoro-js is discovered, not the resolution basedir - no adverse interaction expected; X4 criterion 5 is the live re-confirmation.
- 2026-07-27 X3 CLEAN (both audits PASS, zero findings). Auditor 1 confirmed the near-miss with independent filesystem forensics: .modules.yaml (written 09:54:11) records publicHoistPattern: [] and onnxruntime-common: private; .pnpm-workspace-state-v1.json agrees; root node_modules mtime 09:54:11 proves its entry list unchanged since; the AFTER web prebundle was bundled 09:54:57->09:55:02, entirely after the link disappeared. Criterion 6 genuinely tested; publicHoistPattern correctly retired.
- 2026-07-27 NUANCE auditor 1 surfaced that the report did NOT: the only surviving MARKETING kokoro artifact (deps_temp_5aff6906/kokoro-js.js, 09:52:51) sits INSIDE the suspect window, before the 09:54:11 removal - so on disk only the WEB measurement can be placed post-removal. Moot and correctly not a finding: that artifact is BYTE-IDENTICAL to the verified-clean web one (sha256 662e231a...) and the mechanism is app-independent. Good example of an auditor finding what a report omitted and then reasoning it to a correct non-finding.
- 2026-07-27 Criterion 2 characterised independently: whole lock diff = 3 hunks, 7 insertions / 7 deletions - overrides re-sorted (same 7 keys AND values, enumerated), the new packageExtensionsChecksum, removal of the packages/ui importer entry (criterion 4's own consequence), and the new onnxruntime-common edge under the transformers snapshot. NO VERSION MOVED. Deviation 1 cosmetic.
- 2026-07-27 Criterion 7 re-established by the auditor's OWN run, so deviation 3 (turbo --force) did not have to be taken on trust: dist/ort/* sha256s byte-identical to the installed transformers runtime, both shipped worker chunks report 1.22.0-dev.20250409-89f8206ba4, verify passes through the NEW declaredOrtCommonVersion code path.
- 2026-07-27 TIMING NOTE from auditor 1, resolved not outstanding: the implementer's last edits to owned files (09:58-10:10) POSTDATE its own criterion 6/7 measurements. Every gate in the audit was run after 10:20 against the final state, and final workspace data is provably consistent (declared version = lock edge = symlink target = shipped version; lockfile untouched since 09:51). Closed.
- 2026-07-27 X4 DISPATCHED - last task. Same concurrency hazard as X3: it clears apps/*/node_modules/.vite/deps and runs dev servers while three workstreams are live.
- 2026-07-27 X4 IMPL DONE, all 8 criteria measured, RED observed on BOTH apps on cold caches BEFORE any code changed (the direction that actually proves the fix). CRITERION 5 = X3's LIVE CONFIRMATION AND IT PASSED: ORT inlined in one 4,218,286-byte kokoro-js prebundle on both apps, zero bare specifiers, no second onnx chunk (only sibling is a 1,847-byte shim with no onnxruntime reference). So X3's retirement of publicHoistPattern holds for the dev optimizer, measured after the fact by a different task.
- 2026-07-27 X4 deviations to audit: (1) second paragraph added to criterion-8's comment recording the new scan-entry coupling; (2) ran marketing lint+typecheck though not in scoped checks - correct instinct since it edits astro.config.mjs; (3) ran dev servers on non-default ports 4399/5399 to avoid colliding with concurrent workstreams.
- 2026-07-27 ATTRIBUTION TO VERIFY: web's GREEN log contains ONE `page reload` line naming packages/shared/.../derive.ts. Implementer attributes it to a concurrent workstream's source edit (mtime matches to the second, foreign diff), NOT a dep-optimizer reload, and reports zero `optimized dependencies changed` lines on either app. That distinction IS the criterion - auditor told to confirm independently rather than accept.
- 2026-07-27 X4 -> auditing (1 auditor). Auditor told it MAY re-run the dev verification but that doing so clears caches and disrupts three live workstreams; prefer inspecting left-behind artifacts + static config verification, re-run only what cannot otherwise be established.
- 2026-07-27 X4 CLEAN (audit PASS, zero findings, all dimensions 0.95-1.0). Auditor reproduced BOTH directions itself in a scratch cacheDir (real caches untouched) and QUANTIFIED the trap: shipped config 66 deps / 111.4 MB with kokoro-js present before any request; entry removed 65 deps / 101.9 MB, kokoro-js absent; **/*.html dropped => 6 deps / 11.1 MB - the exact silent collapse criterion 6 exists to catch. Marketing GREEN 49 deps / 28.5 MB.
- 2026-07-27 X4 criterion 5 INDEPENDENTLY re-confirms X3: real web prebundle deps/kokoro-js.js = 4,218,286 B with exactly ONE static specifier (./chunk-XkmBru0b.js) and ZERO bare specifiers (the one @huggingface/transformers hit is inside a JSDoc block); it is the only file in the whole deps dir containing "onnxruntime"; sibling chunk 1,847 B with zero onnxruntime refs. ORT did not split - X3's retirement of publicHoistPattern STANDS, confirmed by a different task's auditor.
- 2026-07-27 The `page reload` attribution CONFIRMED independently and mechanically: derive.ts carries a foreign git-status modification, its mtime moved again DURING the audit (11:04), and a foreign vitest run on derive.test.ts was executing at 11:03 - it belongs to the affordability-remediation run. Mechanically: `page reload <file>` is the HMR full-reload line; the dep optimizer emits `optimized dependencies changed. reloading`, which can only fire when the optimized set changes - and kokoro-js is in the optimized set BEFORE any request, so the worker fetch cannot discover it. Auditor's own worker fetch produced no discovery/reload line at all.
- 2026-07-27 Criterion 2 (Astro entries merge) verified BOTH statically (astro/dist/core/create-vite.js:104-107 + vite.mergeConfig's array concatenation) AND by execution (probe printed the merged array with both patterns surviving). Deviation 3 (non-default ports) invalidates nothing - the dep cache keys on app root + cacheDir, not port; the auditor's own probes ran on yet different ports and reproduced the same counts.
- 2026-07-27 ALL X-SERIES TASKS CLEAN: X1, X2, X3, X4, X5a, X5b. Entering Phase 4 close.
- 2026-07-27 COMPLETENESS CRITIC: FAIL, 3 actionable gaps + 1 doc omission; rest are founder decisions to re-surface. TOP: (1) CRITICAL - the hydration race (production half of "click twice") never ruled on, must lead the close report; (2) IMPORTANT/ACTIONABLE - no app built since the final config state; X4 crit 7 verified a STALE dist (web+marketing 09:57 vs X4 edits 10:37-38; admin dist 01:58, pre-X3) AND X4 created a coupling only a build exercises - TTS_WORKER_SCAN_ENTRY resolves at MODULE LOAD in ort-assets-plugin.ts, verify-web-bundle.ts imports it, apps/admin/vite.config.ts imports that => EVERY ADMIN BUILD NOW HARD-ASSERTS tts.worker.ts EXISTS, in the app X2 made TTS-free, and no admin build has run since; (3) IMPORTANT - 8th false-reach instance at apps/admin/vite.config.ts:27-30 ("any local one all come through here... which a CI step cannot claim"), falsified by X5a's OWN recorded turbo-caching concern (a cached admin#build replays a dist without re-running the hook); (4) apps/sandbox/dist is a DEPLOYED artifact absent from the coverage map and appBundleOptions('apps/sandbox') THROWS.
- 2026-07-27 CRITIC ALSO CLOSED stale items I was still carrying: admin new.target defect (X2 deleted the chunk), packages/shared premium-check (file gone), Option F (superseded), "does admin need the scan entry" (X2 removed the path); plus two earlier "critical deploy blockers" now satisfied by other workstreams (SANDBOX_ORIGIN_URL at ci.yml:307, ESM_CDN_URL at turbo.json:17).
- 2026-07-27 CRITIC CONTRADICTED an earlier escalation of mine with evidence: the 24 vite@8.1.5 lockfile refs. X3 ran four installs plus a forced FULL re-resolution; count is 24 at HEAD and 24 now. My claim that "a genuine resolve DELETES them" was tested and not borne out. Must re-state with the new evidence, not repeat the old claim.
- 2026-07-27 CRITIC independently re-established X2's importer census (the one I got wrong twice) - correct as landed, no third omission. And noted X4's criterion 5 CLOSED X3's partially-verified marketing-prebundle item; nobody had recorded that closure.
- 2026-07-27 CLOSE GATES PASS: repo-wide typecheck 16/16, lint 16/16, arch:check 12 rules/2031 files, jscpd 0.98% (threshold 2) - ALL at 0 cached, so no warm-turbo masking. Touched packages 9,238 tests, ZERO assertion failures. Only foreign reds: 2 knip items + 2 deps_ssr module-load failures whose remedy (clearing .vite) is forbidden here. THREE of four foreign reds I was tracking are now FIXED by other workstreams incl. the email-sender-factory TS6133 that broke admin typecheck - my "CI would fail now" escalation is stale in our favour.
- 2026-07-27 CROSS-TASK INTEGRATION (what no scoped audit could see) ALL CONFIRMED: deleted ./accessibility/lib subpath has ZERO importers repo-wide; exports map <-> disk <-> importers agree on all 18 entries; knip flags neither the new root script nor panel/index.ts; only jscpd clone touching an X-series file is pre-existing CLI boilerplate; both workflows parse with the verify step correctly positioned; no universal-quantifier reach claim survives in either swept file.
- 2026-07-27 CLOSE FIX DONE_WITH_CONCERNS. FINDING 3 PASSED - the risk is CLOSED with executable evidence: turbo build --force on web+marketing+admin, 3 successful / 0 cached, admin's module-load assert did NOT fire, @hushbox/admin#build exited 0 with dist/_headers freshly emitted (proving closeBundle ran), admin dist 1.5 MB / 33 files, zero *tts*, zero *.wasm. verify:web-bundle exit 0 twice.
- 2026-07-27 FINDING 2 was NOT a no-op: two of four workflow-comment claims were FALSE and fixed - "before the artifact anything deploys from is uploaded" (the deploy job deploys from FOUR artifacts, ci.yml:836/891/897/910) and "reads only the dist and the installed packages" (it also reads pnpm-workspace.yaml). The other two were accurate and left verbatim - correct discrimination, not churn.
- 2026-07-27 MY BRIEF NEARLY DESTROYED A RUN RECORD: I specified task-close/impl-report-1.md, but that dir already held impl-report-1.md and -2.md from the 2026-07-25 plan-identifier sweep. Implementer wrote impl-report-3.md instead of overwriting. Third time my brief carried a wrong fact (census x2, exclusivity, now this).
- 2026-07-27 SIDE EFFECT for the founder: rebuilding apps/web/dist replaced its contents; the implementer re-ran merge-marketing-into-web.ts, so the local dist is now build+merge with NO _headers (that step needs generate:env, forbidden to agents). Local artifact state only - nothing committed - but a local verify or preview will see a dist without _headers until someone runs pnpm generate:headers.
- 2026-07-27 CLOSE FIX -> re-auditing.
- 2026-07-27 CLOSE FIX RE-AUDIT: FAIL, one Important finding - NINTH instance of the false-reach class at scripts/build-admin-bundle.ts:19-21 ("the guard belongs where EVERY admin build passes through it"), ADDED BY X5a IN THIS RUN and falsified by the same file's own line 51 (turbo build --filter=@hushbox/admin, so a cached build replays a dist without re-running closeBundle). Third file, and a direct violation of the deletion rule this run wrote.
- 2026-07-27 Finding 3 CONFIRMED independently from artifacts the implementer did not cite: .turbo/cache entries WRITTEN at 12:09:24 (admin 3.7s), 12:09:30 (web 9.2s), 12:09:33 (marketing 13.0s) - cache entries are only written on a miss/--force execution, never on a replay, and turbo does not cache a failed task. Admin manifest lists 36 paths incl. dist/_headers (written only inside closeBundle) and ZERO tts*/ort*/*.wasm. Config mtimes (12:07) precede the builds (12:09), so artifacts came from the FINAL config state. Run's last real risk CLOSED.
- 2026-07-27 Finding 2 confirmed on BOTH halves - the two changed claims were genuinely false (deploy job downloads FOUR artifacts: web-dist ci.yml:835-840, admin-dist :890-895, sandbox-dist :896-901, mobile-dist :909-914; and declaredOrtCommonVersion now reads pnpm-workspace.yaml), and the two left verbatim were genuinely true. Correct discrimination in both directions.
- 2026-07-27 Side effect ASSESSED and harmless: apps/web/dist lacking _headers is purely local - dist is gitignored, CI assembles fresh, and the one bypass path (HB_E2E_PREBUILT) fails LOUD, since loadRules (headers-vite-plugin.ts:222-229) throws a named error rather than serving a CSP-less preview.
- 2026-07-27 ESCALATING THE PATTERN, not the instance: nine instances, three files, several written BY THIS RUN while it was actively removing the same class. Per-instance fixes are trivial; the recurrence rate is the finding. Founder decision on whether reach-comments should exist at all.
- 2026-07-27 NEW FOREIGN BREAKAGE: apps/api/.../smart-model-candidates.ts:190 TS6133 'pinned' (mtime 12:22, after the close batch) breaks @hushbox/admin typecheck. Same class as the earlier email-sender-factory TS6133 that a workstream fixed. CI would fail on it right now.
- 2026-07-27 CLOSE FIX CYCLE 2 DONE - pure deletion verified DIRECTLY by the orchestrator (read the diff) rather than by a tenth audit cycle. "every admin build passes through it" is gone; remaining text is grounded at ci.yml:284-285 and :324-328. Nothing added. eslint/prettier/tsgo/vitest all green. Reasoning for skipping the audit, stated to the founder rather than done silently: pure deletion, mechanically checkable, gates green, and the underlying pattern is already escalated - a further audit would most likely surface a tenth instance of a class awaiting a ruling.
- 2026-07-27 OBSERVED AND LEFT: build-admin-bundle.ts:3 still says "The single admin-bundle build path for E2E". Scoped to E2E (not a universal reach claim) and accurate - reached by pnpm build:e2e:admin from playwright.config.ts and ci.yml:415. Consistent with the X5b auditor's earlier ruling that accurate scoped text must be left verbatim; churning it would be its own defect. Folded into the pattern escalation rather than fixed.
- 2026-07-27 FOREIGN TS6133 'pinned' NO LONGER REPRODUCES - the notifications/models workstream fixed it at 12:24 (now declares _pinned). @hushbox/admin's typecheck blocker is clear. That is the SECOND foreign TS6133 introduced and fixed by another workstream during this run.
- 2026-07-27 RUN COMPLETE. Six tasks (X1, X2, X3, X4, X5a, X5b) + close batch, all clean. Eleven audits total. Unscoped gates green repo-wide at 0 cached. Awaiting founder on: the hydration race (production half of the original symptom), the reach-comment pattern ruling, three ungated deploy artifacts, doc approvals, and the smaller open items enumerated in plan.md.
- 2026-07-27 FOUNDER RULINGS on the close-out proposal: DO items 1,3,4,5,8,9,10,11; SKIP item 2 (browser/manual pass) entirely; SKIP item 7 (reach-comment rule + remaining sweep); SKIP item 12 (label/copy/local _headers). Item 6 REVERSED MY RECOMMENDATION - delete the unused build:web script rather than keep+document it. Recorded as the founder's call, with my overruled recommendation noted so a future reader does not find a deleted script beside a plan advocating keeping it.
- 2026-07-27 SANDBOX MEASURED BEFORE SCOPING (the one item that could have surfaced a live deploy problem): apps/sandbox/dist = 25 files, largest pyodide/pyodide.asm.wasm 9,609,998 B = 36.7% of the 26,214,400 per-file cap. Clear on both Cloudflare limits, so wiring the guard cannot turn a working deploy red. Safe to declare and gate.
- 2026-07-27 Y-SERIES PLANNED: Y1 (4 guard-coverage gaps + _headers pin + turbo inputs, 2 auditors) · Y2 (delete build:web) · Y3 (voice-id single source) · Y4 (hydration race) · Y5 (smoke E2E clicking Listen, after Y4) · Y6 (renames, after Y1+Y2) · Y7 (docs) · Y8 (run existing admin+marketing suites). Waves: A=Y1+Y3, B=Y2+Y4, C=Y5 then Y6, D=Y7 then Y8. packages/ui work SERIALISED (Y3 then Y4) rather than parallel - this run has paid for concurrency assumptions twice already.
- 2026-07-27 Y3 IMPL DONE. Shared Zod enum is now the single source; the engine derives BOTH the type and the runtime list. Derived TTS_VOICES proven byte-identical INCLUDING ORDER to the pre-change literal. Deviations: (1) engine side executed as refactor not red-green - the "no behaviour change" constraint makes a failing ui-side test impossible by construction, but the NEW shared export WAS driven red-green (RED: "TTS_VOICE_IDS is not iterable"); (2) presentation metadata (displayName/accent/gender) stayed in packages/ui as Record<TtsVoice,...> - the TOTAL mapping makes drift a compile error in either direction (both errors quoted), and hoisting would ripple into API contract types for no correctness gain.
- 2026-07-27 Y3 REPORT CLAIM I DO NOT RECOGNISE: it attributes the TtsVoice-vs-TtsVoiceId naming choice to "the plan's §Interfaces note that TtsVoiceId does not exist". My Y3 section contains no such note. Outcome looks right (TtsVoice, re-exported so no consumer import changed) but the justification cites something I did not write - sent to the auditor to judge on merit rather than on the citation.
- 2026-07-27 @hushbox/shared's PACKAGE GATE CANNOT GO GREEN and that is foreign: packages/shared/src/affordability/** is under active edit by another workstream; its failure set changed between two runs 20 min apart (6 -> 83 lint problems, different failing test file). Auditors must judge src/schemas/** plus scoped eslint/tsgo, not the package gate.
- 2026-07-27 Y3 CLEAN (audit PASS, zero findings). MY CHALLENGE WAS WRONG: I flagged the report's citation of "the plan's §Interfaces note that TtsVoiceId does not exist" as something I did not write. The auditor located it - plan.md:41, inside the T-series ## Interfaces section (heading :29): "voice is the real exported union TtsVoice (the plan's earlier TtsVoiceId name does not exist - use TtsVoice)". The report said §Interfaces, not §Y3; I read it as §Y3 and challenged a correct citation. Correcting on the record.
- 2026-07-27 Y3 auditor VERIFIED the total-mapping claim in a scratch compile rather than accepting it: adding an id to the union without a Record entry => TS2741; adding a Record key not in the union => TS2353. BOTH directions are hard compile errors, and the Record carries no ORDER (order lives only in the shared array), so the one thing that could silently drift cannot. Removal, not relocation - confirmed.
- 2026-07-27 Y3 "exactly one declaration" independently re-established by repo-wide grep: only the shared declaration + its .default value, plus the Record keys in the engine. All other hits are gitignored build output, test fixtures that MOCK the module, or this run's own markdown. The one test restating all five (tts-engine.test.ts:50) is a behaviour pin on rendering ORDER, not a second declaration - correctly kept, since deleting it would leave the Select order unpinned.
- 2026-07-27 Y3 TDD deviation ACCEPTED by the auditor with the same reasoning I have hit twice this run: "no behaviour change" makes a ui-side RED impossible by construction; the implementer used AGENT-RULES' refactoring path (green before/after) and drove the genuinely new surface red-green. Auditor checked for anything left unpinned and found only a gap that existed identically at HEAD.
- 2026-07-27 FOREIGN, worsening: apps/api is now broadly broken by the notifications workstream's PushMessage refactor (~30 TS errors: title/data/payload no longer on PushMessage across push-* tests, dev routes, notify-event). Unrelated to this run; noting because apps/web's tsgo also picks up an apps/api error via AppType.
- 2026-07-27 Y4 DISPATCHED (hydration race). Y2 HELD until Y1 clears - Y2 edits scripts/build-web-bundle.ts which imports verify-web-bundle.ts, and Y1 criterion 2 may change appBundleOptions' shape.
- 2026-07-27 Y1 IMPL DONE_WITH_CONCERNS. All six criteria executed on real artifacts, both directions, undone afterwards. DEVIATION: criterion 5's inputs landed in a NEW apps/admin/turbo.json (extends ["//"]) rather than root turbo.json - implementer MEASURED that a root "@hushbox/admin#build" entry does not inherit outputs/dependsOn/env (all empty), so the root form would need hand-copying three fields = banned sync contract.
- 2026-07-27 Y1 SELF-REPORTED TWO CACHE-KEY GAPS, both ruled FIX by me: (1) @hushbox/sandbox#build's hash is unchanged by a verifier edit (measured across change+restore), so a cached sandbox dist can replay under superseded guard rules - a gap THIS TASK created by wiring sandbox, outside criterion 5's literal wording; (2) PRE-EXISTING and wider - admin's same hook also runs generate-headers.ts + headers-vite-plugin.ts, equally absent from inputs, so a cached admin dist can replay with a SUPERSEDED CSP GENERATOR, arguably worse than a superseded bundle guard. Same mechanism, same file; fixing one and not the other would be knowingly shipping half a fix.
- 2026-07-27 Y1 CONSTRAINT recorded for the future: _headers is asserted only on the TTS-FREE path. The merged web-dist's generated _headers cannot simply be asserted too, because the PRE-MERGE apps/web/dist - the artifact build-android.yml now verifies - legitimately has none. A future universal _headers assertion would break the Android path.
- 2026-07-27 Y1 -> auditing (2 independent auditors, deploy path). Y2 STILL HELD until Y1's audits return - cheap patience given the appBundleOptions shape change.
- 2026-07-27 Y4 IMPL DONE (hydration race). Both halves test-pinned; SSR half additionally confirmed in the REBUILT production artifact, and the lazy-load property proven by a before/after artifact diff - the one constraint I added beyond the criteria, because switching an island to client:load while touching initial state is exactly how the engine accidentally becomes eager.
- 2026-07-27 Y4 DEVIATION: one new file outside the named list - apps/marketing/src/page-tests/blog-post-hydration.test.ts - because a test under src/pages/ would be BUILT AS A ROUTE. Follows the existing welcome.astro.test.ts convention.
- 2026-07-27 ORCHESTRATOR RULING on Y4's raised item: the adjacent HIGHLIGHT TOGGLE has the identical dead window and is equally silent on click. Implementer correctly left it (criteria named only Listen) and correctly surfaced it. FIXING IT: a half-hydrated control row is worse than either consistent state - two adjacent controls where one honestly reports it is not ready and the other lies. Same component, same mechanism, one-line extension. The founder approved closing the hydration race; leaving the neighbouring button broken does not close it.
- 2026-07-27 Y4 recorded pre-existing, NOT a regression: tts-engine is already in the blog page's EAGER chunk closure via the accessibility widget's own client:load island in BlogLayout.astro - unchanged before/after. The 2.3MB tts.worker chunk stays click-lazy either way, which is the property that actually matters.
- 2026-07-27 Y4 -> auditing (1 auditor). Y2 still held pending Y1's two audits.
- 2026-07-27 Y1 AUDITOR 1 (criteria): FAIL - and the two Important findings are EXACTLY the two gaps the implementer self-reported and I had already ruled to fix. Auditor did not accept them; it MEASURED both. Sandbox: task hash ac5205a5257e5140 identical across idle -> append a line to verify-web-bundle.ts -> restore, in the same batch where @hushbox/admin#build moved 3bd83366 -> 73863f01 -> 3bd83366. Admin/generate-headers: appending to generate-headers.ts or headers-vite-plugin.ts leaves the admin hash at 79baecf63b92cddf, while the same probe on packages/shared/src/tts-hosts.ts moved it to 627a6f1a1f8b60a4 (proving the probe method works and dependsOn ^build does fold shared in).
- 2026-07-27 Y1 turbo deviation VERIFIED BOTH HALVES independently: auditor temporarily installed the root "@hushbox/admin#build" form with the package file moved aside - resolved definition came back outputs:[] dependsOn:[] env:[], so the base build task genuinely is NOT inherited and the root form would need three hand-copied fields. Package form resolves outputs:["dist/**"], dependsOn:["^build","fetch-pyodide"], env:["ESM_CDN_URL"] plus the merged inputs, and the hash moves on a verifier edit. Both files restored byte-identically (md5 verified, git status clean).
- 2026-07-27 Y1 auditor PROVED both failing directions itself on the real build path and cleaned up: moving apps/sandbox/public/_headers away makes the sandbox build exit 1 with "no _headers at the dist root - this origin ships without its CSP"; planting ort-wasm-audit-probe.wasm makes it exit 1 with "TTS artifact in a bundle declared TTS-free". ONE declaration confirmed by repo-wide grep - APPS_SHIPPING_TTS read only through appBundleOptions at four call sites, no call site hardcodes shipsTts, no workflow YAML encodes a TTS expectation.
- 2026-07-27 Y1 CLI plumbing verified live by the auditor: `pnpm verify:web-bundle dist-ios dist-nope` verified a real dist under the sibling name then exited 1 on the missing one - so a failure in ANY named OTA dist fails the step, not just the first.
- 2026-07-27 STALE RED CLEARED: the apps/api TS2353 (trial-eligibility.ts:120) that both the brief and the Y1 report called foreign is GREEN now - the other workstream fixed it mid-flight. Both @hushbox/scripts and @hushbox/admin typecheck clean.
- 2026-07-27 Holding the Y1 fix dispatch until auditor 2 (deploy-risk lens) returns, to batch.
- 2026-07-27 Y1 AUDITOR 2 (deploy-risk): PASS, zero findings. Answered my specific worry the right way round - the package-level inputs is a strict SUPERSET of what it inherits ($TURBO_DEFAULT$ + .env* + four named files), so NO narrowing and no stale-cache-hit risk. Verified the Android path at artifact level (capacitor.config.ts:23 webDir 'dist' => the verified artifact IS what cap sync packages; apps/web is shipsTts:true so checkHeadersFile correctly does not fire on the header-less pre-merge dist). Judged both self-reported gaps real but NOT blocking (worst case = a skipped re-check on an artifact already verified under previous rules, still strictly better than no verification).
- 2026-07-27 UNINTENDED BENEFIT found by auditor 2, worth keeping: ci.yml's OTA build uses `for ... & done; wait`, which returns 0 even if an OTA build FAILED. The new verification step converts a silently-failed OTA build into a red.
- 2026-07-27 Y4 AUDIT: FAIL, one Important finding - and a genuinely subtle one. The criterion-3 assertion /<button[^>]*aria-label="..."[^>]*disabled/ is VACUOUS: React SSR emits disabled="" BEFORE aria-label, so the trailing `disabled` matches the substring inside class="... disabled:pointer-events-none disabled:opacity-50 ...". Auditor ran the regex against the real emitted tag WITH THE ATTRIBUTE STRIPPED and it still returned true, then mutation-tested: flipping useState(false)->true removes the attribute from SSR markup and ALL THREE new tests stay green. Shipped behaviour is correct (confirmed in the built artifact) but NOTHING PINS IT. The recorded RED was genuine only because the disabled: utility classes did not exist yet - the test lost its teeth in the same edit that made it pass.
- 2026-07-27 Y4 fix cycle 1 batches finding 1 (non-vacuous SSR assertion, proven by the same mutation) with finding 2 (the highlight toggle, per my earlier ruling) - and explicitly warns not to repeat the vacuous-regex mistake on the second control.
- 2026-07-27 Y4 auditor census recorded, NOT in scope: other blog-page islands share the same inherent window - ThemeToggle (x2, client:load), ShareButton (client:visible), NewsletterSignup (client:visible), AccessibilityWidget panel (client:load). Pre-existing, unaltered by Y4.
- 2026-07-27 Y1 FIX CYCLE 1 DONE with the measurement I demanded: all 6 admin-named and all 4 sandbox-named files were NO-MOVE before, MOVED-and-RETURNED after, restores byte-verified. Nothing narrowed - resolved inputs keep ["$TURBO_DEFAULT$",".env*"] verbatim, resolved-file counts exactly +6 (admin 152->159) and +4 (sandbox 54->59). outputs/dependsOn/env inherit identically; env inheritance also confirmed BEHAVIOURALLY by a hash shift when ESM_CDN_URL is set.
- 2026-07-27 Y1 fix HONEST LIMITATION stated by the implementer: a real cache HIT is not demonstrable in this checkout - what was measured is cache-key SENSITIVITY. That is the correct property for this fix and the limit was disclosed rather than glossed.
- 2026-07-27 ORCHESTRATOR ASSESSMENT of the implementer's flagged out-of-scope item (apps/web "has the same structural exposure"): I believe it is a FALSE POSITIVE and sent it to the auditor to adjudicate rather than asserting it. Reasoning: for admin/sandbox the guard runs INSIDE the cached task (closeBundle / build.ts), so a cache hit replays the artifact and the guard never executes - that is the real exposure. For apps/web the guard is a SEPARATE step after the cached task (build-web-bundle.ts runs turbo build -> merge -> verify; and in CI the workflow step runs it), so a cache hit restores the dist and the verify still executes on it. Acting on it would add a third turbo.json for no reason; being wrong would mean a real gap, so the auditor is told to falsify my reasoning explicitly.
- 2026-07-27 Y2 IMPL DONE. NODE_ENV=development pnpm build:e2e ran end to end at exit 0 (turbo 2/2, merge, verify, _headers written); repo-wide grep shows ZERO surviving build:web / --target=prod references in code, comments, workflows or docs; knip byte-identical to a pre-edit baseline. Deviation: buildWebBundle lost its target param (single-valued param => statically-true branches => unreachable branch coverage + unread parameter); only caller is the CLI entry in the same file.
- 2026-07-27 MY RULING on Y2's surfaced design call: parseTarget now returns one value and its result is discarded. KEEP the validation (a stale --target=prod fails loudly instead of silently building e2e - this whole run has been about things claiming one behaviour and doing another), but it must NOT stay named parseTarget when nobody reads the parsed value. Y6 owns durable renames and already depends on Y2, so Y6'S SCOPE IS EXTENDED to rename it as an assertion. Rejected the simpler "delete it and drop --target=e2e" because it makes a stale invocation silently succeed with the wrong intent.
- 2026-07-27 COORDINATION HAZARD hit twice now: Y2's build:e2e rebuilt apps/web/dist and apps/marketing/dist as a side effect while a concurrent task was reading the marketing dist for artifact evidence. Both regenerable and the evidence was re-taken, but concurrent tasks that rebuild shared artifacts need explicit sequencing.
- 2026-07-27 Y2 ATTRIBUTION: scripts/build-web-bundle.ts and package.json already carried uncommitted X5b edits, so git diff vs HEAD conflates them. Y2's package.json change is ONE DELETED LINE; the verify:web-bundle script in that diff is X5b's.
- 2026-07-27 MY ERROR, CORRECTED AND PROPAGATED: I recorded apps/marketing's suite as failing 5 files / 7 tests on VITE_API_URL and passed that to several agents as foreign breakage. It is an artifact of invoking vitest DIRECTLY - through turbo test the package script routes via scripts/with-env.ts which supplies VITE_API_URL, and the suite is fully green with api-url.ts at 100% coverage. Fourth wrong fact I have handed an agent this run (census x2, exclusivity, report filename, now this).
- 2026-07-27 Y4 FIX CYCLE 1 DONE, both findings fixed and mutation-checked BOTH DIRECTIONS: useState(false)->true kills both SSR-disabled pins; neutering the mount effect kills both enable-after-hydration pins. Class-list tests deliberately SURVIVE mutation A (they pin no-layout-shift, not the attribute) and the report says so rather than leaving it to be found.
- 2026-07-27 Y4 fix caught what would have made the OBVIOUS fix wrong: attribute ORDER differs between the two controls. Radix asChild re-orders cloned props, so the highlight toggle emits disabled="" AFTER aria-label while the Listen button emits it BEFORE. A regex of the cycle-1 shape would have ACCIDENTALLY WORKED on the toggle and still failed on Listen - a passing-looking inconsistency. Both now use one order-independent parsed-node toBeDisabled().
- 2026-07-27 Y4 side effect judged acceptable, recorded so it is not rediscovered as a bug: disabled:pointer-events-none also suppresses the toggle's Radix tooltip during the dead window. Not a behaviour change (Radix is not mounted pre-hydration either); the two mechanisms now agree deliberately rather than by accident.
- 2026-07-27 Y2 CLEAN (audit PASS, zero findings). Auditor VERIFIED the tricky attribution itself: git diff -- package.json is exactly two lines, one deleted (Y2's build:web) and one added (X5b's verify:web-bundle, consumed by ci.yml:316,369 / release.yml:154 / build-android.yml:102, none of which Y2 touched). Executed --target=prod and confirmed it exits 1 with a clear message BEFORE any side effect, so the validator I ruled to keep genuinely earns its lines. selectE2eEnvMode still exported and still imported by build-admin-bundle.ts:26.
- 2026-07-27 Y2 auditor's BEST JUDGEMENT CALL: it deliberately did NOT re-run pnpm build:e2e, because that rewrites .env.development/.env.scripts and rebuilds the shared dists - the exact side effect the plan records as having already disrupted a concurrent task. Instead it proved equivalence STATICALLY, line-for-line against git show HEAD: (same generateEnv + selectE2eEnvMode, same turbo argv incl. -- --mode development, same merge, same verify(appBundleOptions(rootDir,'apps/web')), same with-env headers call), all six asserted argument-for-argument by passing unit tests, plus the CLI entry executed for real. AND it stated plainly that its confidence rests on equivalence plus the implementer's run, not its own execution. Correct restraint plus an honest boundary.
- 2026-07-27 knip discrepancy, FOREIGN and worth the founder knowing: the auditor now sees TWO unused files - packages/config/vitest.package.config.ts (known) and packages/shared/src/money.ts (NEW since the implementer's run 30 min earlier). money.ts is a fairly central file (the shared bigint money primitives); another workstream appears to have orphaned it. Not ours, not investigated per AGENT-RULES.
- 2026-07-27 Y2 auditor confirmed the rewritten build-web-bundle.ts docstring NAMES its two callers (playwright.config.ts:96, ci.yml:410) rather than quantifying over them - complies with the deletion rule from X5b fix cycle 2 instead of repeating the pattern that caused it. The tenth false claim did not materialise.
- 2026-07-27 Y1 FIX RE-AUDIT: PASS, one Minor finding. MY ADJUDICATION CONFIRMED CORRECT - apps/web is NOT the same exposure, no third turbo.json. Auditor found no path where a cached @hushbox/web#build ships unverified: the guard is a plain process (tsx scripts/verify-web-bundle.ts), never a turbo task, so it cannot be cache-restored; and two web-dist producers (build-android.yml:87, ci.yml:346-350) do not reach turbo's cache at all. Bonus asymmetry: apps/web's build key IS insensitive to ort-assets-plugin.ts, but there the consequence INVERTS - a cached dist built under superseded emission rules gets checked by the CURRENT guard, so it goes RED rather than shipping silently.
- 2026-07-27 Y1 fix hash claims REPRODUCED independently: 10/10 moved-and-returned, and the auditor added a NEGATIVE CONTROL (scripts/build-web-bundle.ts moves neither hash), proving the measurement discriminates rather than reacting to ambient drift from concurrent workstreams (it saw admin's hash drift four times in one session).
- 2026-07-27 Y1 fix cycle 2 finding [Minor]: apps/sandbox/turbo.json:11-13 claims tts-hosts.ts "belongs to @hushbox/shared, whose build this already depends on" - MEASURED FALSE for sandbox (@hushbox/sandbox#build's deps are @hushbox/config#build + fetch-pyodide; apps/sandbox/package.json declares no @hushbox/shared dep). The protection is real but arrives via turbo's GLOBAL hash, not a dependency edge. The identical sentence in apps/admin/turbo.json IS TRUE. 11th instance of the false-claim class - written THIS SESSION by an agent that had just been briefed on the rule. Founder skipped the repo-wide rule (item 7); recording that the class keeps reproducing without one.
- 2026-07-27 MY MISATTRIBUTION, CORRECTED: I told the founder another workstream had orphaned packages/shared/src/money.ts based on a knip finding. It was OUR OWN Y1 re-auditor's probe artifact (a cp fell through), self-detected and removed. VERIFIED: file does not exist, not in HEAD, git status clean. No other team implicated. Fifth wrong fact I have stated this run.
- 2026-07-27 Y4 CLEAN (fix re-audit PASS, zero findings). Auditor ran BOTH mutations itself on a throwaway copy: mutation A (useState(false)->true) kills exactly the two SSR-disabled pins; mutation B (mount effect neutered) kills exactly the two enable-after-hydration pins; 2 failed / 4 passed in both runs; mutated copy deleted and originals diff-verified byte-identical. Radix ordering CONFIRMED in the built artifact - Listen emits disabled="" BEFORE aria-label, the toggle emits it AFTER - so a cycle-1-shaped regex would have matched the toggle's REAL attribute while matching only the class list on Listen. The parsed-node choice is justified, not rationalised.
- 2026-07-27 Y4 auditor cleared one thing it checked rather than flagging it: the enable-after-hydration tests use RTL render (client render + effects) rather than hydrateRoot over SSR markup, so strictly they pin "enabled once mount effects run" - but mutation B kills them, the SSR half is pinned separately AND confirmed in the artifact, and initial state is identical on server and first client render. Criterion 3 fully covered.
- 2026-07-27 Y4 auditor confirmed no inline script was added and explained why it cannot go stale: generate-headers.ts hashes inline scripts by SCANNING THE BUILT DIST, so no committed hash list exists to drift. Also confirmed zero modulepreload links on the page and the 2.3MB tts.worker chunk referenced nowhere in the island chunk - still click-lazy.
- 2026-07-27 Y5 DISPATCHED (smoke E2E clicking Listen). Y6 still waits on Y1 fix cycle 2.
- 2026-07-27 Y1 CLEAN. Fix cycle 2 verified DIRECTLY by me (read the file + grepped the groundings) rather than via a third audit cycle on three comment lines - stated openly rather than skipped silently. The replacement holds: ORT_DIR (ort-assets-plugin.ts:39, derived from TTS_ORT_WASM_PATH) is the guard's ONLY tts-hosts consumer, and every read (:198/:200/:218/:222/:239) sits inside shipsTts-branch checks, while the TTS-free branch runs checkNoTtsArtifacts/checkHeadersFile/checkPagesLimits. The implementer's phrasing "no check ... reads it" rather than "is not read" is deliberately precise - ORT_DIR IS computed at module load even on the TTS-free path - which is exactly the distinction the previous ten attempts blurred.
- 2026-07-27 Y6 DISPATCHED (durable renames, scope extended with the parseTarget ruling). CRITICAL HAZARD flagged in its brief: apps/admin/turbo.json and apps/sandbox/turbo.json name ../../scripts/verify-web-bundle.ts and ../../scripts/lib/ort-assets-plugin.ts BY PATH in their inputs lists. A rename that misses those two files SILENTLY REVERTS the cache-key fix Y1 just made and measured - the config stays valid, the build stays green, and the hash stops moving. Nothing would catch it except re-running the hash probe.
- 2026-07-28 Y5 IMPL DONE_WITH_CONCERNS. The central assertion was measured UNSATISFIABLE under a faithful stand-in for the original defect - i.e. proven to catch the bug it exists for, not merely to pass. 4 green runs incl. --repeat-each=3 --workers=2 --retries=0, zero flaky.
- 2026-07-28 COLLATERAL DAMAGE TO A FOREIGN WORKSTREAM, verified by me: e2e/global-teardown.ts runs `pnpm generate:env` on EVERY Playwright invocation - not separable from running e2e, so my standing prohibition was unenforceable. It REVERTED .github/workflows/run-ops-script.yml, which carried an uncommitted foreign edit at task start and now matches HEAD. Uncommitted => not recoverable from git. ci.yml/release.yml/build-android.yml remain M. ANY future e2e run in this checkout will do the same to any uncommitted workflow edit. Flagged to the founder; not repaired (not ours, and we do not know what it contained).
- 2026-07-28 Y5 deviations to audit: (1) tagged @chromium-only - Playwright routes dedicated-worker requests only in Chromium, so Firefox/WebKit would really fetch ~90MB from huggingface.co every run; implementer argues the guarded defect is a build-output property not an engine one - auditor to RULE, it is the difference between proportionate and quietly covering a third of the matrix; (2) extended e2e/marketing-roadmap.spec.ts (already hosts the TTS-CSP describe) per CODE-RULES rather than a new spec, filename now under-describes, deliberately not renamed; (3) added TTS_WORKER_BOOT to e2e/config/timeouts.ts (inline numeric timeouts are lint-banned) - auditor to check the value is a justified BOOT bound, not a disguised sleep.
- 2026-07-28 MY SCHEDULING ERROR: Y5's first run died because verify-web-bundle.ts still imported ./lib/ort-assets-plugin.js mid-rename - Y6 was running concurrently. My own stated ordering put Y6 after Y1+Y2 and Y5 after Y4, but I dispatched them in parallel without checking they shared ground. They did. Self-resolved, but that is the third concurrency mistake I have made this run.
- 2026-07-28 Y6 IMPL DONE. Hash probe: all four combinations MOVED and RETURNED, byte-identical restores; resolved inputs 159 (admin) / 59 (sandbox) matching Y1's counts - nothing narrowed. The turbo hazard I flagged was handled: both turbo.json files carry the renamed paths.
- 2026-07-28 Y6 ADDED A BETTER CONTROL THAN I SPECIFIED: a bare move-and-return is weak evidence (consistent with the hash moving for ANY reason, incl. ambient drift). It edited headers-vite-plugin.ts - declared in admin's inputs but NOT sandbox's - and showed admin's hash move while sandbox's stayed byte-identical. That establishes the probe responds to the inputs DECLARATION rather than noise, and is exactly the signal a missed rename would have failed. Future verification of this property should use the differential form.
- 2026-07-28 MY RULING on Y6's raised item: FINISH THE RENAME. Exported symbols still say Web (verifyWebBundle, VerifyWebBundleOptions, collectWebBundleViolations, and the "Web bundle verification failed" runtime message). Implementer correctly stopped at Y6's literal criteria and surfaced it. But verify-bundle.ts exporting verifyWebBundle is worse than either consistent state - a reader cannot tell which name is authoritative, and the message tells an operator "Web bundle verification failed" about an admin or sandbox artifact. Same defect as the filename, same reason, behaviour-free across 6 call sites. Batching with any audit findings.
- 2026-07-28 MEASUREMENT HAZARD recorded for the auditor: turbo baselines drift between readings because admin/sandbox dependsOn reaches @hushbox/api#build, which concurrent workstreams edit constantly; Y6's first probe pass showed a SPURIOUS non-return from this alone. Paired readings must be taken inside one tight window, never against previously recorded hashes.
- 2026-07-28 Y5 AUDIT: CRITICAL FAIL, and the root cause is MY plan. §Y5's Design context asserted the corruption killed the worker "on load, before any model bytes arrived - so catching that class does NOT require the ~92 MB download". FALSE. The bug throws inside Callable's CONSTRUCTOR, which runs only after tokenizer.json/tokenizer_config.json resolve. Contradicted by this run's OWN research (research/first-click-reload.md:41: "The 92 MB model is partially fetched each time before failing"). I wrote the premise, the implementer built to it, and I reported it to the founder as proven evidence.
- 2026-07-28 The auditor reproduced the bug BYTE-FAITHFULLY in the real built chunk and measured: corrupted + all HF requests held (= the shipped spec) => 3 requests, NO error for 25s, transport reaches Stop, EVERY spec assertion satisfied. The test is green against the exact defect it exists for. Only when the 3 JSONs are fulfilled from fixtures does the defect surface (+130ms). The one test between dead production TTS and users manufactures confidence.
- 2026-07-28 Implementer's "faithful stand-in" claim ALSO false: it replaced the whole 2.29MB chunk with a 66-byte throw, moving the failure from instance-construction to module-evaluation time - exactly the axis the held route erases. Its own report noticed the 92ms vs ~0.5s discrepancy and explained it away as body size; that discrepancy WAS the signal the stand-in was wrong.
- 2026-07-28 @chromium-only justification also false: on playwright-core 1.60.0 a URL-loaded {type:'module'} worker's cross-origin fetch IS intercepted in chromium, firefox AND webkit (auditor ran the full path in all three, zero egress). Tag ALLOWED on proportionality; the claimed ~90MB egress risk does not exist.
- 2026-07-28 AUDITOR'S BEST CALL: it deliberately did NOT run Playwright despite my brief saying "re-run it twice" - because the verdict was already decided by a defect the runner cannot surface (it passes), and a run would have irreversibly rewritten tracked workflow files via global-teardown's generate:env in a tree with three live workstreams. It substituted 6 instrumented replications of the identical interaction and verified .github/workflows/ byte-identical before and after. Correct trade, explicitly disclosed.
- 2026-07-28 ESCALATED TO FOUNDER, no fix dispatched: (a) fulfil the 3 JSONs from ~3.6KB fixtures and hold only the weights - surfaces the defect at +130ms, but no POSITIVE signal discriminates past that point, so it becomes a bounded absence-of-error assertion; or (b) scope Y5's claim down to what it proves and leave the new.target class to W1's build-artifact guard, where the founder's own W1 ruling already put it.
- 2026-07-28 Y6 CLEAN (audit PASS, zero findings). Auditor REFUSED to mutate anything (no edit tools, shared checkout with three live workstreams) and measured the cache-key property in a STRONGER non-mutating form off turbo --dry=json: both renamed files appear in each resolved input map at NEW paths with content hashes it verified are exact git hash-object blob hashes. It then found a NEGATIVE CONTROL already in the config - a missed rename contributes ZERO entries, and apps/sandbox/turbo.json declares .env* while that app has no .env* file, contributing exactly zero. The silent signature of the failure mode was sitting in the same file for comparison. Strictly better than the edit probe I specified, and it mutates nothing.
- 2026-07-28 Y6 auditor verified "no behaviour changed" the only way that works here - diffing each file against its HEAD BLOB rather than reading git diff, which conflates Y1/Y2/X5b's uncommitted edits. Y6's real delta: build-seam.ts ONE line, verify-bundle.ts TWO, build-web-bundle.ts only the parseTarget->assertE2eTarget rename.
- 2026-07-28 Y6 FIX CYCLE 1 DONE. Symbol rename complete across 6 call sites; both turbo.json byte-identical before/after (sha256 + blob hash); resolved-input map still 159/59 with live blob hashes; workflows untouched (all four steps invoke the pnpm script, none names a symbol).
- 2026-07-28 THE DOCBLOCK WAS FALSE, NOT STALE - and WE DISPROVED IT OURSELVES IN T1. It claimed tts.worker.ts pins env.backends.onnx.wasm.wasmPaths; that property path does not exist (kokoro-js re-exports transformers' env as a thin wrapper with only a wasmPaths setter). T1's critical finding at the START of this run was exactly this, and the CODE was fixed then - the COMMENT survived.
- 2026-07-28 MY RULING: fix packages/shared/src/tts-hosts.ts:4 and :33 next cycle - they carry the SAME false claim in the shared package, the more-read location. VERIFIED BY ME DIRECTLY: both cite env.backends.onnx.wasm.wasmPaths, while tts.worker.ts:35-41 states plainly there is no env.backends tree and tts.worker.test.ts:11-15 mocks that exact shape so a regression throws. Fixing the less-read copy and leaving the more-read one is worse than fixing neither - a reader who checks finds two comments disagreeing with no way to tell which is authoritative. Held out of this cycle only to keep the tree stable for the re-audit.
- 2026-07-28 Y6 FIX CYCLE 3 DONE. Deleted the whole two-sentence claim at generate-headers.ts:124, not just the property name - correct, because the "cannot drift" half is false INDEPENDENTLY (the fetched host comes from transformers' built-in default, not any shared constant), so no rewrite would have been truthful. THE CLASS IS CLOSED: repo-wide remoteHost leaves exactly one source hit, tts.worker.test.ts:11, which correctly asserts the property's ABSENCE and is real-API pinned. Net across four cycles: FIVE false reach/link claims deleted, ZERO added.
- 2026-07-28 THE FALSE COMMENT WAS CONCEALING A REAL GAP - most useful outcome of the cleanup. Nothing mechanically ties the model-fetch host to the CSP allowlist: if @huggingface/transformers changes its default hub host, the download breaks SILENTLY (CSP blocks a host nobody updated, no test notices). The implementer declined to replace it with a warning comment - "a warning is not a mechanism" - which is exactly the judgement this run has been enforcing. Its proposal: a test asserting the library default equals TTS_MODEL_HOST makes the link REAL rather than described. RECOMMENDED TO FOUNDER, NOT DISPATCHED (new scope beyond the approved Y-series; one test; risk is unchanged by our edit since the protection never existed).
- 2026-07-28 PATTERN, now conclusive across 14 instances: every attempt in this run to write a BETTER version of a reach/link comment produced another false one - including the text written specifically to correct its predecessor. The clauses that survived every audit are the narrow ones describing a CONSTRAINT (why this call follows that one; why the selector is exact), never an INVENTORY (what a file contains, who imports it, what its exports collectively do). Deletion, not rewriting, is what converged.
