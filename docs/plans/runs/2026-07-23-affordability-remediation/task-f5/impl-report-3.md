# F5 — impl report 3

## Objective

Close five validated findings from the cycle-2 panel. **No production behaviour change**: this
cycle edits four test names/comments and one production comment. No production statement was
added, removed, or reordered; no test assertion or fixture was changed.

The founder's ruling that a regenerate may bypass premium for a free-tier caller so long as the
budget covers it was carried in as settled. The exemption, the seam, and every gate are
untouched, and `BILLING.md` §Funding Decision Matrix + §Balance Consumption now state the
ruling — verified read this cycle (`docs/BILLING.md:1169-1178`, `:1219-1222`).

## Files changed

- `apps/api/src/slices/chat/routes.integration.test.ts` — one describe renamed, one test
  renamed, two comments rewritten. Findings 1, 2, 3.
- `apps/api/src/slices/chat/routes.ts` — one comment rewritten (the false `forkId` claim).
  Finding 5. **This is the only production file touched, and only its comment text changed.**

No other file in the repo was edited. The tree carries ~150 other modified files from the
concurrent F4 and F8 workstreams; none of them was opened for edit here.

## Finding 1 — the describe named a route five of its tests do not call

`apps/api/src/slices/chat/routes.integration.test.ts:2080`

Before:

```ts
describe('chat route: POST /chat premium-tier gate', () => {
```

After:

```ts
describe('chat route: premium-tier gate', () => {
```

Matches the cross-route precedent at `:5758` (`chat route: admin-disabled model gate`, no route
qualifier). Reporter output after the change, verbatim (`--reporter=verbose -t "premium-tier
gate"`, EXIT=0, 11 passed):

```
✓ chat route: premium-tier gate > refuses a premium model for a zero-balance caller with 403 MODEL_TIER_LOCKED
✓ chat route: premium-tier gate > admits a non-premium model for the same zero-balance caller (201)
✓ chat route: premium-tier gate > admits the same premium model for a caller with a positive purchased balance (201)
✓ chat route: premium-tier gate > locks a multi-model send when any selected model is premium for a zero-balance caller (403)
✓ chat route: premium-tier gate > does not tier-lock an owner-funded group turn (the caller is not the payer) (201)
✓ chat route: premium-tier gate > does not read the sender wallet at the gate for an owner-funded turn (a sender-read failure is a no-op)
✓ chat route: premium-tier gate > refuses a premium model from a zero-balance FULL-SESSION sender on the guest route (403)
✓ chat route: premium-tier gate > admits a premium model for an owner-funded link guest on the guest route (201)
✓ chat route: premium-tier gate > admits a premium model on a regenerate from the same zero-balance caller (201)
✓ chat route: premium-tier gate > maps a not-started admission outcome on a premium regenerate to 402
✓ chat route: premium-tier gate > refuses a smart-model regenerate when no candidate is affordable (402)
```

The only other occurrence of the old string in the repo is a stale build artifact,
`apps/api/dist/apps/api/src/slices/chat/routes.integration.test.js:1066` — not a source
reference, not regenerated by this task.

## Finding 2 — a positional reference to a sibling test

`apps/api/src/slices/chat/routes.integration.test.ts:2264`

Before:

```
      // A regenerate may run ANY model the payer can afford: the entitlement
      // axis is exempt there because the model was already chosen on the turn
      // being regenerated. The identical caller and model are refused on the
      // send route (the first test in this describe) — that pairing is the
      // exemption's boundary, and only entitlement is exempted.
```

After:

```
      // A regenerate may run ANY model the payer can afford: the entitlement
      // axis is exempt there because the model was already chosen on the turn
      // being regenerated. The same caller shape and the same premium fixture
      // are refused on the send route by 'refuses a premium model for a
      // zero-balance caller with 403 MODEL_TIER_LOCKED' — that pairing is the
      // exemption's boundary, and only entitlement is exempted.
```

The reference is now to the sibling's **name**, so inserting a test above it cannot falsify the
comment.

## Finding 3 — a test name and comment asserting something the test cannot show

`apps/api/src/slices/chat/routes.integration.test.ts:2288`

Name before / after:

```
- still refuses an unaffordable premium regenerate at admission (402)
+ maps a not-started admission outcome on a premium regenerate to 402
```

Comment before:

```
      // The money half of the same ruling: exempting entitlement must not
      // exempt affordability. The caller who is now allowed to SELECT this
      // premium model on a regenerate is still refused when they cannot pay
      // for it, and the refusal is the admission one, synchronously.
```

Comment after:

```
      // Wiring, not money enforcement: the entitlement exemption must return
      // the resolved context rather than a refusal, so a premium regenerate
      // reaches run start at all and the run's not-started admission outcome
      // becomes the caller's 402. The verdict here is a constant supplied by
      // the realtime double, so no change to money enforcement can move it —
      // the money half is carried by 'refuses a smart-model regenerate when no
      // candidate is affordable', whose 402 comes from route code.
```

The "cannot pay for it" sentence is gone. What remains is what the test does pin: the 402 comes
from `fakeRealtime({ started: false, code: 'INSUFFICIENT_ADMISSION' })`, whose `startRun`
returns that outcome unconditionally (`routes.integration.test.ts:408-421`) — so the assertion
still discriminates on the route reaching run start and translating a not-started outcome, and
on nothing else.

## Finding 4 — the corrected money-half reading, as a claim a later task can cite

**Report 2 had it backwards. The plain, corrected statement:**

> Of the two regenerate money pins, **`routes.integration.test.ts:2317` — 'refuses a
> smart-model regenerate when no candidate is affordable (402)' — is the one that carries the
> money half.** Its 402 is produced by route code reading the payer's effective funding, and it
> moves under a money change. **`:2288` cannot move under any money change**: its status is a
> constant handed to the route by the realtime double, so it pins wiring (regenerate reaches
> run start; a not-started outcome maps to 402) and nothing about affordability.

Both halves verified this cycle, not reasoned:

- **`:2317` discriminates — measured.** Deleting only the `allowanceSpending` seed row from the
  test (leaving every other line untouched) flipped it red for exactly the money reason:

  ```
  × refuses a smart-model regenerate when no candidate is affordable (402) 254ms (retry x1)
  AssertionError: expected 201 to be 402 // Object.is equality
  Tests  1 failed | 2 passed | 191 skipped (194)
  ```

  The file was then restored from a pre-inversion copy and verified byte-exact (`cmp` clean).
  No background suite was running during the inversion window.

- **`:2288` cannot discriminate — read.** `fakeRealtime` builds `startRun: () => okAsync(outcome)`
  from the literal passed at the call site. No wallet, allowance, budget, hold or admission
  script participates in producing that status.

Report 2's §"Two pins were GREEN on write" applied the vacuity test correctly to pin 4 (the kill
switch) and inverted it on these two. Cycle 1's and cycle 2's red-first evidence for the
*entitlement* half is unaffected — `:2288` did legitimately go 403 → 402 on the seam change,
which is a wiring change; it is only its billing as "the evidence money still binds" that was
wrong.

## Finding 5 — the false `forkId` comment

`apps/api/src/slices/chat/routes.ts:1186-1189`

Before:

```
      // turn-context/startRun path. The server accepts a guest's `forkId` and
      // validates it downstream; no client surface exposes forking to a guest
      // today, and the affordance is kept for one. The OWNER funds the turn;
      // the guest is the sender.
```

After:

```
      // turn-context/startRun path. The server accepts a guest's `forkId` and
      // validates it downstream, so a guest may send onto an existing branch.
      // A write-privileged guest is also rendered a Fork control, while the
      // fork-management routes are session-classed and answer 401 without a
      // session — so pressing it cannot succeed. That gap is a product one and
      // is not closed here. The OWNER funds the turn; the guest is the sender.
```

No code changed. Each factual half was verified, and — per the run's durable-claim rule — each
is **bounded by an existing gate**, so an edit falsifying the comment reddens a test before it
misleads a reader:

| claim | verified at | the gate that holds it |
| --- | --- | --- |
| a write-privileged link guest is rendered a Fork control | `apps/web/src/lib/message-actions.ts:46-47` (`'link-guest': { write: [… 'fork'] }`); `apps/web/src/components/chat/page/authenticated-chat-page.tsx:518` passes `onFork` unconditionally, and `apps/web/src/routes/share.c.$conversationId.tsx` renders that page | `apps/web/src/lib/message-actions.test.ts:441` — `expect(actions.has('fork')).toBe(true)` for link-guest/write on an assistant message |
| the fork-management routes are session-classed and 401 without a session | `apps/api/src/slices/conversations/routes.ts:1130, 1143, 1178, 1209, 1233` each declare `routeClass('session')` | `apps/api/src/slices/conversations/routes.integration.test.ts:269-277` — the anonymous-401 matrix enumerates all five fork routes |

`conversations/routes.ts` and the web files were read only; F4 owns the first and neither was
edited.

## Tests added

**None, deliberately.** Every finding is a naming or comment defect. The TDD iron law binds
production code, and no production code was written this cycle — a new test would assert
behaviour that already exists and would pass on first run, which the run's Global Constraint 2
names as evidence of nothing. The one production edit is comment text, and its two factual
halves are held by the two pre-existing gates tabled above rather than by a new test.

## Self-gate

| command | result |
| --- | --- |
| `pnpm test:watch apps/api/src/slices/chat/routes.integration.test.ts` | **pass** — EXIT=0, 194/194 |
| `pnpm test:watch … --reporter=verbose -t "premium-tier gate"` | **pass** — EXIT=0, 11 passed / 183 skipped; reporter names quoted above |
| `npx eslint src/slices/chat/routes.ts src/slices/chat/routes.integration.test.ts` from `apps/api`, **after the last edit** | **pass** — EXIT=0, no output |
| `pnpm arch:check` | **pass** — EXIT=0, 13 rules over 2185 files |
| `pnpm typecheck` (repo-wide) | **fail — EXIT=2**, every error inside `chat/domain/runtime.ts`. Attribution below. |
| scoped coverage, one include: `pnpm test:watch apps/api/src/slices/chat --coverage --coverage.include='src/slices/chat/routes.ts'` (two F8-red files excluded) | **pass** — EXIT=0, 35 files / 709 tests; `routes.ts` **97.32 stmts / 95.87 branch / 100 funcs / 100 lines** — identical to cycle 2, as a comment-only change must be |
| the api sweep — `npx tsx scripts/with-env.ts turbo test --filter=@hushbox/api --force` | **fail — EXIT=1**: 14 tests / 5 files, out of 6554 passing. Attribution below. |

### `pnpm test:api` was NOT run as written — and why

`pnpm test:api` is literally `pnpm ensure-stack && tsx scripts/with-env.ts turbo test
--filter=@hushbox/api`. My brief forbids `pnpm ensure-stack` while F4 and F8 are in flight, and
§Known Breakage's first entry is the reason: `ensure-stack-cli.ts` calls `generateEnvFiles`, and
a concurrent regeneration of `.env.development` / `.env.scripts` voids an in-flight suite. I ran
the **second half verbatim**, uncached, with the stack independently verified up (the chat
suites had just run 803 tests against real Postgres). That is `pnpm test:api` minus the
env-regeneration step, so the gate's coverage is the same, and per §Known Breakage a red run
never reaches the coverage report anyway — hence the separate scoped-coverage row.

One process note worth recording: the first attempt at that command failed **EXIT=127,
`command not found: tsx`**, with a one-line log — and the background harness reported the
wrapper's exit 0. Reading the status out of a file rather than the completion notice is what
caught it. Same class as §Known Breakage's wrapper entry.

### Repo typecheck — attributed outward, with the evidence

Every error, in both runs, sits in `apps/api/src/slices/chat/domain/runtime.ts`, which is on
F8's ownership list and is modified in the tree. I never opened it. The decisive evidence is
that **the error set changed between two runs 30 seconds apart**, which only a live edit
produces:

```
run 1: src/slices/chat/domain/runtime.ts(791,9): error TS2304: Cannot find name 'isOwnerFundedTurn'.
run 2: src/slices/chat/domain/runtime.ts(551,30): error TS2339: Property 'andThen' does not exist on type 'boolean'.
       src/slices/chat/domain/runtime.ts(551,39): error TS7006: Parameter 'funded' implicitly has an 'any' type.
       src/slices/chat/domain/runtime.ts(805,9):  error TS2322: Type 'boolean' is not assignable to type 'ResultAsync<boolean, DomainError>'.
```

`isOwnerFundedTurn` exists and is exported from `chat/domain/turn-context.ts:321` — the other
file F8 is mid-edit on. `npx turbo typecheck --continue --force` shows `@hushbox/admin`,
`@hushbox/api`, `@hushbox/e2e`, `@hushbox/scripts` and `@hushbox/web` failing, and **all five
report the same `runtime.ts` lines** via the api project reference. Zero errors in either file
I touched. The 2026-07-26 §Known Breakage note that repo typecheck is "now a usable gate" no
longer holds while F8 is mid-flight; it should be re-read after F8 lands.

### The api sweep's 14 failures, every one attributed

| file | tests | attribution |
| --- | --- | --- |
| `slices/notifications/domain/templates/template-html.test.ts` | 7 | The verbatim §Known Breakage entry (removed Google-Fonts `<link>`, owner outside this run). |
| `slices/billing/routes.integration.test.ts` (`GET /billing/spendable` ×3) | 3 | **F4-owned** — `billing/routes.ts` and this test file are both modified in the tree by that agent, mid-flight. |
| `slices/chat/domain/runtime.integration.test.ts` | 1 | **F8-owned** — the same `runtime.ts` that does not typecheck; the admission hook returns `admitted: true` where the test expects `INSUFFICIENT_ADMISSION`. |
| `slices/chat/domain/settlement.integration.test.ts` | 1 | Same workstream — `chat/domain/settlement.ts` and `workflows/engine/settlement.ts` are both modified in the tree by F8. |
| `slices/chat/routes.integration.test.ts` (Smart Model ×2) | 2 | **My file — checked inward first, then attributed.** Both are `expected 402 to be 201` on Smart Model sends in the `chat route: POST /chat` describe, which this cycle did not touch. **Re-run in isolation: `-t "Smart Model"` → EXIT=0, 6 passed.** The whole file passes 194/194 in isolation, twice. This is the documented shared-`model_catalog` contention class (a smart-model candidate set emptied by a concurrent catalog wipe reads as 402), same family as §Known Breakage's `POST /chat 201→400`. |

Per §Known Breakage I claim nothing from a single green api sweep and nothing from a single red
one; the narrow claim is that my two edited files are green on every isolated run, and that my
diff — four renamed/rewritten strings — cannot produce a 402, a type error, or an admission
verdict.

## Acceptance criteria (the five validated findings)

1. **Describe no longer names a route its tests do not call** — **met**. Before/after and the
   reporter transcript above.
2. **Positional sibling reference replaced by a name** — **met**. Before/after above.
3. **`:2288` renamed and its comment corrected to what it pins** — **met**. Before/after above;
   the "cannot pay for it" sentence is deleted.
4. **The money-half reading corrected in writing** — **met**, §Finding 4, stated as a citable
   claim and verified in both directions (one measured inversion, one read).
5. **The `forkId` comment states what is true; no code changed to match the old comment** —
   **met**. Before/after above, both halves verified and both bounded by an existing gate.

Global-constraint checks that apply to a naming cycle: no plan or task identifier appears in any
string I wrote (constraint 8); no test name contains "and"; the two rewritten comments were
re-read against the file's final state after the last edit.

## Deviations

- **`pnpm test:api` run as its second half only**, for the ensure-stack reason above. Recorded
  as a deviation rather than a substitution because the exit code I report comes from a command
  the plan does not name verbatim.
- **Scoped coverage run excluded two test files** (`runtime.integration.test.ts`,
  `settlement.integration.test.ts`) so the run could reach its coverage report at all — both are
  F8-red and neither imports `routes.ts`. The 35 remaining chat suites include both drivers of
  `routes.ts` (`routes.integration.test.ts`, `routes.test.ts` — the only files importing
  `createChatManifest`), so the denominator is measured by the right suites.

## Concerns and limitations

- **The product gap named in the new `forkId` comment is real and unowned**: a write-privileged
  link guest sees a Fork control whose routes will answer 401. This cycle only stops the comment
  from denying it. Closing it is a product decision (hide the control, or make fork creation
  guest-reachable through the shared guest gate) and belongs with the F4/F6 guest work, not
  here.
- **Repo typecheck is red for the whole run right now**, not just for this task, from F8's
  in-flight `runtime.ts`. Any agent gating on `pnpm typecheck` in the next hour will read a red
  repo that is not theirs.
- **The exemption may still be unreachable from the UI** — carried forward unchanged from report
  2 §Concerns, since nothing this cycle addressed it. It remains an orchestrator/lane-E routing
  question, not a defect in this task.
- **No E2E executed** (Global Constraint 11).

## Confidence

**High.** The cycle is four string edits with no behavioural surface: the two files are green in
isolation, lint and arch are clean, scoped coverage on `routes.ts` is byte-identical to cycle 2,
and the one substantive claim — the corrected money-half reading — was established by a measured
inversion rather than by argument, with the tree restored byte-exact afterwards.
</content>
</invoke>
