---
import LabeledRule from './LabeledRule.astro';

interface Props {
  /** YYYY-MM-DD effective date string from LegalDocumentMeta. */
  date: string;
}

const { date } = Astro.props;
---

<LabeledRule text={`Effective: ${date}`} />
