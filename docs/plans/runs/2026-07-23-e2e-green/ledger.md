# E2E-Green Run Ledger — 2026-07-23

Orchestrator-only. Append-terse. One line per run, per task transition, per audit verdict.

## Concurrent-agent caveat
Another agent is editing `apps/api/src/slices/identity/routes-deletion.integration.test.ts` +
`routes.integration.setup.ts` (test-suite-parallelism run). NOT my work — attribute identity
failures with care; do not touch their files.

## Run history
- Run #0 (baseline, report 2026-07-23T05-31-47): 197 pass, 48 failed, 0 flaky. CPU peak 99%/avg 75.6%, mem 92%. 2 browser crashes (feedback, usage). Streak: 0.

## Cluster inventory (Run #0)
- **A — media seed 503 (KEYSTONE, ~30):** POST /dev/media-conversation → 503. image-generation(all), video-generation(all), multi-model-media, sharing media (img/video), group-realtime-media. Symptom split: some fail "conversation creation failed: 503", others "img/video not visible" (seed succeeded, render failed, OR different retry outcome).
- **B — media-params payload undefined (3):** aspect-ratio/resolution/9:16 "flows through to /chat request" → toBeDefined() undefined. Suspect effort-feature payload-shape regression.
- **C — billing Payment Successful (4):** all billing.spec payment tests → "Payment Successful" not visible.
- **D — sharing text render (4):** "Hello from Alice"/"Echo:" not visible (link-guest-access, link-guest-chat, shared-content invite/message).
- **E — marketing leaderboard (1):** Modality "Image" button not visible.
- **F — saturation crashes (2):** feedback, usage — browser closed / 60s timeout (resource-limit browser crash).
- **G — account-deletion (1):** 4th failed attempt expects 400, got 403. CAUTION: identity concurrently edited by another agent.
- **H — smart-model low-balance (1):** "Your free daily usage can't cover this message" not visible. KNOWN INTENT CONFLICT from 2026-07-20 run (client Math.min vs server worst-case smart-model pricing). Needs founder decision — excluded from fix work until resolved.

## FOUNDER DECISION (H, 2026-07-23)
Smart Model = LEGACY behavior: reserve worst-case over ONLY the AFFORDABLE subset of models (balance-DEPENDENT affordable filter). Smart Model may pick from a subset of cheaper models when those let the reservation pass. Mirror legacy. → Reverses 07-20 Task-35 balance-independent menu. Naturally: $0 wallet blocked (classifier worst-case > $0), real low-bal user allowed (cheap models fit). Requires: (1) server admission affordable-subset-worst-case (was estimate-run.ts:445-459 max over ALL); (2) client affordability mirror via SHARED estimator (One-Impl-Shared); (3) INVESTIGATE legacy exact algorithm (findAffordableCandidates eligible-models.ts:139-208 per 07-20 research). Test smart-model.spec.ts:252 stays AS-IS (expects block). NOT trivial → analyst + fix task.

## KEYSTONE RESOLVED (orchestrator git check)
B's deleted-code hypothesis REFUTED. effort commit 2ab91d7a deleted wire-params.ts (65L) + wire-params.test.ts (85L) ENTIRELY as DEAD CODE — zero remaining source imports of compileWireParams/resolveMediaInputs (grep clean), build + 245 tests ran = no dangling refs. Deletion did NOT break media execution. → Media UNAVAILABLE = A's storage.put→MinIO seam under saturation. A STANDS.
Effort commit media-touch = smart-model-turn.ts (+12), turn-reasoning.ts, integration-setup — reasoning-effort plumbing, NOT media exec. NOT the regression.

## FAILURE TAXONOMY (final, pre-remeasure)
- SATURATION COLLATERAL @12 workers (~43): media A(~30), billing C(4), sharing D(4), crashes F(2), + 3 media-params would red via console-guard on media UNAVAILABLE once glob fixed.
- INDEPENDENT BUGS (mine): B glob (route-payload.ts, test-only); G stale deletion spec (test-only, settled in-repo); E public-stats table isolation (schema=approval OR rollback-test-writes); H smart-model (founder-decided, real code fix).
- FOUNDER GATE: H resolved (legacy affordable-subset). No open intent conflicts.
- ESCALATION PENDING: saturation root-cause fix = architecture/infra decision + OVERLAPS concurrent test-suite-parallelism agent. Analysts unanimous: MEASURED re-run before tuning. → Running full pnpm e2e as measured baseline BEFORE deciding/escalating.

## HUMAN RULINGS (2026-07-23)
1. Saturation → HARDEN. Orchestrator owns it (concurrent agent done, no coordination blocker). Don't lower workers; pin binding resource then harden.
2. Cluster E → IGNORE if just concurrency/temp. Measured re-run (fresh reseed, no vitest) decides: passes→ignore; reproduces→real, reconsider fix B.

## *** RUN #1 UPENDS THE PICTURE (full pnpm e2e, report 2026-07-23T07-11-01) ***
Full pnpm e2e = 6 projects (iphone-15/ipad-pro/chromium/firefox/webkit + auth/admin), ~1466 tests, 74min, 12 local workers, fullyParallel. Result: 575 pass, 8 flaky, 883 FAILED, 111 did-not-run. Load 51.1/20cores, mem 88%, 81 browser crashes.
NOTE: 05-31-47 (48 fails, 503s) was iphone-15 SUBSET only (e2e:fast-shape), built BEFORE my session. Full run is 6× bigger + different dominant failure.
FAILURE HISTOGRAM (883): 187 group-chat 404 + 184 conversation 404 + 25 multi-model 404 + 19 image 404 + 18 video 404 + 6 dev-group 404 = ~443 PERSONA-404 (DETERMINISTIC, even across all 6 projects ~150 each). 278 toBeVisible (downstream of 404). 81 timeouts (browser OOM). Only 4× 503 (storage saturation now minor).
*** NEW KEYSTONE: 443 deterministic 404 on /dev/{conversation,group-chat,media-conversation}. Handlers exist (routes.ts:387/401/424) but seedConversationWork raises OPAQUE 404 for UNKNOWN PERSONA EMAIL (factories.ts:36-43). ⇒ persona users NOT in DB when tests ran. NOT saturation (even distribution, deterministic, 404 not 503/timeout). Absent from iphone-15 subset ⇒ specific to FULL multi-project run. Personas are per-project (testPersonaName base×project) + pooled (test-alice/bob/dave × workerIndex%12, seed-personas.ts). Setup via per-project setup-* projects. ***
STRATEGY IMPACT: e2e:fast (iphone-15) does NOT reproduce the 404 → need multi-project repro. Full run is 74min (expensive). Must fix 404 keystone first (unblocks ~443+278 downstream). Saturation (media 503, browser OOM) is now the SECONDARY problem, still real at full scale (load 51).

## *** KEYSTONE REFRAMED: catalog-404, NOT persona-404 (analyst VERIFIED) ***
443 "creation failed: 404" = dev handlers call pickSeedTextModels BEFORE persona lookup → throws DevSeedError('no text models in catalog') → liftDevWork launders ALL DevSeedError to opaque 404 (routes.ts:104-111, factories.ts:137-139). ROOT: model_catalog TRUNCATED mid-run by CONCURRENT pnpm test/test:api doing unscoped db.delete(modelCatalog) (smart-model-turn.integration.test.ts:151+, chat/routes.integration.test.ts:318, dev/routes.integration.test.ts:359), serialized by withModelCatalogLock (Redis lock spanning VITEST suites ONLY — e2e Worker not a participant, rows never restored). e2e+vitest share ONE PG (.env.development DATABASE_URL). PHYSICAL PROOF: foreign chat-route/* vitest rows in e2e Worker error log; 575 pass (catalog populated) then wiped → 439×404.
⇒ ~721 of 883 (443 catalog-404 + 278 downstream) CAUSED BY DEPARTED CONCURRENT AGENT. User: agent is over → won't recur. Per user "ignore concurrency/temp" → treat as resolved-by-departure (operational). NO code fix required for green.
LATENT HAZARD (flag to user, don't block green): e2e/vitest share one PG, no isolation for globally-read non-namespaced tables. Class-killer = Option B isolate e2e DB (infra, APPROVAL-GATED). Option A = fail-fast distinct 503 for empty catalog (in-authority, changes pinned dev/routes.integration.test.ts:361 contract, doesn't PREVENT collision, diagnosability only). Rec both to user in final report.
OPERATIONAL RULE (enforce): NEVER run pnpm e2e while any implementer runs pnpm test:api (wipes shared catalog). Serialize e2e-verification vs test-running.
Rejected causes: persona-missing (INFERRED, unneeded), worker-crash reindex (FALSIFIED, parallelIndex bounded), catalog refresh (FALSIFIED, onConflictDoUpdate never delete-alls).
Saturation (media-503, browser OOM, load51) MAY be partly catalog-404-retry-amplified → clean run reveals TRUE saturation level.

## RUN #2a (e2e:fast iphone-15, report 2026-07-23T09-05-09): 196 pass, 47 fail, 0 flaky. Load 26.3, mem 81%, 1 crash.
- 0 × 404 (CATALOG-404 CONFIRMED GONE without concurrent vitest ✓). Residual: 6 media-503 + 31 toBeVisible (media not render) + 5 toHaveAttribute + misc = SATURATION cluster (~37 media).
- Task-01 target (aspect-ratio) fails on toHaveURL /chat/new (conversation never created = saturation, NOT glob). Task-01 stays FIXED·UNPROVEN, blocked by saturation.
- *** Task-03 target (smart-model $0-block) STILL FAILS — REAL BOUNDARY BUG. *** smart-model-affordability.ts:214 filter `balance >= classifierWorstCase + floor`. Real catalog has FREE ($0) model → classifierWorstCase=0, floor=0 → `0>=0` ADMITS $0 wallet → client minimumRequiredNanoUsd=0 → generic billing "Using free allowance"+Send ENABLED. Docstring wrongly claims $0 blocks (wrong-comment). Legacy blocked via strict `effectiveAfterClassifier <= 0` (block when balance <= classifierReserve). Unit tests missed it (PRICED catalogs only); client==server BOTH wrongly admit (why panel passed). FIX = add legacy guard: block when balance <= classifierReserve, both server subset gate AND client minimumRequired. TDD w/ FREE-model catalog + $0 → block.
- [Run#2a cycle] Task-03 boundary fixer DISPATCHED (impl-report-3): legacy `balance <= classifierReserve → block` guard on server gate + client threshold; TDD free-model catalog. Money re-audit after.
- [Run#2a cycle] Saturation analyst DISPATCHED → research/saturation-hardening.md: pin binding resource on clean iphone-15 run, recommend hardening @committed 12 workers (NO worker reduction), account for ~13 desktop baseline load.
- SATURATION DIAGNOSIS DONE [graded: agree]: root = HOST CPU OVERSUB (Verified, load 26, desktop baseline ~13). Manifests storage.put→MinIO: 6 seed-503 + 31 slow-render (put→UNAVAILABLE). Retry budget too thin (maxRetries:2/300ms). PURELY CAPACITY (no render code defect). mem/PG-pool NOT binding.
- SATURATION HARDENING DISPATCHED (task-sat, impl-report-1): A=env-scoped wider dev/CI retry storage-factory.ts:36-48 (maxRetries:6/maxDelay:5000, PROD unchanged fail-fast, idempotent LWW); B=MinIO resource floor docker-compose. Rejected C (semaphore=prod code/wrong scope). Analyst med-conf: A may not clear pure-CPU-starve renders → desktop-baseline quiescing = orchestrator-runtime lever, surface if A+B under-deliver. Next run: instrument docker stats/mpstat/ss :9000/free -m.
- Task-sat hardening DONE: A=isProduction?DEFAULT:NON_PROD_STORAGE_NETWORK (maxRetries6/init100/max5000 ~6.3s), storage tests 34 pass, typecheck+lint clean. B SKIPPED (non-swarm compose → reservations no-op; sound). → single auditor DISPATCHED (a31b...).
- Task-03 boundary fixer + Task-sat auditor both RUNNING. When Task-03 fix done → money re-audit. Then instrumented clean full e2e (Run #2).
- Task-sat AUDIT PASS (0 findings). Task-sat CLEAN → FIXED·UNPROVEN.
- *** Task-03 boundary fixer NEEDS_CONTEXT — $0-BOUNDARY DIAGNOSIS WRONG (mine). *** Real trace: NO free model (cheapest ling 10/30); current code already blocks true $0; lowBalancePage user has $0.05 free allowance (fixture zeroes wallets not allowance; "no allowance" comment stale). REAL bug = floor-vs-worst-case divergence: client prices reserve+cheapest FLOOR (0.0167c<$0.05 → affordable), server admission reserves MAX WORST-CASE over floor-affordable subset (gpt-5.5 ~$42 enters via cheap floor) → 402. Client affordable, server 402s. Money panel missed (no-under-reserve verified, not admission-fits-balance). Fixer STOPPED (money-design). → INTENT CONFLICT, FOUNDER GATE. Task-03-boundary EXCLUDED from fix work until answered; saturation + rest continue.
- FOUNDER ASKED (Option A worst-case-subset / B fixture-fix-only / C block-all-low-bal) — NO RESPONSE 60s (away). Standing directive = continue to green. DECISION: DEFER smart-model (excluded, re-ask later). Verified: free allowance is a period-keyed DAILY grant ($0.05) separate from the zeroed wallet (balance.ts:34, charge.ts:65) — confirms money-design question is real. Task-03-main (floor-subset, money-panel-clean) STAYS in tree (implements founder subset decision; only low-bal boundary pends). CONTINUE saturation verification (bigger cluster ~46/47).
- Tree for next run: Task-01(glob)+Task-02(deletion)+Task-03-main(smart-subset)+Task-sat(retry). Smart-model test will fail (excluded/known). Launch instrumented e2e:fast to verify Task-sat cleared media-503.

## RUN #2b (instrumented e2e:fast, report 09-42-47): 202 pass, 43 fail (was 47), 0 flaky. Task-sat retry helped marginally.
INSTRUMENTATION VERDICT (Verified, decisive): binder = HOST CPU OVERSUBSCRIPTION. run-queue r bursts to 99 on 20 cores; wa (iowait) 1-2 (max 10) = NOT disk; idle 8-14; MinIO CPU 0-4% (STARVED not overloaded); ss:9000 ~0 established (NOT connection-bound); mem ~7GB free (NOT memory). 
KEY CORRECTION: at THIS run's start load=1.17 (desktop QUIET), climbed to 24 (sustained, 1.2× over) w/ r=99 BURSTS — driven by the 12 media-workers THEMSELVES, not desktop. Bursts are TRANSIENT → retry-widening IS the right lever (ride them out; client render deadline 30s gives room).
FAILURE CLASSIFICATION (43): 27 STORAGE SEAM (7 seed-503 + 20 render→UNAVAILABLE, put failed server-side) + 4 billing (mock webhook dropped in burst) + 4 sharing-text (chunk-load) + 1 smart-model (excluded) + 5 toHaveAttribute + 2 misc.
HARDENING PLAN: (1) widen non-prod storage retry 6.3s→~16s (maxRetries 6→8, maxDelay 5000; stays under client 30s) to ride transient bursts — should clear most of 27; (2) harden billing dev-mock webhook self-delivery (payment-mock.ts:94-124) w/ bounded retry + loud SafeLogFields (clears 4). Both env/dev-scoped, prod-safe.

## Task transitions
- SMART-MODEL AFFORDABILITY FIX (user "do this fix", founder rule: client-accept ⇔ server-accept). Design (smart-model-affordability-plan.md, simplified per user): ONE shared completionCapTokens per candidate = affordable output ceiling; reasoning=output tokens INSIDE cap (max_tokens=B+H, reasoning-plan.ts:82), NO separate leg; effort feasibility via EXISTING pickClassifiedEffortPlan (effort-dimension.ts) given the cap — no (model×effort) matrix; high-budget=unbound (full ctx); eligible iff cap≥MINIMUM_OUTPUT_TOKENS; reserve=classifier+input+cap×rate≤balance; client==server via shared fn; BICONDITIONAL contract test (the missing One-Impl guard). Replaces floor-eligibility + single-worst-rate cap + undefined→full-ctx fallback + client cheapest-floor. → implementer DISPATCHED (task-smartmodel), DB-free unit self-gate (e2e just finished 17-02-26; avoid catalog collision); full test:api + e2e + 2-auditor money panel at verification. 
- Run 17-02-26 (10 workers, COMPLETED 51min): 784 pass, 9 flaky, 251 fail. (User to confirm clean vs polluted.)
- Smart-model implementer NEEDS_CONTEXT: (1) FORK — plan's per-candidate caps need node to carry per-candidate maxOutputTokens; node has SINGLE params.maxOutputTokens → faithful per-candidate = expand workflow.ts schema + builder + execution + estimator (beyond 5 files, SCHEMA CHANGE). (2) GOOD: server admission ALREADY uses shared spendableFundsNanoUsd (admission.ts:108) = client cushion/allowance → no Lua reconcile. (3) BUG (in-scope): smart-model-turn.ts:315 feeds RAW remainingNanoUsd (no cushion) to affordability filter → paid-within-cushion breaks biconditional. (4) Task-03 shared gate already in place; this is a refinement.
- USER ASKED (single-cap vs full per-candidate) — timed out (away). DECISION (best judgment): SINGLE-CAP (Option B) — satisfies the rule (client==server, no affordable-then-402), contained (~5 files), avoids UNAPPROVED schema change (per-candidate = approval-gated fast-follow). Formulation sent: eligibility per-candidate (afford input+MIN_OUTPUT at own rate vs EFFECTIVE balance incl cushion); single cap = min-over-eligible affordable cap (clamp ≥MIN_OUTPUT, ≤tightest ctx; unbound if ≥ctx); reserve = MAX-over-eligible ≤ effBalance; reasoning inside cap; fix cushion bug; biconditional test across tiers. Implementer resumed.
- USER CORRECTED (single-cap WRONG): per-candidate is right — reservation set once (worst-case, ≤ effBalance); picked model gets the MOST tokens that fit R at its rate (cheap→big, up to context). Classifier picks ONLY from eligible subset (node.candidates=eligible), so any pick is approved; callable ⇔ subset non-empty. → REVERSED implementer to Option A (per-candidate), AUTHORIZED schema/engine expansion (workflow.ts + builder + execution + estimator).
- Smart-model implementer DONE_WITH_CONCERNS (Option A, 580k tok/78min): per-candidate maxOutputTokens in node schema; execution applies resolved candidate's own cap; estimator prices each at own cap; shared admitSmartModel + smartModelMinimumRequiredNanoUsd; client+server share. Self-gate: typecheck 3/3, lint, test:shared 282, test:api 232, test:web 47 (all targeted). SUBTLETIES: (1) output STORAGE (600-1200 nano/tok) folded into cap(m) + priced w/ SAME subtotal markup as estimateRunCeilingNanoUsd → shared reserve == estimator hold (else free-tier 50M blown / 402); (2) cushion bug fixed (payerSpendableNanoUsd, cushion-inclusive); (3) biconditional needs client+server SAME storage context (tier output-ratio + prompt chars); (4) trial left on old single-cap answerMaxOutputTokens (per instruction). Removed exports affordableSmartModelCandidates+smartModelAnswerCap; minimumRequiredNanoUsd internal.
- 2-AUDITOR MONEY PANEL dispatched: A=reserve≤effBalance all tiers + no-under-reserve/estimator-identity + storage keystone + per-candidate cost + high-bal concurrency; B=biconditional/client==server + classifier-eligible-subset + one-shared-impl + schema/engine soundness + trial gating. Targeted DB-free tests. Then orchestrator: full test:api + smart-model e2e.
- MONEY AUDITOR B PASS (0 findings): estimator identity confirmed (candidateCost==estimateRunCeilingNanoUsd → reserve==hold → admitted never 402, free-tier keystone); biconditional structurally sound (admitSmartModel non-null ⇔ effBalance≥smartModelMinimumRequiredNanoUsd, same shared fns, symmetric storage+cushion balance); caps round-trip node schema→execution, reasoning=B+H inside cap; trial gated; estimator hoist = genuine single-source; client/server text-pool filters coincide (Model schema forbids foreign-modality rates on non-text). typecheck 15/15, 290 tests. knip: 1 unused file (packages/config/vitest.package.config.ts) = concurrent-agent, out-of-scope. Awaiting auditor A.
- MONEY AUDITOR A PASS: estimator identity EXACT (byte-identical candidateCost fold + applyMarkup + STORAGE_COST_PER_CHARACTER_NANO=300n both sides → estimateSmartModelNode ≤ reserve ≤ effBalance, no under-reserve); storage keystone; per-candidate execution applies resolved cap; high-bal concurrency balance-independent (pinned rich==richer); cushion fix verified. 2 MINORs (safe direction, but reverse-edge of strict biconditional): (M2) client prices classifier reserve over FULL pool vs server ELIGIBLE subset → client marginally stricter (denies where server admits); (M1) client getUserTier vs server tierForFunding must agree (tier-sized storage threshold; no concrete divergence found, bounded retryable 402 if drift).
- *** MONEY PANEL CLEAN on core (both A+B PASS). *** Per user's STRICT biconditional → fix both Minors (align client→server exact). Fixer dispatched (impl-report-2): M2 align client classifier-reserve to eligible subset (+ exact-threshold test); M1 pin tier-equivalence or single-source. Then re-audit + orchestrator full test:api + smart-model e2e.
- Reverse-edge fixer DONE_WITH_CONCERNS (TESTS-ONLY, no source change → money-panel-blessed impl stands, no re-audit): 
  - M2 REFUTED (correctly): auditor conflated the admission GATE (admitSmartModel non-null, full-pool basis, SAME on client+server → client-deny⟺server-deny ALREADY exact) with the estimator HOLD (eligible-subset, smaller, ≤gate≤balance, only shrinks hold). Literal fix would BREAK forward edge. Added guard tests (shared + real-estimator, eligible⊊fullPool) proving both edges — pass.
  - M1 REAL: client getUserTier (balanceCents>0, cents-truncated) vs server tierForFunding (purchased>0n, nanos) → 0<purchased<1¢ user = client'free'/server'paid' → tier disagreement → bounded biconditional break (retryable 402, no money loss). Proper fix = nano-based getUserTier, touches premium access + client billing broadly → fixer STOPPED per brief. NEEDS user scope ruling.
- Smart-model CORE money-panel CLEAN (both A+B PASS, M2 refuted+tested). M1 sub-cent tier edge → surfaced to user. → smart-model e2e verification launched (bpazjlmtu).
- WORKER REGISTRY (user request): scripts/lib/worker-count.ts — CPU_WORKER_REGISTRY {285K:12, 13900H:10}, 45%-cores default (max(1,round(0.45*availParallelism))), availParallelism clamp, E2E_WORKER_POOL_SIZE=max(resolveLocalWorkerCount(),CI=7). Wired: playwright.config workers=isCI?7:resolveLocalWorkerCount(); seed-personas re-exports pool. AUDIT PASS (on-box 13900H→10, logic/pool/wiring correct, machine-dependent-roster SAFE). Follow-ups: 13900→13900H key, seed.test.ts:89 derived (was pre-existing RED), comment. Vitest 64/0, lint clean. VERIFIED on-box: model "i9-13900H", 20 cores, resolveLocalWorkerCount()=10, pool=10.
- PENDING follow-up (user "other issues after"): smart-model client==server rule (reverse floor/worst-case asymmetry) + floor-vs-worstcase explanation. Pre-existing gate blockers still open (ExecutionContext tsc, use-resolve-default-model coverage).
- FULL pnpm e2e @10 WORKERS LAUNCHED (btnhk5i2j) — first clean full multi-project measurement on fixed tree. Report to user.
- Task-sat v2 (impl-report-2) DONE: storage retry maxRetries 6→8 (~16s); billing mock bounded retry (maxRetries:5/~5s, byEventId idempotent) + LOUD log (removed silent-swallow eslint-disable, Never-Hide-Problems). Optional config fields webhookRetry/telemetry w/ sensible defaults, no prod wiring change. 35 tests pass, typecheck+lint clean. → AUDIT pending (after e2e:fast, test:api collision). → Run #2c instrumented e2e:fast LAUNCHED to measure burst-ride-out.
- *** RUN #2c (report 10-04-27): 200 pass, 45 fail. RETRY WIDENING HAD NO EFFECT (47→43→45 stable across 6.3s/16s). billing still fails (mock retry also starved). CONCLUSIVE: repo retry/resilience hardening does NOT fix CPU oversubscription (retries themselves need CPU scheduling denied by r=83-99 bursts). run-queue still 83. 45 fails all saturation (28 chat/media, 9 sharing, 4 billing, 2 group, feedback, demo). ***
- WALL REACHED: root = 12 media-browsers intrinsically oversubscribe 20 cores (r=99, NOT desktop — desktop was quiet load 1.17 at run start). Quiescing desktop won't help (workers alone oversubscribe). Retry/pool/timeout hardening exhausted. ONLY lever = reduce concurrent CPU load = fewer workers (skill FORBIDS) OR CI (7 workers). Local 12-worker count is tied to persona-pool size (comment "workers≤personas"), NOT hardware-validated. → ESCALATE to user (architecture/config decision + skill forbids unilateral worker reduction). Re-raise smart-model founder gate too. Task-sat v2 audit dispatched (hardening still prod-safe/correct, keep).
- BOTH founder/env questions RE-ASKED — timed out again (user away). DECISION (best judgment): do NOT guess either. Workers = explicit "don't lower" instruction, won't reverse unilaterally despite empirical need. Smart-model = money gate w/ uncertain outcome, won't guess. Autonomous path to green EXHAUSTED; both residual clusters gated on user. Await user. All non-gated work done: catalog-404 resolved, Task-01/02/03-main/sat clean/auditing, hardening prod-safe.
- Task-sat v2 AUDIT PASS (0 findings). ALL FIX/HARDENING TASKS CLEAN: Task-01(glob), Task-02(deletion), Task-03-main(smart-subset money-panel), Task-sat v1+v2(storage retry + billing mock loud/retry).
- FINAL POSITION: will NOT lower worker count (skill's #1 forbidden shortcut, hides not fixes, reserved-for-human, wouldn't reach full green anyway). Autonomous path exhausted. HANDING BACK with 2 decisions:
  1. WORKERS/CPU: local pnpm e2e 12 workers can't pass on 20-core desktop (media browsers oversubscribe, r=99, retries proven ineffective 47/43/45). Options: lower local workers ~6-8 (reversible, CI unaffected) / rely on CI 7-worker / keep trying (near-zero returns). RECOMMEND lower local workers.
  2. SMART-MODEL money gate: floor-eligibility vs worst-case-reserve divergence; $0.05 fixture vs $0 intent. Options A(worst-case-subset)/B(fixture-only)/C(block-all). No clean green without this. RECOMMEND A but outcome uncertain (may flip test to allow).
- FOUND 1 NON-GATED DETERMINISTIC BUG: group-chat-leave.spec "leave from sidebar of a non-active chat" — fails all 3 runs; POST /conversations/<id>/leave returns 200 (server OK) but client leave-confirmation-modal never closes. NOT saturation (API succeeded). Hypothesis: modal-close coupled to post-leave navigation that only fires for the ACTIVE chat; non-active leave → no redirect → modal stays. → analyst dispatched (research/group-leave.md). FIX (non-gated). Cross-run check: only group-leave is deterministic-non-saturation; demo failure intermittent (1/3, saturation); rest saturation (media/billing/feedback/sharing-chunk-load) or smart-model.
- group-leave analyst DONE [graded: agree, Verified chain]: APP BUG, mobile-only, deterministic. LeaveConfirmationModal rendered INSIDE ChatItem (chat-item.tsx:281-286); useLeaveConversation.onSuccess AWAITS invalidateQueries(conversations) (use-conversation-members.ts:168-171) → refetch drops membership → ChatItem+modal UNMOUNT while vaul drawer still open → ActionModal onOpenChange(false) no-op on unmounted → mobile vaul leaves stuck node (desktop Radix removes → mobile-only). NOT a regression (9fe56658 added the first assertion reaching it). Fix A (recommended)=decouple modal to stable sidebar-level owner. Flagged: Delete/Rename share shape (latent, A covers pattern); test may need sibling expectApiErrors/expectConsoleErrors post-leave allowances. → Task-leave DISPATCHED (Fix A, leave-scoped, TDD, flag Delete/Rename). NON-GATED.
- Task-leave DONE: LeaveConversationProvider stable owner in SidebarContent; modal survives list-invalidation unmount. test:web 6004/6004, file 100%, lint/jscpd clean. AUDIT PASS (0 findings): structural fix correct, behavior preserved (isActive redirect, auth/key guards, leave authz untouched), no a11y regression, Delete/Rename honestly flagged. Task-leave CLEAN → verifying via e2e group-chat-leave spec (authoritative mobile oracle; jsdom can't repro vaul).
- 2 PRE-EXISTING GATE BLOCKERS re-confirmed (NOT mine, committed): (1) apps/api/src/middleware/pipeline-bindings.ts:59 ExecutionContext TS2304 (92785bc4) blocks web typecheck via project ref; (2) apps/web/src/hooks/models/use-resolve-default-model.ts branches 87% blocks test:web coverage. Both predate session → pre-push was ALREADY red. Flag in handoff / Phase-4.
- group-leave e2e verify (report 10-50-27, all projects): TARGET "leave from sidebar non-active" PASSES on iphone-15 ✓ (fix works). BUT surfaced 3 failed (firefox) + 3 flaky (ipad/webkit) other leave tests = WS-404 GAP: POST_LEAVE_PREFETCH_404 regex (spec:23-24) covers messages|members|links|keychain|budgets but OMITS /websocket. Client briefly WS-reconnects to left conversation → 404 → guard trips (browser-timing: firefox fails, ipad/webkit flaky, iphone-15 never). PRE-EXISTING gap MASKED by catalog-404 in prior multi-project runs; NOT my modal fix (doesn't touch WS; iphone-15 green). → fixer DISPATCHED (impl-report-2): add websocket to alternation. NON-GATED.
- WS-404 allowance fix DONE (impl-report-2): regex widened +websocket, comment accuracy fix, node regex verify + prettier + eslint pass. Trivial one-token boundary-preserving change (proof = e2e re-run, no separate audit ceremony). → re-verifying group-chat-leave all-projects (bx99ghpjr).
- Re-verify (report 11-00-31): target PASSES iphone-15 ✓; 285 pass, 1 fail (ipad non-owner-navigates: post-leave 403 trips CONSOLE guard — expectConsoleErrors only allows 404) + 2 flaky (firefox non-active stuck-modal TIMING: auto-close waits on awaited invalidation >5s under load; ipad rotates-epoch). WS-404 fix cleared most. → consolidated fix DISPATCHED (impl-report-3): (1) app void the awaited invalidateQueries(conversations) in useLeaveConversation.onSuccess (safe now modal in stable owner → fast close, mirrors pin/mute); (2) test add 403 console allowance. Then re-audit + re-verify. NON-GATED.
- Consolidated leave fix DONE (impl-report-3): de-await invalidation (dropped async, void; 32/32 + 45/45 tests, downstream-independence traced) + 403 console allowance. → audit dispatched (ad7e7e...).
- *** LEAVE RE-VERIFY (report 11-18-58): 288 PASS, 0 FAIL, 0 FLAKY across ALL 6 projects. group-leave FULLY GREEN. *** Task-leave PROVEN across projects. All non-gated deterministic work COMPLETE.
- Task-leave de-await AUDIT PASS (0 findings): downstream-independence airtight, de-await safe, 403 console allowance scoped (defense-in-depth). Task-leave fully CLEAN + e2e-PROVEN (288/0/0 all projects).

## ============ TERMINAL STATE (autonomous work exhausted) ============
ALL CLEAN + (where runnable) VERIFIED: catalog-404(operational/departed-agent), Task-01(glob), Task-02(deletion spec), Task-03-main(smart-subset, money-panel), Task-sat v1+v2(storage retry + billing mock), Task-leave(modal decouple + WS-404 + 403 + fast-close, e2e-PROVEN 288/0/0).
BLOCKED ON USER (2 decisions, both asked+timed-out, user away):
  1. WORKERS/CPU: local 12-worker media suite oversubscribes 20 cores (r=99, retries proven ineffective 47/43/45). ~27 storage-seam saturation fails gated. RECOMMEND lower local workers ~6-8 (reversible, CI unaffected) OR treat CI-7-worker as green target. Skill forbids unilateral worker-lower → won't do without user.
  2. SMART-MODEL money gate: floor-eligibility vs worst-case-reserve divergence + $0.05-vs-$0 fixture. Options A(worst-case-subset, matches founder "reservation pass", outcome uncertain may flip test)/B(fixture-only)/C(block-all). Won't guess money gate.
PRE-EXISTING GATE BLOCKERS (NOT mine, committed pre-session, block pnpm pre-push):
  a. apps/api/src/middleware/pipeline-bindings.ts:59 ExecutionContext TS2304 (92785bc4) → web typecheck red via project ref.
  b. apps/web/src/hooks/models/use-resolve-default-model.ts branches 87% → test:web coverage red.
SCOPE CAVEAT: iteration was iphone-15-only (e2e:fast). group-leave showed multi-project (firefox/webkit/ipad) has browser-specific gaps not seen on iphone-15. Full-green needs a clean multi-project pass (blocked until worker decision) to surface any remaining browser-specific issues.
- *** SCOPE REALIZATION: entire iteration was iphone-15-only (e2e:fast). Multi-project (firefox/webkit/ipad) has browser-specific gaps (WS-404) not seen on iphone-15. Full pnpm e2e green requires validating ALL 6 projects, not just iphone-15. Even post worker/smart-model decisions, full-green needs a multi-project pass to surface remaining browser-specific issues. Flag in handoff. ***
- Residual (iphone-15): 45 fails ALL gated — ~44 saturation (clear if workers lowered) + 1 smart-model (founder). catalog-404 (was ~721 on full run) GONE.
- Run #1 (measured baseline) LAUNCHED: full pnpm e2e, quiet host (no analysts). Decisive for: (a) saturation reproducibility + which resource binds, (b) E transient-vs-real, (c) deterministic-vs-collateral separation.
- H-legacy analyst LAUNCHED → research/smart-model-legacy.md (Task-03 prep).
- plan.md drafted: Task-01 (glob, ready), Task-02 (deletion spec, ready), Task-03 (smart-model, pending legacy), Task-04 (E, conditional-ignore), Task-media (harden, pending resource-pin).
- H-legacy analyst DONE [graded: high-conf, Verified]: legacy formula = reserve = classifierWorstCase + worstCase(maxFees over affordableSubset); empty subset ⇒ 402. Server ONE edit: smart-model-candidates.ts:283 menu.some→menu.filter, null when empty, candidates=subset (estimate-run.ts:566 MAXes subset auto). Client: STOP headline-min pricing, call SAME shared gate. ONE-IMPL-SHARED: hoist gate to packages/shared/src/estimate/ (+ hoist estimateRunCeilingNanoUsd+ratesFromPricing). Money-panel: verify high-bal concurrency not regressed (large wallet subset≈full pool≈old MAX). TRIAL path left untouched (no trial e2e failing, independent authority). → Task-03 READY (full brief in plan.md). Explained to user.
- [Run#1 cycle] persona-404 keystone analyst DISPATCHED → research/persona-404.md.
- [Run#1 cycle] Task-01 (glob) implementer DISPATCHED (e2e/helpers/route-payload.ts, test-only).
- [Run#1 cycle] Task-02 (deletion spec) implementer DISPATCHED (e2e/account-deletion.spec.ts, test-only; MUST NOT touch identity integration files).
- Task-03 (smart-model) HELD until persona-404 footprint known (disjoint apps/api ownership).
- Task-media saturation HELD until keystone fixed (secondary; re-verify reproduces after 404 cleared).
- Task-01 (glob) AUDIT PASS (0 findings, orchestrator agrees): matcher correct, over-match traps (/chatter,/chats) rejected, disjoint from concurrent agent, no-persisted-test deviation verified unavoidable (vitest excludes e2e/**, no enforcement rung). Task-01 CLEAN → label FIXED·UNPROVEN (3 target media-params tests flip only once media works).
- Task-02 (deletion spec) DONE_WITH_CONCERNS: sound deviations — (a) declined apps/api keys.ts import into e2e (boundary), derives count dynamically (loop 400→403, bound 10); (b) read contract via git show HEAD: (concurrent agent has integration file mid-rename). CONCERN to audit: does dynamic loop still PIN exact lockout boundary or weaken to "locks within 10"? → auditor dispatched (single comprehensive 3-lens, proportionate for test-only).
- Task-02 AUDIT PASS. Minor finding: dynamic probe asserts lock fires in [2,10] not exactly maxAttempts+1. ORCHESTRATOR JUDGMENT: valid but NO FIX — exact-count already pinned at integration layer (boundary-legal); e2e role = verify UI surfaces lockout (done); forcing exact count into e2e = hardcode literal (sync-smell) or duplicate authority. Respects One-Impl + defense-in-depth. Task-02 CLEAN → FIXED·UNPROVEN (target: deletion lockout e2e flips when auth-tests project runs).
- Task-03 (smart-model) implementer RUNNING (runs test:api self-gate) → e2e verification HELD until it completes (catalog-collision rule). After Task-03 clean → clean full e2e measures fixed tree + catalog-404 resolution + true saturation.
- Task-03 DONE_WITH_CONCERNS: self-gates all pass (test:shared 2256, test:api 6042, test:web 363, lint/typecheck shared+api). Footprint SMALLER than planned: smart-model-turn.ts + client-billing.ts needed NO change (fix routes through buildSmartModelCandidates + existing generic client affordability). Hoist expanded (bounded, One-Impl): +callManifest/NO_STORAGE/outputTokensOf/3 usage types/classifierReserveLineItems; no apps/api-local media/settlement/Result leaked. Deviation: touched estimate-run.ts (comment only) + estimate-run.test.ts (TDD) — criterion-named, zero logic. → 2-auditor MONEY PANEL dispatched (A=reserve correctness/$0-block/under-reserve/concurrency; B=client==server/One-Impl/hoist-scope).
- Task-03 MONEY AUDITOR B PASS (client==server/One-Impl/hoist lens). Shared pricing gate genuinely single-sourced (priceSmartModelPool), hoist clean (no Result/DB/apps-api leak), money fails closed. MINOR finding: client smartModelPoolFromCatalog (use-prompt-budget.ts:302-320) vs server isEngineTextModel (smart-model-candidates.ts:207-209) use DIFFERENT pool-MEMBERSHIP predicates — pricing shared but which models enter pool differs; parity test feeds identical pool so doesn't exercise divergence. Latent (current catalog doesn't trigger, settlement server-authoritative), but SAME drift class the task kills. Fix candidate: share the text-eligibility predicate. HELD pending auditor A. Confirmed web ExecutionContext tsc error PRE-EXISTING+COMMITTED (92785bc4), not Task-03.
- Task-03 MONEY AUDITOR A PASS (reserve lens). All 5 invariants verified+pinned ($0-block, filter/reserve asymmetry, no under-reserve, high-bal concurrency preserved, nano-bigint). MINOR: stale JSDoc {@link estimateTokensForTier} smart-model-candidates.ts:55 (broken link). Confirmed ExecutionContext tsc PRE-EXISTING (git blame 92785bc4, web pulls api type graph via AppType import in api-client.ts, api-alone typecheck clean).
- *** MONEY PANEL CLEAN (both A+B PASS). *** 2 validated Minors → 1 fixer dispatched (impl-report-2): (1) fix broken JSDoc link; (2) share text-eligibility predicate so client pool membership == server (close residual drift, acceptance-criterion "no drift"). Re-audit next. Then clean full e2e.
- Task-03 fixer NEEDS_CONTEXT: Finding1 (JSDoc) FIXED (comment-only, typecheck+lint green). Finding2 BLOCKED — prescribed shared-predicate fix needs wire Model to carry inputs/outputs (not on wire schema) → would touch list-models.ts + schemas/api/models.ts (out of Task-03 bounds). Fixer rebuttal: auditor-B scenario UNREACHABLE.
- ORCHESTRATOR VERIFIED + JUDGED Finding2 INVALID → DROP: list-descriptors.ts:33-35 isExposed drops empty-pricing AND !isRunnableModelShape (shared model-descriptor.ts:56) BEFORE either pool; comment "multi-output/no-text-input stays hidden". Both client catalog (listModels) + server pool derive from same isExposed set → B's non-text/multi-output divergence UNREACHABLE. Residual = partial-rate text model: client rate-check (UX-preflight) vs server shape-check (engine eligibility) = COMPLEMENTARY independent authorities (CODE-RULES "Identical≠complementary"), divergence in SAFE direction (client conservative, never affordable-then-402). NO fix; NO scope expansion. Respects don't-over-engineer + defense-in-depth.
- *** TASK-03 CLEAN (money panel clean, Finding1 fixed, Finding2 correctly dropped). ALL 3 FIX TASKS CLEAN: Task-01(glob), Task-02(deletion spec), Task-03(smart-model). *** → tree free, no concurrent test → LAUNCH clean full e2e (Run #2).
- Run #2a: e2e:fast (iphone-15 health check, ~7min) LAUNCHED — verifies Task-01(glob→media-params) + Task-03(smart-model) targets + shows iphone-15 saturation. NOT streak-eligible. No concurrent vitest (all subagents done). Then full pnpm e2e for real measurement.
- Run #2a attempt-1 KILLED early (harness/transient, empty output, no orphaned workerd). Relaunched (bj9glfs7g) — now progressing "Running 248 tests using 12 workers" iphone-15.
- HOST ENV FINDING (bears on saturation): e2e host is a LIVE DESKTOP, not dedicated CI. Baseline load ~13 on 20 cores from @supersetdesktop (~65% cpu combined), mutagen sync (16%), cosmic DE, claude agents — BEFORE any e2e. Mem healthy (21Gi avail). ⇒ full 6-project run @12 workers stacks onto load-13 baseline → load 51. Hardening must account for desktop contention; can't assume a clean box.
- PRE-EXISTING GATE ISSUES flagged by Task-03 (NOT mine, for Phase-4 attribution): (1) web typecheck ExecutionContext in pipeline-bindings.ts — pre-existing, reproduced w/ web change reverted [BLOCKS pre-push — investigate Phase-4, may be concurrent-agent tree state]; (2) web coverage use-resolve-default-model.ts 87% (unmodified, pre-session); (3) api coverage identity/routes.integration.setup.ts 88% (concurrent agent's file).
- Phase-1 dispatch: 5 analysts (read-only) covering all 8 clusters:
  - media-503 (A) → research/media-503.md
  - media-params-payload (B) → research/media-params-payload.md
  - billing-payment (C) → research/billing-payment.md
  - sharing-leaderboard-saturation (D/E/F) → research/sharing-leaderboard-saturation.md
  - deletion-403 (G) + smart-model confirm (H) → research/deletion-403-and-smartmodel.md
- Confirmed by orchestrator: G = real runtime 403-vs-400 regression (test edits by other agent can't affect built server). H = same known intent conflict, persists on current tree → founder gate, excluded from fix work.
- Analyst A (media-503 KEYSTONE) DONE [graded: agree, Verified mechanism / Inferred binding-resource]: media cluster = ONE root cause = storage.put→MinIO seam FAILING under host saturation. Two CORRECT fail-closed mappings: 503 (dev seed, routes.ts:104-113) + UNAVAILABLE (real run, media-persist.ts:178-183→interpreter.ts:1046). DISCRIMINATOR: failures are UNAVAILABLE not INTERNAL ⇒ seam is storage.put NOT pg/settlement; mock provider in-process ⇒ not modelCall. 132× workerd broken-pipe, mem 92%, 12 workers (E2E_WORKER_POOL_SIZE). NOT effort-feature (git show --stat: no storage/media/config touched). Rec: Option A harden capacity+backpressure at committed 12 workers, DON'T lower workers, DON'T weaken fail-closed retry (Option B risks masking prod R2). MUST pin binding resource (mem vs MinIO conns) via MEASURED re-run before tuning. img-not-visible = strictly downstream of run UNAVAILABLE, not independent.
- CONVERGENCE: host saturation @12 workers = shared upstream driver of media(A)+billing(C)+crashes(F). CONCURRENT AGENT owns exactly this domain (2026-07-22-test-suite-parallelism, uncommitted in tree). Coordination decision looming.
- Analyst G+H DONE [graded: agree, Verified high-conf]:
  - G (deletion 403): NOT a regression — DELIBERATE change in c6209b02 (maxAttempts 3→2; locked resp 429/TOO_MANY_ATTEMPTS → 403/DELETE_ACCOUNT_LOCKED) for legacy parity, documented (keys.ts:220-224) + ALREADY PINNED by committed integration test routes-deletion.integration.test.ts:172-199. e2e/account-deletion.spec.ts is the STALE straggler (still 3×400-then-429). FIX = update e2e spec to current contract (test-only; loop maxAttempts derived from config, assert 400 INVALID_TOTP_CODE then 403 DELETE_ACCOUNT_LOCKED; fix expectApiErrors/expectConsoleErrors 429→403). Does NOT touch concurrent agent's integration files. Auth-sensitive → flag to founder but intent settled in-repo. Trace showed 400,400,403 (locks on 3rd/index2), spec comment "very first" wrong.
  - H (smart-model): divergence PERSISTS on current tree. Client=list-models.ts:194-206 min pool rate incl $0 free models → client-billing.ts:122-125 free_allowance → send enabled. Server=estimate-run.ts:445-459 worst-case MAX candidate reserve → 402. lowBalancePage zeros BOTH wallets (fixtures.ts:883-888; spec:249 "~$0.01" comment STALE). INTENT CONFLICT → founder gate, EXCLUDED from fix work.
- Analyst D/E/F DONE [graded: agree]:
  - D (sharing text, 4) = SATURATION COLLATERAL. D-1 (3 link tests): lazy JS chunk load fail from starved Vite preview → CatchBoundaryImpl error boundary → text never mounts. D-2 (1 shared-message): API 200 but truncated body → decrypt/parse throw → error state. No product defect. (Optional real UX edge: truncated-200 collapses to permanent "expired" error — separate low-pri improvement, use-shared-message.ts:133-155.)
  - E (leaderboard, 1) = GENUINE INDEPENDENT test-isolation defect, NOT saturation. public-stats-stores.ts:122-139 readLatestPublicStatsSnapshot reads globally-latest public_stats_snapshots row with NO table scope (scope only on Redis cache key, platform-keys.ts:63-68). vitest public-usage-stats.integration.test.ts:42-85 commits snapshot rows to shared local PG → shadows e2e seed → payload text-only, no image → "Image" tab absent. Surfaced by split-api-tests ordering. Fix A=scope table read (SCHEMA CHANGE, approval-gated); B=rollback integration test writes (no migration, weaker); C=reseed ordering (band-aid, rejected).
  - F (feedback+usage, 2) = PURE SATURATION COLLATERAL (browser OOM/CPU-starve mid-action; both functioned until a normal action stalled 52-58s then browser closed). No product defect.
- Analyst B (media-params payload) DONE [graded: agree on glob; keystone conflict flagged]:
  - 3 toBeDefined() failures = CAPTURE-GLOB BUG, NOT payload regression. route-payload.ts:37 page.route('**/chat/**') → regex ^(.*/)chat/(.*)$ requires literal 'chat/', can't match POST /chat (no trailing slash) → capture never fires. Independent of 503. FIX A=RegExp /\/chat(?:\/|\?|$)/ in shared helper. specs byte-identical pre/post effort commit → latent bug, not regression. NOTE: even after glob fix, these 3 will red via console-guard on the concurrent media UNAVAILABLE unless media fixed.
  - *** CONTESTED KEYSTONE: B hypothesizes effort commit 2ab91d7a DELETED compileWireParams/resolveMediaInputs/WireParams from models/domain/index.ts → media node execution → node-failed → UNAVAILABLE (failures.ts:114). This is a DETERMINISTIC CODE REGRESSION, contradicts A (saturation, "no media/storage touched"). B graded INFERRED (didn't trace callers). If node-failed ALSO maps to UNAVAILABLE, A's UNAVAILABLE-vs-INTERNAL discriminator does NOT separate storage-seam from media-exec defect. MUST RESOLVE via quiet-host measured re-run (deterministic regression fails at workers=1/idle; saturation passes at low load). Also verify git diff of models/domain/index.ts directly. *** billing = SATURATION COLLATERAL, not code regression. Charge POST → 200 awaiting_webhook; mock self-webhook fetch dropped under broken-pipe saturation + SILENTLY SWALLOWED (payment-mock.ts:121-124) → wallet never credited → UI hangs "Processing payment...". Decisive: billing fails ONLY in saturated runs, passes in ALL clean runs, count varies (flake signature). Class-fixes (mine, no prod change): B=bounded retry on mock self-delivery; C=make swallowed failure loud (SafeLogFields). Root trigger = host saturation (overlaps concurrent test-suite-parallelism agent). NOTE playwright.config.ts:46-50: workers>personas wrap onto shared wallets → cross-worker admission contention amplifier.
