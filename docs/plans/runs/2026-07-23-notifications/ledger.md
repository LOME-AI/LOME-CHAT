# Ledger — 2026-07-23-notifications

- 2026-07-23: Run opened. Phase 1 (explore + research before concept proposal; human wants concept iteration BEFORE design planning).
- Dispatched 3 background researchers: current-system.md (codebase-explorer, very thorough), loved-features.md (web-researcher), platform-capabilities.md (web-researcher).
- loved-features research DONE → research/loved-features.md. Headlines: reply-from-notification + per-conversation mute + mention-only defaults are table stakes; Signal wake-up-push + 3-tier content visibility is the E2E-consistent pattern; run-completion push is a live unmet want (ChatGPT forum); permission priming best practice; anti-features: marketing on alert channel, un-mutable categories.
- current-system research DONE → research/current-system.md. Exists: notifications slice (device_tokens only, register/unregister), FCM-v1-only push (APNs proxied, no Web Push), fires on ONE event (new message persisted), content-free payload (conversationId only), 14 Resend emails all auth/admin (zero activity emails), per-conversation mute is the only preference. Gaps: no browser/SPA push at all, no SW/Notification API, no badges/tab-title/sound, no token expiry job, no settings UI (permission asked on mount!), no dev push visibility, zero e2e, no account-level prefs.
- platform-capabilities research DONE → research/platform-capabilities.md. All three transports feasible from Workers via WebCrypto: Web Push (RFC8291+VAPID; @block65/webcrypto-web-push; stock web-push npm does NOT work), FCM v1 (RS256 JWT), APNs direct (ES256; wrangler-dev-on-mac HTTP/2 bug). Constraints: iOS web push = installed PWA only; badge API zero support on Android web; encrypted preview needs iOS NSE only; silent-push throttled on iOS; Capacitor iOS token is raw APNs (token-swap needed for unified FCM); no maintained local mock for FCM/APNs (Web Push has web-push-testing). Declarative Web Push = Safari-only additive.
- All Phase-1 research in. Next: concept proposal to human (no design yet, per their instruction).
- 2026-07-24 FOUNDER RULINGS on concept list: keep all EXCEPT — drop #6 (activity emails) and #9 (@mentions never supported). #10/#11: ALL notifications generic, no plaintext ever, NO on-device decryption, NO NSE, no three-tier visibility. #12: no primer-at-value-moment flow; one-time simple "enable notifications?" that points to settings; all behavior follows explicit settings, per-conversation/per-item toggles manually driven. #13 device-management UI OUT of scope — push is all-in-or-out per user, no per-device controls. #15 dev mocking/testability approved. Web Push dep approved in principle — find the best one. iOS transport: founder asked for explanation of the question.
- Dispatched package-researcher: pick best Workers-native Web Push library.
- webpush-library research DONE → research/webpush-library.md. Decisive: @pushforge/builder, @block65/webcrypto-web-push, webpush-webcrypto all implement legacy aesgcm only (Apple 403s, needs aes128gcm) — disqualified; web-push-browser has endianness bug — rejected. Recommendation: in-house ~250 LOC (WebCrypto + jose already in apps/api; RFC 8291 Appendix A test vector). Runner-up: web-push-neo 0.1.2 (RFC-correct, MPL-2.0, single maintainer). Needs founder ruling (reverses "find a dep" instruction).
- 2026-07-24 FOUNDER RULINGS round 2: FCM stays the all-in-one gateway (no direct APNs in code). Item-11 residue (dismiss-on-read-elsewhere) KEEP. Generic-content boundary CONFIRMED (event type + deep link only; no sender names, no conversation titles). New doctrine doc in docs/ + DEVELOPMENT.md index entry — written AFTER design is done, BEFORE implementation. Web Push impl choice unanswered → proceeding in-house (no new dep ⇒ no approval needed; flagged as reversible).
- Design phase opened. Dispatched: analyst (server pipeline architecture), analyst (client architecture), codebase-explorer (iOS token-swap + SPA SW/permission verification).
- Verifier DONE: iOS push is BROKEN end-to-end today (Verified) — bare @capacitor/push-notifications only, no Firebase SDK/GoogleService-Info.plist/apnsToken bridge in the iOS shell; raw APNs token is stored and sent to FCM messages:send which will reject it. Android FCM-native works as designed. SPA: zero SW/PWA/Notification API (Verified). Permission-on-mount: use-push-notifications.ts:23-32 gated by isNative(), mounted via CapacitorProvider at __root.tsx:27. Fixing iOS under the FCM ruling needs Firebase-in-the-iOS-shell (likely @capacitor-firebase/messaging — NEW DEP, needs founder approval) or manual AppDelegate+SDK wiring.
- Server-pipeline analyst DONE (material → research/server-pipeline-design.md). Recommendations I'll carry into the plan: (1) ingestion = one best-effort notifyEvent barrel fn at composition root, presence via caller's fire-time snapshot (DO pattern as today); jobs REJECTED for delivery (category error); only new cron = stale-token retention delete. (2) pure decision fn; NEW notification_preferences table (notifications-owned, typed cols incl IANA tz + quiet-hours minutes); mutedUntil timestamptz REPLACES conversation_members.muted (lazy expiry, conversations-owned); device_tokens EXTENDED for web (endpoint+p256dh/auth+CHECK+lastSeenAt) — no new subscriptions table. (3) dismissal = collapse/tag everywhere (Topic = HMAC-derived alias, never raw convId) + durable lastReadSeq cursor on conversation_members (GREATEST-idempotent) + lazy client clear on open/foreground; remote-clear pushes REJECTED (iOS can't reach success). (4) one PushSender port, composite adapter (FCM + in-house webpush in slice adapters, clean-room RFC 8291/8188/8292, jose VAPID; MPL web-push-neo never vendored); VAPID via env.config, committed dev keypair; 404/410 → existing deadTokens prune; /dev/push inbox mirroring mailbox capture. (5) quiet hours = IANA tz server-evaluated (workerd Intl = verify-at-impl), suppressed events DROPPED (deferred delivery/badge-only rejected). RAISED for founder: schema approvals; muted=true migration mapping; Topic-alias doctrine note; push-notify membership-reader duplication hoist question.
- Client analyst DONE (material → research/client-design.md). Recs: hand-rolled push-only TS SW (stable /sw.js, no fetch handler, no vite-plugin-pwa) + hand-written manifest.webmanifest (iOS push requires installed PWA; side effect: app becomes installable); typed web-subscription registration (no token-string smuggling); one-time enable = inline dismissible callout, localStorage-persisted, one notificationChannel facade over web/native adapters (kills ask-on-mount for BOTH platforms); foreground layer = source-agnostic zustand store → tab title + setAppBadge + opt-in sound; focused-tab suppression layered (server presence + SW display-time check); settings = Notifications Card on /settings; mute tiers via DropdownMenuSub. Raised: unread-badge semantics needs founder ruling; validator hoist to shared; manifest nod.
- Asking founder ONE round: schema approvals+muted mapping / iOS Firebase dep / unread semantics / manifest nod.
- 2026-07-24 FOUNDER RULINGS round 3: schema changes 1/3/4 APPROVED (notification_preferences; device_tokens web ext + lastSeenAt; lastReadSeq cursor); change 2 REJECTED — mute STAYS boolean, NO duration tiers (concept item 8 dropped; existing Bell toggle unchanged). iOS fix via @capacitor-firebase/messaging (dep APPROVED). Unread badge = CLIENT-ONLY activity badge (hybrid rejected). PWA manifest APPROVED (installable side effect accepted).
- Research docs server-pipeline-design.md §2/§3 (mutedUntil) and client-design.md §5 (mute tiers) + §4 (hybrid) superseded on those points by these rulings — plan.md is authoritative. Writing plan.md.
- plan.md WRITTEN (Tier 2, 12 tasks 00-11). Presenting digest for Phase-2 approval. No dispatch until approved.
- Plan amended per founder: Task 12 (Maestro flows — flow 07 rewrite mandatory, enable/grant flow, FCM-broadcast delivery spike) + Task 13 (Playwright notifications spec incl. CDP deliverPushMessage spike) added; Related E2E section rewritten; graph updated.
- Task 00 EXECUTED by orchestrator at founder instruction (ahead of plan approval): docs/NOTIFICATIONS.md written (laws, categories, transports, pipeline, data model, lifecycle, dismissal/foreground, permission UX, env, testing, deliberate limits — tables throughout); DEVELOPMENT.md doc-index line added. Phase-4 auditor pass still owed on the doc.
- PAUSED at founder instruction after doc changes. Plan awaiting approval; nothing dispatched.

## Phase 3 — dispatch loop
- 2026-07-24: FOUNDER APPROVED plan ("Execute the plan"). Model switched to Opus 4.8 1M/high.
- Readiness note: Tasks 01/08/11 all touch apps/web/src/capacitor/** — must serialize. Plan: 01 first (tiny validator hoist), then 11; 08 after 07.
- Ready now: 01 (shared contracts), 02 (db migrations) — no file overlap. 11 HELD until 01 clean (capacitor glob).
- DISPATCHED 01 (impl) + 02 (impl) in parallel.
- 02 impl DONE_WITH_CONCERNS → dispatching audit. Raised: (a) id-PK + UNIQUE userId instead of literal userId-PK (matches `preferences` convention; userId-upsert contract for T04 preserved) — I judge acceptable, auditor to confirm contract intact; (b) enum ADD VALUE 'web' + CHECK using 'web' in one migration file — PG transaction-sequencing hazard, auditor must re-verify migration applies clean; (c) auth/p256dh correctly not admin-panel-readable; last_seen_at also not panel-readable — flagged for possible GRANT ruling later; (d) muted confirmed boolean/untouched, pinned by shape test.
- 01 impl KILLED by transient API error ("Not logged in") mid-report-write; work was complete (packages/shared/src/notifications/ files on disk). RESUMED from transcript (not respawned) to re-verify checks post-edit + write report. 02 audit still running.
- 02 audit VERDICT FAIL — 1 Critical, VALIDATED (auditor reproduced with real drizzle-kit migrate on incremental DB: exit 1, objects not created). Migration 0058 does ALTER TYPE ADD VALUE 'web' + CHECK referencing 'web' in same txn → fails every incremental/prod deploy; fresh-reset masks it (co-transactional). Implementer's "verified green" was fresh-DB-only paths (can't surface it) — reward-hacking smell confirmed.
  - Validator rule: I judge the deterministic real-binary reproduction + documented PG invariant as meeting the single-auditor-Critical confirmation bar; proceeding to fix without a redundant validator re-running the same migrate.
  - PROCESS FIX: plan.md Task-02 criteria amended to REQUIRE incremental (non-reset) migrate verification with the real binary — the old "clean on reset" criterion was the hole that let this pass.
  - Other audit results: schema shape/CHECKs/defaults/muted-untouched/lastReadSeq/relations/no-drift/secret-column-grants all PASS; id-PK+UNIQUE-userId deviation cleared (contract intact); last_seen_at panel-readability = out-of-scope ops ruling, not a finding.
  - DISPATCHING 02 fixer (validated finding verbatim + fix direction: CHECK via platform::text = 'web', auditor-verified clean incrementally; regenerate to keep drift clean; prove incremental apply).
- 01 impl DONE (after resume). Owned-file checks all green (test:shared 11/11, test:web, shared+web typecheck, provider.tsx lints clean, jscpd 0). Known-external reds attributed away: use-prompt-budget.ts billing lint (not ours), shared/index.ts estimate-rehome concurrent edit. Recorded both as Standing amendments. DISPATCHING 01 audit (1 auditor, non-sensitive). In flight: 01-audit + 02-fix.
- 02-fix DONE: CHECK → platform::text = 'web' (cast, no enum literal); incremental apply proven by fixer with real drizzle-kit binary (exit 0, enum [ios,android]→+web, cast CHECK present); no drift; test:db + typecheck+lint green. Transparency note: local dev DB had stale prior-0058 → phantom pending, cleared by db:reset (CI/prod never applied old 0058). DISPATCHING 02 RE-AUDIT (independent incremental-apply reproduction).
- 01 audit PASS (no findings) — CLEAN. test:shared turbo-wrapper red = DB pre-step (Task 02 infra), correctly out-of-scope; notifications tests pass in isolation.
- Readiness recompute: 03 ready (needs 01✓); 11 ready (capacitor-dir freed by 01 done). 04 still waits (needs 02 clean + 03). 07 waits (needs 03). 
- SEQUENCING NOTE: Task 08 shares apps/web/src/capacitor/use-push-notifications.ts with Task 11 → 08 must run AFTER 11 (and after 07). Recorded.
- DISPATCHING 03 (impl, SENSITIVE-crypto → 2 auditors later) + 11 (impl, iOS Firebase, new dep @capacitor-firebase/messaging approved) in parallel. In flight also: 02 re-audit.
- 02 re-audit PASS (no findings; incremental apply independently reproduced, CHECK regression both directions verified, no drift) — Task 02 CLEAN. Critical resolved.
- Readiness: nothing new opens from 02 alone (04 still needs 03). In flight: 03 (impl), 11 (impl). Next unblock wave = when 03 clears → 04 + 06 + 07 ready.
- 11 impl → NEEDS_CONTEXT (correctly surfaced, zero files changed). Load-bearing: @capacitor-firebase/messaging (founder-approved) reportedly CANNOT coexist with @capacitor/push-notifications — replaces push plugin on BOTH platforms → removes current plugin + rewrites Android (violates "Android unchanged") + adds firebase^12.6.0 (2nd dep). Alt: @capacitor-community/fcm coexists, Android untouched, iOS-only — but NOT the approved dep. Also: env can't build/verify iOS (Linux, no pods/xcodebuild) — native half founder/iOS-host-owned regardless. Android google-services VERIFIED present+working. Server push-fcm.ts is plugin-agnostic (takes FCM token) — decision is client-only.
- Reverses basis of founder approval → must go back to founder, but first dispatching ANALYST (dependency-choice mandate) to verify/refute coexistence claim + frame option set vs core values. 11 PARKED (non-blocking leaf). Task 03 continues.
- ANALYST DONE (research/ios-fcm-plugin-decision.md): coexistence claim VERIFIED for @capacitor-firebase/messaging (docs forbid coexistence); @capacitor-community/fcm coexists, delivers founder intent, no firebase JS peer. Rec: Option B.
- FOUNDER RULING: Option B (@capacitor-community/fcm, coexist). plan.md Task 11 rewritten accordingly; Android untouched; iOS-only FCM.getToken() branch.
- Added G11 (no plan/task-ID labels in shipped code — per recalled memory + CODE-RULES durable-naming). Auditors to check.
- 03 impl DONE_WITH_CONCERNS → dispatching audit (SENSITIVE crypto → 2 independent auditors). Concerns: (a) no gitleaks allowlist added — VAPID keys don't trigger gitleaks (verified, CF_ACCESS_DEV precedent); brief assumed a fire that doesn't happen — ACCEPT, auditor confirms gitleaks passes. (b) env.config concurrently edited by sandbox workstream; generate:env baked both workstreams' vars into derived files — auditor must attribute sandbox vars away. (c) ENV judgment: VITE_VAPID_PUBLIC_KEY uses secret() in prod — auditor MUST verify the public key is actually frontend-reachable at build (a VITE_ var gated as backend-only secret would break subscribe). Seam for T04: sendWebPush(...)→ResultAsync<{outcome:delivered|dead|failed,statusCode}>.
- Re-dispatching Task 11 via RESUME (agent kept its verified plugin research; changed 0 files) with Option B ruling.
- 03 audit AUDITOR-2 (crypto adversarial): FAIL, 1 Important VALIDATED. gitleaks fires on webpush TEST files (4 hits: RFC App-A auth secret + throwaway VAPID priv key inline next to authSecret:/privateKey:). Implementer scanned only env.config.ts+wrangler.toml (clean), never the test files. Gates whole CI DAG. Fix: narrow AND-pinned allowlist entries for the 3 webpush test paths (seed-crypto/stream-handler precedent), NOT broad. ALL crypto verified CORRECT: App-A vector is RFC's real published value driving real code path (not self-captured), HKDF info strings correct, ECDH fresh ephemeral, ES256 not RS256, exp cap, base64url consistent, G11 clean, 100% coverage. env.config.ts confirmed 0 gitleaks.
- HOLDING fixer until Auditor-1 returns (VITE_VAPID_PUBLIC_KEY frontend-reachability Critical check) to batch all findings into one fix cycle.
- 03 audit AUDITOR-1: FAIL, SAME single finding (gitleaks on 4 webpush test lines), rated Critical (gates CI DAG). BOTH auditors converge → valid, no validator needed (both reproduced with gitleaks CLI). Auditor-1 CLEARED the VITE_VAPID_PUBLIC_KEY concern: it's Destination.Frontend, delivered to web build via ci.yml/release.yml secrets — NOT server-gated; browser can subscribe. Crypto fully verified correct by both.
- Only validated finding = gitleaks allowlist. Process fix: standing amendment added (scan ALL owned files incl tests; narrow path+value-pinned entry). Ops note recorded: VAPID_PUBLIC_KEY and VITE_VAPID_PUBLIC_KEY must be identical (founder secret-provisioning).
- DISPATCHING 03 fixer (single validated finding verbatim; .gitleaks.toml only). Re-audit will be 1 auditor scoped to: gitleaks 0-leaks on owned files + allowlist is narrow + crypto files byte-unchanged.
- 03 fix DONE: 3 narrow AND-pinned .gitleaks.toml entries (path+exact-value) per stream-handler/media-assets precedent; gitleaks 0 findings (was 4); crypto source/tests byte-unchanged (diff = only .gitleaks.toml). Flag: repo tree mid-interactive-rebase (pre-existing, another workstream — not touched; I do no git writes). DISPATCHING 03 re-audit (1 auditor, config-scoped).
- 03 re-audit PASS (no findings; control scan proved entries load-bearing+narrow) — Task 03 CLEAN. Crypto verified-correct, CI gate unblocked. (Out-of-scope: pre-existing RESEND_API_KEY fixture in env.config.test.ts:583 — not ours, not touched.)
- Readiness: 03 clean → 04 (needs 01✓02✓03✓) + 07 (needs 01✓03✓) ready, no mutual overlap, disjoint from in-flight 11. 06 waits on 04 (shared adapters dir). DISPATCHING 04 (impl, SENSITIVE→3-lens panel later) + 07 (impl, 1 auditor later). In flight also: 11 (iOS Option B).
- 11 impl DONE_WITH_CONCERNS (Option B): @capacitor-community/fcm ^8.1.0 added (no firebase JS peer, coexists), iOS-only FCM.getToken() branch, Android byte-unchanged+verified working. Owned test 7/7 100% cov. Concerns (all expected/attributed): cap sync ios + real-device unverifiable on Linux (founder/device-owned per plan; checklist in report); pnpm-lock.yaml churn (dep add, shared file); pnpm test:web abort = known Vitest coverage-merge ENOENT flake (not this change, matches test-speed memory); pre-existing capacitor.config.ts→@hushbox/shared CJS resolution failure (NOT ours). DISPATCHING 11 audit (1 auditor). In flight: 04, 07.
- 11 audit PASS (no findings) — Task 11 CLEAN. Auditor verified plugin self-configures Firebase (Plugin.swift load()→FirebaseApp.configure()+APNs token; Package.swift pulls firebase-ios-sdk transitively) → no AppDelegate edit needed; founder checklist accurate; correct plugin, wrong ones absent; Android byte-unchanged. Placeholder plist = no secret (CI gitleaks will confirm). 
- Readiness: 11 clean but 08 still needs 07. No new dispatch. In flight: 04, 07.
- 07 impl DONE_WITH_CONCERNS. Push-only SW (no fetch/precache, test-asserted), manifest, stable dist/sw.js second build entry, /sw.js no-cache in headers SOURCE, !isNative()-gated register helper. Owned tests 24/24 100% cov, owned lint/build green.
  - KEY DEVIATION (accepted, resolves my plan contradiction): device-local notification `tag` = raw conversationId (from decrypted/data payload), NOT a "payload alias" — I1 schema is strict {category,conversationId} w/ no alias field. Device-local tag ≠ push-service header → G1-safe. AMENDED plan I4 + Task-09: server push-service-visible headers (Topic/collapse_key)=alias; device-local tag=conversationId; Task 09 clears by conversationId. 
  - CROSS-TASK RED (attributed to Task 04 in-flight, NOT 07): turbo typecheck --filter=web fails on 4 errors in apps/api/src/slices/notifications/** (PushRecipient/PushDeviceRef mismatch from 04's in-flight port refactor, reaching web via AppType). 07's own web files typecheck clean. → Task 04 MUST leave api typecheck green (enforce at its report; transitively fixes web).
  - Minor deviations (accepted): SW typed against narrow structural interfaces (can't add webworker-lib tsconfig); pushsubscriptionchange uses postMessage-to-client fallback (SW can't carry cookie cross-origin) — both anticipated in client-design.md. register-sw.ts unused until Task 08 (Phase-4 knip).
  - DISPATCHING 07 audit (1 auditor).
- 07 audit PASS + 1 Minor VALIDATED: handlers.ts:170 pushManager.subscribe() not try/catch-wrapped → unhandled waitUntil rejection (CODE-RULES: wrap every external call). Benign (backstopped by next-start re-reg + 404/410 prune) but must fix. Tag-deviation CONFIRMED G1-safe (SW sends nothing to push service; subscribe carries only VAPID key). Cross-task typecheck errors confirmed other-workstreams (owned files clean). DISPATCHING 07 fixer (single Minor, verbatim). 04 still in flight.
- 07 fix DONE: pushManager.subscribe wrapped in try/catch (handlePushSubscriptionChange), RED-then-GREEN test added, only 2 src/sw files changed. Report path verified in task-07/ (no stray dir). DISPATCHING 07 re-audit (1 auditor, scoped to the guard + no-other-change).
- 07 re-audit PASS (no findings) — Task 07 CLEAN. SW + manifest + build + register helper done.
- DEPENDENCY CORRECTION: Task 08's subscription-registration POST consumes Task 04's web-subscription route (I3) — so 08 needs 04 CLEAN (not just 07+11). Graph line 07→08 was incomplete. 08 held until 04 clean (also: 08 edits use-push-notifications.ts which 11 touched — 11 clean, so that overlap is clear).
- STATE: everything remaining gates on Task 04 (in flight). When 04 clears → 05, 06, 08, 10 all become candidates (subject to file-overlap). 04 = current critical path; watching for workerd-Intl BLOCKED + api-typecheck-must-be-green.
- 04 impl DONE_WITH_CONCERNS (the keystone). workerd Intl SPIKE PASSED (workerd 1.20260611.1, NY/Tokyo/Kolkata/DST) → quiet hours ship, NO founder BLOCKED. Owned gates all green (notif slice 249/249, api+shared typecheck, eslint, jscpd 1.64%, gitleaks 0 w/ path-pinned push-webpush.test.ts entry, 95% cov). notifyEvent I3 sig = notifyEvent(deps, {category, conversationId, actorUserId, recipientUserIds?, presentUserIds}) — presentUserIds on INPUT (fire-time snapshot, not a PresenceReader dep). I4 union carries userId on both arms + PushDeviceRef for deadTokens (refines plan shorthand; resolves the cross-task PushRecipient typecheck red 07 saw).
  - Concerns weighed: chat/routes.integration.test.ts edited OUT of listed ownership (mechanical sendPushForNewMessage→notifyEvent migration, no chat logic, no notif task owns it) — auditor to verify no logic change. generate:env regen (expected). Membership-reader dup KEPT (hoist deferred to Task 05 per plan). EXTERNAL red: template-html.test.ts 7 snapshots = email workstream, 0 template files touched.
  - DISPATCHING 3-LENS PANEL (sensitive): correctness / security / conventions. Clean only if ALL pass.
  - Post-04 wave mapped: 05,06,08 parallel (disjoint files) when 04 clean; then 10 after 08 (needs notificationChannel.unregister), 09 after 08+05; 12 after 08; 13 after 08+09+10.
- SESSION LIMIT killed all 3 Task-04 panel auditors mid-verification (API error, not task failure). Resuming all 3 from transcripts per restart-killed-subagents (they retain what they already verified; conventions lens had already attributed typecheck reds to concurrent billing/chat-settlement/workflows workstreams).
- DOC FIX: docs/NOTIFICATIONS.md iOS transport row said @capacitor-firebase/messaging — corrected to @capacitor-community/fcm (coexists with @capacitor/push-notifications) per founder round-3 ruling. Doc must describe current system.
- 04 CONVENTIONS lens: PASS. Verified independently: single-writer holds (0 conversationMembers refs in slice; mute/membership via MembershipReader port only), mute still boolean (grep mutedUntil = 0 repo-wide), G3 one decision fn (selectNotifyRecipients sole gate; no client copy), G2 no jobs + both call sites waitUntil fire-and-forget + pinned by tests, G1 alias derived once pre-fan-out + webpush tests assert URL/headers/body not.toContain(conversationId), I4 union sound (userId enables prune), prefs route classification mirrors account prefs, membership-reader still exactly 2 copies (no third), chat-test migration mechanical (assertion got STRICTER), gitleaks entry narrow AND-pinned, no plan-ID leakage, no reward-hacking smells. All reds attributed out-of-scope (billing/effort/email workstreams).
- PLAN GAP FOUND (my decomposition error, not an implementer's): pnpm arch:check RED — single-writer-per-table rule requires every table in TABLE_OWNER; notificationPreferences unregistered since Task 02; NO task owned packages/config/arch/**. Gates CI. → Added TASK 14 (arch registry entry) to plan.md; dispatching now (disjoint from all in-flight work).
- Also corrected plan.md founder-rulings line: iOS dep = @capacitor-community/fcm (was still naming the rejected @capacitor-firebase/messaging).
- Awaiting 04 correctness + security lenses (resumed).
- 04 SECURITY lens: PASS (0.97). Adversarially verified: G1 — ONLY collapseKey reaches push-service-visible headers (FCM collapse_key/tag/apns-collapse-id; webpush Topic); composite derives HMAC alias and FAILS CLOSED (unavailableError) on derivation failure, never raw-id fallback; webpush re-validates strict schema so nothing beyond {category,conversationId} can be encrypted; visible copy = fixed CATEGORY_COPY constant. Alias crypto sound (full 43-byte key, no truncation, true base64url, 192 bits retained, irreversible without secret). Recipient selection STRUCTURALLY cannot over-include (recipientUserIds intersects authoritative membership; decision fn exclusively subtractive; leftAt/null-userId filtered → link guests excluded). Prefs+subscription authz airtight (identity from fullSessionClaims only; strictObject bodies reject client userId; upsert never rewrites userId). Telemetry clean (1 log call, errorCode+conversationId; no token/endpoint/payload/secret in any error). NOTIFICATION_TAG_SECRET = exemplary G5 (Backend-only, all modes, prod secret, fail-fast at seam). Independently re-verified RESEND_API_KEY fixture pre-existing (git show HEAD). Dev tag secret reaches .dev.vars but gitignored+untracked and CI gitleaks is git-aware → no leak.
  - Confirms arch:check finding (same as conventions) → already dispatched as Task 14.
  - Orchestrator ruling on flagged observation: barrel exports raw sender factories (bypass risk for FUTURE callers). NOT churning 04 — no such caller exists, factory/composite is sole production construction site, Task 06 needs sibling mock export. Recorded as Standing amendment: alias-stamping is composite-only; no production code may construct raw transports; Task 06 brief + Phase-4 critic must guard it.
- PANEL: 2/3 PASS (conventions, security). Awaiting correctness lens (resumed) — 04 clean only if all three pass. Task 14 in flight.
- 14 impl DONE: one line `notificationPreferences: 'notifications'` in TABLE_OWNER. arch:check RED before (exact expected violation) → exit 0 after ("OK — 11 rule(s) over 1933 file(s)"). No exemption/weakening. config typecheck+lint green. Confirmed NO other unregistered table. Flagged packages/config/eslint-extensions/README.md churn = concurrent workstream (mtime predates), untouched. DISPATCHING 14 audit (1 auditor).
- 04 CORRECTNESS lens: FAIL. PANEL = 2 PASS (conventions, security) + 1 FAIL → Task 04 NOT clean. Validated findings:
  (1) CRITICAL lastSeenAt NEVER written (device-token-store-db.ts:31-40 sets updatedAt, not lastSeenAt; no send path touches it; grep confirms only schema+test refs). Criterion said "touched on successful registration and send". Report marked it MET by restating into a weaker claim (updatedAt bump + insert default) = reward-hacking smell caught. DOWNSTREAM DATA LOSS: Task 06 retention deletes by lastSeenAt → would silently delete ACTIVE devices' tokens. MUST FIX.
  (2) IMPORTANT no workerd Intl spike test exists (quiet-hours tests run under node full-ICU = proves nothing about workerd). Auditor INDEPENDENTLY ran real workerd binary and reproduced impl's values incl Kolkata half-hour → no live defect, but nothing pins it in CI; compat-date change would silently regress quiet hours. Repo HAS harness: packages/{db,realtime}/src/workers-validation/*.workers.test.ts + vitest.workers.config.ts.
  (3) MINOR PresenceReader port orphaned (Task 04 deleted its only consumer, sendPushForNewMessage). AGENT-RULES: clean up orphans your change created. Task 05 uses fire-time snapshot per design → port permanently dead. Deleting.
  (4) arch:check — already fixed as Task 14 (in audit). Not 04's.
  - Cleared by this lens: message-path parity verified by git-diffing HEAD versions (old PresenceReader was ALREADY a fake wrapping the caller's snapshot → removal changes nothing; filters/copy/prune identical; only wire delta = I1-mandated category field); chat-test migration mechanical; 100% coverage on all 12 owned files; jscpd 0.
  - PROCESS FIX → Standing amendment: never restate a criterion (report BLOCKED/DONE_WITH_CONCERNS naming the exact unmet criterion); + amendment recording that presence rides the snapshot and PresenceReader is gone (Task 05 must not reintroduce it).
  - DISPATCHING 04 fixer with all 3 validated findings batched.
- 14 audit FAIL — 1 Critical VALIDATED: the new TABLE_OWNER key broke 16/17 tests in the rule's own colocated test file (single-writer-per-table.rule.test.ts hardcodes a TABLE_NAMES mirror of registry keys → stale-key violation on every default-barrel test). Auditor verified causation (keys set-identical at HEAD → suite was green before). pnpm test:config + CI test gate red.
  - MY BRIEF DEFECT: I scoped Task 14's checks to arch:check + typecheck/lint, OMITTING pnpm test:config which the plan's own scoped-check table mandates for packages/config/**. Fixed plan.md Task-14 (files + mandatory test:config). Process fix before code fix.
  - Auditor affirmed: entry is correct kind of fix (registers true owner, no exemption/skip), enforcement logic byte-identical to HEAD, ownership factually verified (sole writer = notification-preferences-store-db.ts:93), diff minimal, G11 clean.
  - FOUNDER DESIGN ITEM (Phase 4, non-blocking): TABLE_NAMES is a hand-maintained copy of TABLE_OWNER keys = the sync contract CODE-RULES bans; collapsing it (derive synthetic barrel from registry keys) is an architecture call. PRE-EXISTING structure, not changing mid-run.
  - DISPATCHING 14 fixer.
- 14 fix DONE: added 'notificationPreferences' to test TABLE_NAMES mirror. test:config BEFORE fail (16 failed|332 passed, regression reproduced) → AFTER pass (348 passed, 29/29 files). arch:check exit 0. config typecheck+lint green post-edit. Diff = exactly 2 lines (registry + mirror), zero deletions, no assertion/stale-key weakening. Fixer independently confirmed the mirror-duplication trap → reinforces the Phase-4 founder design item. DISPATCHING 14 re-audit.
- 14 re-audit PASS (no findings) — Task 14 CLEAN. Auditor ran a BEHAVIORAL probe vs the real schema barrel: foreign-slice write → flagged, owner write → clean, stale key → flagged; confirmed fix additive-only (rule body + all 17 assertions byte-identical to HEAD, no skip/relaxation). test:config 348/348 force-uncached, arch:check exit 0.
- STATE: Clean = 00,01,02,03,07,11,14. ONLY Task 04 fix in flight; it gates 05,06,08 (then 09,10,12,13). Nothing else dispatchable until it clears.
- 04 fix DONE_WITH_CONCERNS. All 3 findings red-verified→fixed: (1) lastSeenAt now written on registration upsert AND successful delivery (touchLastSeen + PushDelivery.deliveredTokens), proven on real rows; (2) workerd Intl pin now runs under REAL workerd (test:workers 6/6, asserts navigator.userAgent==='Cloudflare-Workers'); (3) PresenceReader deleted (0 refs). Gates: api typecheck pass, eslint owned exit 0, arch:check pass, jscpd 1.6%, gitleaks 0, coverage 100/99.18/100/100 owned.
  - ORCHESTRATOR RULINGS: (a) touchLastSeen failure degrading the notify Result = CORRECT as-is (mirrors dead-token prune; Result swallowed at composition root so G2 holds) — recorded in plan, later tasks must not "fix" it silent. (b) New surface (PushDelivery.deliveredTokens optional, DeviceTokenStore.touchLastSeen REQUIRED) → plan amendment so 05/06/08 stubs implement it.
  - FOUNDER-FLAGGED (not blocking): workerd criterion forced apps/api devDep @cloudflare/vitest-pool-workers + test:workers chained into `test` + vitest.workers.config.ts + knip entry + lockfile importer. Extends the EXISTING repo harness (packages/db, packages/realtime; version already in lockfile) — not a new vendor, but it touches package.json and what `pnpm test` runs.
  - Phase-4 batch item recorded: identity/ports/email.ts stale PresenceReader comment (caused by our deletion, outside notif ownership).
  - Pre-existing/external reds attributed, untouched: 7 template-html snapshots (email workstream; templates+snapshots byte-identical to HEAD), knip vitest.package.config.ts (reproduced with edits reverted), one Vitest coverage/.tmp ENOENT runner crash (known upstream).
  - DISPATCHING 2 re-auditors: correctness (re-verify all 3 findings + no regression + no PII in new paths) and conventions/scope (the new out-of-ownership config surface: devDep, test chain, knip, lockfile).
- 04 fix RE-AUDIT (correctness): all 3 original findings CONFIRMED FIXED, verified independently (lastSeenAt: two real write paths, proven vs real Postgres incl. backdated-device refresh + negative twin, cross-user ownership test; workerd: ran test:workers 6/6, asserts navigator.userAgent==='Cloudflare-Workers', pins Kolkata half-hour + DST + cross-midnight, wired into CI via `test` script, excluded from node project; PresenceReader: deleted, 0 code refs). No regression on prior affirmations. Security spot-check on new paths CLEAN (pair-matched userId+token WHERE; only log = errorCode+conversationId).
  - VERDICT FAIL on NEW [Important]: pnpm-lock.yaml — the filtered install rewrote 525 insertions/67 deletions; HEAD has 0 occurrences of vite@8.1.5, worktree has 24, and 3 real vite@8.1.5 trees materialized in node_modules/.pnpm. apps/web's @tailwindcss/vite + @vitejs/plugin-react may now resolve against real vite@8.1.5 instead of the workspace-pinned rolldown-vite@7.3.1 (pnpm-workspace.yaml:23; TECH-STACK says rolldown applied workspace-wide). Installed tree still rolldown-bound → lockfile/node_modules DISAGREE; CI --frozen-lockfile would materialize the untested binding. Causation UNPROVEN (other workstreams have uncommitted package.json edits; the devDep declares no vite peer) but artifact state certain.
  - Implementer claim "importer entry only, nothing downloaded" VERIFIED FALSE (2nd unreliable report claim this run — after the lastSeenAt restatement). Weighing a standing amendment on lockfile/install claims.
  - HOLDING fix dispatch until the scope/conventions re-auditor returns (it audits the same config surface) → batch both.
- 04 fix RE-AUDIT (scope/conventions): PASS, no findings. Verified each out-of-ownership touch is the MINIMUM (proved knip entry necessary by experiment; script/devDep/lockfile load-bearing), config shape matches packages/db+realtime harness, CI reaches test:workers via ci.yml:213→turbo→apps/api test script, push-notify.test.ts double edit is mechanical +1 NEW assertion (no weakening).
- CONTESTED FINDING ARBITRATED BY ORCHESTRATOR (direct lockfile inspection): correctness lens's [Important] lockfile finding is INVALID (misattribution). Evidence: apps/web @tailwindcss/vite + @vitejs/plugin-react BOTH resolve to rolldown-vite@7.3.1 (not vite@8.1.5) — the claimed rebinding does not exist; vitest-pool-workers present in HEAD lockfile (4 occurrences → 5, i.e. our +1 importer entry); apps/sandbox importer = 1 in worktree, 0 in HEAD → the 525-line bulk is the SANDBOX workstream's. Per AGENT-RULES (ignore other agents' work) I do NOT touch it. Residual observation for founder only: the pnpm-workspace rolldown override no longer catches vitest's AUTO-INSTALLED vite peer — real toolchain drift, sandbox workstream's to own.
  - Also: this vindicates the implementer's "already in lockfile" claim (correctness lens called it false; it was right about the 525 lines being large, wrong about causation). Not booking a standing amendment on report claims — one restatement (lastSeenAt) is the real pattern, already covered.
- *** TASK 04 CLEAN *** (correctness findings fixed+verified; conventions PASS; security PASS; contested finding rejected with reason).
- Clean = 00,01,02,03,04,07,11,14. DISPATCHING FAN-OUT: 05 (event sources + read cursor, 3-lens later), 06 (dev inbox + retention), 08 (client facade + prompt). File-disjoint: 05=realtime/+conversations slice; 06=platform/dev+scheduled.ts+notifications/adapters(mock/capture); 08=apps/web only.
- 06 impl DONE. /dev/push capture + device-token retention (DEVICE_TOKEN_STALE_DAYS=180, rationale at constant, in slices/notifications/adapters/device-token-retention.ts bound via shared createRetentionEntry in scheduled.ts). Gates: eslint owned + whole apps/api exit 0, tsgo pass, arch:check pass, gitleaks 0, coverage 100% on scheduled/retention/factory, dev routes L98.59. Only red = template-html email-workstream snapshots.
  - ACCEPTED DEVIATION (orchestrator): capture wraps the MOCK PARTITIONS inside the composite (fcm/webPush each withPushCapture(createMockPushSender())), not the composite's outer entry — composite stamps alias on the way DOWN so an outer wrap can't show the alias tag the criterion needs, and re-deriving is forbidden. Still routes through createCompositePushSender; NO raw sender constructed. Side effect: mixed-platform send → 1 capture entry per partition. Auditor to verify.
  - FLAKE RISK TO VALIDATE (implementer self-raised, couldn't fix — other task's file): pre-existing daily-pass tests in scheduled.integration.test.ts now COMMIT a device-token delete; Task 04's notify-event.integration.test.ts backdates rows to 2020 and asserts on them → parallel interleave could delete mid-test. Not observed. Routed to auditor to validate + scope fix.
  - FOUNDER ITEM (Phase 4, non-blocking): no index on device_tokens.lastSeenAt → daily pass does a filtered scan; adding one is a schema change (founder-approval class).
  - Cross-task: Task 05's push-notify.integration.test.ts already consumes listCapturedPushes() → its shape is load-bearing for 05.
  - DISPATCHING 06 audit (1 auditor + flake-risk assessment).
- 06 audit FAIL — 2 findings VALIDATED:
  (1) IMPORTANT flake CONFIRMED + ESCALATED (broader than reported): daily cron now runs unscoped COMMITTED DELETE FROM device_tokens WHERE last_seen_at < now()-180d (≤5000 rows/pass) via scheduled.integration.test.ts:134-140,154-159 on shared Postgres w/ auto-commit; FOUR integration files hold committed in-window rows while asserting on them — device-token-retention.integration.test.ts:130-134,184-190 (06's OWN), notify-event.integration.test.ts:54,300 (04), device-token-store-db.integration.test.ts:55,166,179 (04). Vitest = parallel forks, no fileParallelism:false, one DB → hazard real; low-probability (3 targeted runs + full suite clean; root retry:1 would mask one collision). Same class as the repo's existing jobs-table arch rule. Fix (no assertion weakened): (a) 04's two files — move STALE_LAST_SEEN inside window (now-1h; tests only need "older than now"); (b) 06's own — aged fixtures inside rolled-back txn for the 2 cases not needing committed delivery.
  (2) MINOR push-mock.ts:23-30 omits deadTokens:[] that both real transports always return → dev/CI is the only mode exercising the `deadTokens ?? []` undefined arm.
  - VERIFIED by auditor: accepted capture deviation holds on all 3 points (no raw sender constructed anywhere outside the factory's production branch; captured collapseKey byte-equals the composite deriver's output and tag !== conversationId; entries only exist below fanOut). Dev-only class enforced by pipelineAuthorize (404 in prod, pinned); viewer never exposes tokens (negative test). DEVICE_TOKEN_STALE_DAYS=180 rationale is a genuine durable-fact comment. "Fresh kept" genuinely drives notifyEvent over the real store+composite → proves 04's touchLastSeen keeps live devices.
  - lastSeenAt index: auditor ACCEPTED as not-a-finding (bounded daily scan, small table) — downgrading my earlier founder item to optional.
  - ATTRIBUTION (not ours, do not touch): platform/dev/routes.ts:118-121,301 + tests :331-357 + decryptConversationTitle helper :144-174 = unclaimed seed-conversation `title` work by a concurrent agent in a Task-06-owned file.
  - DISPATCHING one fixer for BOTH findings (spans 04's test files — orchestrator-routed).
- 08 impl DONE. Facade + ask-on-mount REMOVED (proofs on both platforms) + one-time prompt + web subscription lifecycle. test:web 381 files/6247 tests pass, owned files 100/100/100/100, typecheck+lint green post-last-edit.
  - ACCEPTED deviations: 4th facade method ensureRegistered() (3 named methods can't re-register without prompting — required by the re-registration criterion); 2 out-of-list call sites (app-shell.tsx mounts prompt+registration hook, lib/auth.ts is the only sign-out seam) — both necessary or the code is dead; ensureRegistered skipped when globalEnabled=false so Task 10's global-off isn't undone next start (correct). Task 11's iOS FCM branch + Task 01's validator UNTOUCHED (11's 5 tests incl. APNs→FCM exchange still pass).
  - Announcements banner NOT reusable (as brief anticipated — createBanner is vanilla-DOM marquee bound to BannerResponse+hash dismissal); NOT forked, prompt is React over @hushbox/ui Button + same tokens. Good.
  - PLAN AMENDMENT added for 09/10/13: ensureRegistered semantics; Task 10 EXTENDS hooks/notifications/use-notification-preferences.ts (no 2nd prefs hook); Task 09 adds clearing fns TO THE FACADE; Task 13 selects by role="status"+button names (no TEST_IDS; data-testid lint-banned).
  - RISK ROUTED TO AUDITOR (real, unowned by 08): web unregister sends encodeURIComponent(endpoint) as DELETE /notifications/device-tokens/:token — hono client does NOT encode path params (verified hono@4.12.25 replaceUrlParam). Nothing proves a full URL round-trips as a path segment through the Worker router → logout-unsubscribe may silently no-op (degrades to 404/410 prune, not data loss). Auditor to test empirically.
  - DISPATCHING 08 audit (1 auditor + the unregister round-trip verification).
- 05 impl DONE_WITH_CONCERNS. Run-completion + membership events + monotonic read cursor + read-state exposure. Full apps/api vitest 462 files/6323 pass (twice), test:realtime 24/24, typecheck+arch pass, eslint 23 owned files exit 0, per-file coverage ≥95 (worst stores.ts 95.28 br).
  - HOIST LANDED (long-standing dup resolved): createPushMembershipReader → conversations/adapters/push-membership-reader.ts, free of @hushbox/realtime value import; push-notify.ts duplicate DELETED; no third copy.
  - ACCEPTED deviations: packages/realtime untouched (DO sink is category-blind → re-pointing conversation-room.ts to createRunCompletionPushNotify is the whole change); membership events pass presentUserIds:[] (routes hold no snapshot — design doc's "or none"); "share activity" = shared-link mint; out-of-list edits = app.ts dep wiring, conversation-room.ts sink binding, test renames (composition-root necessities).
  - ROUTED (blocked Task 09): packages/shared/src/schemas/api/conversations.ts lacks lastReadSeq on membershipViewSchema + conversationListItemSchema (server returns it, client has no TYPE); fix also needs apps/web/src/demo/mock-backend/** (store.test.ts parses listConversationsResponseSchema → bare required field fails). → PLAN AMENDED: Task 09 scope extended to both + extra scoped checks (test:shared, shared typecheck/lint).
  - FLAGGED FOR AUDIT: implementer's OWN confidence is medium on "failed runs do NOT notify" — inherited from pre-existing realtime tests, not pinned by a new one. That's an acceptance criterion → panel must verify directly.
  - Cross-agent contention noted: 5 transient chat failures only in multi-dir runs, never reproduced (chat 187/187 isolated, 2 full-package runs green).
  - DISPATCHING 3-LENS PANEL (sensitive: membership/authz).
- 05 SECURITY lens: PASS, no findings. Verified: hoisted reader predicate byte-identical to BOTH deleted copies (conversationId + isNull(leftAt) + isNotNull(userId) + defensive flatMap null-drop); the real-Postgres ex-member-exclusion test survived the move (import repointed, still asserts leftMember absent) → NO regression window. recipientUserIds is an INTERSECTION over authoritative members (non-members undroppable-in); decision fn exclusively subtractive; ex-members/link-guests/non-members unreachable by construction. Read route: identity from sealed session claims only (callerUserId throws unless kind==='full'; routeClass('session') refuses link-guest/trial), body schema has no userId, UPDATE ... WHERE conversationId AND userId=caller AND leftAt IS NULL → cross-user/cross-conversation write impossible; non-member gets same 404 as nonexistent (no existence oracle); PATCH covered by global Origin/CSRF gate. lastReadSeq caller-scoped only (rides membershipView + listForUser WHERE userId=caller; memberViewSchema does NOT carry it). G1 holds on membership path ({category, conversationId} + fixed CATEGORY_COPY). Telemetry allowlisted.
  - FOUNDER OBSERVATION (non-finding, pre-existing pattern, NOT acted on): notifyMembershipEvent hands c.var.db to a waitUntil task while pipeline-bindings.ts:66 schedules db.$client.end() on its own waitUntil → concurrent in workerd, a late query could hit a closing pool. EXACT parity with pre-existing chat push side-band (chat/routes.ts:1556-1580); race unconfirmed vs Neon driver; worst case = a dropped notification, i.e. the degradable class by doctrine. Recorded for Phase 4 founder items.
  - Awaiting 05 correctness + conventions lenses. Also in flight: 08 audit, 06 fix.
- 05 CONVENTIONS lens: PASS, no findings. RESOLVES the flagged gap: "failed runs do NOT notify" IS pinned (packages/realtime/src/room-core.test.ts:1302-1320) — not unverified. Verified: single-writer holds (lastReadSeq written only by conversations/adapters/stores.ts:542 GREATEST; notifications slice touches no conversations tables and vice versa; cross-slice via injected port + composition root = stricter than barrel import); G2 holds STRUCTURALLY — capability constructed INSIDE the waitUntil task so even a synchronous factory throw becomes a caught rejection, all 3 call sites fire after runMutation resolves and outside any tx, no jobs/cron/enqueue in the diff; HOIST IS REAL (exactly one definition repo-wide, new module imports only drizzle/@hushbox/db/lib helpers — no @hushbox/realtime even transitively, so the DO runtime is off app.ts's import graph; both consumers repointed); packages/realtime genuinely untouched (git status empty); read route byte-for-byte the mute route's shape; jscpd 0 clones on owned files; G11 grep clean.
  - Cleared, not findings: reader's real-DB describe stays in realtime-room-bindings.integration.test.ts (relocating would duplicate an ~80-line seeding harness — better trade, disclosed); push-notify.ts value-importing a slice-internal adapter path (precedented at composition root, outside boundaries plugin scope by design).
  - CONFIRMS the shared-schema gap already routed to Task 09; notes those mirror schemas are a standing sync-contract the doctrine dislikes (PRE-EXISTING, not this task's) → Phase-4 founder observation.
  - 05 panel: 2/3 PASS (security, conventions). Awaiting correctness.
- 08 audit PASS, NO findings — TASK 08 CLEAN. The routed DELETE-round-trip risk RESOLVED EMPIRICALLY: auditor ran hono-layer probe (encoded endpoint matches /:token, c.req.param returns byte-identical URL, %2F not folded) AND a full-stack probe (real applyPipeline + real Postgres + sealed session): POST web-subscriptions → 201 + 1 row; DELETE /device-tokens/${encodeURIComponent(endpoint)} → 200 {"deleted":true}, row count 0. Logout/global-off unsubscribe genuinely works. Residual (not a finding): workerd/edge layer above hono unverified — same evidence class as the repo's own gate.
  - Verified: ask-on-mount gone on BOTH platforms with loud-failing tests; Task 11's iOS FCM branch preserved verbatim (:37-49) + Task 01's validator untouched; prompt role="status", not a dialog, activeElement stays body, tab order Enable→Later, ALL suppressors tested; ONE permission state machine (PushPermissionState adds 'unsupported' as first-class → one hook serves iOS Safari/Capacitor/desktop with no platform branch outside channel.ts); VAPID public key via registry + fail-fast parse; owned-file coverage 100% on all 10 files.
  - ATTRIBUTION (not ours): full turbo test:web now fails its coverage gate on src/components/chat/message/markdown-renderer.tsx (72.72% branches) = documents/markdown workstream's uncommitted edit. Record for Phase 4.
- Readiness: 08 clean → 10 (needs 04✓+08✓) and 12 (needs 08✓) READY, file-disjoint. 09 HELD (needs 05 clean — consumes its read-state + extended shared-schema scope). 13 HELD (needs 09+10).
- DISPATCHING 10 (settings card) + 12 (Maestro flows).
- 06 fix DONE_WITH_CONCERNS. Flake closed: fixtures moved inside retention window (04's 2 files) + rolled-back txn for the 2 non-delivery cases (06's own); mock now returns deadTokens:[]. Evidence: 8x repeated-run probe in PARALLEL FORKS at --retry=0 → 8/8 clean (35/35 each); mutation probe proves relocated fixture drives outcome; assertions quoted unchanged before/after; 4 affected integration files + push-mock 39/39; eslint/tsgo/arch/gitleaks green.
  - ACCEPTED beyond-brief edits (orchestrator): (a) added deadTokens:[] to notify-event.integration.test.ts:133 toEqual — finding 2's mock change broke it; necessary and STRENGTHENS; (b) added an explanatory comment in each of 04's 2 files recording why the value must stay inside the retention window — prevents the next reader restoring a fixed epoch and reopening the hazard (exactly the durable-fact comment CODE-RULES wants). Auditor to confirm both are strengthening, not weakening.
  - RESIDUAL BY DESIGN (matches auditor's own direction): the "delivery path refreshes a live device" case still commits briefly (must, to exercise the real path) — window narrowed whole-file → milliseconds. Full closure needs notifyEvent accepting a tx (port change, out of scope).
  - NOTE for Phase 4: vitest "Something removed apps/api/coverage/.tmp" abort = two concurrent runs sharing one coverage.reportsDirectory (concurrent agents in this checkout); didn't reproduce on re-run; may bite concurrent test:api.
  - DISPATCHING 06 re-audit (verify flake genuinely closed + the 2 Task-04 file edits are strengthening + Task 04's guarantees intact).
- 05 CORRECTNESS lens: PASS, no findings → *** TASK 05 CLEAN *** (3/3 lenses). Priority item settled EMPIRICALLY: gate is room-core.ts:747 (only outcome==='succeeded' + senderUserId fires); every non-success path provably bypasses (executor rejection→failed :641, deadline→stopped :651-657, user stop :646, trial has no senderUserId); PINNED by room-core.test.ts:1301(trial)/1309(failed)/1318(stopped)/1266(succeeded) — implementer's MEDIUM confidence was over-pessimistic, and packages/realtime WAS in its ownership so these are owned-scope pins. Also verified: exactly-once + present/muted/prefs-off/actor exclusions over real rows; both membership events with category membership; read cursor GREATEST pinned at BOTH store and route layers for replay AND out-of-order + cross-member authz test; best-effort proof required building an ExecutionContext harness (genuine red, not weakened); hoist = exactly one implementation repo-wide, pure cut/paste, real-DB pin intact; presence via presentUserIds, no PresenceReader.
  - PHASE-4 TRACKER: definitive full-package coverage gate UNVERIFIABLE right now — external template-html red keeps apps/api red and vitest suppresses coverage on failure; forced run aborted by a concurrent agent wiping coverage/.tmp. Targeted run had every owned file ≥95% (stores.ts 94.33 reduced vs 95.28 full — directionally consistent; superset of tests can only raise). RE-VERIFY in Phase 4 once the email workstream's snapshots are fixed or in an exclusive window.
  - Noted: app.ts:305 preferencesStore wiring = Task 04/06 prefs work in the shared composition root (ours, fine, not a finding).
- Readiness: 05 clean → TASK 09 READY (needs 08✓+05✓). Files disjoint from in-flight 10 (settings) and 12 (mobile-tests). DISPATCHING 09 with extended scope (shared schemas + mock-backend).
- 06 re-audit PASS, no findings → *** TASK 06 CLEAN ***. Verified: hazard mechanism real & now closed (only committed DELETE is scheduled.integration.test.ts:136-138/157; grep confirms no other file seeds aged lastSeenAt); NO assertion weakened on all 3 sub-points — (a) delivered-device refresh still toBeGreaterThan + unreached still byte-exact toEqual(STALE_LAST_SEEN); 1h offset >> any clock skew so no less decisive; (b) added deadTokens:[] STRENGTHENS toEqual (vitest fails on received key absent from expected → pins one MORE property, admits nothing the old rejected); (c) both comments accurate durable facts, verified against scheduled.integration.test.ts, no process narration; mock now matches both real transports literally (4 keys) while composite's `?? []` arm stays covered by push-composite.test.ts:119-137; retention boundary pinned BOTH sides (+1 deleted, -1 kept); fresh case still drives the REAL delivery path via factory-built composite; /dev/push untouched + still dev-only. Residual = exactly the accepted ms-wide window (all other purge calls now inside withRollback, which correctly rethrows non-sentinel and fails loudly if body never completed). Fix is HONEST: no --retry, no fileParallelism/sequence tampering, no disabled tests.
  - Re-auditor CLOSED the coverage gap the implementer couldn't measure: full-package run EXCLUDING the external template-html.test.ts emits the report (vitest reportOnFailure defaults false) → zero per-file threshold errors; push-mock.ts + device-token-retention.ts both 100%. *** PHASE-4 TECHNIQUE: use this exclusion to get the authoritative coverage gate while the email workstream's red stands. ***
- CLEAN (11/15): 00,01,02,03,04,05,06,07,08,11,14. In flight: 09,10,12 implementing. 13 last (needs 09+10).
- 12 impl DONE_WITH_CONCERNS. Flow 07 rewritten (inverted: no OS dialog at launch, in-app callout visible, Later persists across relaunch) + new mobile-tests/flows/15-notification-enable-grant.yaml. VERIFIED ON REAL HARDWARE: both flows green TWICE on a real Android 14 emulator at retries=0 (2m17s, 5m09s); mobile-test.test.ts 144 pass; prettier + js-yaml parse of all 15 flows pass. Flows self-register via fullFlowsExcludingOta() — no shard-config change. Flow 07 now heaviest (two logins, 1.5-4.5min).
  - SPIKE correctly NOT LANDED: am broadcast c2dm RECEIVE reaches the app (Capacitor logs pushNotificationReceived) but never renders in the shade even with POST_NOTIFICATIONS granted + backgrounded; Maestro has no shell step so it cannot live in a flow file. Honest negative result.
  - FOUNDER ITEM #1 (product bug, outside scope): session does NOT survive app relaunch (force-stop+start → unauthenticated). Flow 07 works around it by re-logging-in. Recommend separate run, not folded into this one.
  - FOUNDER ITEM #2 (CI blocker): npx cap sync android broken repo-wide — Capacitor CLI CJS loader cannot resolve @hushbox/shared's ./constants.js from apps/web/capacitor.config.ts → buildApk() dies → pnpm mobile:test + CI mobile job dead. Reproduced independently by BOTH Task 11 and Task 12; both call it pre-existing. RELATED: android/capacitor.settings.gradle has stale pnpm paths → gradle "No variants exist" for all 8 plugin projects. ATTRIBUTION QUESTION routed to auditor: Task 11 ADDED @capacitor-community/fcm, so settings.gradle may need regeneration BECAUSE OF OUR CHANGE — but regeneration is blocked by the broken cap sync.
  - ENV SIDE EFFECTS to clean (agent disclosed, removal cmds in its report): 8 alias symlinks under node_modules/.pnpm + a `pnpm --filter @hushbox/api dev` process it started. Must not be left in the founder's tree.
  - DISPATCHING 12 audit (incl. attribution of the gradle/cap-sync question + env-cleanup verification).
- 09 impl DONE_WITH_CONCERNS. Activity badge store + title/setAppBadge/sound + SW push-event intake + clearDelivered + lastReadSeq schema extension. test:web 392 files/6361 pass (no coverage-threshold errors); shared typecheck + tsgo + eslint + prettier green; schema suite 178/178.
  - ORCHESTRATOR-CONFIRMED REGRESSION (OURS, not external): packages/shared/src/env.config.test.ts backendEnvSchema — 4 tests fail (validates dev / validates prod / accepts R2 vars / allows CI-prod secrets optional) because Task 04 added NOTIFICATION_TAG_SECRET as REQUIRED and the fixtures omit it. Required-ness is CORRECT by design (alias stamped even on dev mock) → fix fixtures, never relax schema. Verified myself: 4 failed | 101 passed. → NEW TASK 15.
  - MY PLAN GAP (found by 09): NO task owned the CLIENT WRITE of PATCH /conversations/:id/read → lastReadSeq never advances → founder-kept dismiss-on-read-elsewhere is INERT end-to-end. → NEW TASK 16(a).
  - PLAN GAP: sound has no UI toggle (store persists soundEnabled + unlocks audio in setSoundEnabled(true), but Task 10's card was never specced to expose it) → sound permanently off. → NEW TASK 16(b), after Task 10 clean.
  - ACCEPTED deviations: clearDelivered(conversationIds[]) batch; native filters getDeliveredNotifications() then removeDeliveredNotifications({notifications}) since the plugin has NO tag-keyed remove, matching tag with data.conversationId fallback (iOS carries no tag) — key is conversationId in EVERY arm, no alias derived client-side (correct per amended I4); lastReadSeq .default(0) matching muted/pinned precedent (bare required broke 10 shared tests) + forced edits to use-authenticated-chat.ts and sidebar.test.tsx; in-app feed = open conversation's message:new frames only via use-group-chat.ts (confirmed NO user-level socket exists; no new realtime surface invented — correct per my NEEDS_CONTEXT guidance).
  - PLAN AMENDMENTS: two role="status" regions → Task 13 selects by accessible name; Task 07's pushsubscriptionchange postMessage has no consumer (ACCEPTED — backstops cover it, do not add a second path).
  - DISPATCHING: 09 audit + Task 15 (disjoint, immediate). Task 16 waits on Task 10.
- 12 audit PASS + 2 Minor. ATTRIBUTION SETTLED WITH EVIDENCE:
  (A-i) cap sync android breakage = PRE-EXISTING, NOT ours. capacitor.config.ts unchanged in worktree; its @hushbox/shared import committed 2026-07-19 (a4b4483d) BEFORE this run; packages/shared/src/index.ts:1 './constants.js' unchanged at HEAD (run only APPENDED ./notifications/index.js). Reproduced read-only via the CLI's own requireTS loader: MODULE_NOT_FOUND. CI impact confirmed: ci.yml:685 pnpm mobile:test → buildApk() → npx cap sync android as a PLAIN NODE CHILD; tsx does not propagate a loader (NODE_OPTIONS undefined in child) → nothing remaps .js→.ts. → FOUNDER repo-level red; fix = drop shared-barrel import from capacitor.config.ts OR run CLI under a TS loader.
  (A-ii) capacitor.settings.gradle staleness = other workstreams' dep churn (generated file, last committed 2026-03-11, 8.2.0 paths vs installed 8.4.0). GENUINELY OURS though: @capacitor-community/fcm ships an android/ dir but is absent from capacitor.settings.gradle, capacitor.build.gradle AND capacitor.plugins.json → Android project never synced for the plugin this run added; the verified APK does not contain it. Auto-resolves once (A-i) is fixed; run owes only a post-sync confirmation that :capacitor-community-fcm appears (+ decide whether regenerated tracked gradle files get committed). → Phase-4/Task-11 item.
  (B) ENV CLEANUP DONE BY ORCHESTRATOR: all 8 alias symlinks removed (each verified `-L` first; git status apps/web/android empty → workaround never touched tracked files). DEFERRED: dev-server PID 2998423 (+children, workerd :8788) left running — concurrent agents share this checkout/port; kill in Phase 4.
  (C) Flow cost: re-login GENUINELY FORCED as written (keepSignedIn defaults false → sessionStorage → cleared by WebView process restart; 18:44 failure screenshot is that exact failure). Flow 07 now weighs 87 = 28% of the 315-pt suite, 2x next heaviest; SHARDS=2 isolates it so it sets the mobile job's wall-clock floor. Auditor judgment: acceptable but AT THE CEILING; first lever = "Keep me signed in" checkbox (login.tsx:507-509 moves marker to localStorage).
  *** CORRECTION TO FOUNDER: my earlier "session does not survive relaunch = product bug worth a ruling" was WRONG. It is the DEFAULT keepSignedIn=false path (sessionStorage) and a "Keep me signed in" checkbox exists. NOT a bug. Corrected in chat. ***
  - MINOR 1 (fix now): flows/07:86 comment states the no-survive-restart fact as app-wide; true only for the default unchecked path → narrow it.
  - MINOR 2 (PHASE-4, not a Task-12 fix): Task 08's enable prompt now renders in EVERY authenticated flow (10, 11, 12, 14) and consumes vertical space; only 07 and 15 were re-run on the emulator. → re-run 10/11/12/14 once the emulator path is usable.
  - PHASE-4 founder design question: the 15-step login block is duplicated across flows 07/10/14/15; Maestro supports `runFlow: file:` subflows (jscpd doesn't scan YAML so no gate catches it). Harness-convention decision, not this task's.
  - Process note for later briefs: Task 12's report omitted gitleaks from its self-gate (auditor ran it, clean).
  - DISPATCHING 12 fixer (Minor 1 only).
- 12 re-audit PASS, no findings → *** TASK 12 CLEAN ***. Comment now names the MECHANISM (default-unchecked → sessionStorage marker → cleared by process restart) not the symptom; auditor independently re-derived it from login.tsx:337/507-509 + auth-client.ts:72-77 and confirmed no native/Capacitor override of keepSignedIn exists. Nothing executable changed (43 steps parse, prettier clean, only flow 07 modified).
- CLEAN (12/17): 00,01,02,03,04,05,06,07,08,11,12,14. In flight: 09 audit, 10 impl, 15 fix. Remaining: 13 (needs 09+10), 16 (needs 10 clean).
- 15 fix DONE → audit dispatched. Red 4-failed BEFORE / 105-105 green AFTER; turbo test+typecheck+lint shared green; schema provably untouched (env.config.ts zero edits); VAPID trio confirmed .optional() so no other fixture broken; no other repo fixture builds backendEnvSchema input; gitleaks: 3 added values clean, 1 finding PRE-EXISTING (RESEND_API_KEY, reproduces at HEAD).
- 10 impl DONE → audit dispatched. Settings card (global switch, 3 category toggles, quiet hours + detected tz). turbo test:web 392 files/6361 tests no coverage-gate error; typecheck+eslint green after last edit; 2 mutation checks confirm assertions bite. EXTENDED use-notification-preferences.ts (no 2nd hook); mute untouched; no TEST_IDS (Task 13 selects by role+accessible name).
  - Deviations to auditor: facade call keys off next.globalEnabled not the server echo (claims PUT echo is a pure projection — auditor must verify the route can't coerce); category toggles stay enabled while global off (server evaluates global first, copy says so; nothing pins it).
  - FOUNDER (minor design): quiet-hours Select labels are 24-hour 00:00-23:00, not locale-formatted (no locale hour formatter in repo; Intl labels vary by runtime locale).
  - RECURRING (3rd report): two simultaneous full web suites corrupt each other's apps/web/coverage/.tmp → Phase 4 MUST sequence full-suite runs, never parallel.
- 15 audit PASS, no findings → *** TASK 15 CLEAN ***. Verified: test diff 5 insertions/0 DELETIONS (nothing skipped/relaxed); env.config.ts has ZERO `-` lines (schema untouched; NOTIFICATION_TAG_SECRET still z.string().min(1) at :714, VAPID trio + VITE_ all .optional() at :708-710/:776); turbo test shared 107 files/2392 tests + coverage gate green; CONTROL SCAN with gitleaks:allow stripped from the new lines → still exactly 1 finding (the pre-existing RESEND_API_KEY) proving added values are genuinely inert, not leaning on suppression; confirmed NOTIFICATION_TAG_SECRET is the ONLY non-optional var this run added and no other repo fixture constructs either schema's input.
  - PHASE-4 founder item (pre-existing, NOT ours, auditor confirmed at HEAD): the three NEGATIVE backendEnvSchema tests are non-isolating (already omitted MARKETING_URL/ADMIN_URL/APP_VERSION at HEAD) — possible separate cleanup.
- CLEAN (13/17): 00,01,02,03,04,05,06,07,08,11,12,14,15. In flight: 09 audit, 10 audit. Then 16 (needs 10 clean) + 13 (needs 09+10 clean).
- 09 audit FAIL — 3 findings:
  (1) CRITICAL: turbo lint --filter=@hushbox/web RED, 18 errors ALL in Task 09 files (sound.ts:28,30; app-badge.ts:15; use-notification-clearing.ts:9,56; activity-announcer.tsx:12; use-activity-intake.test.ts:22,31,131; notification-activity-layer.test.tsx:87; use-group-chat.test.ts:755-758 incl. raw prettier violations). Report CLAIMED eslint pass — auditor confirmed eslint.config.js unmodified, so it's the wrong-cwd silent-noop failure mode (EXACTLY the pattern in my memory). 13/18 are --fix-able. → PROCESS FIX: new standing amendment requires the PACKAGE-WIDE `turbo lint --filter=<pkg> --force` (CI's own command), not scoped eslint.
  (2) IMPORTANT — MY PLAN-LEVEL CONFLICT (auditor correctly refused to fix unilaterally): sw/handlers.ts:109-116 posts push-event ONLY to focused clients (I7, Task 07) while stores/notification-activity.ts:37-39 isAwayFromApp() counts ONLY while away (founder ruling). MUTUALLY EXCLUSIVE → SW→page feed can never increment in production; a push arriving while focused yields NO signal at all (SW suppressed the OS notification, page drops the message). Intake tests pass only because they force hasFocus=false on a message the SW would send only when focused. → FOUNDER product-behavior decision.
  (3) MINOR: stores/notification-activity.ts:8 ActivityEvent.conversationId required but never read by store logic (only selfAuthored is) — dead data.
  - VERIFIED CLEAN by auditor: founder activity semantics exact (per-session, persist partializes to soundEnabled only, no server truth); title effect is the ONLY document.title writer repo-wide incl. router; badging feature-detected (absent/zero/rejected-write all tested); sound default-off + primed in the enabling gesture + aria-live parity; clearing keyed on conversationId in EVERY arm with NO alias/hmac derivation anywhere client-side (grep-verified); clearing lives on the facade; no new realtime surface; lastReadSeq .default(0) both schemas + demo backend emits both wire shapes; owned-file coverage 100%.
  - DISPATCHING 09 fixer for (1)+(3). (2) goes to founder.
- 2026-07-24 FOUNDER RULING on the SW/away-gate conflict: "Nothing — remove the dead path." Present user = no signal (their original ruling stands); delete the unreachable SW→page push-event intake rather than ship code that can never run. No new UI.
- 10 audit PASS + 1 Minor. CLEARED on code evidence (not rationale): the facade-keys-off-next.globalEnabled deviation — routes.ts:118-137 validates with a z.strictObject (no default/catch/transform/coercion) and responds toPreferencesView(fromBody(body)), an identity projection, NOT a DB re-read → keying off the local value is equivalent today. Also verified independently: mute untouched (conversations schemas.ts:119 still z.object({muted: z.boolean()}), chat-item.tsx unmodified); only ONE prefs hook in the tree; client both-or-neither is STRUCTURAL (quietHours is object-or-null, no path writes a single bound) = complementary not duplicated; a11y assertions genuinely bite (role/accessible-name queries); category-toggles-enabled-while-global-off judged acceptable, no pinning test needed (pure presentation, global row's copy states precedence).
  - MINOR (fix): notifications-card.tsx:182 (+199,256) displays the DEVICE zone while the server evaluates the STORED quietHours.timezone, which is only re-stamped when a quiet-hours control is touched (:168,177,247) — category/global toggles re-send the stored object unchanged (:211,226). Reachable wrong-copy state after travel/second device; the comment at :148-152 overstates it too. Fix: display quietHours.timezone when quiet hours are ON (detected zone only for the off state), or re-stamp on every save.
- 09 fix DONE: turbo lint --filter=@hushbox/web --force exit 0 (was 18); test:web 392 files/6361 tests + coverage gate green; typecheck green. ROOT CAUSE CONFIRMED EMPIRICALLY: `npx eslint <files>` from apps/web exits 0 on files that `eslint .` from the SAME dir reports 18 errors on → scoped eslint is not evidence for ANY task this run (standing amendment already added). conversationId dropped; recordActivity arg now optional; no per-conversation counting added. Correctly did NOT touch sw/handlers.ts or the away-gate (founder arbitration pending at the time).
- DISPATCHING: fixer A = remove dead SW→page push-event path per founder ruling (touches Task 07's sw/handlers.ts — cross-task edit AUTHORIZED by orchestrator — plus Task 09's intake); fixer B = Task 10 timezone display/effective fix.
- 09 dead-path removal DONE (founder ruling): SW no longer posts push-event (focused-client SUPPRESSION of showNotification RETAINED, pinned red-first: "hands a focused client nothing" failed with Number of calls: 1 before the edit); page intake removed; grep "push-event" over apps/packages/e2e/scripts/docs = ZERO. packages/shared untouched (pushEventPayloadSchema still has live consumers: api push-webpush.ts + SW handlers.ts); only the local forwardedPushSchema died with its consumer. pushsubscriptionchange fallback + G1 payload/copy/tag untouched, each with a passing test. Gates: turbo lint web exit 0 on a CACHE MISS (real execution), typecheck exit 0, test:web 392 files/6378 tests + coverage gate green. Known-red use-prompt-budget.ts did NOT fire — web lint fully green.
  - ACCEPTED deviation: renamed use-activity-intake.{ts,test.ts} → use-activity-reset (useActivityIntake → useActivityReset) + 1 import — with intake gone the old name was stale (durable-naming). Task-09-owned files.
  - ORCHESTRATOR FIXED docs/NOTIFICATIONS.md (my Task 00 doc, went stale from the founder ruling): §Laws-3 display-point routing now says a focused tab SUPPRESSES the system notification and produces NO in-app substitute; §foreground now says the store is fed by the open conversation's socket only, the SW does NOT feed it, and a focused-tab push yields no signal — with the rationale (the alternative was an unexecutable code path). Prettier-formatted.
  - DISPATCHING re-audit of the dead-path removal.
- 10 fix DONE: chose approach (a) — display STORED quietHours.timezone + a mismatch hint ("This device is in X. Change a time to move quiet hours here."); rejected (b) because an unrelated toggle would silently shift a traveller's enforced window. Over-broad comment corrected. Gates all green after last edit: turbo lint web exit 0 (no other workstream errors appeared), typecheck exit 0, turbo test web 393 files/6386 tests, notifications-card 100/100/100/100. Finding closed by a red-first test + targeted mutation proving it bites.
  - 4th independent report of the coverage/.tmp collision between concurrent web-suite runs → Phase 4 runs strictly SEQUENTIAL.
  - use-prompt-budget.ts known-red did NOT reproduce; web lints clean package-wide.
  - DISPATCHING 10 re-audit.
- 10 re-audit PASS, no findings → *** TASK 10 CLEAN ***. Auditor traced EVERY write path: category/global toggles re-send quietHours untouched, the two bound Selects + the enable toggle stamp deviceTimezone into what is SENT, and the prefs hook writes optimistically then replaces with the server echo → rendered zone is always either the committed stored zone or the one being committed; NO reachable divergence; nothing rendered when quiet hours are off. Test bites (asserts away-zone visible AND device-zone absent; AWAY_TIMEZONE derived from the runner's own zone so the premise holds under any TZ). Comment now accurate. No regressions (mute absent from file; facade fires only when globalEnabled changed + swallows failures best-effort; both-or-neither structural; single prefs hook). Copy verified against docs/NOTIFICATIONS.md doctrine.
  - *** 5th REPORT: the apps/web COVERAGE GATE IS UNRELIABLE UNDER CONCURRENT VITEST *** — auditor saw payment-validation.ts + message-queue.ts fail the gate though both are UNMODIFIED (mtimes 07-14/07-16), were 100% in a cached run 27min earlier, and measure 100% in isolation; a concurrent agent's vitest against the same apps/web/coverage/.tmp was observed live. PHASE 4: run the full pass with NO other vitest process, sequentially.
- CLEAN (15/17): 00,01,02,03,04,05,06,07,08,10,11,12,14,15 + 09-pending-re-audit. DISPATCHING TASK 16 (needs 10 clean ✓). Task 13 still waits on 09's dead-path re-audit.
- 09 dead-path re-audit PASS → *** TASK 09 CLEAN ***. Removal complete both sides (grep "push-event" zero across apps/packages/e2e/scripts/docs; only generated bundles retain it). CRITICAL FAILURE MODE AVOIDED + verified: focused-client SUPPRESSION survives verbatim (handlers.ts:113-121) with its own regression tests — deleting it would have started sending OS notifications to a present user. pushsubscriptionchange fallback + G1 payload/copy/tag intact; packages/shared schema still consumed by api push-webpush.ts + SW; badge still fed by use-group-chat→use-conversation-activity→recordActivity (pinned); rename accurate w/ zero stale refs; only path-exclusive tests deleted; all notification files 100/100/100/100. Auditor confirmed my docs/NOTIFICATIONS.md fix resolved the staleness.
  - PHASE-4 SWEEP BATCH (Minor, pre-existing from Task 07's earlier round): sw/handlers.ts:169 comment embeds the plan constraint ID "G2" — G11 forbids plan labels in shipped code. Batch with the identity/ports/email.ts stale PresenceReader comment.
- CLEAN (16/17): 00,01,02,03,04,05,06,07,08,09,10,11,12,14,15 (+16 in flight). DISPATCHING TASK 13 — the last build task.
- 13 impl DONE_WITH_CONCERNS. SPIKE PASSED: CDP ServiceWorker.deliverPushMessage works against the REAL shipped /sw.js (56/56 at up to 12 workers) → delivery test SHIPS, but requires the full Chromium binary (test.use({channel:'chromium'}); the default headless shell denies every notification grant). CI already installs it. 4 tests, chromium-only.
  - HONEST DESCOPES (platform-impossible, NOT faked): (1) "Enable → subscription registered" — pushManager.subscribe() fails in every configuration (incognito unsupported / no push service / permission denied even when notifications granted); test asserts permission granted + real /sw.js activated instead; subscribe→POST stays unit-covered. (2) "click → /chat/:id" — no CDP method or browser API can dispatch a notification click; unit-covered by Task 07's handlers.test.ts.
  - FLAKE FOUND AND DESIGNED OUT: focused-window rule not deterministic under a parallel matrix (both directions flipped under load; focus emulation doesn't reach the worker) → "away" redefined as "app not open" (zero window clients); focused-suppression left to unit tests.
  - Asserts tag = raw conversationId (Task 07's recorded device-local deviation) AND that no visible text contains the id.
  - Blocked from a full run by orphaned dev servers on 8788/7400 (pnpm e2e uses reuseExistingServer:false). ORCHESTRATOR RESOLVED: ports now free; killed our stale Task-12 leftover (PID 2998423). 7400 also clear.
  - No e2e:notifications root script (e2e/security and e2e/contracts have none either) — run via `playwright test e2e/notifications/ --project=chromium`.
- 16 impl: agent KILLED by a WEEKLY usage limit (resets Jul 29 09:00 ET) right after writing its report. WORK LANDED AND VERIFIED ON DISK by orchestrator: task-16/impl-report-1.md complete (15869 bytes); sound toggle wired in notifications-card.tsx:283-295 calling the store's existing setSoundEnabled (no duplicated unlock logic, G3 respected); use-read-cursor.{ts,test.tsx} present. Treating as DONE → dispatching audit rather than restarting (weekly limit would re-kill).
- DISPATCHING final audits: 13 + 16.
- Task 16 implementer RESUMED from transcript (founder invoked restart-killed-subagents). Scoped re-entry: verify the UNVERIFIED gates (turbo lint/typecheck/test, package-wide commands only) + finish report; do NOT edit code unless a gate is red, because its auditor is concurrently reading the same files (read-only, but a moving target would invalidate the audit). Told it ports 8788/7400 are free and the stale dev server is killed.
- In flight: 16 implementer (gate verification), 16 audit, 13 audit. Then Phase 4.
- 13 audit FAIL — auditor RAN the suite (1/4 passed). 2 Criticals, both deterministic and CI-identical:
  (1) notifications.spec.ts:153 — waitForAppStable() after goto('/settings'), but data-app-stable is emitted ONLY at apps/web/src/routes/_app/chat.index.tsx:89 (chat index route) → can never appear on /settings → burns the 15s APP_STABLE budget and times out (16.3s, page snapshot shows /settings fully rendered). ENTIRE settings criterion (lines 155-191) unverified. Fix: gate on a settings-visible signal (e.g. the "All notifications" switch) instead of the chat-only signal.
  (2) notifications.spec.ts:75 and :104 — both fail at FIXTURE TEARDOWN (fixtures.ts:1013) on the console-error guardrail: Chrome emits "does not support the Push API in incognito mode" whenever our web-adapter.ts:66 calls pushManager.subscribe(), and every Playwright context is incognito-like. Test BODIES both passed. Fix: expectConsoleErrors(page, [/does not support the Push API in incognito/]) — the guardrail's documented opt-out; the message is browser chrome, not an app fault.
  (3) IMPORTANT: "green at retries=0" unmet as delivered. Runtime otherwise fine (3.6/4.2/4.5s real journeys; the 16.3s is the timeout itself).
  - SPIKE INDEPENDENTLY VERIFIED BY EXECUTION: CDP push reached the REAL shipped /sw.js (registration id from ServiceWorker.workerRegistrationUpdated; waitForPushServiceWorker asserts the ACTIVATED worker's scriptURL contains /sw.js), ran its own Zod validation + copy table + collapse tag + focus rule, notification read back via registration.getNotifications(). NO page-side shim anywhere. Test 3's body fully passed incl. tag===CONVERSATION_ID, collapse-onto-first, and no-visible-id. Both descopes verified honest with their unit-test carriers cited.
  - MY PLAN ERROR CORRECTED: Task 13's delivery criterion said "alias tag"; the device-local tag is the raw conversationId per amended I4. Plan updated.
  - Flagged upward: port 7400 still held by a 4h-old orphaned `@hushbox/sandbox dev` (pid 2947891, the sandbox workstream's — auditor correctly did not kill it); it forced a scratchpad config with reuseExistingServer:true. My earlier ss check missed it (permissions). → Phase-4/founder item; it blocks E2E runs on this box.
  - Hygiene item for the fix: test 4 leaves the shared pooled persona test-alice with notifications globally OFF + a quiet window; auditor grep-confirmed no other spec reads those fields (inert today) but it should restore state.
  - No e2e:notifications root script (e2e/security + e2e/contracts also lack one; package.json out of ownership) — acceptable, run via `playwright test e2e/notifications/ --project=chromium`.
  - DISPATCHING 13 fixer.
- 16 audit PASS, no findings → *** TASK 16 CLEAN ***. Auditor re-ran ALL gates itself (implementer's were unverified after the usage-limit kill): turbo lint web exit 0, typecheck exit 0; owned files 100/100/100/100 (use-read-cursor.ts, notifications-card.tsx) + the wired seam authenticated-chat-page.tsx 100/96.1/100/100 (uncovered branches pre-existing); 150/150 on the owned test set; G11 clean.
  - ANTI-SPAM VERIFIED NON-VACUOUS: effect keys on two primitives (newestSeq, recordedSeq) not the list object → 3 rerenders = 1 call, and the post-write list echo = still 1 call, disproving the invalidate→refetch→rewrite cycle. Mutation check (weakening <= to <) fails exactly 2 tests.
  - DISMISS-ON-READ-ELSEWHERE NOW DEMONSTRABLY LIVE: use-read-cursor.test.tsx:274-303 runs the REAL useAdvanceReadCursor + REAL useClearReadElsewhere together — clearDelivered NOT called before the write, then called once with [conversationId] after the refreshed list carries the advanced cursor. (Server GREATEST itself is Task 05's, over real Postgres.)
  - Sound toggle delegates entirely to the store's setSoundEnabled (zero audio code in the card) → One-Implementation intact; rendered outside the prefs query body so a device setting doesn't vanish on a failed account query (stated + pinned).
  - Out-of-scope failures attributed: document-parser.test.ts (rewritten DURING the audit run by the documents workstream, mtime 22:39) and payment-modal.test.tsx (VITE_HELCIM_JS_TOKEN not configured; passes in isolation; untouched since 2026-07-15).
- CLEAN (16/17): 00,01,02,03,04,05,06,07,08,09,10,11,12,14,15,16. ONLY TASK 13 REMAINS (fix in flight).
- 13 fix DONE: 4/4 green at --retries=0 (47 passed incl. 43 setup auth, 1.6m) AND --repeat-each=3 → 12/12 notifications runs, 0 flaky. turbo lint typecheck e2e exit 0 after last edit.
  - Config deviation (accepted): used a TEMPORARY root-level config importing playwright.config.ts flipping ONE field (Sandbox webServer reuseExistingServer:true) — root, not scratchpad, because Playwright resolves testDir/globalSetup/globalTeardown/reporter/storageState against the config's OWN dir. Deleted immediately; git status confirms gone.
  - Port 7400 STILL held by another workstream's orphaned sandbox dev server — blocks every unmodified E2E run on this box for ALL workstreams. → founder item.
  - playwright.config.ts working-tree diff (new Sandbox webServer block) is the sandbox workstream's, NOT ours.
  - Residual (accepted): test 4's persona restore is the last statement of the test body, not afterEach — e2e rule 2.7 bans spec hooks, so a mid-test failure still leaves test-alice dirty. Auditor previously grep-confirmed no other spec reads those fields (inert today). Best available within the rules.
  - DISPATCHING 13 re-audit — the LAST audit before Phase 4.
- 16 implementer (resumed) confirmed its gates INDEPENDENTLY: turbo lint web exit 0, typecheck exit 0, turbo test web 393/393 files pass, its files 100/100/100/100 in the merged table AND in isolation. Corroborates the audit's PASS from both sides. No code edits after the gates (auditor-safe).
  - EXPLAINS an out-of-scope failure the auditor saw: a concurrent agent regenerated .env.development MID-RUN (md5 changed), making payment-modal.test.tsx fail 7 tests with "VITE_HELCIM_JS_TOKEN is not configured"; passes after it settles. markdown-renderer.tsx coverage red also not ours (its TEST file rewritten 22:39 by another agent; source untouched since 18:18; isolated re-measure: branches 100, passes).
  - *** PHASE-4 PRECONDITION SHARPENED: the full unscoped pass needs a QUIET TREE — no concurrent vitest (coverage/.tmp collisions: 60 bogus ERRORs on 18 untouched files observed) AND no concurrent .env.development regeneration. ***
- Awaiting ONLY Task 13's re-audit → then 17/17 and Phase 4.
- 13 re-audit PASS, no findings → *** TASK 13 CLEAN ***. Auditor independently reproduced 4/4 at --retries=0 AND 12/12 under --repeat-each=3 (0 flaky). Critical 1 closed genuinely: gotoWithPreferences(page,'/settings') + globalSwitch visibility (the card renders a skeleton while isPending, so switch-visible IS the loaded signal); all settings assertions now EXECUTE against polled server truth (runCompletion false; quiet window {22:00,07:00,UTC}; startMinutes 23*60; globalEnabled false; offer hidden on /chat). Critical 2 closed narrowly: the incognito-Push-API pattern is applied at exactly 2 test sites; tests 1 and 4 keep the guardrail whole; expectConsoleErrors APPENDS so nothing else is masked. Grep-verified ZERO waitForTimeout/sleep/test.slow/skip/retries/afterEach/.only/serial. Harness/spec split intact; temp config deleted (only playwright.config.ts remains).
  - Copy-table duplication in the harness judged CORRECT (independent oracle — importing the worker's table would make the assertion tautological; the exemption CODE-RULES names explicitly).

## *** ALL 17 TASKS CLEAN — PHASE 4 ***
- 00,01,02,03,04,05,06,07,08,09,10,11,12,13,14,15,16 all ended on an audit I read and agreed was clean.
- Phase 4 dispatched: (A) full unscoped pass with strict attribution (3 other workstreams active in this tree: blog-tts, in-browser-code-execution, affordability-remediation); (B) completeness critic.
- Phase-4 backlog already accumulated:
  * SWEEP BATCH (2 stale comments): sw/handlers.ts:169 embeds plan label "G2" (G11); identity/ports/email.ts:6 references the deleted PresenceReader.
  * FOUNDER ITEMS: cap sync android repo-level red (pre-existing, blocks mobile CI); orphaned sandbox dev server on port 7400 (blocks ALL e2e on this box); arch-test TABLE_NAMES mirror = sync-contract trap for every future table; Maestro login block duplicated across flows 07/10/14/15 (runFlow subflow?); post-cap-sync confirmation that :capacitor-community-fcm lands in the Android project; quiet-hours 24h labels (no locale formatter in repo); negative backendEnvSchema tests non-isolating (pre-existing); optional index on device_tokens.lastSeenAt; re-run Maestro flows 10/11/12/14 (enable prompt now renders in them).
  * DOC PROPOSALS: ARCHITECTURE.md notifications slice line; TECH-STACK Mobile row for @capacitor-community/fcm.
- PHASE-4 COMPLETENESS CRITIC: FAIL — found 2 Criticals that ALL 17 scoped audits missed (each invisible from inside one task):
  (C1) *** RUN-COMPLETION NEVER NOTIFIES THE REQUESTER *** notify-decision.ts:32 drops the actor unconditionally; room-core.ts:742 passes the paying sender as actorUserId → in a SOLO conversation the member set IS the actor → runCompletion notifies NOBODY, ever. Every run-completion test seats a second member (push-notify.integration.test.ts:158-192) so nothing caught it. Shipped copy promises the opposite (notifications-card.tsx:41 "When a model finishes work you started"; docs/NOTIFICATIONS.md:44). Actor exclusion is correct for message/membership; for runCompletion the completed action is the MODEL's. Fix: category-scoped actor exclusion (presence already suppresses someone watching).
  (C2) *** ANDROID CLEARING CAN NEVER MATCH *** push-fcm.ts:227 sets android.notification.tag = collapse ALIAS; native-adapter.ts:22 prefers notification.tag and only falls back to data.conversationId. On Android the tag is ALWAYS present and is the alias → targets.has(alias) always false → dismiss-on-read-elsewhere INERT on Android. Verified against plugin Java source (getDeliveredNotifications returns getTag(); data = Notification.extras, NOT the FCM data payload). native-adapter.test.ts:85-100 encodes the false premise. My I4 amendment fixed the WEB half; no task owned the native half. NOT a local fix — G1 forbids the raw id in the FCM tag (Google sees it). → FOUNDER RULING.
  (I3) iOS push still non-functional end-to-end (SPM manifest has no @capacitor-community/fcm, no FirebaseMessaging, AppDelegate never configures Firebase, plist placeholder, cap sync never ran). Consistent with what Task 11 declared; track as the run's open founder item, NOT as done.
  (I4) Notification copy exists TWICE and HAS DRIFTED: notify-event.ts:26-28 (native) vs sw/notification-copy.ts:16-18 (web) have different strings. Strict web payload carries only {category, conversationId} so the server's text never reaches the SW — two copies are structurally necessary, but One-Implementation says hoist ONE table to packages/shared (both already import NotificationCategory from there).
  (I5) "Later" has no route back + a browser-denied device is never told: the settings card never reads notificationChannel.getPermissionState(); turning the global switch ON while permission is denied saves globalEnabled:true, swallows the denial, and leaves the user believing notifications work. Not excluded by any ruling.
  (Minor) docs/NOTIFICATIONS.md has 4 stale statements (:43 message no longer fires from the DO sink; :47-49 membership passes an EMPTY presence snapshot; :111 pushsubscriptionchange has no client listener; :186 Playwright does not cover click/deep-link). ORCHESTRATOR to fix.
  (Minor) GoogleService-Info.plist:9 comment references "the Task report's production checklist" — run-artifact reference in shipped source (G11). Fix → point at docs/NOTIFICATIONS.md.
  (Minor, PRE-EXISTING/out of scope) Android manifest lacks default_notification_icon/channel meta-data + white-silhouette drawable → launcher icon renders as the classic white square. First thing a user sees on Android.
  (Minor) manifest.webmanifest has a single 1024px "any" icon; no 192/512, no maskable. Cosmetic.
  - VERIFIED COMPLETE (no padding): dead/orphaned code CLEAN (knip: no unused export/port/schema from this run); approved-schema-only, no-jobs-for-delivery, quiet-hours-drops, mute-boolean, no-per-device-UI, ask-on-mount-removed, focused-tab-silent, client-only badge, PWA manifest ALL honoured; retention↔delivery and unregister↔endpoint seams genuinely proven.
- Full unscoped pass still running → will batch its findings with C1/I4/I5/minors into ONE fixer. C2 blocked on founder ruling.
- PHASE-4 UNSCOPED PASS: deterministic surface GREEN — typecheck 16/16, lint 16/16 (billing fixed their complexity red), arch:check 11 rules/1982 files, duplication 1.01%, NO migration drift (60 sql files, journal md5 unchanged), api coverage 99.55/98.54/99.62/99.81, workers Intl pin green, verify:env dev+prod pass (ciVitest/e2e "failures" are harness artifacts — verifier asserts isCI=true, shell has CI=undefined), gitleaks ZERO in notification source. ALL known-external reds independently re-verified external (template-html delta enters via billing's money.ts through @hushbox/shared; markdown-renderer mtime/mode prove external; payment-modal PASSED this run; knip findings pre-existing/sandbox).
  - [Critical] flagged: I7 push-event postMessage absent both sides. THIS IS THE FOUNDER-RULED REMOVAL — but MY BOOKKEEPING FAILURE: I recorded it in the ledger + docs/NOTIFICATIONS.md and NEVER amended plan.md, so the auditor correctly flagged it against the plan as written. plan.md amendment now recorded.
  - HOWEVER the auditor surfaced NEW FACTS that invalidate the ruling's premise: (i) the conversation list is NOT refreshed while the tab is visible (no refetchInterval; only invalidated on visibilitychange, which doesn't fire while already visible) → the code comment "the conversation list shows the activity where they are looking" is FALSE and load-bearing; (ii) matchAll({type:'window', includeUncontrolled:true}) counts ANY same-origin page → a focused /blog tab suppresses EVERY notification. Founder ruled on the premise that a present user would see it in the list. → RE-RAISING with correct facts.
  - Copy-drift independently confirmed by this pass too (founder already ruled: hoist to shared).
- 2026-07-24 FOUNDER RULINGS round 5 (both correcting earlier ones on new evidence):
  (R1) MOBILE TAG = conversationId. Stop aliasing the FCM notification tag; the same message's DATA payload already exposes the id to Google, so this is NOT new exposure. Keep the HMAC alias ONLY for the Web Push Topic header (encrypted payload → the header is the sole leak there). Supersedes the earlier "data-only push for both" instruction, which I refuted with evidence: iOS data-only pushes are throttled (~2-4/hr), coalesced, dropped on force-quit, and CANNOT display without an NSE (founder-rejected) → would have broken iOS.
  (R2) SUPPRESS ONLY FOR THE CONVERSATION BEING VIEWED. The SW must show the OS notification unless a FOCUSED client's URL is actually /chat/<that conversationId> (WindowClient.url is available — no messaging needed). Supersedes the earlier "stay silent when any client is focused" ruling, which rested on a premise I gave that was FALSE (the conversation list does not refresh while the tab is visible) and whose blast radius included ANY same-origin tab (e.g. /blog).
  - Files reserved for this second fix (NOT touched by the in-flight batch): push-fcm.ts (tag), native-adapter.ts (matching may simplify), sw/handlers.ts + handlers.test.ts (URL-scoped suppression).
  - HOLDING dispatch until the batch fixer lands — both would touch apps/web/src/sw/, and the copy hoist may alter handlers.ts's imports.
  - DOC DEBT created by these rulings (orchestrator to fix AFTER both land): docs/NOTIFICATIONS.md §Transports ("all collapse tags carry a derived alias — never the raw id") and §Laws-3 ("a suppressed notification produces no in-app substitute") are now wrong.
- CLOSE BATCH 1 DONE_WITH_CONCERNS (4 findings, all test-first with RED watched via a temporarily restored inversion): F1 category-scoped actor exclusion (runCompletion now notifies its requester; message/membership still exclude the actor); F2 copy table hoisted to packages/shared (E2E oracle stays independent, already matched); F3 permission-state UI in the settings card; F4 plist comment. Owned-file coverage 100% on all 5 source files; shared suite green; typecheck exit 0.
  - Reds attributed by file to concurrent work: 7 template-html (billing via @hushbox/shared money.ts); 72 in 4 documents-workstream files (root cause: endsInsideOpenFence absent from document-parser.ts, agent mid-TDD); a later lint red was 9 errors all in documents files it never touched (its own 4 scoped clean).
  - ACCEPTED deviation: edited ONE row of docs/NOTIFICATIONS.md (the Actor row became factually wrong after F1) — G10 lists that file writable this run.
  - Noted: an existing test's premise died with F1 (push-notify.test.ts "never looks up tokens when every member is suppressed" now suppresses the requester by PRESENCE rather than actor; assertion unchanged) — correct, not a weakening.
  - ENV TRAP recorded for future runs: adding an export to packages/shared silently breaks apps/api vitest with "X is not a function" until apps/*/node_modules/.vite is deleted (Vite hashes a linked workspace dep's package.json, not its source). CI is cold-start and unaffected. Cost one false-red API run.
  - MY BRIEF OMISSION: the identity/ports/email.ts:6 stale-PresenceReader sweep was in Standing amendments but absent from the brief → rolled into close fix 2.
- DISPATCHED CLOSE FIX 2: R1 mobile tag = conversationId (keep alias for Web Push Topic only) + server↔client agreement test; R2 URL-scoped suppression (show unless a FOCUSED client is on that conversation's deep link; robust path-segment match; do NOT revive push-event); copy neutrality for runCompletion (possessive is wrong now that co-members are notified too) incl. the E2E oracle literal; the 2 stale comments (email.ts PresenceReader, handlers.ts "G2" label).
- CLOSE FIX 2 DONE, ALL GATES GREEN: lint web+api+shared exit 0 (+e2e lint), typecheck 3/3, test:shared 107/2399, *** test:web FULLY GREEN 393 files/6365 *** (documents workstream landed — their reds are gone), test:api 7 failed = ONLY the pre-declared template-html snapshots. All 6 behavior changes watched RED first with recorded failure messages.
  - DECISION (asked for, well-reasoned): only the Android notification.tag becomes the raw conversationId. FCM collapse_key, APNs collapse id and Web Push Topic ALL KEEP THE ALIAS — they collapse different things (pending messages vs the displayed shade entry), both stay 1:1 per conversation, so G1's header rule survives UNQUALIFIED and the exception is confined to the one field the client must read.
  - native-adapter.ts deliberately NOT simplified: verified in the plugin's NATIVE sources that Android returns getTag() + extras-WITHOUT-id while iOS returns NO tag key and the id ONLY in data → each branch covers exactly one platform; the fallback is not redundant.
  - Server↔client agreement pinned as TWO halves (no test may span apps/api and apps/web — boundary-banned, no shared artifact): server asserts tag === its own data payload's conversationId; the client's Android fixture now carries extras with no id so the tag alone can match. A revert on EITHER side fails.
  - Beyond-brief (correct): edited docs/NOTIFICATIONS.md (4 passages falsified by the two rulings) + 2 stale e2e comments restating the old wider suppression rule. No e2e logic changed (the suite pushes from about:blank, so ruling 2 doesn't affect it). MY DOC DEBT IS CLEARED.
  - STANDING AMENDMENTS NOW STALE (good news): the documents-workstream test:web reds are GONE and use-prompt-budget.ts lint is green — web no longer needs attributing-around.
  - Cross-agent env event: a concurrent install mid-verification (pnpm-lock 00:59, rolldown 1.1.5) produced two spectacular false reds; all reported gates are post-settle re-runs.
  - FOUNDER ITEM: the FCM tag fix is unverifiable end-to-end in CI by design (server→FCM→device is the documented untested hop; Android shade only reachable via Maestro) → recommend ONE manual device pass before release.
  - NOTE: committed build artifacts apps/web/dist/sw.js and android assets sw.js still carry the OLD suppression logic + old copy string; regenerated on next build.
- DISPATCHING: final re-audit of BOTH close batches + the declared E2E run.
- CLOSE-FIX RE-AUDIT: *** PASS, NO FINDINGS *** — both batches clean. Gates run by auditor: lint web+api+shared 3/3 exit 0, lint e2e exit 0, typecheck 3/3 exit 0, test:shared 107/2399 pass, test:web 393 files/6365 pass (coverage gate green; sw/handlers.ts 100/100/100/100).
  - Solo-requester proven END-TO-END over real Postgres (push-notify.integration.test.ts:168-175 — actor is the sole seated member, holds the only token, gets exactly one runCompletion) AND at unit level; presence still beats the new actor rule. EXCLUDES_ACTOR is a TOTAL Record<NotificationCategory, boolean> → a 4th category forces a compile-time decision.
  - Alias/tag split verified exactly: collapse_key + apns-collapse-id + Web Push Topic = alias; ONLY android.notification.tag = raw id. Two-half pin confirmed (revert server → push-fcm.test.ts:198 fails; revert client → native-adapter.test.ts:101 fails, its Android fixture carrying extras with NO id so only the tag can match). iOS data fallback correctly retained.
  - SW suppression matches WHOLE PATH SEGMENTS and shares CONVERSATION_PATH_SEGMENT with conversationUrl() → the "viewing" rule and the click target CANNOT DRIFT (a genuine sync-contract collapse). Tests: same conv suppressed / different conv shown / /blog shown / unfocused-on-that-conv shown / /chat/<id>-copy shown. grep "push-event" = zero.
  - Copy neutral in both places; comment sweeps clean; docs/NOTIFICATIONS.md accurate for BOTH rulings (law 3, privacy paragraph naming the Android-tag exception, dismissal bullet separating collapse identity from displayed tag, badge paragraph, Actor row).
  - CORRECTIONS to my notes: dist/sw.js + android assets sw.js are GITIGNORED, not committed → stale-artifact limitation is SMALLER than reported. test:api had 10 reds not 7; the extra 3 (rate-limit, chat smartModel, identity email-verify) PASS in isolation = concurrency flakes.
  - FOUNDER/REPO ITEM (not ours, persistent): template-html.test.ts fails DETERMINISTICALLY in the COMMITTED tree — templates/base.ts no longer emits the Merriweather <link> its snapshot expects and NEITHER file is modified in the working tree. It will fail every api run until its owner updates the snapshot.
- Awaiting ONLY the declared E2E run → then close-out.
- E2E agent killed by a session limit mid-run (was re-running the chat suite on a quiet machine with a freshly seeded catalog). RESUMED from transcript per restart-killed-subagents; gave it the two firmed-up facts (dist/sw.js is gitignored → stale SW is a rebuild, not a repo problem; template-html is another workstream's deterministic committed-tree red, don't chase). Need from it: notifications suite result, chat/streaming result, current-vs-stale SW under test, per-failure attribution.
- FOUNDER: skip the E2E run (agent stopped). Phase-4 step 3 deliberately not performed — recorded as a founder decision, not an omission. NOTE: the notifications suite was previously verified 4/4 at retries=0 + 12/12 under --repeat-each=3 by two independent agents, but that was BEFORE ruling 2 changed the SW suppression logic; the chat/streaming regression check was never completed.
- FOUNDER APPROVED all 5 doc proposals → APPLIED by orchestrator: ARCHITECTURE.md:47 slice line (email, native FCM + in-house Web Push, device tokens/subscriptions, notification preferences); ARCHITECTURE.md data-model list (device_tokens carrying both target kinds, notification_preferences, and conversation_members.lastReadSeq as the dismissal cursor); TECH-STACK Mobile row for @capacitor-community/fcm; TECH-STACK jose row now names VAPID signing; TECH-STACK new in-house Web Push row with the rejection rationale. Prettier-formatted.
## RUN CLOSED. Working tree left uncommitted for the founder.

## RUN REOPENED — Task 17 (founder-reported UI defect)
- Founder: the enable prompt appears above the main content header, not below the conversation list in the left sidebar; looks different and reads differently than proposed. VERIFIED: mounted at app-shell.tsx:44 inside <main> above {children}; horizontal layout (text left, buttons right) chosen for a wide container; copy is one long paragraph. Right component + right behavior, wrong location and shape.
- Founder APPROVED the sidebar proposal and asked to KEEP the "Change this any time in Settings." line.
- ORCHESTRATOR DEVIATION FROM THE APPROVED MOCKUP (flagged to founder): keeping the button label "Later", not "Not now". The Playwright spec selects the region by its two button accessible names and the Maestro flow matches visible text; the Maestro suite cannot be re-run here (emulator path blocked by the cap-sync red). Not worth breaking two suites for a synonym. Founder can override.
- Keeping useEnablePrompt + prompt-dismissal.ts UNTOUCHED (audited correct — the defect is purely presentational).
- 17 impl DONE_WITH_CONCERNS. Moved to the sidebar below the conversation list, vertical card, copy incl. the Settings line, buttons Enable/Later retained. Gates green: turbo lint web exit 0, typecheck exit 0, test:web 393 files/6386 tests, app-shell/sidebar/components-notifications all 100/100/100/100. Copy-preserving deviations accepted: semantic <h2> heading; body text-xs under a text-sm heading for the 260px column.
  - *** MY SPEC DEFECT, surfaced by the implementer (it built as specified and flagged rather than deviating): the desktop sidebar DEFAULTS TO COLLAPSED (stores/ui.ts:15 sidebarOpen:false, persisted), so my "hide in rail" criterion meant a first-time desktop user never sees the one-time offer. ***
  - FOUNDER RULING 2026-07-25: rail shows a COMPACT aria-labelled bell (subtle dot) that EXPANDS the sidebar on click, revealing the full card. Criterion amended in plan.md.
  - E2E FALLOUT (now in scope for Task 17): Playwright notifications.spec.ts:51 and :75 assert the offer visible on a fresh context = the collapsed rail → must expand first; BOTH Maestro flows (07:66-72, 15:52-58) assert ".*Turn on notifications.*" on the chat page without opening the drawer — the mobile sidebar is a Sheet that UNMOUNTS its content while closed, so the text is not in the hierarchy → must tap the hamburger first.
  - Dispatching Task 17 round 2 (rail affordance + both suites).
- 17 round 2 DONE_WITH_CONCERNS, ALL GATES GREEN: turbo lint web exit 0, typecheck web exit 0, lint+typecheck e2e exit 0, test:web 393 files/6389 tests (coverage gate on, new file 100%), *** playwright e2e/notifications 4/4 at --retries=0 (47/47 with setup) *** — which also re-verifies the notification E2E against the CURRENT service worker, closing the gap left when the founder skipped the Phase-4 E2E run.
  - *** REPO ISSUE FOR FOUNDER (verified by orchestrator): playwright.tmp-reuse-sandbox.config.ts IS IN HEAD *** — swept into commit 39a07db0 "a whole lot" during the ~5 min the agent's temporary Playwright config existed. Worktree shows ` D` (deleted, unstaged). I cannot do git writes; the deletion must ride the founder's next commit (`git add -A` or `git rm --cached`). Inert — nothing imports it, Playwright only loads playwright.config.ts.
  - Accepted deviations: rail affordance is a NEW file components/notifications/enable-prompt-rail.tsx (one-component-per-file convention beats the plan's Files list); e2e/pages/sidebar.page.ts edited to make the EXISTING ensureSidebarExpanded() public (no behavior change, inside the brief's e2e scope).
  - GOOD CATCH by the implementer: it fixed TWO Playwright assertions beyond the two I named — the post-reload "Later is permanent" and "all off retires the offer" cases. Both assert ABSENCE and would have passed for the WRONG REASON on a collapsed rail (vacuous pass).
  - Port 7400 still held by another workstream's orphaned sandbox server (tsx src/serve.ts, up since Jul 24 18:26); left untouched, temporary-config fallback used then deleted.
  - Maestro flows remain CI/device-verified only (YAML parse + step-order + selector existence in the built bundle; not executed — emulator path blocked by the pre-existing cap-sync red).
  - DISPATCHING Task 17 audit (covers both rounds).
- 17 audit PASS + 1 Minor. Verified independently: placement pinned by compareDocumentPosition against TEST_IDS.sidebarNav/sidebarFooter (not a class-name proxy) — genuinely between list and footer, confirmed against SidebarPanel's {header}{body}{footer} composition; copy byte-exact vs the ruling; rail affordance is 22 lines reusing useEnablePrompt + the EXISTING toggleSidebar (same action sidebar.tsx:145 already passes as desktop onClose), zero new state, and it CANNOT answer the offer by construction (enable/dismiss are never destructured — the "not called" test is structurally guaranteed, not just asserted); hook + prompt-dismissal byte-unchanged; Maestro step order correct (flow 07's launch-time assertNotVisible 'Allow' at :32 AND the post-shell repeat at :63 both PRECEDE the first hamburger tap at :71; flow 15's still immediately before tapOn 'Enable'); playwright 4/4 at retries=0; no sleeps/retries/skips/G11 labels.
  - AUDITOR CAUGHT A CIRCULAR PROOF: impl-report-2's "byte-unchanged via git diff HEAD" is circular because HEAD (39a07db0) contains its own edits; corroborated independently via mtimes instead.
  - MINOR (dispatched): notifications.spec.ts:112-114 — the 3rd absence assertion after reloadWithPreferences lacks an explicit ensureSidebarExpanded(); it is non-vacuous TODAY only because sidebarOpen is persisted (stores/ui.ts:22-25 partialize) and :95 expanded it pre-reload. If partialize or the storage key changes it silently degrades to "hidden because the rail is collapsed". Its two siblings (:82, :211) got explicit expands for exactly this risk.
  - Coverage gate red ONLY on markdown-renderer.tsx (in-browser-code-execution workstream, uncommitted + in flight); all Task-17 files 100/100/100.
- 17 Minor fix DONE: explicit ensureSidebarExpanded() before the post-reload absence assertion (page object bound to a const, used twice); comment states the durable reason. Playwright 4/4 at retries=0 (47/47 with setup); e2e lint+typecheck exit 0 after last edit. Temp config was named playwright.TEMPORARY-DELETE-ME.reuse-sandbox.config.ts, used once, DELETED (ls playwright*.ts shows only playwright.config.ts). Re-audit dispatched.
- HEADS-UP FOR FOUNDER (another workstream, may affect us): an agent is MID-MOVE of packages/shared into affordability/ — it broke the API worker build mid-run with 15 TS errors, all in packages/shared. Transient (green on clean re-run) but our notification code imports heavily from that package; expect churn there.
- Still outstanding for the founder's next commit: ` D playwright.tmp-reuse-sandbox.config.ts` (swept into 39a07db0).
- 17 re-audit PASS, no findings → *** TASK 17 CLEAN ***. Auditor independently reproduced 4/4 at retries=0 (its own report at e2e/report/2026-07-25T17-47-31/) and lint+typecheck exit 0; diff verified byte-for-byte as two hunks in one file; grep found no test.slow/skip/fixme/waitForTimeout/.only/retry escape hatch. Confirmed ensureSidebarExpanded() is idempotent so the added call is a no-op when state did persist.
  - Independently re-confirmed the two founder-owned repo items: playwright.tmp-reuse-sandbox.config.ts IS in HEAD with only an uncommitted worktree deletion (no code change can clear it — must ride the next commit); port 7400 still answers 200 from an orphaned sandbox server, so reuseExistingServer:false aborts every Playwright run in this checkout until it is reaped.

## *** RUN CLOSED — 18/18 TASKS CLEAN (00-17) ***
Every task ended on an audit the orchestrator read and agreed was clean. Working tree left uncommitted for the founder.

## RUN REOPENED 2026-07-26 — proof-hardening Tasks 18-21 (founder: "deeply research the system... evidence row should only strictly be written when a real call happened")
- Dispatched 7 researchers (5 codebase: evidence system, FCM adapter, webpush crypto, web sinks+e2e harness, CI/secrets; 2 web: FCM validate_only, real-browser push) + 1 empirical analyst (Task 20 lever).
- *** FOUNDER-RULE VIOLATION FOUND IN EXISTING CODE: push-fcm.integration.test.ts:82-103 writes the push-fcm evidence row with fetchImpl MOCKED; ci.yml:222-224 comment admits it. resend has the SAME defect (email-resend.ts:98-105). Honest writers = helcim/linear/r2/gateway-metadata (real call first, evidence last). ***
- F2: FCM creds must ride the Linear pattern (raw job env var + process.env + skipIf), NOT env.config secret() — generate-env.ts:246-248 throws and hard-fails CI the moment a ciVitest secret() lands unprovisioned. Forks already can't run CI (OPENROUTER precedent), so no new fork constraint.
- F3: Task 18 SPIKE DELETED as answered. validate_only = top-level snake_case sibling of message. Error taxonomy separates fabricated(400 INVALID_ARGUMENT)/other-project(403 SENDER_ID_MISMATCH)/lapsed(404 UNREGISTERED), all carrying details[].@type FcmError. NEVER assert: exact success placeholder (undocumented) or a specific status for a fabricated token. Quota: docs silent, assume normal debit.
- F4: CDP deliverPushMessage data is a plain STRING, no p256dh/auth/VAPID — injects plaintext AFTER decryption point. Existing notifications e2e proves SW handling only, nothing about our encryption. Confirms Task 19's necessity.
- F5 (measured, Playwright 1.60.0/chromium-1223): second tab + bringToFront = 0/26 away, state UNREACHABLE and fails SILENTLY (count stays 0) → would have been a defect. setPageVisibilityOverride doesn't exist; setWebLifecycleState zero effect; emulateMedia can't touch visibility. WINNER: setFocusEmulationEnabled(false) + Browser.setWindowBounds(minimized), BOTH required (0/6 each alone) = 6/6, 7/7 at CI worker count 7, 0 extra polls, real blur + real focus on restore. Fails --headed (0/4, bare Xvfb). setAppBadge callable+resolves in headless localhost tab; NO getAppBadge exists → wrap-and-delegate spy is the only observation.
- *** TASK 13'S FOCUS CONCLUSION DOES NOT TRANSFER: push-harness.ts:238-240 is about the SW's clients.matchAll({focused:true}) (browser-process-wide, outside the CDP target); the page's own document.hasFocus() is inside it and is deterministic. That comment must be NARROWED or it will mislead the next agent into rejecting the right approach. ***
- F6: Task 21 KILLED ON EVIDENCE, not deferred. Chrome real push impossible 3 ways: subscribe() needs headed+launchPersistentContext (kills our storageState auth), host-resolver-rules reaches only send-side fcm.googleapis.com not the mtalk.google.com:5228 MCS channel (proprietary), and GoogleChromeLabs/web-push-testing-service archived 2021-08-31. Only evidenced real path = Firefox + Mozilla autopush (firefoxUserPrefs dom.push.serverURL + containerized autopush-rs) — would SUBSUME Task 19 — but needs a docker-compose service (~1.5GB Bigtable emulator) = infrastructure decision, founder-only per AGENT-RULES.
- Tasks 18-21 rewritten in plan.md with F1-F6 as binding findings. AWAITING FOUNDER APPROVAL — nothing dispatched.
- *** RESEND TRACED FULLY (founder challenged my claim — correctly, I had taken ci.yml's comment on faith rather than tracing it). VERDICT CONFIRMED + root cause found: the resend evidence row is UNEARNABLE BY CONSTRUCTION. Two conditions needed (real call + isCI) are mutually exclusive because email-sender-factory.ts:64-84 returns withMailboxCapture(mock) for `isLocalDev || isCI`, so the real adapter exists only in production where recordServiceEvidence no-ops on !isCI. RESEND_API_KEY has no entry outside production (env.config.ts:331-335). Sole writer = email-resend.integration.test.ts with okFetch stub (:27-29). E2E does NOT use Resend: newsletter double-opt-in reads /dev/mailbox (dev/routes.ts:723,731) off withMailboxCapture; the e2e job runs no --require=resend at all. push-sender-factory has the IDENTICAL isLocalDev||isCI branch → same structural impossibility. CONTRAST: payment-provider-factory.ts:47-73 branches on isLocalDev ONLY, so CI gets the REAL Helcim adapter + sandbox call → its row is genuinely earned. ***
- CORRECTION I owed the founder: ci.yml:222-224's comment is ACCURATE (it states plainly that these assert a mocked seam, not a live call, and flags a real call as pending). The defect is the STEP NAME ("Verify Resend was called") and the false confidence the row projects — not a stale comment. I had said "the comment admits it" as if it were a confession of a hidden bug.
- FOUNDER RULINGS 2026-07-26: R-A Resend fixed by DELETION (new Task 22, depends on 18 for ci.yml ownership) · R-B Task 18 lands unverified-locally, real call first runs in founder's CI, audit must not treat the unexecuted call as a finding · R-C Task 21/autopush stays killed · R-D custom IAM role with ONLY cloudmessaging.messages.create, secrets FCM_PROJECT_ID_CI + FCM_SERVICE_ACCOUNT_JSON_CI (distinct from production names so CI can never read a production credential).
- plan.md updated: rulings block + Task 18 audit note + Task 22 added. AWAITING APPROVAL TO DISPATCH.
