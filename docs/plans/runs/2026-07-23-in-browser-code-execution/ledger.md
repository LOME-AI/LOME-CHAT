# Ledger — in-browser code execution / rendering (document panel)

- 2026-07-23: Run opened. Phase 1 research dispatched:
  - analyst → research/package-landscape.md (package choice: Sandpack vs WebContainers vs alternatives; TECH-STACK lists Sandpack as *planned* — a prior to verify)
  - codebase-explorer → research/codebase-integration.md (document panel, mermaid rendered/raw toggle, Streamdown plugin wiring, CSP/Capacitor, system-prompt assembly, dep conventions)
- 2026-07-23: codebase-integration research DONE (research/codebase-integration.md). Key facts (Verified by explorer):
  - Document panel exists: `apps/web/src/components/document-panel/document-panel.tsx` + `useDocumentStore`; `Document` type already has `'html'|'react'` in its union — unrendered seam.
  - Rendered/raw toggle is one line to generalize: `supportsRawToggle = type === 'mermaid'` (document-panel.tsx:283); showRaw defaults false.
  - Content enters panel via `markdown-renderer.tsx:97-124` pre-override (≥15-line fences → DocumentCard).
  - CSP generated by `scripts/generate-headers.ts` → `_headers`; no frame-src set (default-src 'self' governs iframes); srcdoc-sandbox precedent at dev.emails.tsx:76-82; has 'unsafe-eval'/'wasm-unsafe-eval'.
  - System prompt: single builder `packages/shared/src/prompt/system-prompt.ts` (buildTurnSystemPrompt) shared client+server — the ONLY place to add artifact instructions (price-parity guarantee).
  - Streamdown lazy-import inline transform (vite.config.ts:82-92) — new lazy plugin chunks need same treatment.
  - Capacitor WebView CSP enforcement unverified (gap). New dep needs founder approval (AGENT-RULES).
- Unknowns log (open):
  - U2 → resolved (see above). U3 → resolved (system-prompt.ts). U4 partially resolved (Capacitor gap remains).
- 2026-07-23: package-landscape research DONE (research/package-landscape.md). Recommendation: Sandpack classic templates, self-hosted bundler; WebContainers/react-runner/Renderify rejected; Pyodide additive; Nodebox templates = commercial EULA, must allowlist-guard.
- 2026-07-23: FOUNDER RULINGS (one round + follow-up):
  - R1: Engine = Sandpack, self-hosted bundler from day one (fork-and-pin contingency accepted).
  - R2: Scope = web stack (HTML/CSS/JS + React + Vue) AND full Pyodide Python in THIS run, behind a type→renderer seam.
  - R3: npm deps = arbitrary (CDN-resolved by bundler).
  - R4: Mobile = MUST WORK on Capacitor iOS/Android — hard v1 acceptance criterion.
  - R5: A Maestro test must prove mobile rendering (new native-e2e infra — Playwright can't drive WebViews).
- 2026-07-23: Phase-1 second research round dispatched:
  - package-researcher → research/sandpack-selfhost.md (bundler build/pin path, arbitrary-deps config, React 19)
  - web-researcher → research/maestro-capacitor.md (Maestro + Capacitor + CI feasibility)
  - analyst → research/pyodide-renderer-design.md (worker, output rendering, micropip, loading UX)
  - Open unknowns: U6 bundler build path · U7 maestro infra shape · U8 pyodide renderer design · U4 capacitor verification (spike task in plan).
- 2026-07-23: FOUNDER DIRECTIVE: definitively establish Sandpack + Pyodide work on Capacitor BEFORE the run starts. Dispatched two web-researchers (research/sandpack-capacitor-compat.md — SW-in-WKWebView/App-Bound-Domains/capacitor:// iframe postMessage; research/pyodide-capacitor-compat.md — WASM/memory/workers in WKWebView + Android WebView). Plan will make an on-device spike Task 0, gating all feature work; research alone is not definitive.
- 2026-07-23: sandpack-selfhost research DONE (research/sandpack-selfhost.md). Key: use LibreChat fork's prebuilt bundler.zip (bundler-v12, 29.6MB) or their Docker image — upstream build is stale Node-16; bundlerURL must be a DIFFERENT origin; classic templates = react/react-ts/vanilla/vue/angular/solid/svelte (allowlist the template prop; node/nextjs/vite-* = Nodebox EULA); React 19 confirmed in react template; CSP needs frame-src <bundler-origin>. ⚠ LOAD-BEARING CAVEAT: even self-hosted, runtime npm dep fetches hit hard-coded prod-packager-packages.codesandbox.io (+jsDelivr/unpkg fallbacks) — full independence needs patch+rebuild; self-hosting alone does NOT fully kill the CodeSandbox-CDN dependency for arbitrary deps. Surface to founder in plan digest.
- 2026-07-23: maestro-capacitor research DONE (research/maestro-capacitor.md). Maestro healthy (cli-2.7.0, Apache-2.0, active). ⚠ iframe-inside-WebView assertion on iOS is weakly evidenced (open bug #2293: WebView id-selectors fail on iOS while Android works; blogger gave up on iframe traversal); Android WebView path is proven — our own suite already uses `androidWebViewHierarchy: devtools` on docker-android with known quirks (inputText #2718 workaround, assert-on-button-not-deep-text convention). CI: Android on Ubuntu+KVM works; iOS needs macOS runners (10× minutes). Implication for plan: mobile proof = Android Maestro flow (deterministic, extend existing suite); iOS assertion strategy = screenshot/visual or manual device check — founder decision in digest.
- 2026-07-23: existing-Maestro-suite map DONE (appended to research/maestro-capacitor.md). Suite = mobile-tests/ (13 Android-only flows, budtmo/docker-android, `pnpm mobile:test` → scripts/mobile-test.ts, CI job mobile-test on blacksmith runner, persona `tmu`). Conventions: launchApp clearState, extendedWaitUntil, visible-text or literal HTML `id:` selectors, `androidWebViewHierarchy: devtools` everywhere; no evalScript; inputText ~10s/char workaround; assert on buttons not deep text; NO flow touches iframe content yet. No iOS wiring exists despite apps/web/ios/. → Mobile proof task extends this suite (Android); iOS strategy = digest decision.
- 2026-07-23: pyodide-renderer-design DONE (research/pyodide-renderer-design.md). Recommended design: dedicated sandbox origin (assets-only Worker, e.g. pysandbox.hushbox.ai) hosting a cross-origin iframe with the Pyodide worker inside — plain same-origin worker REJECTED for security (Pyodide `js` bridge would give LLM code credentialed fetch to our API + device-key IndexedDB access); opaque-sandbox iframe REJECTED (Safari blocks blob workers from null origin → no iOS). Self-hosted pinned Pyodide 314.x (largest asset 9.6MB < 25MiB Pages limit), loadPackagesFromImports + micropip (PyPI hosts allowlisted only in sandbox-origin CSP), explicit Run button, stdout→aria-live console, matplotlib agg→PNG→<Img>, warm worker + fresh globals, worker.terminate() = stop/reset/memory mechanism (no SAB interrupt — COOP/COEP rejected), input() fails fast. RAISED for digest: new sandbox origin = new infra (founder approval), shared type→renderer seam with Sandpack workstream, `pnpm add pyodide` approval, iOS jetsam ~2GB kills whole app → terminate aggressively.
- 2026-07-23: pyodide-capacitor-compat DONE (research/pyodide-capacitor-compat.md). VERDICT: iOS must-spike (not blocked), Android must-spike (lower risk). Pyodide latest = 314.0.2. WASM works in WKWebView (iOS 16+, wasm-unsafe-eval). Two historical iOS wasm-loading bugs fixed before our Capacitor ^8.4.0. Live risk class: iOS-only WebKit wasm engine bugs (wasm-gc broke Pyodide on iOS 18.3.1+, Pyodide routed around). OPEN: iOS per-page memory ceiling sources conflict 5–10× (300–450MB vs 1.5–3GB); no runtime-RSS benchmark for numpy/pandas; plain Worker under capacitor:// unverified; prior art thin (no shipped Capacitor+Pyodide app found). Researcher resumed to read pyodide discussion #4764 (Ionic/Capacitor iOS local-load failure) before verdict finalizes.
- 2026-07-23: sandpack-capacitor-compat DONE (research/sandpack-capacitor-compat.md). VERDICT: **iOS WKWebView = BLOCKED** — bundler's getServiceWorker() hard-throws when navigator.serviceWorker absent, no fallback path (verified in bundler source); WKWebView has no SW support (caniwebview 07/2026, Apple DTS 02/2025), App-Bound Domains doesn't cover iframes, Capacitor #7069 closed not-planned. Android = must-spike, no blocker found (SW since API 24; unknown = storage-partitioning of cross-origin iframe SW). postMessage across capacitor://↔https not origin-validated in traced code (one gap: bundler iframe-side listener uninspected). No prior art of Sandpack in any mobile WebView. **CONFLICT with ruling R4 (must work on mobile) — escalated to founder with option set before plan is written.**
- 2026-07-23: pyodide discussion #4764 read (appended to research/pyodide-capacitor-compat.md). Was the known wasm MIME bug; resolved same-day via Info.plist workaround, superseded by Capacitor 5.1.0 core fix — mapping directly verified in this repo's @capacitor/ios@8.4.0 (node_modules, WebViewAssetHandler line 477). Verdict unchanged: must-spike, not blocked. New spike checklist item: fix lives in native Swift reaching apps/web/ios/ only via cap sync/pod install — verify materialized project at spike time (repo's ios/ has no Pods installed).
- 2026-07-23: claude-artifacts-mechanism research DONE (research/claude-artifacts-mechanism.md): sandboxed iframe on claudeusercontent.com, postMessage injection, react-runner in-iframe, cdnjs-only scripts, 4-library allowlist, NO service worker; works in stock WKWebView; mobile artifacts live since 2025-07.
- 2026-07-24: FOUNDER RULING R6: **NO dual implementation** — hybrid (Sandpack web/Android + separate iOS renderer) REJECTED for testing/maintainability; do not re-propose. Direction: research how ChatGPT, Gemini, Grok, Claude (done) and others render artifacts, then pick ONE codepath that works on web + iOS + Android. Sandpack (SW-dependent) is effectively dead unless evidence overturns. Dispatched competitor/OSS mechanism research round.
- 2026-07-24: chatgpt-canvas research DONE (research/chatgpt-canvas-mechanism.md). Canvas: sandboxed env on *.oaiusercontent.com (double-iframe + postMessage JSON-RPC verified for Apps SDK; Canvas reuse Inferred); Python = client-side Pyodide (Verified at launch); no SW on main app (sandbox-origin SW unknown); exact React transpile pipeline = public gap. ⚠ Canvas is NOT on iOS/Android at all ("coming soon", OpenAI docs mid-07/2026) and was retired from GPT-5.5 (05/2026, legacy models only). Net: OpenAI never solved mobile artifacts — weak prior art; Claude remains the only proven mobile artifact renderer.
- 2026-07-24: gemini-grok research DONE (research/gemini-grok-mechanisms.md). Gemini Canvas: full iOS/Android support (Google help pages, Verified); sandbox rides Google's *.usercontent.goog isolation domain family (Verified as family; exact Canvas subdomain = gap); sibling AI Studio "Build" resolves npm via import maps → aistudiocdn.com (Google's esm.sh mirror, Verified — Canvas by analogy Inferred); SW inferred non-functional in-preview. Grok: mechanism undocumented, mobile status unknown (last datum 03/2025 web-only). Perplexity Labs: mobile "less reliable" (weak). DeepSeek: no artifact feature. Net: SECOND proven mobile artifact renderer (Gemini) = same pattern — isolation-domain iframe + import-maps/CDN modules, no SW; import-map+esm.sh-mirror approach now has Google precedent.
- 2026-07-24: oss-artifact-renderers DONE (research/oss-artifact-renderers.md). All arbitrary-npm React artifact OSS (LibreChat, lobe-chat) = Sandpack = SW-dependent. SW-free shipping mechanisms: srcdoc iframe for HTML (open-webui/big-AGI/lobe HTML path) + Pyodide worker for Python. NO production SW-free arbitrary-npm React renderer package exists; closest = webllm/renderify (Babel+esm.sh, Preact-compat, 23 stars, 02/2026 — immature). Mechanism research COMPLETE. Synthesis presented to founder: recommend in-house single renderer (sandbox origin + sandboxed iframe + import maps → esm.sh + in-browser JSX transpile) = the Claude/Gemini pattern, one codepath all platforms, shares sandbox-origin infra with Pyodide.
- 2026-07-24: FOUNDER RULINGS R7–R9: R7 = single in-house renderer (sandbox origin + sandboxed iframe + import maps → esm.sh + in-browser JSX transpile) APPROVED as direction. R8 = artifact network access BLOCKED in v1 (Claude-style CSP: module loading only, no fetch/connect) — anti-exfiltration posture. R9 = security-risk tests required (sandbox containment: no app-origin reach, no fetch, no popups/top-nav, worker teardown). Conceptual design + remaining open questions presented for confirmation before plan writing.
- 2026-07-23: CORRECTION (founder): Maestro + Capacitor already set up with an existing test suite — my "no Maestro in repo" was an unverified Assumed claim; R5 is extend-the-suite, not new infra. Web research retained only for the nested-sandboxed-iframe assertion question; dispatched codebase-explorer to map the existing suite (appends to research/maestro-capacitor.md).
  - U1: Which package? (analyst in flight)
  - U2: Exact integration point mirroring mermaid rendered/raw toggle (explorer in flight)
  - U3: How/where system prompt is assembled + how to advertise capability to the model (explorer in flight)
  - U4: CSP/COOP-COEP constraints, Capacitor WebView implications (both in flight)
  - U5: Human decisions to collect in ONE round after research: scope of languages/runtimes, npm-dep support, offline/self-host bundler stance, mobile behavior, security posture.
