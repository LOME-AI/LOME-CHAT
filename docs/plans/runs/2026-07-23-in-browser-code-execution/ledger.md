# Ledger — in-browser code execution / rendering (document panel)

- 2026-07-23: Run opened. Phase 1 research dispatched:
  - analyst → research/package-landscape.md (package choice: Sandpack vs WebContainers vs alternatives; TECH-STACK lists Sandpack as *planned* — a prior to verify)
  - codebase-explorer → research/codebase-integration.md (document panel, mermaid rendered/raw toggle, Streamdown plugin wiring, CSP/Capacitor, system-prompt assembly, dep conventions)
- 2026-07-23: codebase-integration research DONE (research/codebase-integration.md). Key facts (Verified by explorer):
  - Document panel exists: `apps/web/src/components/document-panel/document-panel.tsx` + `useDocumentStore`; `Document` type already has `'html'|'react'` in its union — unrendered seam.
  - Rendered/raw toggle is one line to generalize: `supportsRawToggle = type === 'mermaid'` (document-panel.tsx:283); showRaw defaults false.
  - Content enters panel via `markdown-renderer.tsx:97-124` pre-override (≥15-line fences → DocumentCard).
  - CSP generated by `scripts/generate-headers.ts` → `_headers`; no frame-src set (default-src 'self' governs iframes); srcdoc-sandbox precedent at dev.emails.tsx:76-82; has 'unsafe-eval'/'wasm-unsafe-eval'.
  - System prompt: single builder `packages/shared/src/prompt/system-prompt.ts` (buildTurnSystemPrompt) shared client+server — the ONLY place to add artifact instructions (price-parity guarantee).
  - Streamdown lazy-import inline transform (vite.config.ts:82-92) — new lazy plugin chunks need same treatment.
  - Capacitor WebView CSP enforcement unverified (gap). New dep needs founder approval (AGENT-RULES).
- Unknowns log (open):
  - U2 → resolved (see above). U3 → resolved (system-prompt.ts). U4 partially resolved (Capacitor gap remains).
- 2026-07-23: package-landscape research DONE (research/package-landscape.md). Recommendation: Sandpack classic templates, self-hosted bundler; WebContainers/react-runner/Renderify rejected; Pyodide additive; Nodebox templates = commercial EULA, must allowlist-guard.
- 2026-07-23: FOUNDER RULINGS (one round + follow-up):
  - R1: Engine = Sandpack, self-hosted bundler from day one (fork-and-pin contingency accepted).
  - R2: Scope = web stack (HTML/CSS/JS + React + Vue) AND full Pyodide Python in THIS run, behind a type→renderer seam.
  - R3: npm deps = arbitrary (CDN-resolved by bundler).
  - R4: Mobile = MUST WORK on Capacitor iOS/Android — hard v1 acceptance criterion.
  - R5: A Maestro test must prove mobile rendering (new native-e2e infra — Playwright can't drive WebViews).
- 2026-07-23: Phase-1 second research round dispatched:
  - package-researcher → research/sandpack-selfhost.md (bundler build/pin path, arbitrary-deps config, React 19)
  - web-researcher → research/maestro-capacitor.md (Maestro + Capacitor + CI feasibility)
  - analyst → research/pyodide-renderer-design.md (worker, output rendering, micropip, loading UX)
  - Open unknowns: U6 bundler build path · U7 maestro infra shape · U8 pyodide renderer design · U4 capacitor verification (spike task in plan).
- 2026-07-23: FOUNDER DIRECTIVE: definitively establish Sandpack + Pyodide work on Capacitor BEFORE the run starts. Dispatched two web-researchers (research/sandpack-capacitor-compat.md — SW-in-WKWebView/App-Bound-Domains/capacitor:// iframe postMessage; research/pyodide-capacitor-compat.md — WASM/memory/workers in WKWebView + Android WebView). Plan will make an on-device spike Task 0, gating all feature work; research alone is not definitive.
- 2026-07-23: sandpack-selfhost research DONE (research/sandpack-selfhost.md). Key: use LibreChat fork's prebuilt bundler.zip (bundler-v12, 29.6MB) or their Docker image — upstream build is stale Node-16; bundlerURL must be a DIFFERENT origin; classic templates = react/react-ts/vanilla/vue/angular/solid/svelte (allowlist the template prop; node/nextjs/vite-* = Nodebox EULA); React 19 confirmed in react template; CSP needs frame-src <bundler-origin>. ⚠ LOAD-BEARING CAVEAT: even self-hosted, runtime npm dep fetches hit hard-coded prod-packager-packages.codesandbox.io (+jsDelivr/unpkg fallbacks) — full independence needs patch+rebuild; self-hosting alone does NOT fully kill the CodeSandbox-CDN dependency for arbitrary deps. Surface to founder in plan digest.
- 2026-07-23: maestro-capacitor research DONE (research/maestro-capacitor.md). Maestro healthy (cli-2.7.0, Apache-2.0, active). ⚠ iframe-inside-WebView assertion on iOS is weakly evidenced (open bug #2293: WebView id-selectors fail on iOS while Android works; blogger gave up on iframe traversal); Android WebView path is proven — our own suite already uses `androidWebViewHierarchy: devtools` on docker-android with known quirks (inputText #2718 workaround, assert-on-button-not-deep-text convention). CI: Android on Ubuntu+KVM works; iOS needs macOS runners (10× minutes). Implication for plan: mobile proof = Android Maestro flow (deterministic, extend existing suite); iOS assertion strategy = screenshot/visual or manual device check — founder decision in digest.
- 2026-07-23: existing-Maestro-suite map DONE (appended to research/maestro-capacitor.md). Suite = mobile-tests/ (13 Android-only flows, budtmo/docker-android, `pnpm mobile:test` → scripts/mobile-test.ts, CI job mobile-test on blacksmith runner, persona `tmu`). Conventions: launchApp clearState, extendedWaitUntil, visible-text or literal HTML `id:` selectors, `androidWebViewHierarchy: devtools` everywhere; no evalScript; inputText ~10s/char workaround; assert on buttons not deep text; NO flow touches iframe content yet. No iOS wiring exists despite apps/web/ios/. → Mobile proof task extends this suite (Android); iOS strategy = digest decision.
- 2026-07-23: pyodide-renderer-design DONE (research/pyodide-renderer-design.md). Recommended design: dedicated sandbox origin (assets-only Worker, e.g. pysandbox.hushbox.ai) hosting a cross-origin iframe with the Pyodide worker inside — plain same-origin worker REJECTED for security (Pyodide `js` bridge would give LLM code credentialed fetch to our API + device-key IndexedDB access); opaque-sandbox iframe REJECTED (Safari blocks blob workers from null origin → no iOS). Self-hosted pinned Pyodide 314.x (largest asset 9.6MB < 25MiB Pages limit), loadPackagesFromImports + micropip (PyPI hosts allowlisted only in sandbox-origin CSP), explicit Run button, stdout→aria-live console, matplotlib agg→PNG→<Img>, warm worker + fresh globals, worker.terminate() = stop/reset/memory mechanism (no SAB interrupt — COOP/COEP rejected), input() fails fast. RAISED for digest: new sandbox origin = new infra (founder approval), shared type→renderer seam with Sandpack workstream, `pnpm add pyodide` approval, iOS jetsam ~2GB kills whole app → terminate aggressively.
- 2026-07-23: pyodide-capacitor-compat DONE (research/pyodide-capacitor-compat.md). VERDICT: iOS must-spike (not blocked), Android must-spike (lower risk). Pyodide latest = 314.0.2. WASM works in WKWebView (iOS 16+, wasm-unsafe-eval). Two historical iOS wasm-loading bugs fixed before our Capacitor ^8.4.0. Live risk class: iOS-only WebKit wasm engine bugs (wasm-gc broke Pyodide on iOS 18.3.1+, Pyodide routed around). OPEN: iOS per-page memory ceiling sources conflict 5–10× (300–450MB vs 1.5–3GB); no runtime-RSS benchmark for numpy/pandas; plain Worker under capacitor:// unverified; prior art thin (no shipped Capacitor+Pyodide app found). Researcher resumed to read pyodide discussion #4764 (Ionic/Capacitor iOS local-load failure) before verdict finalizes.
- 2026-07-23: sandpack-capacitor-compat DONE (research/sandpack-capacitor-compat.md). VERDICT: **iOS WKWebView = BLOCKED** — bundler's getServiceWorker() hard-throws when navigator.serviceWorker absent, no fallback path (verified in bundler source); WKWebView has no SW support (caniwebview 07/2026, Apple DTS 02/2025), App-Bound Domains doesn't cover iframes, Capacitor #7069 closed not-planned. Android = must-spike, no blocker found (SW since API 24; unknown = storage-partitioning of cross-origin iframe SW). postMessage across capacitor://↔https not origin-validated in traced code (one gap: bundler iframe-side listener uninspected). No prior art of Sandpack in any mobile WebView. **CONFLICT with ruling R4 (must work on mobile) — escalated to founder with option set before plan is written.**
- 2026-07-23: pyodide discussion #4764 read (appended to research/pyodide-capacitor-compat.md). Was the known wasm MIME bug; resolved same-day via Info.plist workaround, superseded by Capacitor 5.1.0 core fix — mapping directly verified in this repo's @capacitor/ios@8.4.0 (node_modules, WebViewAssetHandler line 477). Verdict unchanged: must-spike, not blocked. New spike checklist item: fix lives in native Swift reaching apps/web/ios/ only via cap sync/pod install — verify materialized project at spike time (repo's ios/ has no Pods installed).
- 2026-07-23: claude-artifacts-mechanism research DONE (research/claude-artifacts-mechanism.md): sandboxed iframe on claudeusercontent.com, postMessage injection, react-runner in-iframe, cdnjs-only scripts, 4-library allowlist, NO service worker; works in stock WKWebView; mobile artifacts live since 2025-07.
- 2026-07-24: FOUNDER RULING R6: **NO dual implementation** — hybrid (Sandpack web/Android + separate iOS renderer) REJECTED for testing/maintainability; do not re-propose. Direction: research how ChatGPT, Gemini, Grok, Claude (done) and others render artifacts, then pick ONE codepath that works on web + iOS + Android. Sandpack (SW-dependent) is effectively dead unless evidence overturns. Dispatched competitor/OSS mechanism research round.
- 2026-07-24: chatgpt-canvas research DONE (research/chatgpt-canvas-mechanism.md). Canvas: sandboxed env on *.oaiusercontent.com (double-iframe + postMessage JSON-RPC verified for Apps SDK; Canvas reuse Inferred); Python = client-side Pyodide (Verified at launch); no SW on main app (sandbox-origin SW unknown); exact React transpile pipeline = public gap. ⚠ Canvas is NOT on iOS/Android at all ("coming soon", OpenAI docs mid-07/2026) and was retired from GPT-5.5 (05/2026, legacy models only). Net: OpenAI never solved mobile artifacts — weak prior art; Claude remains the only proven mobile artifact renderer.
- 2026-07-24: gemini-grok research DONE (research/gemini-grok-mechanisms.md). Gemini Canvas: full iOS/Android support (Google help pages, Verified); sandbox rides Google's *.usercontent.goog isolation domain family (Verified as family; exact Canvas subdomain = gap); sibling AI Studio "Build" resolves npm via import maps → aistudiocdn.com (Google's esm.sh mirror, Verified — Canvas by analogy Inferred); SW inferred non-functional in-preview. Grok: mechanism undocumented, mobile status unknown (last datum 03/2025 web-only). Perplexity Labs: mobile "less reliable" (weak). DeepSeek: no artifact feature. Net: SECOND proven mobile artifact renderer (Gemini) = same pattern — isolation-domain iframe + import-maps/CDN modules, no SW; import-map+esm.sh-mirror approach now has Google precedent.
- 2026-07-24: oss-artifact-renderers DONE (research/oss-artifact-renderers.md). All arbitrary-npm React artifact OSS (LibreChat, lobe-chat) = Sandpack = SW-dependent. SW-free shipping mechanisms: srcdoc iframe for HTML (open-webui/big-AGI/lobe HTML path) + Pyodide worker for Python. NO production SW-free arbitrary-npm React renderer package exists; closest = webllm/renderify (Babel+esm.sh, Preact-compat, 23 stars, 02/2026 — immature). Mechanism research COMPLETE. Synthesis presented to founder: recommend in-house single renderer (sandbox origin + sandboxed iframe + import maps → esm.sh + in-browser JSX transpile) = the Claude/Gemini pattern, one codepath all platforms, shares sandbox-origin infra with Pyodide.
- 2026-07-24: FOUNDER RULINGS R7–R9: R7 = single in-house renderer (sandbox origin + sandboxed iframe + import maps → esm.sh + in-browser JSX transpile) APPROVED as direction. R8 = artifact network access BLOCKED in v1 (Claude-style CSP: module loading only, no fetch/connect) — anti-exfiltration posture. R9 = security-risk tests required (sandbox containment: no app-origin reach, no fetch, no popups/top-nav, worker teardown). Conceptual design + remaining open questions presented for confirmation before plan writing.
- 2026-07-24: FOUNDER RULINGS R10–R12: R10 = auto-render html/react, explicit Run for python (approved). R11 = NO iOS-specific testing (no iOS CI/Maestro/spike automation; Android spike + suite only; iOS risk accepted on research evidence). R12 = v1 scope confirmed: single-file artifacts; languages html/js/react-jsx/python; Vue dropped; multi-file designed-for not built. All unknowns closed → writing plan.md.
- 2026-07-24: FOUNDER RULINGS R13–R15 + doc written: R13 = Maestro proof programmatic, NO screenshots → `document-render-status` app-DOM element (T4 ships it, T9 asserts it). R14 = terminology is "documents" (docs/DOCUMENTS.md, packages/shared/src/documents/); "artifact" banned from shipped code. R15 = Phase 4 pauses before running E2E (Playwright + Maestro); all other close checks run. E2E tests + Maestro flow content APPROVED. Plan amendments A4–A6 recorded. docs/DOCUMENTS.md WRITTEN (founder-directed, spec-of-record ahead of implementation; reconcile at close) + DEVELOPMENT.md index line added (founder-authorized doc edits). PAUSED after doc changes per founder instruction — awaiting review before T0 dispatch.
- 2026-07-24: PLAN APPROVED. Phase 3 begins. System-prompt text (T5) presented + approved as part of plan. Dispatching T0 (spike, gates all).
- 2026-07-24: **T0 spike CLEAN (GO gate passed).** All 4 cells PASS (Leg A render + Leg B python × desktop Chromium 150 + Android WebView 113) with real artifacts (valid PNGs, on-device screenshot). Report task-00/impl-report-1.md. Orchestrator verified the load-bearing findings; no separate auditor spawned (throwaway spike, conclusions re-encoded test-first in T1/T2/T3). T0 → clean.
- 2026-07-24: AMENDMENT A7 (from T0, folded into plan + DOCUMENTS.md):
  - T3: Pyodide runs MAIN-THREAD in the iframe, NOT a worker (module worker unspawnable from blob:null in opaque `allow-scripts` sandbox; Pyodide 314 rejects classic workers; keeping strong sandbox/no-allow-same-origin forces main-thread). Stop = parent tears down iframe (not worker.terminate). Security unchanged (origin isolation is the wall); G1 intact. UX note: long sync runs block the frame → console can't stream live, teardown is the escape. Supersedes research/pyodide-renderer-design.md 1c.
  - T2: renderer bootstrap MUST be a classic script + import map injected before first dynamic import() (Android WebView 113 ignores late import maps; Chromium 150 masked it). Transpiler = SUCRASE (40KB/4ms vs Babel 637KB/16ms). React docs mount default export.
  - T1: prove capacitor://localhost shell embedding the https iframe (T0 couldn't build the real APK under BOUNDS — tested via Chrome 113 == WebView 113; low risk). Assets need application/wasm MIME + permissive CORS.
  - Measurements: Pyodide init ~2s (local assets), first-run-to-PNG ~10-12s emulator; Android whole-app PSS ~91MB, device stayed ~1.85GB/4GB free. Low-RAM phones = plan-accepted MEDIUM risk.
- 2026-07-24: Dispatching T1 (ready — only dep T0 now clean; no file overlap in flight).
- 2026-07-24: T1 impl DONE_WITH_CONCERNS (task-01/impl-report-1.md). apps/sandbox stood up: 40 tests / 100% cov, dev+build smoke-tested, verify:env pass for the new vars all modes. Concerns = clean carry-forwards (T2 esm-stub fixtures, T3 owns public/pyodide+src/python + completes wheel closure from pyodide-lock.json, T4 needs VITE_SANDBOX_ORIGIN_URL frontend mirror + render-status element, T6 owns all CSP incl frame-ancestors capacitor://localhost). Subdomain defaulted sandbox.hushbox.ai (matches founder "a subdomain"). Repo-wide knip/arch:check red = concurrent OTHER-agent work (webpush/notifications/TTS), NOT T1 — ignored per AGENT-RULES. Dispatched single T1 auditor (scoped, security-adjacent focus: G4 credential-free, CORS/MIME/CORP baseline no credentialed-wildcard, env no-fallbacks, pinning, durable naming).
- 2026-07-24: T1 AUDIT = FAIL on ONE valid finding (durable-naming): 15 run-internal IDs (A7/T0/A2/R8/R1/"spike"/"T2-T3 owns") in shipped comments — CODE-RULES §Durable Naming + G9. Comment-only, CONFIRMED. Everything else strong PASS (security baseline exactly right incl. no credentialed-wildcard; 100% cov; env no-fallbacks; A7 capacitor deferral confirmed legitimately bounded; pinning per G10). Process-fix applied: this is a recurring class → standing rule added to fixer + all future implementer briefs (state durable fact without the label); saved to memory [[sdd-plan-ids-leak-into-comments]]. Dispatched fixer (impl-report-2), finding verbatim, bounded to named files. Re-audit to follow.
- 2026-07-24: T1 fix DONE (impl-report-2): 15 flagged + 2 self-found same-class label occurrences stripped, durable facts kept; gates green (40 tests/100%, lint exit 0). Orchestrator VERIFIED via grep sweep (zero banned labels in apps/sandbox; env.config hit was a base64 false-positive). **T1 → CLEAN.** One residual: wrangler.toml:4 dangling "ARCHITECTURE §sandbox" pointer (section doesn't exist) → routed to T6 as a bounded micro-item (plan updated).
- 2026-07-24: Readiness recompute after T1 clean. READY (independent files, no overlap): T2 (packages/shared/src/documents + apps/sandbox/src/render) + T6 (scripts/generate-headers.ts + apps/sandbox _headers/wrangler). HELD: T3 (imports T2's bridge contract — single-writer T2), T5 (needs T2 final capability facts). Dispatching T2 + T6 parallel. Both briefs now carry the standing no-plan-IDs-in-comments rule.
- 2026-07-24: T6 impl DONE_WITH_CONCERNS (task-06/impl-report-1.md). Read full report (CSP is the security wall — decisions hinge on exact strings). CORRECTED my terse-return misread: connect-src DOES include pypi.org + files.pythonhosted.org → micropip works. Final CSP: app frame-src `'self' <sandbox-origin>` (per-mode, no-loosen pinned, 100% cov); sandbox `default-src 'self'; script-src 'self' 'wasm-unsafe-eval' blob: https://esm.sh; worker-src 'self'; connect-src 'self' https://pypi.org https://files.pythonhosted.org; img-src 'self' blob: data:; style-src 'self' 'unsafe-inline'; font-src 'self' data:; frame-ancestors https://hushbox.ai capacitor://localhost http://localhost; base-uri 'self'; form-action 'self'`.
  - SHARP CATCH (verify in audit): frame-ancestors adds http://localhost because capacitor.config.ts androidScheme:'http' → Android WebView origin is http://localhost (omitting = Android embed blocked). Legit deviation.
  - TWO OPEN (T3-determined, NOT audit-novel): (1) script-src omits 'unsafe-eval' — if Pyodide 314 needs it under strict CSP, T6 widens; (2) connect-src no CDN beyond PyPI — if micropip path hits a CDN, add it. T0 spike ran no-CSP so unverified. → T3 brief MUST run under T6's real CSP + report needed widening; T6 final-reconcile + single-audit after T3.
  - CARRY-FORWARD T7 (critical): static _headers CSP enforced only by Cloudflare/wrangler in prod; T1 Node dev-server sends no CSP → T7 must serve sandbox via wrangler (or inject CSP) or containment tests pass WITHOUT exercising the CSP.
  - Dispatching 2 auditors for T6 NOW (security boundary) scoped to correctness/minimality/no-loosen/frame-ancestors-coverage; the two tightness-questions marked known-open.
- 2026-07-24: T6 auditor 1/2 (correctness lens) = PASS, no findings. Independently verified: capacitor.config.ts:28 androidScheme:'http' (http://localhost required, not over-permissive); R8 boundary correct (esm.sh script-src-only, excluded from connect-src, exact-set pinned); no app directive loosened; frame-ancestors no wildcard covers web+iOS+Android. Noted concurrent TTS_MODEL_CONNECT_SRC in same file = other-agent Kokoro work, correctly NOT attributed to T6. Holding T6 verdict for auditor 2 (adversarial bypass lens) + T3 reconcile (unsafe-eval/CDN).
- 2026-07-24: T6 auditor 2/2 (adversarial bypass lens) = FAIL, 3 valid findings (two-lens split — this is why the security wall got 2 auditors):
  1. [Important, valid, T6-file] WebRTC egress not blocked — connect-src doesn't cover WebRTC; RTCPeerConnection→attacker STUN/TURN = unbounded exfil. Violates R8/invariant 3. Fix: `webrtc 'block'`. CONFIRMED CSP-gap, PLAUSIBLE e2e (T7 probe confirms).
  2. [Minor, valid, T6-file] dns-prefetch hostname leak — fix `X-DNS-Prefetch-Control: off`.
  3. [Important, valid, CROSS-TASK→T4] mobile app-origin ships NO CSP (bundled Capacitor assets bypass _headers) → sandboxed doc can self-navigate its frame to evil.com/?<secret>; contained on web by parent frame-src, UNCONTAINED on mobile. Needs app-origin CSP via <meta http-equiv> in apps/web/index.html. T6 did its job for web; this is a plan gap.
  Correctness auditor found none of these — adversarial lens earned its keep.
  JUDGMENT: all 3 valid. Amendment A8 written: T6 fixes 1+2; T4 gets new criterion (mobile app-origin frame-src via meta, VERIFY child self-nav blocked on WebView); T7 corpus expands (WebRTC probe, dns-prefetch, child-self-nav under MOBILE config, + serve sandbox with real CSP via wrangler). DOCUMENTS.md security table + invariant 3 updated (spec-of-record for T4). Process lesson: T7's original corpus wouldn't catch any of these.
  Dispatching T6 fix (findings 1+2). T6 still also pending T3 reconcile (unsafe-eval/CDN) — batch final re-audit after both if possible.
- 2026-07-24: T6 fix DONE (impl-report-2): webrtc 'block' + X-DNS-Prefetch-Control: off added, 88/88 tests 100% cov, RED-verified, additive-only. Impl flagged webrtc 'block' likely unimplemented on iOS WKWebView → T7 on-device probe must confirm (defense-in-depth added regardless). Orchestrator VERIFIED delta via grep (both present; connect-src + frame-ancestors byte-identical). T6 STATUS = impl-complete + audit-findings-closed; PARKED pending: (a) T3 reconcile (unsafe-eval/CDN), then (b) final adversarial re-audit of complete policy, (c) T7 runtime WebRTC confirmation. Not on critical path (only T7 consumes T6). No dispatch possible until T2 lands (T3/T5 blocked on T2; T4 needs T2+T3). Waiting on T2.
- 2026-07-24: T2 impl DONE_WITH_CONCERNS (task-02/impl-report-1.md). All green: bridge 100% cov, sandbox suite 86 pass/100% cov, render pipeline proven end-to-end in real headless Chromium (HTML/JS+DOM/React-npm-import/syntax-error→typed-error). Bridge contract PUBLISHED (@hushbox/shared): RunnableDocumentKind, DocumentErrorCode, LoadingPhase (incl. python phases), ConsoleStream, ResultOutput; Init/Run/Stop + Ready/Rendered/Console/Result/Error/Loading messages + safe parsers. React resolves react/react-dom@19.1.0 from esm.sh, automatic JSX runtime (authors don't import React) — for T5.
  - Renderer ships as committed drift-tested public/render.js (606KB, sucrase+bootstrap bundled), regen via build:render.
  - Caught MY plan bug: task bodies cite A7 but §Amendments jumped A6→A8. FIXED — A7 now added to plan §Amendments (T0-spike findings).
  - TRACKED carry-forwards: (a) esm-stub — T1 dev-server serves extensionless as octet-stream, can't do esm.sh /pkg@ver+subpath scheme; T2 self-serves in its own browser test; **T7/T8 need the real dev-server /esm-stub to serve JS-MIME modules + route versioned/subpath URLs** → assign as dev-server fix before T7/T8. (b) T2 edited T1-authored apps/sandbox/package.json (deps: sucrase 3.35.1 [approved], esbuild 0.25.12, @playwright/test 1.60.0 + build:render), vitest.config.ts, eslint.config.js (ignore generated render.js), root pnpm-lock.yaml → audit must check esbuild/@playwright/test are pre-existing-in-repo or flag for founder approval.
  - DISCIPLINE: holding T3/T4/T5 until T2 audited clean (don't build on unverified central contract; audit is fast). Dispatching 2 T2 auditors (contract-correctness + security/untrusted-execution lens).
- 2026-07-24: T2 contract auditor = FAIL, 2 findings. Everything else PASS (bridge exact/complete vs Interfaces, enums cover T3/T4, A7 ordering STRUCTURALLY enforced — verified 2 classic scripts + IIFE no-top-import + injectImportMap-before-dynamic-import, import-map all specifier forms, CDN from config, parent-origin never trusted).
  1. [Important] esbuild@0.25.12 new direct dep → auditor flagged Must-Ask. RESOLUTION: covered by standing founder dep-permission grant [[deps-update-permission]] (build-time only, already transitive via vite/tsx); surfacing to founder for transparency, NOT blocking. @playwright/test NOT new (repo E2E runner).
  2. [Minor] specifier.test.ts:65 test name "artifact" → G9 violation, sole occurrence → batch into T2 fix.
  Fixed MY doc bug: DOCUMENTS.md had 3 stale "worker" refs (mermaid L44, bridge table stop, panel-UX Stop) from incomplete A7 update → corrected to main-thread/frame-teardown.
  Batching T2 fix (artifact rename) with security auditor's findings — WAITING on 2nd T2 auditor (a06b4d1ebf9c948bd) before dispatching fixer. T3/T4/T5 still held for T2 clean.
- 2026-07-24: T2 security auditor = FAIL, 1 Important finding (contract+impl both MISSED it — 2nd lens earned keep again):
  - [Important, CONFIRMED grep] render.js imports @hushbox/shared BARREL → env-config registry inlined into the PUBLIC sandbox-origin bundle (prod var NAMES: DATABASE_URL/OPAQUE_MASTER_SECRET/CF_ACCESS_*/ADMIN_SQL_PANEL_DATABASE_URL; dev secret-shaped VALUES: VAPID priv key, FCM service-account JSON w/ Google API key). Violates credential-free-origin invariant + CODE-RULES no-hardcoded-secrets.
  - Severity = Important not Critical: no PRODUCTION secret VALUES (runtime markers); dev values already committed in .env.development ("no secrets"); bundle not deployed (no commit this run) → no live exposure. But structural landmine → MUST FIX before any deploy.
  - Affirmations solid: parent-origin never trusted, no untrusted exec in trusted realm, bundle reproducible from source, requestId no injection sink, isBareSpecifier rejects data:/blob: schemes.
  - Fix direction: narrow `@hushbox/shared/documents` subpath import (env registry never enters graph) + bundle-content assertion test. Root cause GENERALIZES to T3 (also bundles to sandbox origin) → Amendment A9 written + memory [[sandbox-origin-no-shared-barrel]].
  - JUDGMENT: valid, grep-confirmed (no separate validator needed). Batching T2 fix: (1) narrow-import + bundle-content assertion [Important], (2) specifier.test.ts "artifact"→"documents" rename [Minor]. esbuild = standing-grant-covered, no action. Dispatching T2 fixer. T3/T4/T5 STILL held for T2 clean.
- 2026-07-24: T2 fix DONE (impl-report-2): env registry GONE from render.js (606KB→539KB, all 7 markers grep-0), narrow `@hushbox/shared/documents` subpath export added, bundle-content assertion RED-verified + green, artifact→specifier rename. Orchestrator VERIFIED via grep (render.js markers = 0; subpath export present; no artifact in render/documents). **T2 → CLEAN.** (Env note: fixer saw repo mid-git-rebase = external/pre-existing; ran no git writes; not my concern.)
- 2026-07-24: T2 clean → widest fan-out. READY (no file overlap): T3 (apps/sandbox/src/python + public/pyodide) + T5 (packages/shared/src/prompt). Dispatching both parallel. T3 also produces T6's reconcile input (unsafe-eval/CDN under real CSP). T4 still held (needs T2+T3). Briefs carry A7 (main-thread), A9 (narrow-import+bundle-assertion), no-plan-IDs-in-comments rule.
- 2026-07-24: T5 impl DONE_WITH_CONCERNS (task-05/impl-report-1.md). Runnable-documents prompt section added to buildTurnSystemPrompt (+1130 chars ≈283 tokens, matches estimate), TDD'd, owned-file gates green (prompt tests 24/24). Inverted 2 pre-existing system-prompt.test.ts assertions that pinned the OLD absent-capability state (legit — capability now exists).
  - EXPECTED cross-task consequence (tracked, NOT a defect): prompt change moves canonical request hash 38c94f4a→db959d83 → breaks pinned baseline in apps/api language-adapter.test.ts (~L1188) + invalidates AI-call cassettes embedding the system prompt. Test's own comment anticipates it.
  - CLOSE-PHASE ITEMS (new): (1) update the hash pin in language-adapter.test.ts (mechanical); (2) re-record affected cassettes (out-of-band, restricted OpenRouter creds — operational, likely founder/CI). Both surface at Phase-4 test:api. Flag to founder at close.
  - Dispatching single T5 auditor (prompt text, not sensitive): section covers all required facts (4 kinds+tags, default-export/no-import/bare-specifier, python Run+auto-install+no-input, single-file, no-network, visual-ask bias), tone consistent, phrases pinned, inverted assertions legit, hash/cassette consequence expected. T4 still held (T3 running).
- 2026-07-24: T5 audit = PASS, no findings. Content complete (all required facts, no over-promise vs v1), single-source G6 confirmed (RUNNABLE_DOCUMENTS_GUIDANCE in base-preamble.ts only, appended once; client price-preview + server both consume buildTurnSystemPrompt — no 2nd copy), inverted assertions legit, hash/cassette drift = expected close-phase item. **T5 → CLEAN.**
- 2026-07-24: Fixed stale plan T4 objective ("artifact documents"/"ArtifactKind" → "runnable documents"/"RunnableDocumentKind from @hushbox/shared/documents") before T4 dispatch. WAITING on T3 (only in-flight; T4 needs T2✓+T3; T6 parked for T3 CSP reconcile). Nothing else dispatchable.
- 2026-07-24: T3 impl DONE_WITH_CONCERNS (task-03/impl-report-1.md). Pyodide 314 main-thread (A7) /python.html: numpy/matplotlib-PNG/micropip/traceback/fresh-globals/loading-phases/input()-fail-fast, 106/106 tests 100% cov no-pole.
  - ★ T6 RECONCILE CLOSED, NO CHANGE: Pyodide works under strict CSP as-is (no unsafe-eval; connect-src = exactly self+pypi+pythonhosted; verified under default-src 'none' with ZERO CSP violations; micropip hits only those 2 hosts). The last empirical security-model unknown resolved clean. → T6 policy final, no widening. T6 = config-clean; WebRTC/DNS runtime-closure confirmation owned by T7 (in-browser probe).
  - No bridge-enum gap.
  - CI CARRY-FORWARDS (cluster with T2 esm-stub): (a) micropip test hits LIVE PyPI → needs local wheel stub for zero-live-network CI doctrine; (b) sandbox suite requires gitignored public/pyodide/** → CI must run fetch-pyodide before tests. Both = test-infra, pre-T7/T8. Track for close/infra.
  - T4 carry-forward: python drive = init(stash)+run(execute); terminal success = result{outputs} NOT rendered; stop = parent teardown; map input_unsupported code in panel.
  - Dispatching 2 T3 auditors (untrusted-exec surface, like T2): correctness lens + security lens. SECURITY LENS MUST CHECK A9 — did python bundle inline the @hushbox/shared barrel/env registry (the exact T2 bug)? + bundle-content assertion present. T4 held until T3 clean.
- 2026-07-24: T3 correctness auditor = PASS. Re-verified: main-thread (grep 0 workers in src+331KB bundle), narrow import + A9 bundle assertion green (env leak did NOT recur), T6-reconcile REAL (harness serves exact strict CSP as HTTP header, Python executes under it → "no T6 change" genuinely proven). 106/106 100% no-pole confirmed.
  - [Minor, cross-task not T3-defect] browser-harness.ts:27-29 hardcodes sandbox CSP copy mirroring T6's policy → drift risk (CODE-RULES One-Implementation-Shared). Fix direction: single shared CSP constant consumed by _headers + harness + T7. ACTION: fold into T7 prep (T7 serves real CSP, natural single-source home). Track.
  - Caveat: the "default-src 'none' zero-violations" was an out-of-band manual probe, not a committed test; committed harness restricts script-src+connect-src (the load-bearing directives) — verdict still supported.
  - Holding T3 verdict for security auditor (env-leak lens) before unlocking T4.
  - NOTE: git snapshot shows heavy apps/api billing/chat/settlement churn = concurrent OTHER-agent work, NOT this run → attribute carefully at Phase-4 unscoped pass.
- 2026-07-24: T3 security auditor = PASS. T2-class env-leak DECISIVELY prevented (bundle grep-clean of 7 markers AND all 56 env-var names; A9 assertion guards regression). Containment strong: no JS capability to Python, code only→runPythonAsync, no injection sinks, no secret logging. 2 Minors both cross-task/optional: (1) fetch-pyodide wheels no SRI (exact-pin+immutable jsdelivr paths → met; hardening only → A10 accepted-residual, founder-call); (2) teardown-kills-spinning-run test can't run from top-level harness → T4 owns (added to T4 criteria).
  - **T3 → CLEAN (both lenses PASS).** Both untrusted-exec surfaces (T2 renderer, T3 python) now security-audited; env-leak caught once (T2), prevented from recurring (T3).
  - **T6 → CLEAN (config)**: reconcile closed no-change (T3 proved Pyodide runs under strict CSP), correctness-PASS + bypass-findings-fixed-verified; WebRTC/DNS runtime-closure confirmation owned by T7.
  - Folded into plan: T4 criteria (teardown test + explicit G3 attr-pin + mobile CSP verify), T7 (single-source CSP constant, repoint T3 harness), A10 (SRI residual).
  - Dispatching T4 (app integration — largest task; unblocked T2✓+T3✓). Will get 2 auditors (UX/correctness + security: iframe sandbox attrs + mobile app-origin CSP are containment-critical).
- 2026-07-24: T4 impl DONE_WITH_CONCERNS (task-04/impl-report-1.md). Runnable documents live in web app: dispatch per kind, generalized Rendered/Raw toggle, python Run/Stop/console(aria-live)/PNG-via-Img, document-render-status element, mobile app-origin CSP meta (A8), VITE_SANDBOX_ORIGIN_URL frontend mirror. Gates green (test:web, typecheck, eslint 11 files, jscpd 0). Real-iframe lifecycle exercised.
  - FOUNDER-AWARENESS (not a fix): generate:env (needed for the new frontend var) also materialized PRE-EXISTING VAPID/webpush registry drift into TRACKED .github/workflows/*.yml + apps/api/wrangler.toml — deterministic/correct output but from the concurrent webpush workstream, not documents. Leave as-is (reverting = half-generated registry); founder should separate VAPID changes into webpush commit when staging. Surfaced to founder.
  - Mobile CSP verified SHAPE-only in jsdom (can't enforce CSP) → runtime blocking proof = T7 (web browser) + T9 (WebView), per A8. Expected.
  - Minor deviation: js/python cards reuse codeIcon test-id (new icon-registry entry out of apps/web bounds) — acceptable.
  - Carry-forward T7/T8/T9 selectors: iframe queryable by document title; #document-render-status data-status='rendered' on bridge rendered (python terminal='complete'); toggle "Show raw"/"Show rendered"; python "Run" button; VITE_SANDBOX_ORIGIN_URL in registry.
  - Dispatching 2 T4 auditors: UX/correctness lens + SECURITY lens (iframe sandbox attrs exact G3, mobile CSP meta correctness + no web-_headers-weakening, console/error text rendered as TEXT not HTML = no app-origin XSS from untrusted output, teardown kills iframe). T7/T8/T9 held for T4 clean.
- 2026-07-24: T4 security auditor = PASS, no findings. All 5 checks CONFIRMED: sandbox="allow-scripts" exact (negative-asserted), untrusted output = text nodes + MIME-pinned PNG only (error.message NEVER rendered, only code→fixed copy = no app-origin XSS), mobile CSP meta well-formed/per-mode/can't-fail-open, postMessage scoped targetOrigin (never *) + event.source-identity binding (correct for opaque "null" origin) + Zod + stale-drop, only {kind,code,requestId} cross boundary. Praised source-identity-over-origin choice as subtle-correct. Holding T4 for correctness auditor.
- 2026-07-24: Added T10 (E2E/CI test-infra: real-CSP-served sandbox + single-source CSP constant [T3-audit Minor] + esm-stub fixtures [T2] + micropip determinism [T3] + pyodide CI fetch wiring). Consolidates T2/T3 carry-forwards; prereq blocking T7+T8; independent of T4. Dispatching T10 now (apps/sandbox+CI, no conflict with T4 audit). T7/T8 held for T10+T4; T9 held for T4.
- 2026-07-24: T4 correctness/UX auditor = PASS, no findings. render-status contract exactly right (flips to 'rendered' ONLY on bridge rendered, 'complete' only on python result; test pins that 'loading' does NOT flip it → Maestro/Playwright proof can't lie); a11y solid (role=log aria-live console, role=alert error, iframe title, sr-only mirror); teardown via key-remount double-guarded; 155/155, cov ≥95 per file. **T4 → CLEAN (both lenses PASS).**
  - EXTERNAL BLOCKER (not mine, track for Phase 4): turbo lint @hushbox/web fails on src/hooks/billing/use-prompt-budget.ts:407 (complexity 11) = concurrent BILLING workstream file (M in git, not T4-owned). Will trip my Phase-4 lint pass → ATTRIBUTE to billing workstream, do NOT fix.
  - Refined close-items (from ci.yml inspection): (1) hash-pin update language-adapter.test.ts → db959d83 = MANUAL mechanical, MUST do at close; (2) cassette re-record — CI test job has record-on-miss (OPENROUTER_API_KEY_RESTRICTED) so may self-heal in CI, but LOCAL Phase-4 test:api will miss on prompt-dependent tests → founder/CI territory (agents lack the restricted key). Flag at close.
  - ALL FEATURE CODE BUILT + CLEAN (T0-T6, T4). Remaining = test layer: T7/T8/T9, ALL gated on T10 (shared infra, running) + T4✓. T10 = current gate.
- 2026-07-24: T10 impl DONE_WITH_CONCERNS (task-10/impl-report-1.md). Parts 1-4 solid (served CSP===constant===_headers proven by test; 100% cov). Micripip: chose STRICTER than plan opt-a — Playwright route-replay from committed fixtures (apps/sandbox/test-fixtures/pypi/), NO test-mode CSP allowance, prod CSP byte-identical (drift-proven). Single-source CSP constant established. Concerns: shared-file touches (turbo.json fetch-pyodide dep, ci.yml test+e2e jobs, fetch-pyodide.sh idempotency guard, browser-harness.ts additive) all disclosed/legit; CI-yaml not locally-runnable. T7/T8 rely on: served real CSP, /esm-stub fixtures (react/react-dom/canvas-confetti), pyodide assets pre-suite, installPyPIInterception pattern (e2e/ replicates routes — no cross-pkg import). jscpd 1-clone 0.63%<2% = pre-existing T2/T3, correctly not-mine.
  - Dispatching 1 T10 auditor (security-aware infra lens): PROVE prod CSP byte-identical after single-source refactor (top concern) + served===constant test real + micropip determinism test-only (no prod weakening) + esm-stub correct + fetch-pyodide CI wiring sound + coverage honest.
  - Post-T10-clean sequencing: T7 (e2e security corpus + owns shared e2e sandbox/PyPI-intercept fixtures) ∥ T9 (mobile-tests/, separate dir); T8 AFTER T7 (shares e2e fixtures).
- 2026-07-24: T10 audit = PASS, no findings. Prod CSP byte-identical CONFIRMED (csp.ts SANDBOX_CSP === _headers === served, drift+served tests; build.ts dist = public/+config.js only, dev-server/esm-stub NOT in dist → zero test machinery in prod). Micropip determinism via Playwright route (prod CSP untouched, strictly better than plan opt-a). fetch-pyodide graph <NONEXISTENT> for unrelated pkgs. **T10 → CLEAN.**
  - DISCREPANCY (auditor flagged, NOT a T10 defect): my plan/brief text said sandbox `default-src 'none'`; the REAL T6-shipped value is `default-src 'self'` (T6 report-2 = additive-only changes, default-src untouched). T10 correctly preserved 'self'. 'self' is SECURE (non-enumerated fallback directives permit same-origin only on a credential-free static origin = no exfil; T6 bypass auditor reviewed it clean). FIXED plan text 'none'→'self'. Founder FYI: keep proven 'self' (recommended) or optionally tighten to 'none' (needs re-verify Pyodide/renderer, ~zero security gain).
  - **ALL BUILD + INFRA CLEAN (T0-T6, T4, T10).** Fanning out final verification: T7 (e2e security corpus, owns shared e2e sandbox/PyPI fixtures) ∥ T9 (mobile-tests Maestro, separate dir). T8 held for T7. All reference T10's real served CSP + esm-stub + fixtures.
- 2026-07-24: T9 impl DONE_WITH_CONCERNS (task-09/impl-report-1.md). Maestro Android flow proving HTML doc renders on-device via #document-render-status (programmatic, no screenshots R13). Structurally sound: 144/144 mobile-test.test.ts, cov 100/96/100/100, YAML valid + auto-discovered. Can't run emulator locally → founder mobile:test = acceptance gate (A6, expected).
  - CONCERNS: (1) bounds expansion — added harness seedDocumentConversation via dev-only /dev/conversation (Maestro can't HTTP-seed, mock echo can't emit fenced doc) → AUDIT must verify file footprint (mobile-tests/ + scripts/mobile-test.ts harness calling EXISTING dev route, NOT a new apps/api endpoint). (2) config.ts unchanged — registration auto (readdirSync+flowWeight), correct. (3) PRIMARY UNVERIFIED RISK: does Maestro devtools-hierarchy surface the sr-only render signal? Only emulator confirms; if not → small T4 affordance fix. Flag to founder.
  - Dispatching 1 T9 auditor: file footprint in-bounds + dev-seed dev-only/sound + flow follows conventions + asserts right signal + unverified items correctly founder-scoped + no run-internal labels.
  - T7 still running; T8 held for T7.
- 2026-07-24: T7 impl DONE_WITH_CONCERNS (task-07/impl-report-1.md) — runtime security corpus. Authored full corpus, held WebRTC as test.fixme, built shared e2e fixture (e2e/helpers/sandbox-harness.ts DocumentSandboxHarness) + playwright.config Sandbox webServer + HB_SANDBOX_PORT allowlist (T8 reuses). T7 itself sound. TWO CRITICAL/IMPORTANT findings in apps/sandbox (out of T7 bounds, correctly surfaced):
  1. ★ CRITICAL — `webrtc 'block'` NOT enforced in Chromium (draft directive). T7's RUNTIME probe observed a REAL srflx ICE candidate → stun.l.google.com over UDP. WebRTC exfil OPEN, not iOS-residual. My earlier "webrtc 'block' closes it" was WRONG — only runtime verification caught it. This is the run's most important find.
  2. IMPORTANT (blocks T8) — frame-ancestors `http://localhost` (portless=80 only) blocks the ported dev/e2e app origin from embedding sandbox. Fix → `http://localhost:*`.
  JUDGMENT: both valid (WebRTC = concrete runtime evidence, accepted). Amendment A11 written. Fix = 2 layers: (a) sandbox bootstrap (render+python) deletes RTCPeerConnection globals before untrusted code; (b) CSP tighten default-src→'none' + frame-src/child-src/worker-src/object-src 'none' (blocks realm-recovery of deleted globals) — RESOLVES default-src question, 'none' now REQUIRED not optional. + frame-ancestors localhost:*. DOCUMENTS.md security updated. Founder informed.
  Dispatching coordinated sandbox WebRTC/frame-ancestors fix (owns apps/sandbox: csp.ts + render/** + python/** bootstraps + tests; RE-VERIFY Pyodide+renderer under default-src 'none'). Then T7 un-fixmes WebRTC probe + confirms embedder. T8 held for T7. T9 audit running.
- 2026-07-24: T9 audit = FAIL, 1 Critical (static, not emulator-gated): flow nav id: selectors (sidebar-trigger/chat-link/document-card/document-panel) target elements with only data-testid, no literal HTML id → Maestro id: needs literal id → flow fails at first tap, never reaches render-status. (Security PASS: dev-seed via existing dev-only /dev/conversation route, 404s in prod, no apps/api edits; file footprint in-bounds; render-status element DOES have literal id, T4 correct there.) [Minor] report claimed "follows conventions" — contradicted, no code impact.
  - FIX (cross-task, single owner for consistency): singletons (sidebar-trigger, document-panel) get literal id in apps/web components; list items (conversation link) + document card tapped by stable TEXT (suite convention for lists) — mirror existing flows' nav pattern. Touches apps/web (T4 components, additive) + mobile-tests flow. Independent of WebRTC fix (diff packages) → parallel. render-status assertion structurally fine (has id); sr-only-visible-to-devtools remains emulator-gated (founder mobile:test).
  - Dispatching mobile-nav fix. STATE: WebRTC fix (ae584) running (apps/sandbox); mobile-nav fix (apps/web+mobile-tests); T7 held (un-fixme after WebRTC fix); T8 held (after T7).
- 2026-07-24: WebRTC fix DONE (task-11-webrtc-fix/impl-report-1.md). WebRTC neutralized in render+python bootstraps (globals deleted, construction-throws asserted in real frames); prod CSP now default-src 'none' + frame/child/worker/object 'none' + frame-ancestors localhost:*; webrtc 'block' kept; other directives byte-identical; drift test updated; 128 tests/100%. Pyodide RE-VERIFIED under 'none' (core/figures/micropip pass).
  - ★ NEW BLOCKER surfaced by the mandated re-verification (fixer refused to loosen, raised it): renderer does NOT run under real script-src — html inline <script> + React inline importmap blocked without 'unsafe-inline'. Identical under OLD default-src 'self' → pre-existing, orthogonal to WebRTC change. HIDDEN because T2 render browser test never served a CSP (T3 python test did — the asymmetry). This CORRECTS my earlier "validated end-to-end" overstatement re: the render path.
  - JUDGMENT: 'unsafe-inline' is FORCED (html kind IS inline scripts; static CSP can't nonce user inline scripts) + SAFE (containment = origin isolation + network lockdown, NOT script-src; = Claude/Gemini posture). Amendment A12 + DOCUMENTS.md updated. Founder informed prominently, veto offered, proceeding (forced+safe, follows R2 feature requirement).
  - Dispatching render-CSP fix (apps/sandbox, builds on WebRTC fix state): add 'unsafe-inline' to script-src + render browser test MUST serve real CSP + prove html-inline/js/react-importmap render under it (root-cause test-gap fix). Then ONE combined security audit of final apps/sandbox state (WebRTC + default-src 'none' + unsafe-inline + render-under-CSP). Then T7 un-fixme.
  - Parallel: mobile-nav fix (a40e8df) running on apps/web+mobile-tests (no apps/sandbox conflict).
- 2026-07-24: render-CSP fix DONE (task-12-render-csp/impl-report-1.md), no concerns. Final script-src = `'self' 'unsafe-inline' 'wasm-unsafe-eval' blob: https://esm.sh` (only directive changed vs WebRTC fix; default-src 'none' + frame/child/worker/object 'none' + connect-src + frame-ancestors byte-identical). RED-verified (3 render tests fail w/o unsafe-inline → GREEN; RED requires CSP-served, confirming root-cause test gap closed). html-inline/js/react-importmap render under served CSP; WebRTC still neutralized. 131 tests/100%. Drift test updated + explicit unsafe-inline pin. (Proactive-expansion showed a NO-CSP-header version of render.browser.test — STALE pre-fix snapshot; RED-verification logically requires CSP-serving, so current file serves it — audit confirms.)
  - Dispatching ONE combined apps/sandbox security audit (WebRTC fix + render-CSP fix, both unaudited): correctness/CSP-integrity lens — verify render test SERVES real CSP (resolve expansion Q), script-src exact, default-src 'none'+frame/worker/object 'none' intact, WebRTC neutralized both bootstraps, drift served===constant===_headers, python still green under CSP, no env-leak regression. T7's corpus re-run = the complementary ADVERSARIAL runtime check (un-fixme WebRTC).
  - Parallel: mobile-nav fix (a40e8df) still running.
- 2026-07-24: mobile-nav fix DONE_WITH_CONCERNS (task-09/impl-report-2.md). Fixed selectors: id="document-panel" (singleton, apps/web — the only apps/web change), sidebar → hamburger-button (existing literal id; corrected audit's root-cause: sidebar-trigger id exists but = footer account-menu inside closed drawer, NOT list opener), card→text 'HTML', chat-row→text 'Mobile render proof'. Did NOT id the DocumentCard (component-global → duplicate ids with ≥2 docs → tapped by text). apps/web tests green (typecheck, owned-lint, document-panel 46/46; mobile-test 144/144).
  - LAST BLOCKING GAP (out of that fix's bounds): seeded conversation is UNTITLED → renders "Decrypting..." → no unique chat-row text → flow fails at chat-row on emulator. Fix needs dev-route conversationBodySchema to accept optional `title` (apps/api dev route) + documentSeedPayload to pass 'Mobile render proof' (scripts). Flow already aligned to that title.
  - Authorizing scoped dev-only test-infra fix (apps/api platform/dev route optional title — dev-only/404-in-prod, minimal + scripts seed payload). Founder noted. Then T9 re-audit → flow runnable (founder mobile:test = final gate).
  - Dispatching dev-route-title fix. Parallel: sandbox-security combined audit (a61944) running.
- 2026-07-24: Combined sandbox-security audit = PASS, no findings. WebRTC constructors deleted-and-locked (configurable:false/writable:false/undefined) in shared neutralize-webrtc.ts, both bootstraps invoke before startup; default-src 'none' + frame/child/worker/object 'none' close EVERY realm-recovery vector the auditor could construct (blob import() stays same-realm; no nested context/worker reachable); 'unsafe-inline' confined to script-src, doesn't undermine neutralization; CSP byte-identity pinned (csp.ts===_headers===served); render.browser.test:82 NOW SERVES the real CSP (test gap CONFIRMED closed); no env-leak in rebuilt bundles. **WebRTC fix + render-CSP fix → CLEAN. apps/sandbox FINAL.** (CSP directive order differs from brief listing — CSP is order-independent, not a defect.)
  - Note: createDevConversation ALREADY accepts title param (domain ready) → dev-route-title fix just exposes it on the route schema (simpler).
  - Dispatching T7 un-fixme (e2e/, sandbox now final): activate WebRTC probe (should now PASS — RTCPeerConnection throws, no ICE candidate), confirm frame-ancestors localhost:* unblocks ported embedder, re-run full corpus under final default-src 'none' + 'unsafe-inline'. Parallel: dev-route-title fix (af33) running. T8 held for T7.
- 2026-07-24: dev-seed-title fix DONE (task-09/impl-report-3.md). Dev route accepts optional title → createDevConversation (dev-only, routeClass unchanged, 404 prod, back-compat: omit→untitled); seed sets 'Mobile render proof'. routes.integration.test 64/64 (RED→GREEN decrypt-based), mobile-test.test 144/144, owned eslint clean. `turbo lint @hushbox/api` RED = 19 errors ALL in concurrent-churn files (estimate-run/trial-smart-model/interpreter/smart-model tests, all M, NOT mine) → attribute at Phase 4, don't fix. (Expansion "eslint config not found" = mis-invoked lint from wrong dir, environmental artifact, ignore.) Agent brought Docker stack up for integration tests.
  - Both T9 fixes done (nav selectors + seed title). Dispatching T9 re-audit (covers impl-report -1/-2/-3): selectors resolve to real attrs/text, seed titles dev-only, id=document-panel additive/clean, no product change, render-status assertion sound; emulator run = founder gate (not a defect).
  - T7 un-fixme running. T8 held for T7.
  - PHASE-4 TRACKING: apps/api package lint currently RED from concurrent OTHER-workstream unformatted code → my full-lint close pass will fail on non-mine files → attribute to those workstreams, surface to founder, don't fix.
- 2026-07-24: T7 un-fixme DONE_WITH_CONCERNS (task-07/impl-report-2.md). ★ WebRTC now BLOCKS at runtime — executed vs real render.js: RTCPeerConnection undefined, new throws TypeError, NO ICE srflx/relay, NO STUN egress. Chromium logs `webrtc` directive UNRECOGNIZED → confirms JS-deletion is the wall (engine-agnostic → iOS rides same layer). frame-ancestors localhost:* confirmed (ported embed works, port-80 workaround removed). Full corpus GREEN under final CSP (default-src 'none' + 'unsafe-inline' + frame/child/worker/object 'none'): fetch/XHR/WS/beacon/popup/top-nav/modal/parent-probe/child-self-nav(both deliveries)/teardown all blocked; 'unsafe-inline' opened NO exfil path. Also found+fixed (in bounds) a founder-run blocker: Chromium 150 Local-Network-Access aborted the loopback iframe when parent had "public" address space → fixed by serving parent from real loopback origin (no launch flag needed; T8 inherits). Residuals = founder full-suite run + HB_PREVIEW_PORT for app-frame-src test + iOS WKWebView WebRTC manual.
  - Dispatching T7 audit (security-cert completeness lens): corpus covers all channels, tests non-tautological (already evidenced — caught 2 real bugs + WebRTC probe flipped egress→blocked), WebRTC un-fixme genuinely EXECUTES the attack, shared fixture sound for T8, LNA fix sound. Then T7 clean → T8.
  - T9 re-audit running.
- 2026-07-24: T7 audit = FAIL, 1 Important (coverage gap): the credential-free-sandbox-origin invariant (DOCUMENTS.md inv-4 + plan.md:363-365 — the test the SUBDOMAIN decision was GATED on) has NO assertion. Header-pin test GETs /render.html but never asserts no Set-Cookie/credentials; confirmed absent everywhere. impl-report-1 FALSELY claimed it was "folded into the header pin" — auditor caught it by reading code. Everything else PASS: WebRTC un-fixme = genuine execution (auditor-confirmed), LNA fix sound, CSP genuinely single-sourced, all other channels covered + non-tautological (would flip red if defense removed).
  - JUDGMENT: valid Important. Fix = trivial (assert response.headers()['set-cookie'] undefined on the GET the pin test already makes). Dispatching T7 credential-assertion fix. Then T7 clean → T8.
  - T9 re-audit still running.
- 2026-07-24: T9 re-audit = PASS, no findings. **T9 → CLEAN.** All selectors resolve to real attrs/text (verified vs components+parser): hamburger-button (real id, drawer opener), document-panel (T9-added literal id, additive), model-selector-button (real id), 'HTML' (card title via getLanguageDisplayName), 'Mobile render proof' (seed title, decrypt round-trip proven routes.integration.test:328), 'Preview rendered' (rendered-state-only, terminal for html — can't lie). Dev-route dev-only/404-prod/back-compat. mobile-test 144/144, dev-route 64/64, document-panel 46/46. Founder mobile:test = on-device gate; sr-only-devtools-visibility emulator-gated (poss. small T4 affordance fallback). turbo lint @hushbox/api RED = concurrent churn, not T9.
  - STATE: CLEAN = T0-T6, T9, T10, sandbox-security-fixes. T7 credential-fix (a8230634) running → then T7 clean → T8 (last task to start). T8 held for T7.
- 2026-07-24: T7 credential-fix DONE + orchestrator-VERIFIED (grep: line 204 `expect(response.headers()['set-cookie']).toBeUndefined()` + WHY comment). Invariant-4 gap closed. **T7 → CLEAN.** All containment channels have genuinely-executing tests incl. credential-isolation (subdomain gate).
  - Dispatching T8 (LAST task): product-flow E2E, reuses T7 fixture (served-CSP harness + esm-stub + LNA fix). Deterministic setup via dev-seed route (title+document-content messages, like T9). Then Phase 4 close (PAUSE before E2E execution per founder R15/A6).
- 2026-07-24: T8 impl DONE_WITH_CONCERNS (task-08/impl-report-1.md). New e2e/chat/runnable-documents.spec.ts + page-object ext: (1) HTML render-in-iframe + in-frame click interactivity + Raw/Rendered toggle + status→rendered; (2) React imports canvas-confetti (esm-stub) → transpile+mount; (3) Python Run→console stdout + matplotlib PNG + status→complete; (4) syntax-error→error card + status→error. All seeded via dev route, retries=0, no live network. Static gates green (tsgo, eslint); full suite founder-gated (stack down, A6).
  - Sound deviations (auditor to confirm): interactivity proven in HTML scenario not React (esm-stub React is minimal, no hooks/synthetic events offline → React covers transpile+import+mount); did NOT use DocumentSandboxHarness (synthetic-parent containment class) — drives the REAL panel (correct for product flow); reused served-CSP sandbox + esm-stub, zero duplicate sandbox-serving. Only owned files (spec + page-object).
  - Dispatching T8 audit (product-flow completeness/non-tautology lens). After T8 clean → all tasks done → Phase 4 close (PAUSE before E2E per R15/A6).
- 2026-07-24: T8 audit = PASS, no findings. Selectors verified vs real source, render-status non-tautological (keys on real bridge state), React deviation sound, determinism clean, infra reuse correct. **T8 → CLEAN.**
- 2026-07-24: ★ ALL TASKS CLEAN (T0-T10). BUILD PHASE COMPLETE. Entering Phase 4 close (E2E execution PAUSED per founder R15/A6). Dispatching: (1) close-verification (unscoped gates on this run's surface + attribute vs concurrent churn), (2) completeness critic (missing criteria/integration/doc gaps). Then present doc diffs + consolidated decision list + E2E pause to founder.
- 2026-07-24: Completeness critic DONE. Run coherent+complete; cleared worry-list (card-creation seam BUILT not assumed; G11 no new lazy chunk; G8 a11y present; DOCUMENTS.md security consistent). ONE substantive gap: #1 streaming raw→rendered NOT implemented — DOCUMENTS.md says show-raw-while-streaming/render-on-fence-close, but code (document-card.tsx:63-78 re-anchor on hash mutation) LIVE-renders while streaming → React/JS transpile-error flicker until fence closes. No task owned it, no test. → FOUNDER DECISION (implement fence-gate [rec, small follow-up, not blocking] vs soften doc). Minor: frame-src dual authority (_headers + index.html meta) textually mirrored w/ "never diverge" comment (acceptable, different delivery). Doc updates outstanding: TECH-STACK Sandpack-planned x2, ARCHITECTURE no sandbox origin, DEVELOPMENT local-stack omits HB_SANDBOX_PORT, DOCUMENTS WebRTC-list 3→4 (FIXED now, mine to maintain).
  - Waiting on gate-verification (ae040d) before final close summary. Will present: gate result, streaming decision, doc diffs (TECH-STACK/ARCHITECTURE/DEVELOPMENT), consolidated op/decision list, E2E pause.
- 2026-07-24: knip fix applied + orchestrator-VERIFIED (fixer stalled on the 34-min full run but work is correct): knip.jsonc valid JSONC, apps/sandbox block declares 6 entries (serve/build/2 build-scripts/2 bootstraps) + ignoreDependencies wrangler + global ignore docs/plans/runs/**. Resolves all our false-positive unused findings by construction. Close gate green on our surface.
- 2026-07-24: ★ RUN COMPLETE (build + verification). All tasks T0-T10 + all mid-run fixes clean; this-run surface GREEN on typecheck/lint/tests/duplication/knip; concurrent-workstream reds (api/web full-lint) attributed away. Phase-4 close presented to founder: streaming-gate DECISION, doc diffs (TECH-STACK/ARCHITECTURE/DEVELOPMENT), op list (hash-pin/cassette/VAPID-spillover/unsafe-inline/SRI/api-lint), E2E execution PAUSED per R15/A6 (founder runs mobile:test + full Playwright). No commit (tree is founder's). Awaiting founder decisions.
- 2026-07-24: CLOSE-PHASE DEFECT found + fixed (founder asked whether hand-written code respects repo checks): `.prettierignore` did not cover this run's committed generated assets → `prettier --check .` was RED on public/render.js + public/python.js, and `pnpm format` would have REWRITTEN the bundles, breaking the byte-equality drift test. Fixed: ignore the 2 bundles + public/pyodide/ + test-fixtures/ (captured registry responses replayed verbatim), each with a durable why-comment. VERIFIED: zero unformatted files across apps/sandbox; drift tests 8/8 green. Note: `wrangler.toml`/`.whl` "no parser" errors are PRE-EXISTING repo-wide (apps/api/wrangler.toml identical) — prettier-over-repo was never a clean gate; ESLint is the formatting gate (DEVELOPMENT.md) and it already ignored the bundles.
  - Remaining gate judgment-calls surfaced to founder (not fixed): (1) no root `test:sandbox` script though every other package has one + DEVELOPMENT.md lists them (root `pnpm test` = unfiltered turbo test DOES cover sandbox, so no gate is missed — convenience/doc consistency only); (2) `arch:check` SOURCE_GLOBS omits apps/sandbox (rules are api/web/packages-oriented; the sandbox's key structural invariant — never import the @hushbox/shared barrel — is guarded more strongly by the bundle-content assertion on built output).
- 2026-07-24: FOUNDER RULINGS on close package: (1) implement streaming fence-gate — "showing the user an error is not acceptable" → dispatched task-14-fence-gate; (2) doc diffs ALL APPROVED, write durable+dense → APPLIED (below); (3) committed bundles ENDORSED, no change; (4) cassettes ignore (self-healing) but hash pin explained + fixed → task-15 DONE (38c94f4a→db959d83 read from actual failure, 48/48 green, 2 hunks: literal + stale comment replaced by a durable statement of what the pin IS); (5) VAPID/generate:env spillover — ignore; (6) unsafe-inline + Pyodide-SRI posture CONFIRMED as-is; (7) E2E runs approved later by founder.
- 2026-07-24: DOCS APPLIED (founder-approved, prettier-clean): TECH-STACK — removed both Sandpack "(planned)" rows, added Code-Execution rows for Document sandbox/Sucrase/Pyodide, Hosting row now names the 2nd assets-only Worker, monorepo tree lists apps/sandbox. DEVELOPMENT — local-stack sentence now includes the sandbox origin + HB_SANDBOX_PORT. ARCHITECTURE — sandbox+module-CDN nodes and the sandboxed-iframe edge in the system map, new §Runnable documents (trust boundary is an origin; containment is the network lockdown not script-src; no-SW by construction; python main-thread), new deliberate-limits bullet excluding SW-bundlers + COOP/COEP runtimes. Sandpack now appears only as a rejected design. DOCUMENTS.md reformatted by prettier (tables), content unchanged.
- 2026-07-24: DOC RECONCILIATION applied (founder: "state what is, not what isn't"): CODE-RULES:321-324 replaced the wrong "100% hits / miss is a failure / recording out-of-band" rule with the mechanism (unchanged request replays; changed request makes one real call on the restricted key and records it for next run; steady state = warm cache, zero charged calls → CI-CASSETTES.md). DEVELOPMENT:45-46 "cold-cache miss" → request-changed framing. language-adapter.test.ts pin comment restated positively. CI-CASSETTES.md unchanged (was already correct — the drift was a loaded-doc summary diverging from the detail doc it summarizes). Verified: prettier clean, eslint 0, 48/48.
- 2026-07-24: fence-gate impl DONE_WITH_CONCERNS (task-14-fence-gate/impl-report-1.md). Streaming signal = own CommonMark fence scan (document-parser unterminatedFenceSource) + isStreaming prop — deliberately NOT Streamdown's useIsCodeFenceIncomplete() (goes false the instant the message ends → would flip a TRUNCATED doc into render, the exact error case). Terminal mid-fence death settles to 'incomplete': raw + calm note + toggle WITHHELD (no user action can hand truncated code to a renderer); exit driven by stopStreaming (called on success AND catch). Auto-render short-circuits before sandbox/mermaid branches → no iframe per token, no error reachable from partial source. Gate applies to mermaid too (same error class). Two required extras: store splits refreshActiveDocument (streaming re-anchor) vs setActiveDocument (explicit open) with activeSelectionId — the raw-choice reset was keyed on the content-hash id and stomped the user's Raw pick every token; card re-anchors on source-state change (a closing fence adds no code text).
  - CONCERN (environmental): `pnpm test:web` could not complete — 3 attempts aborted by a shared apps/web/coverage/.tmp race with ANOTHER agent's concurrent web run. Touched+consumer files 360 tests pass, renderer+panel 87 pass, markdown-renderer cov 97/100/100/96.7 vs 95 gate, jscpd 0. Needs a quiet-repo full run → assigned to the auditor.
  - Dispatching 1 fence-gate auditor (UX/correctness lens; security posture strictly reduces — it renders less, not more).
- 2026-07-24: fence-gate audit = FAIL, 1 Important + 1 Minor. Full pnpm test:web PASSED for the auditor (373 files/6158 tests/exit 0) → implementer's coverage-dir race was environmental, CLOSED. Coverage ≥95 all changed files; jscpd 0; render-status contract, teardown, raw-choice survival, a11y, mermaid-inclusion, share-route gating all VERIFIED intact.
  1. [Important, CONFIRMED by probe vs remark-parse] document-parser.ts:91-101,121 `unterminatedFenceSource` diverges from CommonMark in 3 shapes and EVERY divergence FAILS OPEN (no sourceState → falls through to sandbox/mermaid with partial code → transpile_failed error card = the founder's forbidden case): (a) closing fence carrying an info string treated as a close (readFenceMarker never rejects trailing non-whitespace); (b) fence indented ≥4 spaces inside a container skipped by the `indent > 3` guard (only valid at top level) — hits the VERY common "1. Example:\n\n    ```jsx" list-nested shape; (c) fence inside a blockquote. Auditor verified the reachability chain markdown-renderer.tsx:94 → document-panel.tsx:214 gate skipped → sandbox mounts+inits partial source.
  2. [Minor, PLAUSIBLE] document-card.tsx:74-82 re-anchor effect seeds previousRef with the current doc → a card that unmounts/remounts (Virtuoso 800px overscan) never pushes state; if it scrolls out while its doc is open and the stream then ends, panel stays 'streaming' showing "Preview starts when the response finishes." after it finished. Safe direction (still gated), recoverable by re-click, mechanism predates this task — but now user-visible and wrong.
  Dispatching fix. GOVERNING INVARIANT for the fixer: the scan's failure mode must be OVER-gating, never rendering — any ambiguity shows raw.
- 2026-07-24: fence-gate fix DONE (task-14/impl-report-2). Rejected BOTH options I floated, with reasons that hold: (i) Streamdown's own useIsCodeFenceIncomplete uses `^[ \t]{0,3}(\`{3,}|~{3,})` with NO info-string rejection → carries the SAME 3 divergences (reusing it fixes nothing — my "use the renderer's parser" suggestion was wrong); (ii) remark-parse = new apps/web dep, approval-gated, no gain. NEW DESIGN: `openFenceAnchors` (does source end inside an open fence + which 2 lines bound it) with EVERY approximation resolving to "open", + `isOpenFenceBlock` (suffix match on either anchor) whose only misfire is gating an EXTRA block; safety derived from position (an unclosed fence necessarily runs to end of message). Deleted unterminatedFenceSource; an indented fence-looking run now reads as an OPENER (reverses a pass-1 rule+test — over-gates, which the invariant prefers). Found a 4th divergence the audit missed: Streamdown REPAIRS incomplete markdown and rewrites the last code line of an unclosed `~~~` fence → tail-only matching failed open → fixed by matching either anchor. Tests 381 files/6247 green (private coverage dir), per-file cov pass, jscpd 0 in owned files, eslint 0.
  - turbo typecheck lint --filter=@hushbox/web FAIL = 100% concurrent other-agent work: apps/api conversations/routes.ts missing ConversationEventNotification export (3 errs — apps/web typechecks through AppType, so a broken api export breaks web), untracked notification-channel/web-adapter.ts, untracked use-enable-prompt.test.ts, modified reasoning-effort-menu.test.tsx. NOT this task. Flag to founder: the notifications/webpush workstream currently breaks the shared type surface.
  - Dispatching re-audit (adversarial: try to find a shape that STILL fails open; and prove no over-gating regression — normal closed documents must still preview).
- 2026-07-24: fence-gate re-audit (pass 2) = FAIL, 2 Critical, both CONFIRMED with reproductions through the real pipeline. ROOT CAUSE (single, now named): the gate identifies WHICH block is open by matching TEXT, but that text has already been rewritten by Streamdown's remend() repair — each pass found a NEW way for that coupling to fail open.
  1. isOpenFenceBlock suffix-matches PARSED code vs anchors from RAW message; remend rewrites head (singleTilde/comparisonOperators — position-independent) and tail (all others — end-anchored); both guards are ```-only so ~~~ fences are never guarded. Confirmed: (a) `~~~mermaid\ngraph TD` = FIRST FRAME of every tilde mermaid doc → MermaidDiagram mounts `graph TD~~`; (b) `~~~jsx` w/ `~` in string → sandbox transpiles partial JSX; (c) PLAIN ``` fences too when the count of ``` runs before the doc is ODD (remend's naive toggle inverts parity → treats block body as prose).
  2. document-parser.ts:107 counts indent in CHARACTERS; CommonMark counts tab=4 columns → `\t```` closer passes the ≤3 test and "closes" a fence remark leaves open → sandbox handed source ending in literal `\t``` `. NBSP same class.
  GOOD: no over-gating regression (extensive probe set), the 3 pass-1 divergences genuinely fixed, Streamdown-helper claim VERIFIED (same divergences — rejecting reuse was right), render-status/teardown/raw-survival/a11y/share/mermaid all intact, remount re-publish fixed. Also: turbo typecheck lint --filter=@hushbox/web now PASSES — the concurrent notifications/webpush breakage landed its fix.
  ORCHESTRATOR DECISION (cycle 3 of 3, design change not another patch): DELETE the per-block text-matching step entirely. Parser reduced to ONE conservative boolean per message ("does this raw source end inside an open fence?", every unmodelled construct → open, reads ONLY raw source — never needs to agree with remend/splitter/parsed text). Gate at MESSAGE granularity: open-fenced message → ALL its documents gated. ACCEPTED TRADE-OFF (founder informed, veto offered): an earlier complete document in the same message is also gated until the message settles — auditor already classified sibling over-gating as the sanctioned direction. Plus: indent measured in CommonMark columns (tab→next multiple of 4), only ASCII space/tab count, anything else → fence stays OPEN.
  If this fails audit, STOP and escalate the whole problem to the founder rather than loop (3-cycle cap).
- 2026-07-24: fence-gate pass-3 audit = FAIL (3rd). 2 Critical + 1 Minor, all in the ~60-line pure parser, all with exact one-line fixes the auditor verified against mdast/micromark ground truth:
  1. [Critical] document-parser.ts:127 `hasInfoString` uses String.trim() → strips ALL Unicode whitespace; CommonMark allows only space/tab after a closing run, so ```` ```  ```` (or U+3000, FF, VT) leaves the fence OPEN while the gate calls it closed → sandbox gets truncated JSX. Fix: `/^[ \t]*$/.test(rest.slice(index + length))`.
  2. [Critical] document-parser.ts:139 `Math.abs(marker.column - open.column) <= 3` measures the closer against the OPENER's column; CommonMark measures against the CONTAINER's content column. Fail-open matrix (mdast ground truth): o=1,c=4 · o=2,c=4-5 · o=3,c=4-6 (o=0 safe). Fix: `marker.column <= open.column`.
  3. [Minor] same comparison over-gates the mirror shape (list-indented opener + unindented closer → complete doc never previews); the same one-line fix resolves it.
  CONVERGENCE TREND (why this is not blind looping): pass-1 escapes were VERY common shapes (list-indented fence, first frame of every tilde mermaid doc); pass-3 escapes need NBSP/U+3000 after a closing fence or a specific indent matrix — auditor itself notes "realism is lower". Auditor ENDORSES the design ("the design change is the right one"), Priority-3 deviation verified sound, Priority-4 all intact, no over-gating regression otherwise, 392 files/6361 tests pass with zero test failures (non-green = concurrent notifications workstream only).
  **HONORING THE 3-CYCLE CAP: STOPPED. Escalated to founder with options (fix / ship-as-is / stop) rather than dispatching a 4th pass unilaterally.** Awaiting decision.
- 2026-07-24: FOUNDER chose option 1 (one more fix pass). Dispatched targeted parser-only fix (bounds: document-parser.ts + its test ONLY — pass-3 design endorsed, gate wiring verified correct, do not redesign): FIX1 hasInfoString → `/^[ \t]*$/` (ASCII space/tab only, not String.trim's full-Unicode strip); FIX2 close test → `marker.column <= open.column` (was Math.abs(...) <= 3 vs the OPENER's column; CommonMark measures vs CONTAINER content column); FIX3 = the Minor mirror over-gate, resolved by FIX2. Flagged a trade-off for explicit pinning: `<= open.column` is stricter than CommonMark the other way (closer indented MORE than opener but within the 3-col allowance now over-gates) — sanctioned by the invariant but must be a recorded, tested choice not an accident. Tests RED-verified through the real MarkdownRenderer pipeline + full no-over-gating regression set.
- 2026-07-24: parser fix pass DONE (impl-report-4), all gates green: pnpm test:web --force 392 files/6378 tests exit 0; typecheck+lint pass; eslint 0; jscpd 0.09%; document-parser.ts 100/100/100. Both Criticals gate, Minor renders, stricter-than-CommonMark trade-off pinned by 2 tests (old three-space-closer assertion INVERTED not deleted). Two unbriefed-but-forced extras: split('\n')→split(/\r?\n/) (Fix 1 made a trailing CR read as info-string content; caught by the pre-existing CRLF regression), and tests added to markdown-renderer.test.tsx (tests only, required by the brief's pipeline RED-verification).
  - ⚠ IMPLEMENTER SELF-REPORTED a SECOND CommonMark divergence in the Fix-3 shape: micromark reads the unindented run as container-break + a NEW empty fence, so that message DOES end inside an open fence while the scan says it does not. Claimed safe because a closing candidate carries no info string → any block it really opens has no language → never extracted as a document. **That chain is the load-bearing argument and is PRIORITY 1 of the final audit — verify end-to-end for every runnable kind incl. mermaid, not on trust.**
  - Final audit dispatched (also: hunt NEW fail-opens from the two widened surfaces — `<= open.column` and the CRLF split change; full no-over-gating set; priorities 5 regression set).
- 2026-07-24: **`pnpm dev` BREAKAGE — root-caused and FIXED (this run's bug).** Founder reported @hushbox/sandbox#dev exit 1 killing the whole dev fan-out. Diagnosis: turbo's `dev` task declared `passThroughEnv: ["HB_*"]`; turbo STRIPS anything unlisted, so `HB_SANDBOX_PORT` reached the process but `ESM_CDN_URL` (registry var, doesn't match HB_*) did NOT → serve.ts:16 buildSandboxConfigScript fail-fast → turbo aborts all 7 dev tasks. (My first repro — running the package script directly, bypassing with-env — was self-inflicted and pointed at the WRONG var; corrected by testing the real with-env→turbo path.) Verified ESM_CDN_URL IS in .env.scripts and IS injected by with-env — the loss was purely turbo's env filter.
  FIX: turbo.json dev.passThroughEnv → ["HB_*", "ESM_CDN_URL"] + durable comment (turbo strips unlisted vars; a registry var a dev server reads directly must be named). VERIFIED: failure mode changed from "ESM_CDN_URL is not set" to EADDRINUSE — only reachable if the env now arrives — and the server answers HTTP 200 on /render.html and /config.js. turbo parses the comments (2.9.18 dry-run ok), prettier accepts them byte-identically, no repo script strict-JSON-parses turbo.json. EADDRINUSE was my own overlapping test runs; `dev:clean` already covers HB_SANDBOX_PORT.
  WHY IT ESCAPED: T1's acceptance said "pnpm dev serves the origin" and its audit passed package-scoped checks, but no agent ever ran the integrated `pnpm dev` fan-out — the fail-fast fired in a path no test covers. LESSON: a new dev-stack service needs its turbo passthrough exercised, not just its package scripts.
- 2026-07-24: Founder chose option 1 for the fence gate — dispatched the micromark/mdast rewrite (engine as implementation, not just test oracle; message-level design retained; entire hard-won test corpus must keep passing; perf/memoization required since it runs per-token while streaming).
- 2026-07-24: DEPLOY/CI AUDIT (founder asked: subdomain setup? env vars wired to ci.yml?). Verified, 3 real gaps — all this run's:
  1. **NO SANDBOX DEPLOY STEP EXISTS.** ci.yml deploys API + Web(Pages) + Admin only. Sandbox is never built or deployed. Needs: fetch-pyodide → `pnpm --filter @hushbox/sandbox build` (assembles dist = public/ + generated config.js) → `wrangler deploy` in apps/sandbox, with ESM_CDN_URL in that step's env (config.js is baked at build time).
  2. **DNS: yes, manual work needed as configured.** wrangler.toml uses `[[routes]] pattern="sandbox.hushbox.ai/*" zone_name="hushbox.ai"` (matches admin). Routes create the ROUTE, not DNS — Workers routes only match hostnames already resolving to Cloudflare, so a proxied DNS record for sandbox.hushbox.ai must exist (admin's was presumably created by hand). ALTERNATIVE (recommended, offered to founder): `custom_domain = true` → wrangler provisions DNS + cert on first deploy, zero manual Cloudflare work. No custom_domain anywhere in repo today. Token scope: same zone + same operation as the admin worker deploy → existing CLOUDFLARE_API_TOKEN should cover it (Inferred).
  3. **`SANDBOX_ORIGIN_URL` MISSING from ci.yml's GENERATED `headers-env` block → DEPLOY-BLOCKING.** The "Generate _headers" step runs `pnpm tsx scripts/generate-headers.ts` DIRECTLY (not via with-env → no .env.scripts fallback) with only VITE_API_URL in env; generate-headers fail-fasts on an absent SANDBOX_ORIGIN_URL by design (T6). Production deploy would fail at that step. Fix belongs in the env generator so the var lands in the generated block.
  WIRED CORRECTLY: VITE_SANDBOX_ORIGIN_URL (in GENERATED build-env block, prod value https://sandbox.hushbox.ai, lines 278+326); HB_SANDBOX_PORT (dev/test only, per-mode). ESM_CDN_URL absent from ci.yml — harmless ONLY because the sandbox is never built there; required once gap 1 is fixed. NO same-class turbo bug in test/build: sandbox tests pass ESM_CDN_URL as an explicit function argument, not from env.
- 2026-07-24: FOUNDER RULINGS on deploy: sandbox → `custom_domain = true` (wrangler provisions DNS+cert; no dashboard work); admin DNS → founder creates by hand (admin CANNOT use custom_domain — apps/api/wrangler.toml:53 claims admin.hushbox.ai/api/* so the hostname is shared between two Workers, and a custom domain binds a whole hostname to one Worker — durable fact, recorded in the sandbox wrangler comment); CI changes FOLD INTO EXISTING JOBS (a new step in the existing deploy job is fine, a new job is not); cache Pyodide.
  Dispatched task-16-deploy (4 changes): (1) wrangler.toml routes→custom_domain; (2) turbo.json build.passThroughEnv += ESM_CDN_URL (same strip-class as the dev bug — build.ts:38 buildSandboxConfigScript(process.env) would fail-fast in CI); (3) env GENERATOR teaches build-env←ESM_CDN_URL and headers-env←SANDBOX_ORIGIN_URL then regenerates ci.yml (no hand-edits inside GENERATED markers — the "Generate _headers" step runs generate-headers.ts directly, NOT via with-env, so it has no .env.scripts fallback and currently fail-fasts → deploy-blocking); (4) new "Deploy Sandbox assets Worker" STEP after the admin one in the EXISTING deploy job + Pyodide cache keyed on fetch-pyodide.sh hash (mirroring/adding to the test job's approach). turbo build already dependsOn fetch-pyodide → no new build step needed; implementer to verify that holds in the deploy job.
- 2026-07-24: fence-gate micromark rewrite DONE_WITH_CONCERNS (impl-report-5; agent was killed by a weekly usage limit mid-report and was RESUMED FROM TRANSCRIPT per the restart skill, not respawned — gates re-confirmed after restart). Engine = micromark event stream (2 codeFencedFence tokens = closed, 1 = open; mdast can't express this without re-deriving closing-fence rules). Rule: last fenced block open ⟺ <2 fence lines AND has an info string; engine throw ⇒ true (fail-closed). Perf: module cache of (last source, answer) + no-backtick-growth reuse — 3104-token replay of an 18.6KB message = 2 engine runs/16ms vs 3105 runs/1421ms. Bespoke scanner DELETED (readFenceMarker/closesFence/FenceMarker/CLOSING_RUN_TAIL/TAB_STOP/BLOCKQUOTE_PREFIX/stripBlockquotePrefix/line-splitting; grep clean). Both prior defects gate; full corpus passes. Dep: micromark ^4.0.2 declared in apps/web (was transitive-only). THREE assertions corrected (the named three-space closer, its unnamed pipeline twin, and 'fence-looking line indented 4 spaces' which the engine reads as an indented code block — no fence, no language, never a document; remend leaves that input byte-identical).
  - ⚠ SELF-REPORTED NEW fail-open OPENED by this change: the gate parses core CommonMark but Streamdown parses with remark-GFM — a fence indented under a GFM footnote definition reads closed to the gate, open to the renderer. Verified one-line fix (parse({extensions:[gfm()]})) blocked only by the brief's one-dependency cap. → RESUMED the agent to apply it (dep cap was a default, not worth preserving over a known fail-open; deps pre-approved), re-confirm corpus unchanged, add a RED-verified footnote test, and state plainly whether any OTHER remark/Streamdown dialect difference remains unmodelled. Audit deferred until that lands (don't audit a known-incomplete state).
- 2026-07-24: VERIFIED turbo.json is correct (git diff, not my earlier regex parse which conflated tasks): build gained dependsOn fetch-pyodide + passThroughEnv ESM_CDN_URL; dev gained ESM_CDN_URL; test's HB_TEST_SCOPE untouched; outputs dist/** unchanged. No regression.
- 2026-07-24: task-16-deploy DONE_WITH_CONCERNS. custom_domain=true set (bare hostname, no zone_name//*, wrangler --dry-run clean, admin untouched); both GENERATED blocks carry their var (generator-side only, no marker hand-edits) AND the fix was extended to release.yml's build-env-web-release (that step also runs whole-workspace pnpm build — I'd have missed it); deploy step inside the EXISTING job after admin, API/Web/Admin untouched; Pyodide caching already existed in test+e2e jobs (useblacksmith/cache@v5, key on fetch-pyodide.sh hash) → added the identical step to the build job, nothing to cache in deploy.
  - ★ IMPLEMENTER CAUGHT A FALSE PREMISE IN MY BRIEF: the deploy job runs NO build, so apps/sandbox/dist would not exist — the turbo build→fetch-pyodide dependency holds in the BUILD job. Fixed by adding Upload/Download sandbox build artifact steps mirroring admin. My "verify the build happens in the deploy job" instruction was wrong; unchallenged it would have shipped a broken deploy.
  - Also corrected my second false premise: `build` never declared passThroughEnv ["HB_TEST_SCOPE"] (that's `test`); build had none.
  - MY RULING on its raised concern: passThroughEnv is EXCLUDED from turbo's hash → a sandbox dist built against the local test stub could be replayed from cache for a production build (wrong artifact, correct-looking cache hit). Ruled: change build to `env: ["ESM_CDN_URL"]` (supplies AND hashes; the value is baked into /config.js so it materially determines output). "No CI job builds it under non-prod env today" is a fact about the present, not a property to depend on. `dev` stays passThroughEnv (no cached artifact). Resumed the agent to apply + verify the hash actually differs across values.
  - CROSS-WORKSTREAM (founder-visible, not ours): `pnpm generate:env` rewrites markers in ALL FOUR workflow files, so it flushed the concurrent push-notification workstream's uncommitted registry drift (VAPID_*, NOTIFICATION_TAG_SECRET, VITE_VAPID_PUBLIC_KEY) into ci.yml/release.yml/build-android.yml/run-ops-script.yml. Same class as the earlier T4 spillover. Pre-existing failures also theirs: generate-env.test.ts verify-secrets expectation stale vs +131 uncommitted env.config.ts lines; seed-run/refresh-catalog-run load failures on a stale vite dep-optimizer cache.
- 2026-07-24: task-16-deploy audit = FAIL, 1 Important. release.yml:127-134 `prepare-web` "Generate _headers" step has a HAND-WRITTEN env block (only VITE_API_URL); SANDBOX_ORIGIN_URL absent and generate-headers.ts:352-357 fail-fasts on it — CONFIRMED by reproducing the exact step env. Runs directly (not via with-env → no .env.scripts fallback). Breaks the release iOS/App-Store path (prepare-web → build-ios) deterministically. Root cause predates this task, but the task extended into release.yml for ESM_CDN_URL and left the sibling half → release.yml is half-repaired. No GENERATED markers there, so the generator can't reach it. → Resumed agent: wrap that step's env in generator-owned markers (single source of truth; a bare literal would rot at the next registry change) + mirror the Pyodide cache into prepare-web (it now transitively runs fetch-pyodide, ~26MB, uncached, in an ~8-min job) + pin with generator tests incl. the negative case.
  EVERYTHING ELSE PASS, independently reproduced by the auditor: turbo hashes differ across ESM_CDN_URL values (f9219d72 vs db9f99a6, `specified.env=["ESM_CDN_URL"]`); unset ⇒ build fail-fasts and leaves dist untouched (no silent wrong artifact); generate:env idempotent (all 9 workflow YAMLs byte-identical over 2 runs → blocks are genuinely generator output); verify:env production pass; wrangler --dry-run "Read 26 files"; ARTIFACT CHAIN traced literally end-to-end (build → upload `sandbox-dist` @ apps/sandbox/dist → deploy job needs:[build] → download same name/path → deploy ~150 lines later; wrangler reads [assets] directory "./dist"); admin wrangler.toml untouched + still [[routes]] (correct — api claims admin.hushbox.ai/api/*); API/Web/Admin deploy steps byte-unchanged; Pyodide cache identical across test/build/e2e jobs, each preceding its consumer; no other workflow step runs a workspace build so omitting ESM_CDN_URL there is correct. Known out-of-scope failures were exactly the declared set (notifications-workstream verify-secrets staleness + 2 stale vite dep-optimizer load failures).
- 2026-07-24: fence-gate GFM gap CLOSED (impl-report-5 addendum). parse({extensions:[gfm()]}); micromark-extension-gfm ^3.0.0 + micromark ^4.0.2 declared in apps/web (lockfile additive). Footnote case gates — and the RED was rigorous: the FIRST shape produced NO card (remark-rehype drops an unreferenced footnote definition), so they found the reachable shape (definition WITH reference), where RED was "expected undefined to be 'streaming'" on a REAL card = fail-open observed, not argued. Corpus re-confirmed not assumed: all 60 ground-truth inputs re-run core-vs-GFM, zero differences, no existing assertion changed, 147 passing tests. Other dialects CHECKED: math ($$) deliberately unmodeled — its only divergence is one-way over-gating (math can hide a fence from the renderer, never create one); remend leaves fences/indented code byte-identical; rehype plugins are render-time and cannot change fence state. Claim: "no remaining difference can report an open fence closed." 393 files/6410 tests green, parser 100/100/100.
  - Final audit dispatched to ATTACK that claim (GFM constructs incl. nested containers/tables/task-lists, the math one-way direction, remend re-verification, and micromark VERSION SKEW vs Streamdown's transitive parser — a version split would be a silent dialect divergence).
  - ⚠ THIRD SIGHTING, separate issue: raw HTML `<pre><code class="language-…">` reaches the card-interception path WITHOUT ever being a fence — so the fence gate cannot cover it by construction. Flagged by the pass-3 audit (Minor) and twice by the implementer. Same product bar ("never show an error") but a different code path. → SURFACE TO FOUNDER as its own decision; excluded from the current audit to avoid muddying it.
- 2026-07-24: release.yml headers fix DONE + orchestrator-VERIFIED (config with directly observable evidence → self-verified rather than another audit cycle): headers-env block now generator-owned with BOTH vars (rationale comment moved ABOVE the marker so nothing hand-written sits inside); reproduction FLIP confirmed by the implementer (old env → exit 1 fail-fast; new env → exit 0, wrote _headers, 14 pages/32 blocks); Pyodide cache at release.yml:119 precedes "Build web" at :124. NEW CROSS-WORKFLOW INVARIANT TEST (generate-env.test.ts:1484) scans real .github/workflows/*.yml, filters steps running generate-headers.ts, asserts each env sits inside a headers-env marker — with `expect(running.length).toBeGreaterThan(0)` guarding against a vacuous pass. Kills the class repo-wide, not just the one broken file. Honest reporting noted: 2 of 3 tests passed on write (generator is workflow-file-agnostic; the defect was release.yml markup, not generator code) and they said so rather than dressing it as red-first. Gates: generate:env idempotent (all workflows byte-identical over 2 runs), verify:env production pass, 9 YAMLs parse, prettier clean, eslint 0, 112 pass/1 pre-existing notifications-workstream fail. **task-16-deploy → CLEAN.**
- 2026-07-25: **STRATEGY REVERSAL (founder-driven, and correct).** Founder asked: "why predict whether an error will happen — can we just run it and definitively know?" Yes. The sandbox already reports failures over the bridge as a typed `error`, so completeness never needed inferring. New rule: run it; suppress the error while the message streams; show it once streaming ends. Needs only `isStreaming` (authoritative from chat state) — zero markdown parsing, zero dialect agreement with Streamdown. Kills the entire failure class (5 audits / 5 divergences: hand-rolled rules ×4, then GFM, then remark-math) AND the raw-HTML `<pre><code>` hole the fence gate could not reach by construction. My error: docs/DOCUMENTS.md said "preview activates when the fence closes" and I took the fence as the MECHANISM rather than a description of the desired outcome — every round then optimized fence detection instead of questioning it.
  Founder accepted all trade-offs: HTML live-builds as it streams (never errors); the "response ended before this block closed" wording is lost (a dead stream now shows the normal compile error); debounce re-init; Python unaffected (explicit Run).
  Dispatched 2 parallel tasks: task-17-run-and-observe (apps/web — delete endsInsideOpenFence + both micromark deps + SourceFenceContext + sourceState gating + the fence corpus; implement error-suppression-while-streaming, last-good-render retention, ~300ms debounce; MUST PRESERVE the store selection split, card remount re-publish, and document-render-status) ∥ task-18-seed-showcase (scripts/seed.ts — a "Document showcase" conversation for EVERY dev persona: interactive HTML, React+canvas-confetti, Python numpy+matplotlib, mermaid, compile-error React, runtime-error React, and a no-language block that must stay a plain code block; idempotent across two db:seed runs). Streaming needs no seeding — the local mock echoes at ~60ms/chunk, so pasting a document into the composer streams it back at observable speed.
- 2026-07-25: task-18-seed-showcase DONE. New scripts/lib/seed-documents.ts + colocated test; seeds all 3 DEV_PERSONAS (alice/bob/charlie; mallory deliberately excluded as the locked persona) = 3 conversations × 8 messages. Idempotency proven against the LIVE DB, not just unit tests: 4 real `pnpm db:seed` runs, showcase rows stable at 3×8 (max sequence 8), dev-owned conversations steady at 159. 7 documents: html 42 lines (interactive counter), jsx+canvas-confetti 28, python numpy/matplotlib+print 22, mermaid 15, jsx unclosed-tag 18 (compile error), jsx throws-on-mount 19 (runtime error), untagged log block 16 (must stay a plain code block — only the missing language keeps it out; shouldExtractAsDocument returns false without one). 42 tests pass, typecheck pass, eslint 0 on all 5 files. Pre-existing non-mine failures confirmed (seed-run/refresh-catalog-run vite dep-optimizer load failure — reproduced against HEAD's own test file, so not caused by this run; generate-env verify-secrets = notifications workstream).
- 2026-07-25: ★ SEED AGENT FOUND A PRODUCT DEFECT while building the runtime-error fixture: a React document that throws on MOUNT may never produce a bridge `error` — bootstrap.ts posts errors only from its own try/catch, React 19 surfaces render throws asynchronously (reportError → window 'error' event), and the frame installs NO window-level handler → the panel can sit at "loading" forever. Minor under the old predict-design; LOAD-BEARING under run-and-observe, because the strategy treats silence as "still working". Dispatched task-19-sandbox-error-reporting (apps/sandbox, no conflict with task-17's apps/web): window error + unhandledrejection capture scoped to the PRE-`rendered` phase (a live document's own later runtime errors must not tear down a working preview), a per-kind render DEADLINE as the real anti-hang guarantee (cold Pyodide legitimately ~10-12s vs a fast web render), a check of whether the python path has the same gap, and reuse of existing error codes unless one genuinely cannot express a case.
- 2026-07-25: task-19-sandbox-error-reporting DONE_WITH_CONCERNS. ★ DEFECT WAS WORSE THAN DIAGNOSED (measured against real React 19.2.7, not stubs): the frame posted a FALSE `rendered` at t=86ms and the throw arrived 4ms later as a LOST window error → the user got a silently EMPTY preview, not a hang. Fixed: mount-throwing React now reports error{code:'runtime_error'} with the real stack and NO `rendered`, verified end-to-end through the committed bundle. Boundary: exactly one terminal message per request; window error/unhandledrejection fail the render only while unsettled; `rendered` posted only after a microtask drain + one macrotask (measured to land after React's scheduled mount) — a failure up to one task after the render call belongs to the render, anything later belongs to the live document (post-render throws + broken subresources leave the preview alone, 2 tests). Deadlines: web 20s armed on `init`; python 60s armed on `run` but CLEARED at `executing` (a long computation is not a hang, and a main-thread-blocking loop cannot fire a timer anyway) — both proven with mocked page clocks so the suite never waits them out. Python execution already covered (awaited try/catch); genuine gap found = hung Pyodide LOADER produced zero messages → that is the new load deadline. No window capture added to python: could not construct a case it would catch → no test, no code (correct discipline). 17 files/138 tests, 100% coverage.
  - SHARED BRIDGE CHANGED (additive): new `timed_out` code — nothing existing expresses "nothing arrived" honestly. Breaks apps/web typecheck at exactly 1 line (document-sandbox.tsx:154 Record<DocumentErrorCode,string>) → messaged task-17 in-flight to add the copy entry; it needs no other handling since the deadline arrives as an ordinary bridge `error` and task-17's rule already suppresses-while-streaming.
  - Its own out-of-scope observation, now dispatched back to it: esm-stub.ts mounts react SYNCHRONOUSLY, so E2E reaches the error by a different path than production React — which would make the new seeded "jsx throws on mount" fixture prove a path users never take. Asked it to align the stub's mount timing with real React's scheduled mount and report if any other test's behavior moves (a stub that had to be wrong for tests to pass would itself be a finding).
- 2026-07-25: task-19 esm-stub follow-up DONE. Stub now matches real React's mount timing (createRoot().render() schedules via microtask → MessageChannel, reports a component throw via reportError rather than throwing back to the caller); RED-verified BOTH ways against the old synchronous stub. Went further than asked, correctly: pointed render.browser.test.ts at `resolveEsmStub` instead of its PRIVATE react/react-dom copies, so all 13 render browser tests — incl. the mount-throw — now run against the real shared fixture set and still report error{runtime_error} with no `rendered`. Those private copies WERE the drift vector and are deleted (duplication down). That left react-runtime.ts with no Node importer at 0% coverage → fixed AT THE CAUSE (esm-stub.test.ts now derives fixture paths from REACT_RUNTIME_VERSION instead of hardcoding 19.1.0) rather than by a token test → package back to 100%. No test's behavior moved, nothing weakened. 17 files/140 tests.
  - Env note for other agents in this checkout: @rolldown/binding-linux-x64-gnu lost its files in node_modules mid-session (every vitest run died with "Cannot find native binding"), self-repaired minutes later. Nothing in the task touches node_modules.
  - Dispatched the task-19 audit (adversarial: try to produce a LOST error or a DOUBLE terminal message — multiple throws, throw+rejection, throw during re-init, a stale request's throw after a new init; verify the deadline timers are cleared on terminal/teardown so a leaked timer can't fire into a replaced request; try to construct the python async case the implementer said it could not, to confirm the restraint was right; confirm the stub still never ships to production).
- 2026-07-25: task-19 audit = FAIL, 2 Important, BOTH reproduced against real React 19.2.7 through the committed bundle (dev AND prod builds):
  1. settleTick()'s one-macrotask boundary lands after React's RENDER task but BEFORE its PASSIVE-EFFECT task → a component whose useEffect throws on mount posts `rendered` with an EMPTY tree and no error. The exact failure the task existed to kill, via the most common failure mode in generated React (ref/DOM access in an effect). Direction: React 19's `createRoot(root,{onUncaughtError})` attributes render AND commit/effect errors deterministically per-request, no timing race.
  2. reportUncaught() attributes any window error to whatever request is PENDING, but the panel re-inits the SAME frame ~every 300ms and never remounts → a stale document's late throw fails the NEXT healthy request and sticks an error card over a working preview (reproduced: r1 throws at t=300ms, r2 inited at t≈280ms receives it at t=303ms while the root holds r2's correct output). Inverts the task's own criterion one request over; risk INTRODUCED by this change (before it, no window error was reported at all). Direction: bind capture to the owning request.
  CONFIRMED GOOD (auditor attacked and failed to break): core render-throw fix real; settle()'s requestId+null fence survived double-throw, throw+rejection, mount-throw, and a leaked 20s deadline firing into a replaced request; python restraint CORRECT (tried 3 constructions — proxied JS callback, orphaned asyncio task, JS rejection from Python — none loses a terminal message, and a window capture there would convert successes into failures); deadlines armed/cleared where claimed with mocked page clocks; esm-stub follow-up verified incl. that the private-copy deletion weakened nothing. Noted: 60s python deadline covers interpreter + numpy/matplotlib wheel download, so a genuinely slow first load could false-timeout — reasoned tradeoff, not a defect. Also disclosed (pre-existing config): both bootstraps are excluded from the per-file coverage gate as browser entries, covered only by browser integration tests.
  Dispatched the fix with both directions + the constraint that if the only sound html/js fix needs the panel to remount per init, FLAG it rather than reach into apps/web (cross-task trade-off), and to state any residual honestly rather than paper it with a heuristic that could fire wrongly — a missed error being less harmful than a false error over a working preview.
- 2026-07-25: task-17-run-and-observe DONE_WITH_CONCERNS. Prediction machinery DELETED (grep-verified repo-wide: endsInsideOpenFence, sourceState, DocumentSourceState, SourceFenceContext, IncompleteSourceView; micromark + micromark-extension-gfm dropped from apps/web). Debounce 300ms. Last-good render via a `hasRendered` flag in the reducer + a `displayStatus` VIEW rule — good split: the reducer never lies about what the frame reported, only presentation suppresses (so document-render-status stays truthful for the Playwright/Maestro proofs). Raw-HTML `<pre><code class="language-jsx">` pinned both sides of the settle — it WAS producing a real error card mid-stream before, so the strategy change closed that hole as predicted. 7 of 9 pipeline tests RED-verified through the real renderer→card→store→panel→bridge path. Gates: 393 files/6365 tests, typecheck+lint 2/2, jscpd 0 clones, per-file coverage ≥95 (parser/panel/status/card/store 100). Added the `timed_out` copy entry as instructed. DEVIATION (sound): mermaid has no bridge to observe → waits for its message to settle, keyed on isStreaming alone, no text parsing.
  - ★ CROSS-TASK RISK it flagged (same root cause as the sandbox audit's stale-error finding — the frame is now REUSED, and the bootstrap assumed it was not): injectImportMap appends a NEW <script type="importmap"> per init, and renderReact calls createRoot on the same container per init. Both were only safe because the panel used to remount the iframe every token. On engines honoring only the first import map, a bare specifier first appearing in a LATER token fails to resolve on the FINAL attempt — i.e. a React doc that gains an import mid-stream breaks exactly when it completes. → Routed to the sandbox agent as a third item in its fix pass, with the key test it must add: a SECOND init on a live frame carrying a specifier absent from the first (the audit had already noted NO test performs a second init on a live frame — which is where the stale-error finding lived too).
  - DOCUMENTS.md §Panel UX updated by me (docs are mine to maintain): run-and-observe described, prediction-by-fence recorded as tried-and-abandoned with the reason, plus the HTML-builds-up and mermaid-waits behaviors.
  - Dispatched the task-17 audit (attack the new rule: error card for unfinished code = Critical; permanently hidden genuine error = Important; the isStreaming-flip race; stale bridge messages; debounce cancellation + the guarantee that final content always gets an init).
- 2026-07-25: task-18-seed-showcase audit = **PASS, no findings. → CLEAN.** Verified by EXECUTION not reading: ran the real sucrase transpiler (same options as the sandbox) over all 3 JSX fixtures — confetti transpiles with auto-injected react/jsx-runtime (so "authors never import React" holds), the compile-error fixture throws SyntaxError at parse (BISECTED: the same component with balanced tags transpiles fine, so the unclosed tags are genuinely the cause), the runtime-error fixture transpiles cleanly and can only fail at mount → the parse-time vs mount-time split is real; numpy 2.4.3 + matplotlib 3.10.8 wheels confirmed self-hosted so python runs offline, printing 4 lines AND leaving a figure (both output paths); the HTML inline script really executes under the actual sandbox CSP (script-src 'self' 'unsafe-inline'). Idempotency re-proven with the auditor's OWN 2 db:seed runs on top of the implementer's 4 — 8 messages each, max(sequence)=8, distinct=8, next_sequence=9, dev-owned conversations steady at 159; mechanism is createDevConversation's pinned-id early return over seedUUID, inheriting charlie's existing guarantee rather than a delete-then-insert. Mallory's exclusion CONFIRMED structural (she is ADMIN_TARGET_PERSONA, deliberately kept out of DEV_PERSONAS so demo flows never log in; her locked state + -2.5 USD balance intact after re-runs). Crypto/epoch shape byte-for-byte identical to charlie's (same factory). Whole-DB conversation growth per run belongs to E2E pooled personas — pre-existing, untouched.
  - Note (not a finding, do not revert): scripts/seed.ts:412 `baseCostNanoUsd` → `billableCostNanoUsd` is FORCED by the concurrent BILLING workstream's rename in apps/api/src/platform/dev/seed-billing-history.ts:81 — @hushbox/scripts cannot typecheck without it. Value unchanged. Another cross-workstream line riding in this run's diff.
  - DESIGN QUESTION → ACTED ON: MIN_LINES_FOR_DOCUMENT = 15 existed twice (apps/web document-parser.ts:23 authoritative + scripts/lib/seed-documents.ts:20 mirrored). CODE-RULES §One Implementation, Shared names a mirrored constant as the banned shape — if the web threshold rises, seeded fixtures silently stop being documents while the scripts test still passes at 15. Dispatched task-20 to hoist it to packages/shared/src/documents (narrowest scope covering both callers; exported from the narrow subpath apps/web already uses), with a required DEMONSTRATION that bumping the shared value moves BOTH sides — a shared constant a consumer has quietly re-pinned is no better than the mirror.
- 2026-07-25: task-19 fix-pass-2 DONE_WITH_CONCERNS — all 3 findings fixed, 17 files/145 tests, 100% cov, both bundles rebuilt + drift tests pass.
  1. Effect-phase throws: `onUncaughtError` passed to createRoot and bound to the CREATING request — one deterministic channel covering render+commit+effect. Rejected onCaughtError (an error boundary handled it ⇒ the document works as written) and onRecoverableError (reports a recovery, not a failure) — both correct calls. Timing is measured structure not padding: turn 1 = commit, turn 2 = passive-effect flush ⇒ react settles after 2 turns, every other kind after 1. Real React: useEffect throw → error{runtime_error}, no `rendered`, root empty.
  2. Cross-request attribution: window path bound to an explicit `captureRequestId` open ONLY while a document's own code runs — html = insertion + its settle turn (insertion blocks the loop so nothing interleaves); js = only the settle turn AFTER the import resolves (the seconds-long fetch window was precisely what handed r1's timer to r2); react = NEVER open (React's callback is authoritative). settle() closes the capture with the request. Verified: fresh-2 now gets `rendered`, no error.
  3. Frame reuse: import map is now ONE element per frame declaring only NEW specifiers (delta, not union — re-declaring resolved ones was discarded + logged 3 warnings per re-init, now 0); React root UNMOUNTED and re-created per init rather than reused, because each root's handler binds to its own request and reuse would re-introduce the mis-attribution. Verified: second init importing a new specifier renders, 1 map, 0 warnings.
  RESIDUALS ACCEPTED (consistent with my missed-error-over-false-error ruling): a late async error in a shared realm carries no request identity and is now DROPPED rather than guessed; ~1-task html capture window a stale timer could land in; js deferred-throw during module evaluation usually missed; a react tree time-slicing past turn 1 on a slow device would settle first and drop the later error. Test honesty noted: the second-init test's RED was only its structural assertion because this Chromium honors multiple import maps — they did NOT manufacture a red for the single-map engine.
  - ★ ESCALATED THEIR HONEST LIMIT rather than accepting it: the single-element map does not rescue an engine honoring only the FIRST map, and OUR OWN Maestro emulator is Android System WebView 113 — exactly that engine — while the seeded React+canvas-confetti fixture is exactly the shape that hits it when the specifier arrives mid-stream. Dispatched a BOUNDED investigation: can we delete import maps entirely by rewriting bare specifiers to absolute CDN URLs at transpile time? Load-bearing empirical unknown they must answer first: do esm.sh's returned modules import their OWN deps by bare specifier or absolute URL (fetch react-dom through the real CDN and look)? If absolute → the map serves only the document's own specifiers, rewriting removes the class, delete the map machinery. If bare → the map is load-bearing for esm.sh's graph, stop and we record the loud typed import_failed on old engines as accepted. Explicitly forbade the panel-remount option (it would trade away the last-good-render behavior just built).
- 2026-07-25: task-20-shared-threshold DONE. MIN_LINES_FOR_DOCUMENT hoisted to packages/shared/src/documents/extraction.ts, re-exported from documents/index.ts, consumed via the narrow `@hushbox/shared/documents` subpath; both local copies deleted; document-parser deliberately does NOT re-export it so no consumer can re-pin. DRIFT REMOVAL DEMONSTRATED (not argued): bumping 15→400 flipped web `shouldExtractAsDocument('typescript', 20)` true→false AND failed 2 scripts fixture-threshold tests; restored, both suites green. Gates: shared 108 files/2401 tests, web 393 files, scripts suites, typecheck+lint 6/6, eslint 0 in all three package dirs. The constant also lands on the top-level barrel (index.ts already re-exports ./documents) — no collision, noted as a surface change. The untagged-fixture doc block was MOVED onto the fixture rather than dropped (durable fact preserved).
  - It found a THIRD mirror the hoist did not reach → dispatched task-21: scripts/mobile-test.test.ts:1192 asserts a hardcoded 15 with a comment pointing at the now-DELETED web constant (a comment citing a nonexistent constant is worse than none); scripts/mobile-test.ts:495 restates the number in prose; e2e/ui/document-panel.spec.ts builds fixtures at exactly 15/5 lines — boundary-pinned data that silently stops being a document if the threshold rises and stops testing the negative case if it falls. Decision rule given: after the change, raising the shared constant must leave NO test passing on a stale assumption — proven by the same temporary-bump demonstration; per-fixture choice between deriving from the constant (where the test is ABOUT the boundary) and deliberate headroom (where the fixture merely needs to be a document), justified either way.
  - Env note: machine under heavy load from several concurrent agents (load avg ~32, multiple parallel vitest runs) — the coverage-directory and native-binding races several agents hit are explained by this; agents instructed to re-run rather than report false reds.
- 2026-07-25: task-19 fix-pass-3 DONE — ★ IMPORT MAPS DELETED ENTIRELY. Empirical answer to the load-bearing unknown (fetched the REAL CDN, not recalled): esm.sh emits ABSOLUTE URLs for its own dependency graph, never bare specifiers — `curl https://esm.sh/react-dom@19.1.0/client` → `import "/react@19.1.0/es2022/react.mjs"`; confirmed across scoped (@tanstack/react-query), subpath (react-dom/server) and deep transitive modules (the one bare-looking token was esm.sh's internal CJS require shim, not an import). So the map only ever served the DOCUMENT's own specifiers → rewriting removes the whole class. import-map.ts + its test DELETED; injectImportMap/assembleImportMap/scanBareImports/the frame's map element all gone, no second mechanism left. New render/resolve-imports.ts rewrites through the SAME moduleUrlFor the map used, so pins/author-versions/scopes/subpaths resolve byte-identically; the renderer's own react/react-dom now resolve by URL too, preserving ONE React instance. html got strictly MORE precise (only `<script type="module">` bodies rewritten vs scanning the whole document). VERIFIED against LIVE esm.sh through the committed bundle using the seeded fixture's exact shape: canvas-confetti arriving on the SECOND init of a live frame → both rendered, root "confetti loaded", 0 map elements. **This kills the old-Android-WebView class — our own Maestro emulator (WebView 113) is no longer a broken case.** Edge cases: computed `import(name)` left as written and fails at runtime as unresolvable — no regression (the old static scan never contained it either), pinned by a test; residual imprecision = import-shaped text inside a string literal gets rewritten (a changed string, never a broken import), where the old design had the same imprecision producing a harmless spurious map entry.
  - Docs updated by me: DOCUMENTS.md mermaid node + web-renderer bullet, TECH-STACK.md Code-Execution row — all now say specifier rewriting, no import map; prettier clean; zero stale "import map" refs left in the docs I own.
  - Routed to task-21 (already in e2e): e2e/chat/runnable-documents.spec.ts:49 still says "import-map resolution" — assertions unaffected, sentence now wrong.
  - Dispatched a FRESH audit of fix-passes 2+3 (the sandbox has changed substantially since its last audit): attack all three fixes, and specifically attack resolve-imports.ts (re-exports, side-effect imports, `import type`, dynamic import literal vs computed, scoped/subpath/pinned specifiers, string-literal false positives, and the ONE-React-instance requirement), plus independently re-verify the esm.sh absolute-URL claim the whole design now rests on.
- 2026-07-25: task-17 audit = FAIL, 1 Critical + 2 Minor. Strategy + demolition CONFIRMED clean (parser back to pure text-only, both micromark deps gone from package.json AND every lockfile importer, reducer/view split keeps document-render-status honest, new pipeline tests genuinely drive renderer→card→store→panel→bridge and close the raw-HTML hole).
  1. [Critical, CONFIRMED, MEASURED] document-sandbox.tsx:359-366 displayStatus + :471-477 debounce — suppression keys on isStreaming ALONE with no term for "an attempt against the CURRENT code hasn't reported yet". When the message settles while `code !== initializedCodeRef.current` (re-init still in the 300ms debounce), displayStatus returns the SUPERSEDED attempt's error → error card for code the frame was never given. (a) partial jsx + settle-in-same-commit ⇒ data-status="error" + role="alert" visible; (b) rendered doc broken by later tokens ⇒ error painted OVER the last good render, FLASHED=true cleared_after_ms=291. Exactly the founder bar the reversal exists to satisfy, on the most ORDINARY shape (message ends right after its fence), all runnable kinds. This is the isStreaming-flip race I explicitly briefed the auditor to probe. Fix: gate on PENDING WORK too.
  2. [Minor, CONFIRMED] document-card.tsx:70-87 remount re-publish fires only when activeDocumentId === document.id, but the id is a CONTENT HASH — a card that unmounts mid-stream (list virtualizes) and remounts after content grew matches neither branch and publishes nothing ⇒ panel suppresses failures INDEFINITELY showing "Preview starts when the code is ready." forever. Predates the task; the new rule made the consequence worse.
  3. [Minor, attribution UNRESOLVABLE between two uncommitted tasks] markdown-renderer.test.tsx lost 7 generic tests present at HEAD (bold/italic, GFM tables, GFM strikethrough, empty content, custom className, blockquotes, malformed markdown) — the repo's ONLY coverage of Streamdown GFM/prose rendering. Restore from HEAD regardless of who deleted them.
  UNDISCLOSED-BUT-SOUND deviation the auditor caught: displayStatus:364 exempts PYTHON from suppression, so a Run pressed on a half-written python doc DOES show its error. Correct (the user explicitly asked it to run) but absent from the report's Deviations and from the doc → I added it to DOCUMENTS.md §Panel UX.
  Also confirmed intact: debounce genuinely batches (3 edits ⇒ 1 extra init) and cancels on switch/close; settled content ALWAYS gets an init; stale requestIds dropped; render-status reachable only via the real bridge messages; store split, Rendered-default, teardown, share route, a11y all fine; mermaid deviation sound (partial mermaid cannot reach MermaidDiagram, pinned by 2 tests).
  Fix dispatched to the task-17 agent (resumed from transcript) with all three items.
- 2026-07-25: task-21-threshold-mirrors DONE_WITH_CONCERNS → orchestrator-VERIFIED CLEAN (static props checked myself; E2E runtime is founder-gated so no audit could do better). ★ FOUND 2 LATENT E2E BUGS: REACT_DOC and SYNTAX_ERROR_DOC in e2e/chat/runnable-documents.spec.ts were 14 body lines — one BELOW the threshold — while their comments claimed "(≥15 lines)". Neither would have produced a document card, so BOTH tests would have failed at card lookup on the founder's first suite run. Deriving the sizes from the shared constant is what exposed them. Exactly the value of the derive-don't-mirror rule.
  Final state verified by me: extraction.ts back to 15; scripts/mobile-test.test.ts:74 imports the constant and :1197 asserts against it with a durable comment; e2e/helpers/documents.ts derives (threshold+headroom for "just needs to be a document", threshold-1 for the boundary-negative); zero '15 lines' literals; zero "import map" references anywhere in e2e/ (3 sites fixed, folded in). (My first grep missed the scripts import only because head -4 truncated it — checked again rather than reporting a false discrepancy.)
  Honest notes from the agent: scripts/mobile-test.ts:495 needed NO change (its prose already cited the constant by name with no number — my brief wrongly expected an edit); bounds deviation = new e2e/helpers/documents.ts rather than duplicating sizing across two specs (right call); bump demo (15→25→15) briefly mutated packages/shared while concurrent agents were running — restoration grep-verified, but a concurrent shared-package reader could in principle have seen 25 for those seconds. Low risk, disclosed; if the in-flight task-17 fix reports anything threshold-shaped I will re-check.
  Bump demo result: BEFORE the fix the scripts test passed stale; AFTER it failed with "expected 18 to be greater than or equal to 25"; e2e helper outputs moved 19→29 and 14→24 — nothing left pinned.
- 2026-07-25: task-19 fix-passes-2+3 audit = FAIL, 1 Important + 6 Minor.
  ★ [Important, CONFIRMED, and it CORRECTS the implementer's own residual] bootstrap.ts:376-377/:55/:99/:266-275 — react settles `rendered` after a FIXED 2 turns, so a failure in ANY LATER COMMIT ROUND is dropped by settle() and the panel is told the render succeeded while the preview is EMPTY. The implementer disclosed this as "time-slicing past turn 1 ON A SLOW DEVICE"; the auditor reproduced it DETERMINISTICALLY on a fast desktop, 3/3, real react@19.1.0 via live esm.sh through the committed bundle (messages exactly loading:transpiling, loading:loading-modules, rendered; #document-root === ""). Trigger is not slowness — it is any SECOND commit round. Worst shape (a): an effect that setStates, then a second effect touching ref.current — i.e. the "ref/DOM access in an effect" pattern the PRIOR audit already named the most common generated-React failure, merely preceded by a state update. Also (b) second-round render throw, (c)(d) Suspense+lazy child throwing on render/in effect. Controls all still pass (first-round throw, useEffect/useLayoutEffect/child-effect on mount, 20 001-node tree) so the boundary is precisely bounded. → Directed: settle on a QUIESCENCE signal (observe root mutations, settle when the tree stops committing) rather than a turn count; if no sound signal exists, re-state the residual on its TRUE trigger set (any second commit round, any device) and I will re-rule it — but I will not accept "slow device edge" for something deterministic.
  6 Minors routed with it: build-bundle.ts:24-27 still claims the classic-script format exists for import-map injection (CONTRADICTS fix-pass-3's claim that build-bundle comments were updated); public/render.html:10-16 and react-runtime.ts:8 likewise stale — and all three now OBSCURE the real reason the classic-script constraint survives (WebRTC neutralization must run before untrusted code); resolve-imports.ts:33-46 has duplicated comment paragraphs (edit residue); unhandledrejection has ZERO tests and is unreachable for html (works for js); the html module-script residual is broader than disclosed (a module script with no imports that simply throws, and one importing a 404, both report `rendered`). packages/shared/src/env.config.ts:138 also mentions the import map — flagged to me, outside their package.
  CONFIRMED STRONG (auditor attacked and failed to break): import-map deletion — independently re-fetched the real CDN (react-dom, react/jsx-runtime, canvas-confetti, @tanstack/react-query + 3 deep transitive modules: ZERO bare specifiers), 6 consecutive inits on one live frame with new/removed/scoped/subpath/pinned specifiers all rendered, 0 map elements, `grep -c importmap` on both bundles = 0, ONE React instance verified via a hooks document; per-request capture survived 5 stale-error shapes incl. a 3-request sequence; exactly one terminal message under double throws/throw+rejection/throw+microtask/init-in-flight; deadline fires under mocked clock and the leaked timer does not fire into the replacement; error-boundary-CAUGHT throws correctly NOT reported; every rewrite edge case correct (re-exports, side-effect, multi-line from, dynamic literal, scoped/subpath/pinned/range, import type, relative/absolute/URL/data untouched, computed + template-literal left as written). One precision correction: a regex literal containing `from '<bare>'` IS rewritten and yields a SyntaxError — loud, never a silent wrong render.
  Also confirmed: the apps/web `timed_out` blocker is RESOLVED (document-sandbox.tsx:170 carries the entry).
- 2026-07-25: Two agents killed by a session limit. `ab55bc1c1b1439ba1` (panel audit) had ALREADY delivered its full verdict — that failure was only its waiter dying; NOT resumed. `aad98333af11a307a` (task-17 fix) was mid-work ("Now restoring the generic renderer tests that went missing") → RESUMED FROM TRANSCRIPT per the restart skill, with instructions to re-run any gate whose result predates the interruption.
- 2026-07-25: task-19 fix-pass-4 DONE_WITH_CONCERNS. React now settles on a MutationObserver QUIESCENCE signal, not a turn count (baseline 2 turns, +1 turn per observed DOM change until a turn passes quietly, bounded by the 20s deadline) — so the whole chained mount sequence (commit→effects→setState→commit→effects) stays inside the render window. Both deterministic shapes FIXED: chained-state-then-ref.current and second-round-render-throw now report error{runtime_error} with NO `rendered`, empty root. All controls still correct (first-round throw, mount-effect, layout-effect, 20 001-node tree → error; healthy doc, healthy chained state, forever-animating 16ms interval, live doc throwing from a 300ms timer → rendered with preview intact). Comments fixed in build-bundle.ts/render.html/react-runtime.ts/resolve-imports.ts, all now stating the REAL classic-script reason (a module script is deferred; the page script must neutralize WebRTC before any document code runs). Minors closed: unhandledrejection tested where it fires (js, RED-verified by removing the listener) and honestly scoped as unreachable for html; html module-script residual restated as ANY module-script failure. 150 tests, 100% cov, bundles rebuilt.
  ★ MY RULING — residual ACCEPTED, on the implementer's MEASURED terms (they retracted the earlier "slow device/time-slicing" wording, which was wrong): the window ends at the first genuinely idle gap — sync chain IN, awaited microtask chain IN, setTimeout(…,0) IN, setTimeout(…,100) OUT, lazy() fetch OUT, on ANY device. Rationale for accepting: (1) that boundary is a defensible definition of "the document is now live" — a commit triggered 100ms later by the document's own timer IS the document doing its own thing, not mounting; (2) the genuine remaining gap is Suspense+lazy, which needs a dynamic import inside a SINGLE-FILE document — rare; (3) the alternative they identified but did not take (allow a post-`rendered` error for the react kind, since onUncaughtError is unambiguous evidence THIS request's tree failed) requires relaxing the bridge's one-terminal-message rule AND teaching the panel, i.e. apps/web — reintroducing exactly the false-error-over-a-working-preview risk we deliberately eliminated. A bounded MISS beats a possible FALSE error. Documented in DOCUMENTS.md §Panel UX (react quiescence + the idle-gap boundary + the html module-script miss), so it is a recorded limit rather than folklore.
  Dispatched a TIGHTLY-SCOPED audit of pass 4 only (passes 2-3 already clean — regression sweep only): attack for a third dropped shape NOT behind an idle gap (deeper chains, useMemo/render-phase throw in a later round, queueMicrotask-driven chains, state updates inside observer callbacks); and equally hard in the OPPOSITE direction — a forever-animating document must NOT hold the window open to the 20s deadline (that would make every animated document settle via `timed_out`, a Critical regression); plus re-measure the five boundary points, since I ruled on those exact numbers.
- 2026-07-25: pass-4 audit = FAIL. **MY RULING IS WITHDRAWN — it rested on a wrong measurement.**
  ★ [Critical, CONFIRMED 4/4, real react@19.1.0 via committed bundle] bootstrap.ts:177-196 awaitTreeQuiescence waits for ONE quiet turn, but a React commit round spans TWO (commit turn mutates, passive-effects turn is quiet) → the loop exits on round 2's effects turn and any failure in round 3+ is DROPPED: `rendered`, no error, permanently empty root. Failing doc is the canonical generated-React shape (loading→setData→canvasRef.getContext in three chained effects). Mutation trace proves NO idle gap: mutations at 210.40/211.00/212.10ms, `rendered` posted at 212.10ms — success declared in the same millisecond React unmounted the torn-down tree. Throw in round 2 errors correctly; rounds 3/4/5 all report rendered+empty. Reproducible IN-SUITE against the committed esm-stub (`stub depth-2` → rendered, root "") so the RED test is writable without live esm.sh.
  [Important] The residual table I ruled on is WRONG: `setTimeout(…,0)` measured OUT (6/6), not IN. Confirmed as stated: awaited microtask IN, setTimeout(100) OUT, lazy()/Suspense OUT; sync chain IN **only at depth 1**. So "the first genuinely idle gap" is both LOOSER (a 0ms timer is not an idle gap) and TIGHTER (a synchronous depth-3 chain also falls out) than the code's real boundary. My acceptance is void; I will re-rule on corrected numbers after the fix. This is precisely why the brief ordered a re-measure of the five points rather than a re-litigation.
  [Minor] public/_headers:34-35 still references injecting an inline importmap script — its mirror in src/csp.ts WAS corrected, so the two now contradict.
  CONFIRMED GOOD: no over-holding anywhere — 16ms-interval animation, rAF loop, recursive setTimeout(0) animation, MessageChannel mutation loop, finite 1s animation and a live doc throwing from a 300ms timer ALL settled `rendered` in 145-236ms with previews intact; the 20s deadline never became the settle path. Auditor notes the irony: the single-quiet-turn rule is exactly what guarantees that — so the fix must satisfy BOTH constraints and measure, not reason. Also confirmed: one terminal message per request across ~40 probes; deadline arms/clears; unhandledrejection tested where it fires; drift tests compare the committed bundle byte-for-byte (so the attacks ran the real artifact); the four comment corrections are substantive (classic-script constraint now justified on deferred-module-scripts vs WebRTC-neutralization ordering).
  Fix dispatched with both constraints stated as pulling against each other + the in-suite RED test + a required re-measure of the full boundary set. DOCUMENTS.md softened to stop asserting "a failure anywhere in it is reported" (untrue as measured) and to point at the sandbox tests as the record of where the boundary sits; it gets a precise restatement once the corrected numbers land.
- 2026-07-25: task-17 fix DONE (all 3 findings). Critical: displayStatus gained a `superseded` term (`initializedCode !== null && initializedCode !== code`), with `initializedCode` moved from a ref INTO reducer state because the difference between it and the current code decides what renders; suppression is now `isStreaming || superseded`, so a message settling with a re-init still in the debounce keeps the last good render (or source) until the queued attempt reports. Both measured reproductions pinned, and MUTATION-CHECKED (forcing superseded=false turns the pipeline test red and nothing else). Card: `claimsRemountedSelection` re-claims on mount when type matches and content STRICTLY extends the open document — strictness is load-bearing (equal content hashes to the same id = already an id match; requiring growth keeps the "does not claim when a different document is active" case green). 7 generic tests restored VERBATIM from HEAD — and attribution resolved honestly: they were THIS agent's deletion (inside the line span it removed), not the fence-gate task's. Restoring them exposed that markdown-renderer.test.tsx mocks the store PARTIALLY (the card's new activeDocument read crashed every card test until the mock gained `activeDocument: null`) — flagged to the auditor to confirm that completed a harness gap rather than masking a real integration problem. Python exemption confirmed deliberate and now in the report's Deviations. Gates: 393 files/6378 tests, typecheck+lint 2/2, jscpd 0, per-file ≥95.
  - Its "still open" list carries 2 STALE items (working from older context): apps/sandbox per-init import map + createRoot were FIXED by the concurrent task (import maps deleted outright), and the DOCUMENTS.md §Panel UX lines it cites were already rewritten by me. Told the auditor not to report either.
  - COVERAGE FLAKE now reported by THREE separate agents: one full run fails markdown-renderer.tsx branches at 12/16 (75%) with all 6378 tests passing; identical tree reports 16/16 on re-run. Concurrent vitest instances colliding over v8's raw-coverage staging. Environmental — surface to founder at close so a single red coverage run is not misread as a real shortfall.
  - Re-audit dispatched: attack `superseded` (initializedCode null; code changing and changing BACK while an intermediate attempt is in flight; debounce cancelled mid-flight by switch/close; a stale error for a superseded requestId after a newer `rendered`; python exemption unreachable by non-python), attack `claimsRemountedSelection` (one document's content a strict PREFIX of another's — can a card steal a selection?; same content different kind), and specifically check whether the store-mock completion fixed a harness gap or MASKED an integration problem.
- 2026-07-25: task-17 fix re-audit = **PASS → task-17 CLEAN.** 393 files/6378 tests, typecheck+lint 2/2, jscpd 0, per-file coverage all ≥95 (the v8 branch flake did NOT reproduce this run). Both measured reproductions held. Adversarial probes all closed: initializedCode===null unreachable (requestId fence drops pre-init messages); code changing and changing BACK is safe (initializedCode is set synchronously to exactly the posted code); **suppression can never become permanent** — the debounce condition is the exact NEGATION of `superseded`, so superseded ⟹ a timer is scheduled, and the only cancel-without-reschedule is unmount; stale error for a superseded requestId dropped; python exemption unreachable by non-python (narrowed kind). Restored tests proven VERBATIM by structure not prose — they appear as CONTEXT lines in the diff vs HEAD, count delta = exactly the 5 new streaming cases. Store-mock question I flagged: CONFIRMED a harness gap, NOT masking — the partial mock left activeDocument undefined so the strict-equality guard threw on every card render; the real store declares and initialises both.
  - [Minor, ACCEPTED as recorded] claimsRemountedSelection matches on (type, strict byte-prefix), so a DIFFERENT document whose content strictly extends the open one can claim the panel on its own mount — shape: an "add a line to this file" answer re-emitting A byte-identically plus lines, virtualized out and back while A stays open. Consequence is a visible re-anchor, NOT corruption (activeSelectionId does not bump ⇒ no teardown, no lost Raw choice); unreachable without a remount, for equal content, or across types. Closing it needs block identity (message id / block index) at the parser seam = a `Document` shape change, a separate task rather than a blind fix. Tradeoff already documented in-code at document-card.tsx:64-68.
- 2026-07-25: OPEN FOUNDER DECISION (asked, not yet answered): the last residual — a React commit round starting >~50ms after the last DOM change (Suspense/lazy, or a >50ms-deferred state update) fails AFTER `rendered` was posted, so the tree tears down and the user sees a BLANK preview with no error card. Options put to them: (1) let the react kind report an error AFTER `rendered` — safe because onUncaughtError is unambiguous evidence THIS root's tree died, so there is no working preview left to damage; needs the one-terminal-message rule relaxed for react + panel support (small change in two packages), my recommendation; or (2) accept and document. The window cannot simply be widened: the live-document control (a 300ms post-mount timer throw, which SHOULD keep its preview) is only 6× away from the 50ms boundary.
- 2026-07-25: FOUNDER RULING — option 1 approved: the react kind may report an `error` AFTER `rendered`. Dispatched as ONE task spanning three packages (apps/sandbox render + packages/shared bridge docs + apps/web document-sandbox.tsx) deliberately — splitting it would leave a window where the frame emits a message the panel does not understand. Required semantics: react's error channel stays open past settle while EVERY other path keeps today's behavior (window capture still closed after settle, non-react kinds still exactly one terminal message, requestId fence and deadlines untouched); the panel clears the rendered state (the preview is a corpse) and surfaces the error THROUGH the existing suppression rule rather than around it — hidden while `isStreaming || superseded`, shown once settled, no second display path.
  - Flagged for measurement, not assumption: does onUncaughtError also fire for a render triggered by a user interaction (click handler → setState → re-render throws)? If so a live document crashing on a click now shows an error card where it previously blanked silently — I consider that an IMPROVEMENT and intend to keep it (the tree is torn down either way), but it is a behavior change beyond the residual, so it must be measured and stated. Separately must confirm what does NOT reach the handler: a throw directly inside an event handler with no re-render must still leave a working preview alone.
  - Also required: confirm document-render-status transitions rendered→error correctly, since the Playwright and Maestro proofs assert on that element and a healthy document must still reach `rendered` and stay there.
- 2026-07-25: pass-5 audit = FAIL, 1 Critical + 1 Important — and its Critical is what pass 6 (the founder-approved option 1) closes. THE CRITICAL: the quiet window renews only on DOM-OBSERVABLE change, so a commit round that mutates nothing buys no time — the clock runs against the last DOM CHANGE, not the last COMMIT. **The discriminator is DOM visibility, not timing.** Confirmed 4/4 each vs the pass-5 bundle: (1) the canonical canvas shape at 30ms hops (well inside the claimed IN region) returns `[rendered]` root "" when the rendered output is identical each round, while the byte-identical doc whose text CHANGES reports the error; (2) a PURELY SYNCHRONOUS chain, no timers at all, constant output — caught at depths 1-8, DROPPED at 9/10/12, i.e. a continuous mount sequence with no idle gap anywhere; (3) createPortal to document.body mutates outside the observed root, same. [Important] residual table wrong on 3 rows: setTimeout(50) is a KNIFE EDGE (IN IN OUT across runs), not inside — ≥55ms is outside; setTimeout(25) two-hop is OUT with constant output; "sync chain any depth" holds only to depth 8 with constant output.
  ★ Auditor verified that against the IN-FLIGHT pass-6 snapshot the canvas case becomes `[rendered, error:runtime_error]` — reporting late failures MITIGATES the Critical. This is the fence-gate lesson one level down: any rule that PREDICTS when rendering finished will keep having blind spots; observing what React reports makes the timing far less load-bearing. The founder's option-1 ruling is worth more than it was framed as.
  Sent to the pass-6 agent mid-work: verify the mitigation across ALL THREE shapes (not just canvas); the esm-stub CANNOT express the mutation-invisible class (esm-stub.ts:105 rebuilds the container every commit) so either make it capable without weakening existing tests or leave that coverage to browser probes rather than writing a test that cannot fail; update the now-false "exactly one terminal message per request" comment at bootstrap.ts:105-112; and restate the residual in post-pass-6 terms rather than carrying the pass-5 framing forward.
  CONFIRMED GOOD in pass 5, no action: over-holding genuinely solved (16ms/rAF/setTimeout(0)/MessageChannel all settle 595-778ms, previews intact; a 45ms mutator rides the budget acceptably; nothing near the deadline). ★ The constants were verified on SLOW HARDWARE properly — CPU throttling 1x→10x moved the max inter-round gap only 8.4→15.6ms, so the 50ms window keeps ~3.2× margin and a 12-round mount ~3.3× under the 400ms budget; React's inter-round gap is scheduler-yield-bound, NOT CPU-bound. That closes the slow-device concern I raised with measurement rather than argument. Both new tests proven to fail against a broken rule (rebuilt the bundle with the rule broken two ways via an esbuild onLoad transform, no repo files touched). _headers ↔ csp.ts byte-identity pinned.
  NOTE: the auditor's first suite run failed on an in-flight pass-6 edit landing mid-audit; its second run was green. Target-moved-during-audit is a hazard of this much concurrency — its measurements are all explicitly against the pass-5 bundle.
- 2026-07-25: task-19 fix-pass-6 DONE (founder-approved option 1, three packages, one protocol change). SCHEMA UNCHANGED — the message shape is identical and `requestId` already ties a late failure to the render it invalidates; semantics documented on RenderedMessage + ErrorMessage rather than left ambiguous. Panel clears hasRendered via a new `currentAttemptRendered` flag (a dead preview is no longer offered as the last good render) and surfaces the error through the EXISTING suppression rule only — no second display path. INTERACTION QUESTION ANSWERED BY MEASUREMENT (what I asked for): click→setState→re-render throws ⇒ `rendered, error:runtime_error` root ""; click→setState→effect throws, same; a click handler throwing with NO re-render does NOT reach the handler and leaves the preview intact. So a live document crashing on a click now shows an error card where it previously blanked silently — an improvement, and the root genuinely IS empty in those cases. ALL THREE pass-5 audit shapes mitigated 4/4: canvas constant-output+30ms hops → [rendered, error]; synchronous depth-12 constant-output → [rendered, error]; createPortal → [error] ALONE (root never mutates so it never settled). Healthy control → `rendered` only.
  - DECLINED the in-suite mutation-invisible test WITH A REASON rather than forcing one (exactly the judgment asked for): the stub's rounds are microseconds apart, so reproducing the class would mean faking React's scheduler yield with real sleeps — slowing and destabilising every react test; the post-settle MECHANISM is already covered in-suite by the deferred-round test, class-specific reproductions stay in browser probes.
  - Invariant comment at settle() updated to state the one exception and why. RESIDUAL RESTATED post-pass-6: for react the timing boundary no longer decides whether the reader is TOLD, only whether the error arrives alone or after `rendered` — so the audit's three timing corrections no longer describe a user-visible gap. What genuinely remains unreported: an error the document's own error boundary CATCHES and a bare event-handler throw (both deliberate — the preview still works), an async throw a react document starts outside React, html module-script failures, and a js deferred throw after the import resolved. All missed-not-false; none leaves the panel waiting.
  - Fixture bug found and fixed en route: the stub kept committing scheduled rounds AFTER an uncaught error, resurrecting a torn-down tree — the cause of a 1-in-3 flake in the depth-3 test, now 3/3 stable.
  - Final audit dispatched: find every consumer that assumed one-terminal-per-request (a consumer silently ignoring the second message would REINTRODUCE the silent empty preview); attack the panel (late error while streaming/superseded, a late error for an OLD requestId after a newer render, two late errors, late error then successful re-init); independently reproduce the interaction claims + boundary cases (useEffect cleanup on unmount, startTransition, error-boundary-CAUGHT must stay silent); verify render-status transitions rendered→error while a HEALTHY doc still reaches `rendered` and STAYS (the mobile proof depends on it); judge the declined test and confirm the flake is actually gone.
- 2026-07-25: ★ task-19 fix-pass-6 audit = **PASS** → sandbox CLEAN (one comment-only Minor dispatched). 20 documents probed against the COMMITTED bundle with real react@19.1.0 from live esm.sh. Every interaction row reproduced exactly. Boundary cases the brief added, all correct: useEffect cleanup on unmount → [rendered, error] root "" (React tears the whole root down, so the card describes reality); startTransition throw → [rendered, error]; the document's own error boundary CATCHING → [rendered] only, fallback shown, silent (both mount-time and 150ms-deferred). ALL consumers of the one-terminal assumption audited — panel reducer (the only one needing teaching), document-render-status (pure mirror, transitions rendered→error only when !isStreaming && !superseded), e2e page object (toHaveAttribute polls, unaffected), Maestro flow (asserts an HTML document, untouched, and no healthy doc emits anything after `rendered`), python bootstrap (separate settle) — NONE silently ignores the second message. Panel attacks all held: late error while streaming → status streaming, no alert, and the dead frame COLLAPSES to h-0 rather than showing the corpse; superseded → same then the debounced attempt takes over; OLD requestId dropped at BOTH fences (frame latestRequestId + panel requestIdRef); two late errors collapsed; late-error-then-re-init → full recovery. Rewritten pinned tests still mean what they claim (both directions of hasRendered turn red if forced constant). Fixture fix SHARPENS fidelity (matches real React's post-error teardown), no assertion relaxed; flake gone — 8 consecutive green runs, 22/22 each.
  - The DECLINED test was judged correctly declined, with reasoning STRONGER than the implementer's own: even with the ~5-line stub change, the quiescence loop waits a full 50ms hop before its first re-read while the stub's rounds are a microtask apart, so every round still lands pre-settle and is caught — "such a test could not fail; writing it would have been the reward-hacking move".
  - [Minor, comment-only] bootstrap.ts:154 claims a tree "reports its death once", but reactFailureReported is set only on the post-settle branch, so TWO pre-settle failures in one commit round post two errors for one request (CONFIRMED 3/3 with sibling throwing useEffects). No user impact (panel error branch idempotent; shared-bridge wording not contradicted) — but it states a guarantee the code does not make. Dispatched.
  - Also noted (not a defect): the createPortal row was measured [rendered, error], not [error] alone — with no root mutations quiescence exits after one hop and `rendered` posts at ~60ms. Outcome-equivalent, panel-identical; the residual's mechanism sentence was imprecise and is being corrected.
- 2026-07-24: CORRECTION (founder): Maestro + Capacitor already set up with an existing test suite — my "no Maestro in repo" was an unverified Assumed claim; R5 is extend-the-suite, not new infra. Web research retained only for the nested-sandboxed-iframe assertion question; dispatched codebase-explorer to map the existing suite (appends to research/maestro-capacitor.md).
  - U1: Which package? (analyst in flight)
  - U2: Exact integration point mirroring mermaid rendered/raw toggle (explorer in flight)
  - U3: How/where system prompt is assembled + how to advertise capability to the model (explorer in flight)
  - U4: CSP/COOP-COEP constraints, Capacitor WebView implications (both in flight)
  - U5: Human decisions to collect in ONE round after research: scope of languages/runtimes, npm-dep support, offline/self-host bundler stance, mobile behavior, security posture.
