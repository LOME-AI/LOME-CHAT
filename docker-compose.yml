name: ${COMPOSE_PROJECT_NAME:-hushbox}

services:
  postgres:
    image: postgres:18-alpine
    # Ephemeral test/dev DB: data lives in RAM with durability off, rebuilt from
    # migrations + seed on every `db:up`. fsync and disk writes are pure overhead
    # here, and dominate the write-heavy test path.
    command: >-
      postgres -c max_connections=1000 -c fsync=off
      -c synchronous_commit=off -c full_page_writes=off
    ports:
      - '${HB_POSTGRES_PORT:-5432}:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hushbox
      PGDATA: /pgdata
    tmpfs:
      # size caps the RAM ceiling; the seeded test DB uses far less. tmpfs is
      # lazy, so the cap is a guardrail against a runaway, not a reservation.
      - /pgdata:size=2g
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 5

  neon-proxy:
    image: ghcr.io/neondatabase/wsproxy:latest
    environment:
      APPEND_PORT: 'postgres:5432'
      ALLOW_ADDR_REGEX: '.*'
      LOG_TRAFFIC: 'true'
    ports:
      - '${HB_NEON_PORT:-4444}:80'
    depends_on:
      postgres:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    ports:
      - '${HB_REDIS_PORT:-6379}:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 5

  serverless-redis-http:
    image: hiett/serverless-redis-http:latest
    ports:
      - '${HB_REDIS_HTTP_PORT:-8079}:80'
    environment:
      SRH_MODE: env
      SRH_TOKEN: local_dev_token
      SRH_CONNECTION_STRING: 'redis://redis:6379'
      # Backend Redis pool size. Image default is 3; 12 Playwright workers each
      # funnel several Redis HTTP ops per request, saturating that tiny pool and
      # tripping fail-closed 503s. Sized well above 12 workers x per-request ops.
      SRH_MAX_CONNECTIONS: 100
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'wget -qO- --header="Authorization: Bearer local_dev_token" --header="Content-Type: application/json" --post-data=''["PING"]'' http://127.0.0.1:80/ || exit 1',
        ]
      interval: 5s
      timeout: 3s
      retries: 5

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    ports:
      - '${HB_MINIO_API_PORT:-9000}:9000'
      - '${HB_MINIO_CONSOLE_PORT:-9001}:9001'
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/data
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9000/minio/health/live']
      interval: 5s
      timeout: 3s
      retries: 5

  minio-setup:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 minioadmin minioadmin &&
        mc mb -p local/hushbox-media-dev
      "

  # android-emulator: managed by scripts/mobile-test.ts via direct `docker run`
  # so the script can scale to mobile-tests/config.ts SHARDS containers with
  # distinct per-shard ADB ports and names. Compose stays for the dev-stack
  # services above.

volumes:
  minio_data:
